node_modules/
build/
.svelte-kit/
src/lib/paraglide/
drizzle/
pnpm-lock.yaml

# Generated / vendored
*.lock
*.lockfile

# CSV fixtures — Prettier has no CSV parser (errors on them), and these are
# byte-exact test oracles (intentional BOM / CRLF / preamble); never reformat.
*.csv

# Captured HTML fixtures (Phase 9 Telegram t.me/s parser oracles). These are
# real upstream markup snapshots the parser keys on; Prettier's HTML parser
# both errors on hand-trimmed/synthesized blocks AND would rewrite the exact
# structure under test. Same "byte-exact oracle, never reformat" rule as *.csv.
tests/fixtures/**/*.html

# Documentation and planning — managed manually
.planning/
docs/
*.md

# Workflows and config — keep authored formatting
.github/
.dockerignore
.gitignore
.env.example

# Project metadata
messages/
project.inlang/

# Playwright HTML reports (generated)
playwright-report/
test-results/

# Claude Code session state — agent worktrees, settings, todos. Untracked
# but prettier scans them by default; explicitly ignore so lint stays
# green even when local agent state is present.
.claude/
