// Inert $app/environment stub for the vitest browser project. Tests run in
// a real (headless) browser, so browser=true / building=false. See
// vitest.config.ts $appAlias.

export const browser = true;
export const building = false;
export const dev = true;
export const version = "test";
