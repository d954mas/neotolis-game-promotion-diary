// Inert $app/paths stub for the vitest browser project. See
// vitest.config.ts $appAlias.

export const base = "";
export const assets = "";
export function resolveRoute(id) {
  return id;
}
export function resolve(id) {
  return id;
}
