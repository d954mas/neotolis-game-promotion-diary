import { describe, it } from 'vitest';

// Wave 0 placeholder for VALIDATION 18/19 (Paraglide message resolution + locale-add ergonomics).
// Plan 01-09 (Wave 4) lands Paraglide config + messages/en.json + the dashboard string.
describe('paraglide i18n', () => {
  it.skip('m.dashboard_title resolves to English at runtime', () => {
    /* Plan 01-09 */
  });

  it.skip('adding messages/ru.json is content-only (no source-code change)', () => {
    /* Plan 01-09 */
  });
});
