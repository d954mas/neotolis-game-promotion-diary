import { describe, it } from "vitest";

/**
 * Wave 0 placeholder test file (Plan 02-01 — Phase 2 Wave 0).
 *
 * Per Phase 1 Wave 0 invariant: every later task ships into a test that
 * already exists. The it.skip stubs below are EXACT names — implementing
 * plans (02-NN) replace `it.skip` with `it` and add the assertions.
 *
 * If you are an executor on a later plan and the test you need is NOT in
 * the it.skip list below, the gap is in this Wave 0 plan — fix it here,
 * NOT by silently adding a new it() in your plan's commit.
 */
describe("URL ingest paste-box (INGEST-02..04, twitter/telegram event create)", () => {
  it.skip("02-06: INGEST-02 youtube paste creates tracked item with title", () => {
    /* placeholder — implementing plan: 02-06 */
  });
  it.skip("02-06: INGEST-03 is_own auto decision via youtube_channels match", () => {
    /* placeholder — implementing plan: 02-06 */
  });
  it.skip("02-06: INGEST-03 toggle is_own", () => {
    /* placeholder — implementing plan: 02-06 */
  });
  it.skip("02-06: INGEST-04 malformed URL rejects + no half-write", () => {
    /* placeholder — implementing plan: 02-06 */
  });
  it.skip("02-06: INGEST-04 oembed 5xx no row", () => {
    /* placeholder — implementing plan: 02-06 */
  });
  it.skip("02-06: INGEST-04 oembed 404 unavailable no row", () => {
    /* placeholder — implementing plan: 02-06 */
  });
  it.skip("02-06: twitter paste creates events row kind=twitter_post", () => {
    /* placeholder — implementing plan: 02-06 */
  });
  it.skip("02-06: reddit paste returns inline info, no row created", () => {
    /* placeholder — implementing plan: 02-06 */
  });
});
