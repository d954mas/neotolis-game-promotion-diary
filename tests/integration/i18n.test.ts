import { describe, it } from 'vitest';

// Wave 0 placeholder for VALIDATION 18 (Paraglide message resolution at runtime).
// Plan 01-09 (Wave 4) lands the Paraglide compile output + the dashboard message.
describe('i18n at runtime', () => {
  it.skip('rendered dashboard HTML contains English message text', () => {
    /* Plan 01-09 — render +page.svelte containing {m.dashboard_title()}; assert string in HTML */
  });
});
