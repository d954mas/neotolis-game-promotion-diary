// Twitter/X feed + chart visualization (Wave-0 scaffold).
//
// Named it.skip placeholders — Plan 11-04 flips these live. A populated
// share_count (+ raw components) snapshot must flow through the metric-series
// loader to a shares series that renders on /games/[id] (mirrors the TikTok
// share-metric viz).
//
// Requirements: VIZ-05 / PLAT-03.
import { describe, it } from "vitest";

describe("twitter feed + chart viz", () => {
  it.skip("[11-04] snapshot share_count + raw components populated -> metric-series returns a shares series (VIZ-05)", () => {});

  it.skip("[11-04] the shares series renders on /games/[id] for a twitter_post event (PLAT-03)", () => {});

  it.skip("[11-04] a twitter_post event surfaces in /feed with its card props + thumbnail marker", () => {});
});
