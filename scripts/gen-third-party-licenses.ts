// D-19b THIRD_PARTY_LICENSES.md generator. Filled in by plan 06-06.
// Will shell `pnpm licenses list --prod --json`, drop machine-specific
// `paths`, sort by name+version, and emit a deterministic Markdown table for
// the `git diff --exit-code` regen gate. STUB: no-op so CI is not blocked.
process.exit(0);
