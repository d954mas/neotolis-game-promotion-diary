---
phase: 03.0.2-dependency-refresh-inserted
plan: 02
type: execute
wave: 1
depends_on:
  - "03.0.2-01"
files_modified:
  - package.json
  - pnpm-lock.yaml
autonomous: true
requirements:
  - 03.0.2-C2
commit_label: "Commit 2 (per RESEARCH.md ordering — was D-04 row #6)"
must_haves:
  truths:
    - "package.json declares oauth2-mock-server at ^8.x (latest stable)"
    - "pnpm-lock.yaml resolves oauth2-mock-server to a 8.x version"
    - "smoke CI job passes — full OAuth dance through oauth2-mock-server against the production image still works"
    - "git history contains one commit with subject ^chore\\(deps\\): bump oauth2-mock-server"
  artifacts:
    - path: "package.json"
      provides: "Updated devDependencies pin for oauth2-mock-server"
      contains: "oauth2-mock-server"
    - path: "pnpm-lock.yaml"
      provides: "Lockfile snapshot matching the new package.json pin"
  key_links:
    - from: "tests/smoke/lib/oauth-mock-driver.ts"
      to: "oauth2-mock-server 8.x"
      via: "import { OAuth2Server } from 'oauth2-mock-server'"
      pattern: "from ['\"]oauth2-mock-server['\"]"
    - from: "tests/smoke/self-host.sh"
      to: "oauth2-mock-server boot + token endpoint contract"
      via: "smoke job (production-image OAuth dance)"
      pattern: "oauth2-mock-server"
---

<objective>
Land the second atomic commit of Phase 03.0.2: bump `oauth2-mock-server` from ^7.2.0 to ^8.2.2.

This is the second-lowest-risk commit and lands second per RESEARCH.md Pattern 1. v8 drops Node 18 support and switches to "Universal ESM," but documented OAuth/OIDC protocol surface is unchanged — the smoke harness (`tests/smoke/self-host.sh` + `tests/smoke/lib/oauth-mock-driver.ts`) is the load-bearing assertion. Our smoke test boots oauth2-mock-server, drives the genericOAuth dance against the production image, and asserts authenticated landing; if anything in the mock-server contract drifted, smoke fails immediately.

Purpose: Validate the smoke gate continues to fire correctly under a fresh OAuth mock version before the genuinely OAuth-touching commit (better-auth, plan 10) lands.
Output: One commit `chore(deps): bump oauth2-mock-server from ^7.2.0 to ^8.2.2` on `feat/phase-03.0.2-dependency-refresh`; CI green on all three jobs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-CONTEXT.md
@.planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-RESEARCH.md
@.planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-VALIDATION.md
@AGENTS.md
@package.json
@tests/smoke/lib/oauth-mock-driver.ts
@tests/setup/oauth.ts
@scripts/dev-mock-oauth.mjs

<interfaces>
<!-- Current pin (from package.json line 72): -->
"oauth2-mock-server": "^7.2.0"

<!-- Target pin (RESEARCH.md table line 139): -->
"oauth2-mock-server": "^8.2.2"

<!-- API contract used (per RESEARCH.md commit 6 §"Code-touch surface area"):
     - `OAuth2Server` class import — UNCHANGED in v8
     - `start()` / `stop()` lifecycle — UNCHANGED in v8
     - Configuration shape (issuer URL, well-known endpoint) — UNCHANGED in v8
     - Token endpoint / discovery endpoint contract — UNCHANGED per v8 release notes
     The ONLY breaking change is Node 18 drop (we are on 22, safe). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump oauth2-mock-server 7→8 and commit</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>
    - package.json (current pin: `"oauth2-mock-server": "^7.2.0"` — line 72)
    - tests/smoke/lib/oauth-mock-driver.ts (verify imports + API usage shape matches v8 contract)
    - tests/setup/oauth.ts (test-setup integration with the mock server)
    - scripts/dev-mock-oauth.mjs (dev-convenience entry point, also imports oauth2-mock-server)
    - .planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-RESEARCH.md §"Commit 6 — oauth2-mock-server 7 → 8"
    - .planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-CONTEXT.md §D-02 (green CI per commit) and §D-07 (smoke is the load-bearing assertion)
    - AGENTS.md §"Workflow" → squash-merge model; per-commit branch hygiene matters even though master gets one squash.
  </read_first>
  <action>
    Pre-bump audit:
    1. Confirm on branch: `git rev-parse --abbrev-ref HEAD` MUST output `feat/phase-03.0.2-dependency-refresh`.
    2. Confirm previous commit landed cleanly (Plan 01 is `depends_on`): `git log --oneline -1` MUST show `chore(deps): bump @types/supertest from ^6.0.0 to ^7.2.0`.
    3. Confirm working tree clean for `package.json` + `pnpm-lock.yaml`: `git status --porcelain package.json pnpm-lock.yaml` MUST be empty.
    4. Spot-check API usage surface — these files MUST be reviewed and their import / call shape MUST match the v8-documented surface (no `new OAuth2Server(...)` constructor signature change, no rename of `start()` / `stop()` / `issuer.url`):
       ```bash
       grep -nE "from ['\"]oauth2-mock-server['\"]|require\\(['\"]oauth2-mock-server['\"]\\)" tests/smoke scripts/dev-mock-oauth.mjs tests/setup
       ```
       Expected: at least one import in each of `tests/smoke/lib/oauth-mock-driver.ts`, `tests/setup/oauth.ts`, `scripts/dev-mock-oauth.mjs`. Note the line and rerun grep after bump if any test fails.

    Perform the bump:
    5. `pnpm add -D oauth2-mock-server@^8.2.2` (caret per devDeps convention).
    6. `pnpm install --frozen-lockfile` MUST exit 0.
    7. Run local verification (D-02 + D-07 — smoke is load-bearing for this bump):
       ```bash
       pnpm lint
       pnpm typecheck
       pnpm test:unit
       pnpm test:integration
       ```
       All MUST exit 0. The full local smoke run (`bash tests/smoke/self-host.sh`) requires Docker + a clean image build (~3-5 min); if Docker is unavailable locally, rely on CI smoke job — but DO NOT push until lint + typecheck + unit + integration are all green locally.

    Fix-up (only if integration tests fail):
    8. If `pnpm test:integration` fails with an oauth2-mock-server error (e.g. constructor signature, server start hook, token endpoint shape), per CONTEXT.md §D-02 the fix lands in the SAME atomic unit. Options:
       (a) Amend the bump commit if not yet committed.
       (b) Land a follow-up `fix(03.0.2): adjust oauth-mock-driver for v8 <reason>` commit alongside.
       Either is acceptable; both commits MUST be CI-green individually per D-02.

    Commit:
    9. `git add package.json pnpm-lock.yaml` (+ any test fix-up files if step 8 applied).
    10. Commit:
       ```bash
       git commit -m "$(cat <<'EOF'
chore(deps): bump oauth2-mock-server from ^7.2.0 to ^8.2.2

oauth2-mock-server v8 drops Node 18 support (we are on Node 22 — safe)
and ships as Universal ESM. Public OAuth/OIDC contract — issuer URL,
discovery endpoint, token endpoint, JWKS — is unchanged per upstream
release notes.

Verification: smoke job (tests/smoke/self-host.sh) is the load-bearing
assertion per CONTEXT.md D-07 — it boots oauth2-mock-server, drives the
full genericOAuth dance against the production image, and asserts the
user lands authenticated. Local pnpm lint + typecheck + test:unit +
test:integration also pass.
EOF
       )"
       ```
    11. `git push origin feat/phase-03.0.2-dependency-refresh`. Wait for CI on this commit before starting Plan 03.
  </action>
  <verify>
    <automated>
      grep -q '"oauth2-mock-server": "\^8\.' package.json
      grep -q "oauth2-mock-server@8\." pnpm-lock.yaml
      pnpm install --frozen-lockfile
      pnpm lint && pnpm typecheck && pnpm test:unit && pnpm test:integration
      git log -1 --format=%s | grep -qE '^chore\(deps\): bump oauth2-mock-server'
      # After push, CI smoke job MUST be green:
      # gh run list --branch feat/phase-03.0.2-dependency-refresh --limit 1 --json conclusion,jobs --jq '[.[0].conclusion, .[0].jobs[]|.name + ":" + .conclusion]'
      # Expected: lint-typecheck=success, unit-integration=success, smoke=success.
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"oauth2-mock-server": "\^8\.' package.json` exits 0.
    - `grep -q "oauth2-mock-server@8\." pnpm-lock.yaml` exits 0.
    - `pnpm install --frozen-lockfile` exits 0 with no diff.
    - `pnpm lint` exits 0.
    - `pnpm typecheck` exits 0.
    - `pnpm test:integration` exits 0 (proves any test-touching imports of oauth2-mock-server still resolve & behave).
    - `git log -1 --format=%s` matches `^chore\(deps\): bump oauth2-mock-server`.
    - `git diff HEAD~1 HEAD --stat` shows changes ONLY in `package.json` + `pnpm-lock.yaml` (+ optionally test-harness driver files IF step 8 fix-up was needed — those edits MUST be justified in the commit body).
    - After push: CI run on this commit shows `smoke` job status `success` — this is the D-07 load-bearing assertion for this bump.
  </acceptance_criteria>
  <done>
    Branch has commit `chore(deps): bump oauth2-mock-server from ^7.2.0 to ^8.2.2` (possibly accompanied by a `fix(03.0.2): ...` commit if smoke harness needed adjustment). CI green on all three jobs including smoke. Branch ready for Plan 03 (dotenv 17).
  </done>
</task>

</tasks>

<verification>
1. `git log feat/phase-03.0.2-dependency-refresh --oneline | head -2` shows the new bump commit on top of Plan 01's commit.
2. CI on the latest commit: all three jobs green. Smoke is the load-bearing assertion per D-07.
3. No leaked unrelated edits.
</verification>

<success_criteria>
- Atomic commit `chore(deps): bump oauth2-mock-server from ^7.2.0 to ^8.2.2` landed on branch.
- Smoke CI green on the commit.
- 2 of 10 atomic units complete.
</success_criteria>

<output>
After completion, create `.planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-02-SUMMARY.md` recording: commit SHA(s), CI run URL (smoke job link in particular), any fix-up adjustments to `tests/smoke/lib/oauth-mock-driver.ts` if applied, and lockfile delta size for `oauth2-mock-server`'s transitive tree.
</output>
