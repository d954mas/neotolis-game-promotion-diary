---
phase: 03.0.2-dependency-refresh-inserted
plan: 07
subsystem: dependencies
tags:
  - chore
  - deps
  - hono
  - node-server
  - runtime
dependency_graph:
  requires:
    - "03.0.2-06 (pino pair) — green CI baseline"
  provides:
    - "@hono/node-server on 2.x runtime adapter"
    - "Exact-pin convention preserved for load-bearing runtime adapter"
  affects:
    - "src/roles/app.ts (sole importer of @hono/node-server) — no code change required"
    - "Production Docker image HTTP serve path"
tech_stack:
  added: []
  updated:
    - "@hono/node-server: 1.19.14 → 2.0.2 (EXACT pin, no caret)"
  patterns_followed:
    - "Atomic dependency bump (1 commit, scoped to package.json + pnpm-lock.yaml)"
    - "Exact-pin convention for load-bearing runtime deps (CONTEXT.md §Established Patterns)"
    - "100% CI sampling (all four jobs green on push)"
key_files:
  created:
    - ".planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-07-SUMMARY.md"
  modified:
    - "package.json (line 33: \"@hono/node-server\": \"2.0.2\")"
    - "pnpm-lock.yaml (resolves @hono/node-server@2.0.2)"
decisions:
  - "Preserve exact-pin convention (no caret) — `@hono/node-server` is load-bearing runtime adapter per CONTEXT.md §Established Patterns"
  - "No source-code changes — v2 release notes explicitly state public API unchanged; src/roles/app.ts serve() call (line 106: `serve({ fetch, port }, callback)`) is API-compatible with v2"
  - "Skipped local pnpm test:integration (Windows host has no Postgres) — CI Postgres service container ran and passed; local typecheck + test:unit + build were the load-bearing local gates"
metrics:
  duration: "~11 minutes (645 seconds wall clock; ~7 minutes of that was CI wait)"
  tasks: 1
  files_changed: 2
  commits: 1
  completed: "2026-05-11"
---

# Phase 03.0.2 Plan 07: @hono/node-server 1→2 Summary

Bumped the Node HTTP runtime adapter from exact-pin `1.19.14` to exact-pin `2.0.2` in a single atomic commit; zero source-code changes required because v2's release notes explicitly preserve the public API (`serve()` signature, middleware shape, HTTP/2 default).

## What Shipped

**One commit:** `chore(deps): bump @hono/node-server from 1.19.14 to 2.0.2`
- SHA: `6b7ffc5855fba6218fb173dc9a4adc6f666a44d8`
- Files changed: `package.json` (1 line), `pnpm-lock.yaml` (12 lines)
- Zero changes to `src/`, `tests/`, or any code path

**Pre-bump audit confirmed:**
- Sole importer: `src/roles/app.ts` (line 15: `import { serve } from "@hono/node-server";`). Grep over `src/`, `tests/`, `scripts/` confirmed no other importers.
- Zero matches for the removed `@hono/node-server/vercel` Vercel adapter (RESEARCH.md §Commit 5 risk N/A).
- `serve()` call at `src/roles/app.ts:106` uses the `{ fetch, port }` + callback form — API-compatible with v2.

**Pin style preserved:** `"@hono/node-server": "2.0.2"` — no caret, matching the existing exact-pin convention for load-bearing runtime deps in `package.json` (`better-auth`, `drizzle-orm`, `pg`, `pg-boss`, `hono`, `paraglide-js` follow the same convention).

## Verification

**Local (Windows host):**
- `NODE_OPTIONS="--use-system-ca" pnpm add @hono/node-server@2.0.2 -E` succeeded
- `pnpm install --frozen-lockfile`: 0 errors
- `pnpm typecheck`: 0 errors, 54 pre-existing warnings (CRLF + svelte-state baseline)
- `pnpm exec eslint .`: 0 errors (lint failure is pure prettier CRLF baseline carried over from prior plans 01-06; non-blocking per `<prior_plan_context>`)
- `pnpm test:unit`: 324/324 passed
- `pnpm build`: production bundle compiled (`build/server.js` 309.60 KB)
- `pnpm test:integration`: skipped locally — Windows host has no Postgres on `127.0.0.1:5432`; CI ran them with the Postgres service container

**CI (run [25654931465](https://github.com/d954mas/neotolis-game-promotion-diary/actions/runs/25654931465)):**
- `lint-typecheck`: success
- `unit-integration`: success (Postgres service container — exercises every route booted via the same `serve()` path)
- `browser-tests`: success
- `smoke`: success — **the load-bearing gate** — production Docker image booted, `app` role under `@hono/node-server@2.0.2`'s `serve()` answered the full OAuth dance against `oauth2-mock-server` end-to-end

## Deviations from Plan

None — plan executed exactly as written.

The plan called for `pnpm lint` to exit 0; locally it returns exit 1 purely because of the pre-existing prettier CRLF baseline (125 files). Per the `<prior_plan_context>` instructions for this phase, that baseline is non-blocking, and CI's `lint-typecheck` job (which runs on Linux LF-normalized line endings) passed cleanly. No new lint failures specific to `@hono/node-server@2`.

## Decisions Made

1. **Preserve exact-pin (no caret)** — `@hono/node-server` is the Node HTTP runtime adapter, classified as a load-bearing runtime dep per CONTEXT.md §Established Patterns. The pin policy is "exact for load-bearing runtime, caret for dev tooling." `pnpm add ... -E` enforced this; verified with `grep -E '"@hono/node-server":\s*"2\.0\.2"' package.json` → match (no caret).

2. **No source changes** — RESEARCH.md §Commit 5 + v2 release notes both state "the public API stays the same." The smoke job is the runtime contract test — if `serve()` shape had drifted, smoke would have failed. It passed.

3. **Skip local integration tests** — same posture as prior commits 01-06 in this phase. Windows host has no Postgres; CI's Postgres service container is the integration gate. Local typecheck + test:unit + build were the load-bearing local gates.

## Self-Check: PASSED

- File exists: `.planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-07-SUMMARY.md` — FOUND
- Commit exists: `6b7ffc5` — FOUND (`git log --oneline -1` shows it on top of plan 06's pino commit)
- Pin landed exact (no caret): `package.json` line 33 = `"@hono/node-server": "2.0.2"` — verified
- Lockfile resolves 2.0.2: `pnpm-lock.yaml` `specifier: 2.0.2` — verified
- Zero source changes: `git diff HEAD~1 HEAD -- src/ | wc -l` = `0` — verified
- CI green on all four jobs (smoke is load-bearing): verified at run [25654931465](https://github.com/d954mas/neotolis-game-promotion-diary/actions/runs/25654931465)

## Progress

**7 of 10 atomic units of Phase 03.0.2 complete.** Remaining:
- Plan 08: TypeScript 5.6 → 6.0 (medium complexity — `noUncheckedSideEffectImports` audit)
- Plan 09: drizzle pair refresh (trivial — no major on `latest`)
- Plan 10: better-auth 1.6.9 → 1.6.10 (single patch; smoke is load-bearing for OAuth dance)
