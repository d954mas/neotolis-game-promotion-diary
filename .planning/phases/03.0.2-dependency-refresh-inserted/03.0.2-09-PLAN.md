---
phase: 03.0.2-dependency-refresh-inserted
plan: 09
type: execute
wave: 1
depends_on:
  - "03.0.2-08"
files_modified:
  - package.json
  - pnpm-lock.yaml
autonomous: true
requirements:
  - 03.0.2-C9
commit_label: "Commit 9 (per RESEARCH.md ordering — was D-04 row #9, drizzle pair) — Path A: no major on `latest`"
must_haves:
  truths:
    - "drizzle-orm@latest is re-verified at commit-time via `npm view drizzle-orm dist-tags` (RESEARCH.md valid-until 2026-05-25; re-verify if approaching expiry)"
    - "If drizzle-orm@latest is still 0.45.2 on commit-day, Path A applies: drizzle-orm stays at 0.45.2 (no-op re-pin), drizzle-kit caret bumped to ^0.31.10"
    - "If drizzle-orm@latest has flipped to 1.0.0 stable, STOP and surface to user before proceeding (Path B is large refactor — see RESEARCH.md §Open Question 1)"
    - "pnpm db:check exits 0 with NO diff after the bump (forward-only migration rule per D-08)"
    - "All drizzle queries in tests/integration pass on the new resolved versions"
    - "Generated migration files under drizzle/ are UNTOUCHED (no edits to merged migrations per AGENTS.md forward-only rule)"
    - "git history contains one commit for the drizzle pair refresh (whichever path applied)"
  artifacts:
    - path: "package.json"
      provides: "drizzle-orm + drizzle-kit pins after refresh (Path A: drizzle-orm unchanged exact-pin '0.45.2', drizzle-kit '^0.31.10')"
      contains: "drizzle"
    - path: "pnpm-lock.yaml"
      provides: "Lockfile snapshot for the drizzle pair"
  key_links:
    - from: "src/lib/server/db/schema/**/*.ts"
      to: "drizzle-orm 0.45.2 (or 1.0 under Path B — out of scope without user OK)"
      via: "import { pgTable, ... } from 'drizzle-orm/pg-core'"
      pattern: "from ['\"]drizzle-orm"
    - from: "drizzle.config.ts"
      to: "drizzle-kit 0.31.x"
      via: "config consumed by `drizzle-kit generate` / `check`"
      pattern: "drizzle-kit"
---

<objective>
Land the ninth atomic commit of Phase 03.0.2: the drizzle pair refresh — `drizzle-orm` + `drizzle-kit` together as one atomic unit per CONTEXT.md §D-05 (peer-dep pairing: they share generated SQL contract, must land together).

**Critical scope finding (RESEARCH.md Key Finding #1, verified 2026-05-11):**
- `drizzle-orm@latest = 0.45.2` (we are already on `0.45.2` exact-pin — NO major available on `latest` tag).
- `drizzle-kit@latest = 0.31.10` (we are on `^0.31.0` — caret-range patch available).
- `rc` tag is at `1.0.0-rc.2` — NOT `latest`.

Phase scope statement (CONTEXT.md §<domain>) says "Bump every remaining major-version dependency to its latest **stable**." Under strict reading, there is NO major bump available for drizzle-orm or drizzle-kit on `latest` as of 2026-05-11.

**Path A (recommended by RESEARCH.md):** Re-pin drizzle-orm `0.45.2` → `0.45.2` (no-op, documents the audit-trail entry that user wanted drizzle in scope per D-09); bump drizzle-kit `^0.31.0` → `^0.31.10` (caret-range patch refresh; lockfile updates). Behavior unchanged. D-08 forward-only migration rule trivially preserved (no SQL drift expected from a patch bump of drizzle-kit; verify via `pnpm db:check`).

**Path B (NOT recommended without user OK):** Land 1.0.0-rc.2 on both. Violates the phase scope ("latest **stable**"); introduces the Casing API breaking change across every schema file; requires refactor of `src/lib/server/db/schema/*.ts` and `src/lib/sources/*/server/schema/*.ts`. RESEARCH.md §Open Question 1 explicitly designates this as a re-ask-the-user decision: if `drizzle-orm@latest` has flipped from 0.45.2 to 1.0.0 stable during this phase's execution window (research valid-until 2026-05-25), STOP and surface to user.

**The executor's first action MUST be re-verifying `npm view drizzle-orm dist-tags`. If `latest` is no longer 0.45.2, treat this plan as INCONCLUSIVE and exit with a CHECKPOINT message rather than landing Path B unilaterally.**

Purpose: Honor user's D-09 intent ("обновить все зависимости" — drizzle in scope) by landing the audit-trail entry; preserve generated-SQL invariant; defer Path B to a future phase or explicit user decision.
Output (Path A): One commit `chore(deps): drizzle pair refresh — no major on latest` on `feat/phase-03.0.2-dependency-refresh`; pnpm db:check no-diff; CI green on all three jobs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-CONTEXT.md
@.planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-RESEARCH.md
@.planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-VALIDATION.md
@AGENTS.md
@package.json
@drizzle.config.ts

<interfaces>
<!-- Current pins (from package.json):
"drizzle-orm": "0.45.2"     (line 41, EXACT pin in dependencies)
"drizzle-kit": "^0.31.0"    (line 65, caret in devDependencies)

Target pins under Path A (RESEARCH.md):
"drizzle-orm": "0.45.2"     (unchanged — no major on latest)
"drizzle-kit": "^0.31.10"   (caret-range patch bump)

Path B (NOT recommended without user OK):
"drizzle-orm": "1.0.0-rc.2" (RC, not latest — would violate "latest stable" phase scope)
"drizzle-kit": "1.0.0-rc.2"
- Requires updating every src/lib/server/db/schema/**/*.ts for the Casing API change
- Requires updating every src/lib/sources/*/server/schema/**/*.ts likewise

D-08 invariant (forward-only migration rule, AGENTS.md): pnpm db:check MUST exit 0 with no diff after the bump. The `drizzle/` directory's existing migration files are TREATED AS HISTORY — never edited, never down-migrated.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify drizzle-orm dist-tag, then apply Path A (or pause for user if Path B is on the table)</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>
    - package.json (current pins: lines 41, 65)
    - drizzle.config.ts (FULL FILE — verify drizzle-kit's config shape; a patch bump of drizzle-kit MUST NOT require any config change. If 0.31.10 changed the config shape, the bump becomes minor-functional, not patch — re-evaluate.)
    - .planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-RESEARCH.md §"Commit 9 — drizzle pair" (Path A / Path B detailed comparison) AND §Open Question 1 (re-verify-before-commit mandate) AND §Pitfall 4 (warning: do NOT land RC unilaterally)
    - .planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-CONTEXT.md §D-08 (forward-only migrations — no edits to merged migration files) and §D-09 (drizzle is in scope per user intent) and §"Claude's Discretion" (planner picks path A vs path B; D-09 says "in scope" but doesn't say "land RC")
    - AGENTS.md §"Practices" → "Migrations forward-only, run at boot" and "Locked stack versions"
  </read_first>
  <action>
    Re-verification (MANDATORY first action per RESEARCH.md §Open Question 1):
    1. Confirm on branch + previous commit landed: `git log --oneline -1` MUST show the Plan 08 typescript commit.
    2. Re-verify drizzle-orm latest dist-tag at commit time:
       ```bash
       npm view drizzle-orm dist-tags
       ```
       Expected per RESEARCH.md (valid-until 2026-05-25): `latest: 0.45.2`, `rc: 1.0.0-rc.2` (or newer rc), `beta: 1.0.0-beta.X`.
       - **If `latest` is still `0.45.2`:** Path A applies — proceed to step 4.
       - **If `latest` has flipped to `1.0.0`** (stable): STOP. RESEARCH.md says: "surface to user before proceeding." Halt this task, emit a CHECKPOINT message describing the situation, and wait for explicit user direction (land 1.0.0 stable with the full Casing API refactor in this phase, OR defer drizzle to a follow-up phase). DO NOT land Path B unilaterally.
    3. Same re-verification for drizzle-kit:
       ```bash
       npm view drizzle-kit dist-tags
       ```
       Expected: `latest: 0.31.10` (or newer 0.31.x). Capture the exact `latest` value for the commit body.

    Apply Path A:
    4. drizzle-orm — re-pin at the same version (this is the audit-trail "we considered it" entry):
       ```bash
       # No package.json edit needed if the pin is already "0.45.2"; explicitly re-pin to make the intent visible:
       pnpm add drizzle-orm@0.45.2
       ```
       Verify: `grep -E '"drizzle-orm":\s*"0\.45\.2"' package.json` exits 0 (EXACT pin preserved, no caret).
    5. drizzle-kit — caret bump:
       ```bash
       pnpm add -D drizzle-kit@^0.31.10
       ```
       (Caret per devDeps convention; resolved version moves from whatever lockfile had to the latest 0.31.x.)
    6. `pnpm install --frozen-lockfile` MUST exit 0.

    D-08 verification (forward-only migrations):
    7. ```bash
       pnpm db:check
       ```
       MUST exit 0 with no diff. `db:check` runs `drizzle-kit check`, which compares the committed migration files against the schema and asserts no drift. If db:check reports drift:
       - Inspect the diff. If drizzle-kit 0.31.10 patches a tiny SQL formatting concern that doesn't change semantics, AGENTS.md says: "any SQL drift produces a NEW migration file, never edits to merged ones." Generate a new migration via `pnpm db:generate` and verify it's semantically a no-op (e.g. just a regenerated checksum, no actual DDL). If it's a true no-op, the new file lands in the same commit.
       - If db:check reports an actual schema mutation (DDL difference), this is a P0 — surface to user. The patch bump should not introduce a schema mutation.
    8. Confirm no edits to existing migration files:
       ```bash
       git diff HEAD~1 HEAD -- drizzle/ | grep -E "^---|^\+\+\+" | grep -vE "^\+\+\+ b/drizzle/" | head -5
       ```
       Expected: zero lines showing "deleted" or "modified" existing files; only NEW files appearing under `drizzle/` are acceptable.

    Full local verification:
    9. ```bash
       pnpm lint
       pnpm typecheck
       pnpm test:unit
       pnpm test:integration
       ```
       All MUST exit 0. `test:integration` exercises real Drizzle queries against the Postgres service container — proves runtime behavior is unchanged.

    Commit:
    10. `git add package.json pnpm-lock.yaml` (+ any new `drizzle/*.sql` file from step 7 IF a no-op regen was needed).
    11. Commit:
       ```bash
       git commit -m "$(cat <<'EOF'
chore(deps): drizzle pair refresh — no major on latest

CONTEXT.md D-09 brought drizzle-orm + drizzle-kit IN scope after user
push-back on the "out of scope" classification. RESEARCH.md verified
on 2026-05-11 that there is NO major-version bump available on the
`latest` dist-tag for either package:
- drizzle-orm@latest  = 0.45.2 (we are already on 0.45.2 exact-pin)
- drizzle-kit@latest  = 0.31.10 (we were on ^0.31.0)
- rc tag is at 1.0.0-rc.2 — NOT `latest`; the phase scope says
  "latest stable" so RCs are out.

Re-verified at commit time: `npm view drizzle-orm dist-tags` →
latest: [INSERT today's latest value]
`npm view drizzle-kit dist-tags` →
latest: [INSERT today's latest value]

Changes:
- drizzle-orm:  0.45.2 → 0.45.2 (re-pin; documents the in-scope audit entry)
- drizzle-kit:  ^0.31.0 → ^0.31.10 (caret-range patch refresh; lockfile updates)

D-08 forward-only migration rule preserved: pnpm db:check passes with
no diff (schema unchanged; no new migration files needed). All Drizzle
queries in tests/integration continue to pass.

Trigger for follow-up: when drizzle-orm 1.0.0 stable releases to the
`latest` dist-tag, schedule a follow-up dep-refresh phase covering the
Casing API breaking change (every schema file in src/lib/server/db/schema/
and src/lib/sources/*/server/schema/ would need the snakeCase/camelCase
import adjustment).
EOF
       )"
       ```
    12. `git push origin feat/phase-03.0.2-dependency-refresh`. Wait for CI green before Plan 10.
  </action>
  <verify>
    <automated>
      # Path A confirmed at commit time:
      npm view drizzle-orm dist-tags 2>&1 | grep -E "^\s*latest:" | grep -qE "['\"]?0\.45\."
      # drizzle-orm pin unchanged exact:
      grep -E '"drizzle-orm":\s*"0\.45\.2"' package.json
      # drizzle-kit caret bumped:
      grep -E '"drizzle-kit":\s*"\^0\.31\.[0-9]+"' package.json
      grep -q "drizzle-kit@0\.31\." pnpm-lock.yaml
      pnpm install --frozen-lockfile
      pnpm db:check      # D-08 invariant — no-diff
      pnpm lint && pnpm typecheck && pnpm test:unit && pnpm test:integration
      git log -1 --format=%s | grep -qE '^chore\(deps\): drizzle pair'
      # Confirm no edits to merged migration files (only additions allowed):
      git diff HEAD~1 HEAD -- drizzle/ --name-status | grep -vE "^A\s" | grep -vE "^$" | wc -l  # MUST output "0"
    </automated>
  </verify>
  <acceptance_criteria>
    - First action `npm view drizzle-orm dist-tags` confirmed `latest = 0.45.x` (Path A). If it returned `1.0.x`, this plan SHOULD have halted with a checkpoint instead of proceeding — verify by inspecting the executor's pre-flight log.
    - `grep -E '"drizzle-orm":\s*"0\.45\.2"' package.json` exits 0 (EXACT pin, no caret).
    - `grep -E '"drizzle-kit":\s*"\^0\.31\.[0-9]+"' package.json` exits 0.
    - `pnpm install --frozen-lockfile` exits 0.
    - `pnpm db:check` exits 0 with NO diff (D-08 invariant — this is the load-bearing gate for this bump).
    - `pnpm test:integration` exits 0.
    - `git diff HEAD~1 HEAD -- drizzle/ --name-status | grep -vE "^A\s|^$" | wc -l` outputs `0` (no edits/deletes to merged migration files; only `A` additions allowed, and zero additions is also acceptable if no new migration was needed).
    - `git log -1 --format=%s` matches `^chore\(deps\): drizzle pair`.
    - After push: all three CI jobs `success` — `unit-integration` job in particular includes drizzle-query-touching tests.
  </acceptance_criteria>
  <done>
    Branch has commit `chore(deps): drizzle pair refresh — no major on latest`. drizzle-orm re-pinned at 0.45.2 (audit-trail). drizzle-kit caret bumped to ^0.31.10. `pnpm db:check` no-diff. No edits to merged migration files. CI green on push.

    OR (if dist-tag re-verification revealed drizzle-orm 1.0 stable shipped during execution window): this plan emitted a CHECKPOINT and is NOT marked done; user direction is awaited before any drizzle commit lands.
  </done>
</task>

</tasks>

<verification>
1. `git log feat/phase-03.0.2-dependency-refresh --oneline | head -9` shows the new drizzle commit on top of Plans 01-08.
2. CI on the latest commit: all three jobs green.
3. `pnpm db:check` locally still no-diff — confirms D-08 forward-only migration invariant.
4. `npm view drizzle-orm dist-tags` re-confirmation value recorded in SUMMARY.md.
</verification>

<success_criteria>
- drizzle-orm re-pinned at 0.45.2 (Path A).
- drizzle-kit caret bumped to ^0.31.10.
- D-08 invariant: pnpm db:check no-diff.
- No edits to merged migration files.
- 9 of 10 atomic units complete.

OR: explicit user-acknowledged CHECKPOINT for Path B (drizzle 1.0 stable surfaced; phase awaits direction).
</success_criteria>

<output>
After completion, create `.planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-09-SUMMARY.md` recording: commit SHA, the verbatim output of `npm view drizzle-orm dist-tags` at commit time, both resolved versions in pnpm-lock.yaml, `pnpm db:check` exit code, CI run URL, and explicit confirmation that NO merged migration files were edited.
</output>
