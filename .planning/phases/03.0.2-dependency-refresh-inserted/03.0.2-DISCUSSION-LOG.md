# Phase 03.0.2: Dependency Refresh - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-11
**Phase:** 03.0.2-dependency-refresh-inserted
**Areas discussed:** PR strategy, Grouping, Risk handling, Scope

---

## Gray area selection

Four areas presented; user selected ALL four (Стратегия PR, Группировка бампов, Обработка сложных бампов, Уточнить скоуп).

---

## Area 1 — PR strategy

| Option | Description | Selected |
|--------|-------------|----------|
| 1 phase-PR, N коммитов (Recommended) | feat/phase-03.0.2 branch → atomic commit per major/group with green CI on each, single final squash-PR. Aligns with memory; preserves AGENTS.md spirit. | ✓ |
| N отдельных PR-ов | Each bump its own branch + PR. Violates memory "1 phase = 1 PR" rule. | |
| 1 PR, 1 коммит | Everything in one massive commit. Hides which dep broke if CI fails. | |

**User's choice:** Option 1 — single phase-PR with N atomic commits inside.
**Notes:** Resolves the AGENTS.md "Locked stack versions" vs memory "1 phase = 1 PR" tension by treating per-major atomicity at the commit level, not the PR level.

---

## Area 2 — Grouping

| Option | Description | Selected |
|--------|-------------|----------|
| Family grouping (Recommended) | Peer-dep pairs together (eslint-plugin-svelte + svelte-eslint-parser; pino + pino-pretty; ESLint core family). Standalone for the rest. ~8 commits. | ✓ |
| Strict 1 dep = 1 коммит | Each package its own commit, even when peer-dep forces WIP commits with broken lint-typecheck. Violates D-02 (CI green per commit). | |
| Looser: 1 коммит = 1 область | Grouped by function (lint stack, logging, misc). 3-4 commits. Mixes TS6 with ESLint — harder to bisect. | |

**User's choice:** Family grouping.
**Notes:** Final commit count becomes ~10 after scope expansion in Area 4.

---

## Area 3 — Risk handling

| Option | Description | Selected |
|--------|-------------|----------|
| Best-effort, defer hard bumps to follow-up phase (Recommended) | If a bump requires >30 min of code rework, roll back and create phase 03.0.3 for it. Keeps phase scoped. | |
| Довести всё в этой фазе любой ценой | Fix whatever the bumps surface, even TS6 50+ errors. Phase doesn't close until all bumps land. Risk: phase may stretch to days; blocks Phase 3.1. | ✓ |
| Skip and document in ROADMAP | If a bump doesn't go cleanly, roll back and note "deferred" — no new phase. Risk: tech debt smears, may be forgotten. | |

**User's choice:** "Довести всё в этой фазе любой ценой" (complete every bump in this phase no matter the effort).
**Notes:** Combined with the Area 4 scope expansion this raises the worst-case effort substantially (drizzle/better-auth additions to the must-land list), but the user explicitly accepted that trade-off.

---

## Area 4 — Scope confirmation

| Option | Description | Selected |
|--------|-------------|----------|
| Issue #23 final (Recommended) | 13 packages, pg-boss 12 already done. drizzle/better-auth/pg/svelte/hono explicitly OOS per ROADMAP. | |
| Add minor/patch refresh of remaining | After 13 majors, additional chore(deps): minor+patch refresh commit. Plus: clean lockfile. Minus: scope creep. | |
| Add better-auth/drizzle/pg/hono bumps | Lift the OOS pins. Plus: complete refresh. Minus: each is its own phase by complexity. | |
| **Other (free text)** — "Задача обнвоитьв се зависимости. почему что-то стало out of scope" | User questioned the OOS classification — pushed for "update ALL deps". | ✓ |

**User's choice:** Free-text — "Задача обновить все зависимости. почему что-то стало out of scope" (translation: "task is to update all deps. why was something marked out of scope").

**Follow-up dialogue:** Claude explained the OOS rationale for each pin (svelte/hono/pg already on latest major; drizzle pre-1.0 + every bump touches schema; better-auth recently shipped to prod + touches OAuth boundary). Offered four sub-options:
1. Keep issue #23 scope (safe)
2. Expand to 15 — add drizzle pair + better-auth
3. Add only better-auth
4. Add only drizzle pair

**User's choice (sub-option):** 2 — expand to 15 (drizzle-orm + drizzle-kit + better-auth IN; svelte/hono/pg remain OOS because already on latest major).

**Notes:** D-09 captures this expansion with the rationale; D-08 captures the forward-only migration invariant for drizzle; D-07 captures special-vigilance items for the three load-bearing bumps.

---

## Closing question

| Option | Description | Selected |
|--------|-------------|----------|
| Пиши CONTEXT.md (Recommended) | Accepted decisions are enough for researcher + planner to break into waves. | ✓ |
| Порядок коммитов | Lock sequencing here vs leave to planner. | |
| Критерии верификации | Specify CI vs CI+local vs CI+manual UAT per commit. | |
| Обновить AGENTS.md | Edit "Locked stack versions" practice text now. | |

**User's choice:** Write CONTEXT.md — sequencing and verification details delegated to planner.

---

## Claude's Discretion

- Order of the 10 commits inside the branch (sequencing strategy).
- Whether to use single-commit atomic units or atomic-unit-as-N-commits (e.g., `bump` + `fix-up`).
- Per-bump PR body rationale wording.
- Whether to land an AGENTS.md "Locked stack versions" clarification commit at end of phase.

## Deferred Ideas

- AGENTS.md "Locked stack versions" practice text update (Claude's discretion — last commit of phase, or follow-up PR).
- `chore(deps): minor+patch refresh` follow-up PR (separate from this phase).
- Renovate/dependabot setup for ongoing minor/patch refresh (separate phase or backlog).
- 25 UI/architecture todos surfaced by `gsd-tools todo match-phase` — none relevant to dep refresh, all listed in CONTEXT.md `<deferred>` so they don't re-surface.
