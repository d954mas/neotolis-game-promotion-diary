---
phase: 03.0.2-dependency-refresh-inserted
plan: 07
type: execute
wave: 1
depends_on:
  - "03.0.2-06"
files_modified:
  - package.json
  - pnpm-lock.yaml
autonomous: true
requirements:
  - 03.0.2-C7
commit_label: "Commit 7 (per RESEARCH.md ordering — was D-04 row #5, @hono/node-server)"
must_haves:
  truths:
    - "package.json declares @hono/node-server at exact-pin 2.x (matches existing exact-pin convention for load-bearing runtime deps)"
    - "pnpm-lock.yaml resolves @hono/node-server to a 2.x version"
    - "src/roles/app.ts (sole importer) still boots the production server unchanged"
    - "smoke CI job passes — production-image HTTP serve still works via serve()"
    - "tests/integration green (route integration tests boot via the same serve path)"
    - "git history contains one commit (or one + fix-up) for the @hono/node-server bump"
  artifacts:
    - path: "package.json"
      provides: "Updated dependency pin for @hono/node-server (exact, no caret)"
      contains: "@hono/node-server"
    - path: "pnpm-lock.yaml"
      provides: "Lockfile snapshot for @hono/node-server 2.x tree"
  key_links:
    - from: "src/roles/app.ts"
      to: "@hono/node-server@2 serve() function"
      via: "import { serve } from '@hono/node-server'"
      pattern: "from ['\"]@hono/node-server['\"]"
---

<objective>
Land the seventh atomic commit of Phase 03.0.2: bump `@hono/node-server` from exact-pin `1.19.14` to exact-pin `2.0.2`.

Per RESEARCH.md §"Commit 5", this is a TRIVIAL bump: v2's only documented breaking changes are dropping Node 18 (we're on 22 — safe) and removing the `@hono/node-server/vercel` Vercel adapter (we don't import this — verified by grep). The release notes explicitly state "the public API stays the same": no change to `serve()` signature, middleware shape, or HTTP/2 default. The smoke CI job exercises the full HTTP path through `serve()` on the production image; if the contract changed, smoke fails.

Pin style: `@hono/node-server` is currently pinned EXACT (no caret) at `"1.19.14"` per the existing load-bearing-runtime-deps convention (CONTEXT.md §"Established Patterns"). Preserve the exact-pin convention — bump to `"2.0.2"` (no caret).

Purpose: Bring the Node HTTP runtime adapter to its latest stable; preserve exact-pin convention for load-bearing runtime deps; preserve self-host parity via the smoke gate.
Output: One commit `chore(deps): bump @hono/node-server from 1.19.14 to 2.0.2`; CI green on all three jobs (smoke is the load-bearing gate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-CONTEXT.md
@.planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-RESEARCH.md
@.planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-VALIDATION.md
@AGENTS.md
@package.json
@src/roles/app.ts

<interfaces>
<!-- Current pin (from package.json line 33):
"@hono/node-server": "1.19.14"  (EXACT, no caret — load-bearing runtime adapter)

Target pin (RESEARCH.md table line 138):
"@hono/node-server": "2.0.2"    (EXACT, preserve convention)

Breaking changes (RESEARCH.md §"Commit 5"):
- Drops Node 18 support, requires Node 20+. We're on Node 22 — safe.
- Removes @hono/node-server/vercel Vercel adapter. We don't import (verified via grep — RESEARCH.md).
- Public API stays the same — release notes explicit.

Sole importer: src/roles/app.ts.
Verify pre-bump that this is the ONLY file importing @hono/node-server:
  grep -rn "from ['\"]@hono/node-server" src/ tests/
Expected: matches limited to src/roles/app.ts (per RESEARCH.md). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump @hono/node-server 1→2 (preserve exact-pin) and commit</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>
    - package.json (current pin: `"@hono/node-server": "1.19.14"` — line 33; note the EXACT pin style, no caret)
    - src/roles/app.ts (the sole importer — verify the `import { serve } from '@hono/node-server'` shape, the `serve(...)` call signature, and any options passed — all UNCHANGED in v2 per release notes)
    - .planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-RESEARCH.md §"Commit 5 — @hono/node-server 1 → 2"
    - .planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-CONTEXT.md §"Established Patterns" → package.json pin style (load-bearing runtime libs stay EXACT pinned)
    - AGENTS.md §"Practices" → "Locked stack versions"
  </read_first>
  <action>
    Pre-bump audit:
    1. Confirm on branch + previous commit landed: `git log --oneline -1` MUST show the Plan 06 pino commit.
    2. Verify sole-importer claim:
       ```bash
       grep -rn "from ['\"]@hono/node-server['\"]" src/ tests/ scripts/
       ```
       Expected: matches ONLY in src/roles/app.ts. If new importers surface (e.g. from a worker/scheduler role added since RESEARCH.md), each one's import + usage shape MUST be reviewed against v2's documented surface (still `serve()`-shaped).
    3. Confirm we don't import the removed Vercel adapter:
       ```bash
       grep -rn "@hono/node-server/vercel" src/ tests/ scripts/
       ```
       Expected: zero matches. If non-zero, the bump's surface is larger — surface to user.

    Perform the bump (EXACT pin — drop the caret):
    4. `pnpm add @hono/node-server@2.0.2` (no `-D` flag — `@hono/node-server` is in `dependencies`; no caret on the version spec to preserve exact-pin convention).
    5. Verify the pin landed exact (not with caret):
       ```bash
       grep -E '"@hono/node-server":\s*"[^"]+"' package.json
       ```
       Expected: `"@hono/node-server": "2.0.2"` (NO caret). If pnpm added a caret, edit `package.json` manually to remove it, then `pnpm install --frozen-lockfile` to re-lock.
    6. `pnpm install --frozen-lockfile` MUST exit 0.

    Run local verification:
    7. ```bash
       pnpm lint
       pnpm typecheck
       pnpm test:unit
       pnpm test:integration
       pnpm build      # production build — verifies tsup/vite still bundle correctly
       ```
       All MUST exit 0. The `pnpm build` step is included here because @hono/node-server is in the production runtime path; a build break would surface before smoke.

    Commit:
    8. `git add package.json pnpm-lock.yaml`. NO source files should be touched.
    9. Commit:
       ```bash
       git commit -m "$(cat <<'EOF'
chore(deps): bump @hono/node-server from 1.19.14 to 2.0.2

v2 drops Node 18 support (we are on Node 22 — safe) and removes the
@hono/node-server/vercel Vercel adapter (we don't import this — verified
via grep). v2 release notes explicitly state the public API stays the
same: serve() signature, middleware shape, HTTP/2 default — all
unchanged. src/roles/app.ts (sole importer) needs no code change.

Pin style preserved: exact "2.0.2" (no caret) per the existing
load-bearing-runtime-deps convention in package.json.

Verification: pnpm lint + typecheck + test:unit + test:integration + build
all pass locally. Smoke CI job is the load-bearing gate — production
image's HTTP serve path is exercised against oauth2-mock-server end-to-end.
EOF
       )"
       ```
    10. `git push origin feat/phase-03.0.2-dependency-refresh`. Wait for CI green before Plan 08.
  </action>
  <verify>
    <automated>
      # Pin landed EXACT (no caret):
      grep -E '"@hono/node-server":\s*"2\.0\.2"' package.json
      # No caret on this specific package:
      grep -E '"@hono/node-server":\s*"\^' package.json | wc -l  # MUST output "0"
      grep -q "@hono/node-server@2\." pnpm-lock.yaml
      # No source changes leaked:
      git diff HEAD~1 HEAD -- src/ | wc -l  # MUST output "0"
      pnpm install --frozen-lockfile
      pnpm lint && pnpm typecheck && pnpm test:unit && pnpm test:integration
      pnpm build
      git log -1 --format=%s | grep -qE '^chore\(deps\): bump @hono/node-server'
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '"@hono/node-server":\s*"2\.0\.2"' package.json` exits 0 (EXACT pin, no caret).
    - `grep -q "@hono/node-server@2\." pnpm-lock.yaml` exits 0.
    - `pnpm install --frozen-lockfile` exits 0.
    - `pnpm lint` exits 0.
    - `pnpm typecheck` exits 0.
    - `pnpm test:integration` exits 0 (route integration tests boot via the same `serve()` path).
    - `pnpm build` exits 0 (production bundle compiles).
    - `git diff HEAD~1 HEAD -- src/ | wc -l` outputs `0` (zero source-code changes).
    - `git diff HEAD~1 HEAD --stat` shows ONLY `package.json` + `pnpm-lock.yaml` changed.
    - `git log -1 --format=%s` matches `^chore\(deps\): bump @hono/node-server`.
    - After push: all three CI jobs `success` — `smoke` job in particular MUST pass (it boots the production image and exercises serve()).
  </acceptance_criteria>
  <done>
    Branch has commit `chore(deps): bump @hono/node-server from 1.19.14 to 2.0.2`. Exact-pin convention preserved. Zero source changes. Smoke CI green on push.
  </done>
</task>

</tasks>

<verification>
1. `git log feat/phase-03.0.2-dependency-refresh --oneline | head -7` shows the new @hono/node-server commit on top of Plans 01-06.
2. CI on the latest commit: all three jobs green; smoke is the load-bearing assertion for this bump.
3. Spot-check the pin style: `grep '"@hono/node-server"' package.json` shows NO caret.
</verification>

<success_criteria>
- @hono/node-server on exact-pin 2.0.2.
- Smoke CI green.
- Pin convention preserved (load-bearing runtime dep = exact pin).
- 7 of 10 atomic units complete.
</success_criteria>

<output>
After completion, create `.planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-07-SUMMARY.md` recording: commit SHA, CI run URL (smoke job link), and confirmation that exact-pin (no caret) convention was preserved.
</output>
