---
phase: 03.0.2-dependency-refresh-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
autonomous: true
requirements:
  - 03.0.2-C1
commit_label: "Commit 1 (per RESEARCH.md ordering — was D-04 row #8)"
must_haves:
  truths:
    - "package.json declares @types/supertest at ^7.x (latest stable)"
    - "pnpm-lock.yaml resolves @types/supertest to a 7.x version"
    - "pnpm typecheck exits 0 after the bump (zero callers but tsc still scans)"
    - "git history contains one commit with subject ^chore\\(deps\\): bump @types/supertest"
  artifacts:
    - path: "package.json"
      provides: "Updated devDependencies pin for @types/supertest"
      contains: "@types/supertest"
    - path: "pnpm-lock.yaml"
      provides: "Lockfile snapshot matching the new package.json pin"
  key_links:
    - from: "package.json devDependencies['@types/supertest']"
      to: "pnpm-lock.yaml @types/supertest entry"
      via: "pnpm install --frozen-lockfile"
      pattern: "@types/supertest@7\\."
---

<objective>
Land the first atomic commit of Phase 03.0.2: bump `@types/supertest` from ^6.0.0 to ^7.2.0.

This is the lowest-risk commit and is intentionally first per RESEARCH.md Pattern 1 — a type-only dev dependency with ZERO callers in the codebase (verified via grep). Landing this first validates the per-commit CI pipeline (lint-typecheck + unit-integration + smoke all green) on the cheapest possible change before riskier commits follow.

Purpose: Ship the phase's first atomic unit; prove the CI gating shape works end-to-end on the branch before any real code surface is touched.
Output: One commit `chore(deps): bump @types/supertest from ^6.0.0 to ^7.2.0` on `feat/phase-03.0.2-dependency-refresh`; package.json + pnpm-lock.yaml updated; CI green on all three jobs (lint-typecheck, unit-integration, smoke).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-CONTEXT.md
@.planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-RESEARCH.md
@.planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-VALIDATION.md
@AGENTS.md
@package.json

<interfaces>
<!-- Current pin (from package.json line 59): -->
"@types/supertest": "^6.0.0"

<!-- Target pin (RESEARCH.md table line 141): -->
"@types/supertest": "^7.2.0"

<!-- Verified surface: ZERO `import supertest` callers exist in src/ or tests/.
     `supertest` (runtime) and `@types/supertest` (types) are stranded deps
     from earlier phases — see RESEARCH.md commit 8 section "Code-touch
     surface area" and Open Question 4. This commit follows the strict
     reading of D-04: bump only. Defer "remove stranded supertest" to a
     follow-up PR; flag in the phase PR body. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump @types/supertest 6→7 and commit</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>
    - package.json (current pin: `"@types/supertest": "^6.0.0"` — line 59)
    - .planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-RESEARCH.md §"Commit 8 — @types/supertest 6 → 7" (zero-callers verification, strict-bump-only contract)
    - .planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-CONTEXT.md §D-04 (canonical commit list) and §D-09 (no escape hatch on bumps in scope)
    - AGENTS.md §"Practices" → "Locked stack versions" + "Conventional Commits" (per-bump commit shape `chore(deps): bump X from A to B`)
  </read_first>
  <action>
    Pre-bump audit (must pass before touching package.json):
    1. Confirm on branch: `git rev-parse --abbrev-ref HEAD` MUST output `feat/phase-03.0.2-dependency-refresh`. If not on that branch, abort and surface the mismatch.
    2. Confirm zero callers — run the grep audit verbatim:
       ```bash
       grep -rn "from ['\"]supertest['\"]\|require(['\"]supertest['\"]" src tests 2>&1
       ```
       Expected: zero matches. If non-zero, STOP and re-read RESEARCH.md §Open Question 4.
    3. Confirm working tree clean for `package.json` + `pnpm-lock.yaml`: `git status --porcelain package.json pnpm-lock.yaml` MUST be empty.

    Perform the bump:
    4. `pnpm add -D @types/supertest@^7.2.0` (use caret per existing devDeps convention; @types/supertest is dev tooling, not a load-bearing runtime lib, so caret-range is correct per CONTEXT.md §"Established Patterns" → package.json pin style).
    5. Verify lockfile drift: `pnpm install --frozen-lockfile` MUST exit 0 (proves the lockfile matches the new package.json without needing another resolve).
    6. Run local verification (D-02 mandate — green per commit before push):
       ```bash
       pnpm lint
       pnpm typecheck
       pnpm test:unit
       ```
       All three MUST exit 0. If any fails, the failure is NOT a "next commit will fix it" — diagnose and amend before push.

    Commit:
    7. Stage exactly `package.json` and `pnpm-lock.yaml` (nothing else): `git add package.json pnpm-lock.yaml`.
    8. Commit using a HEREDOC so the body survives multi-line formatting:
       ```bash
       git commit -m "$(cat <<'EOF'
chore(deps): bump @types/supertest from ^6.0.0 to ^7.2.0

@types/supertest 7 tracks supertest 7 (already pinned at ^7.0.0 since
Phase 1). v7 is a type-only package; v7 has no documented runtime changes
beyond Node-version drops which we satisfy via engines.node ">=22.12".

Note for follow-up: both `supertest` and `@types/supertest` have ZERO
import references in src/ or tests/ (stranded deps from earlier phases).
Consider a follow-up `chore(deps): drop unused supertest` PR. Out of
scope for this phase per D-04 strict-reading.

Verification: pnpm lint + pnpm typecheck + pnpm test:unit pass locally;
CI green on lint-typecheck, unit-integration, smoke.
EOF
       )"
       ```
    9. Push to origin: `git push origin feat/phase-03.0.2-dependency-refresh`. CI runs automatically on push.
  </action>
  <verify>
    <automated>
      # 1. package.json shows the new pin
      grep -q '"@types/supertest": "\^7\.' package.json
      # 2. pnpm-lock.yaml resolves @types/supertest to a 7.x version
      grep -q "'@types/supertest@7\." pnpm-lock.yaml || grep -q '"@types/supertest@7\.' pnpm-lock.yaml
      # 3. lockfile is in sync with package.json
      pnpm install --frozen-lockfile
      # 4. lint + typecheck pass
      pnpm lint && pnpm typecheck
      # 5. commit subject matches Conventional Commits per-bump shape
      git log -1 --format=%s | grep -qE '^chore\(deps\): bump @types/supertest'
      # 6. CI must be green on the resulting commit (manual check after push):
      #    `gh run list --branch feat/phase-03.0.2-dependency-refresh --limit 1 --json conclusion --jq '.[0].conclusion'`
      #    MUST output "success" before the next plan starts.
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"@types/supertest": "\^7\.' package.json` exits 0.
    - `grep -q "'@types/supertest@7\." pnpm-lock.yaml` OR `grep -q '"@types/supertest@7\.' pnpm-lock.yaml` exits 0 (pnpm 9 lockfile format).
    - `pnpm install --frozen-lockfile` exits 0 with no lockfile diff.
    - `pnpm lint` exits 0.
    - `pnpm typecheck` exits 0.
    - `git log -1 --format=%s` matches regex `^chore\(deps\): bump @types/supertest from \^?6\.[0-9.]+ to \^?7\.[0-9.]+$` (date-flexible).
    - `git diff HEAD~1 HEAD --stat` shows exactly two files changed: `package.json` and `pnpm-lock.yaml` (no test edits, no source edits).
    - After push: `gh run list --branch feat/phase-03.0.2-dependency-refresh --limit 1 --json conclusion --jq '.[0].conclusion'` outputs `success`.
  </acceptance_criteria>
  <done>
    Branch has one new commit `chore(deps): bump @types/supertest from ^6.0.0 to ^7.2.0`. package.json + pnpm-lock.yaml updated. Local lint + typecheck pass. CI green on lint-typecheck + unit-integration + smoke jobs after push. No source-code edits leaked into the commit.
  </done>
</task>

</tasks>

<verification>
After the task completes:
1. `git log feat/phase-03.0.2-dependency-refresh ^master --oneline` shows exactly one new commit (this one) since the branch diverged from master (or one more than what existed pre-plan if the branch already had earlier commits).
2. CI run on the latest commit is green on all three jobs: `lint-typecheck`, `unit-integration`, `smoke`. Verify via: `gh run list --branch feat/phase-03.0.2-dependency-refresh --limit 1`.
3. No code-surface edits — only `package.json` + `pnpm-lock.yaml` changed.
</verification>

<success_criteria>
- One atomic commit on `feat/phase-03.0.2-dependency-refresh` bumping `@types/supertest` to ^7.2.0.
- CI green on the commit (all three jobs).
- No leaked edits to source or tests in this commit.
- Phase budget on track: 1 of 10 atomic units complete.
</success_criteria>

<output>
After completion, create `.planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-01-SUMMARY.md` recording: commit SHA, exact pre-bump and post-bump versions from the lockfile, CI run URL, and the stranded-deps observation flagged for the phase PR body.
</output>
