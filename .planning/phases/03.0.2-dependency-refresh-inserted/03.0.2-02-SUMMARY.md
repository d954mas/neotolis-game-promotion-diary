---
phase: 03.0.2-dependency-refresh-inserted
plan: 02
subsystem: infra
tags: [dependencies, pnpm, oauth2-mock-server, smoke, ci]

# Dependency graph
requires:
  - phase: 03.0.2-dependency-refresh-inserted
    provides: "Plan 01 (@types/supertest 6→7) landed; CI pipeline validated; draft PR #26 open; Windows-host pnpm/Prettier workarounds documented"
provides:
  - "oauth2-mock-server pinned at ^8.2.2 (was ^7.2.0); lockfile resolves oauth2-mock-server@8.2.2 (was 7.2.1)"
  - "Smoke job (D-07 load-bearing OAuth-dance assertion) green on the fresh mock-server version — proves the genericOAuth round-trip survives the v7→v8 jump before better-auth lands in plan 10"
  - "Lockfile delta: 293 lines rewritten / -35 net lines — v8's transitive dep tree is smaller than v7's"
affects: [03.0.2-03, 03.0.2-04, 03.0.2-05, 03.0.2-06, 03.0.2-07, 03.0.2-08, 03.0.2-09, 03.0.2-10]

# Tech tracking
tech-stack:
  added: ["oauth2-mock-server@8.2.2 (replaces 7.2.1) — Node 18 dropped, Universal ESM; OAuth/OIDC contract unchanged"]
  patterns: ["Per-commit CI gate validated on a contract-bearing dep (smoke job is the assertion, not just lint/typecheck)"]

key-files:
  created: []
  modified:
    - package.json
    - pnpm-lock.yaml

key-decisions:
  - "No source code edits required. v8's release notes promise the public OAuth/OIDC surface (issuer URL, discovery, token, JWKS, OAuth2Server class, service event names beforeUserinfo/beforeTokenSigning) is unchanged from v7; all four callers (tests/smoke/lib/oauth-mock-driver.ts, tests/setup/oauth.ts, scripts/dev-mock-oauth.mjs, dev-mock-oauth.mjs at repo root) typecheck without modification AND the smoke job — which actually drives the live OAuth dance — passed on first try. Strict-bump-only contract per D-04 honored: the atomic unit is exactly the dep pin change."
  - "Smoke job is the load-bearing assertion per CONTEXT.md D-07. It boots oauth2-mock-server@8.2.2 inside the production Docker image, drives Better Auth's genericOAuth dance (discovery → authorize → token → userinfo), and asserts the session cookie lands. Smoke green on run 25652696679 → v8's wire-level contract is identical to v7's for our usage pattern."
  - "Reused Plan 01's two Windows-host workarounds (NODE_OPTIONS=--use-system-ca for pnpm registry access, prettier --write before staging for CRLF normalization) — no rediscovery, no new deviations. Confirms the workarounds are stable for the remaining 8 commits in the phase."

patterns-established:
  - "Plan 01's pre-flight gate replays cleanly: (a) confirm branch, (b) confirm previous commit landed, (c) confirm clean working tree, (d) grep callers to assert the API surface we use survives the bump. Took ~30s and caught nothing surprising — the gate is cheap insurance."
  - "Lockfile shrinking on dep bumps is a positive signal — fewer transitive deps in v8 (net -35 lines) means a smaller attack/dependency surface. Worth tracking in summary for the phase-PR body when it goes for review."

requirements-completed: [03.0.2-C2]

# Metrics
duration: ~8min
completed: 2026-05-11
---

# Phase 03.0.2 Plan 02: oauth2-mock-server 7 → 8 Summary

**`oauth2-mock-server` bumped from ^7.2.0 to ^8.2.2 in one atomic commit `bcdd610`; smoke job (the load-bearing OAuth-dance assertion per D-07) green on first try — proves Better Auth's genericOAuth round-trip is intact under the fresh mock version before the actual better-auth bump lands in plan 10.**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-05-11T05:44:25Z
- **Completed:** 2026-05-11T05:52:13Z
- **Tasks:** 1
- **Files modified:** 2 (`package.json`, `pnpm-lock.yaml`)
- **Files created:** 0

## Accomplishments

- **`oauth2-mock-server` bumped 7.2.1 → 8.2.2** in atomic commit `bcdd610`. Caret pin `^8.2.2` per devDep convention. Lockfile resolved to `oauth2-mock-server@8.2.2` in both manifests (devDeps entry + tarball-resolved entry).
- **Smoke job green on first try (1m 24s).** This is the D-07 load-bearing assertion for this bump — it boots the production Docker image, starts `oauth2-mock-server@8.2.2`, drives Better Auth's genericOAuth dance against it (discovery → authorize → token → userinfo), captures the session cookie via `tests/smoke/lib/oauth-mock-driver.ts`, replays it against `/api/me`, and asserts the user lands authenticated. Green means v8's wire-level contract is identical to v7's for our usage pattern.
- **No source code edits required.** The public API surface our four callers use (`OAuth2Server` class, `issuer.keys.generate()`, `start(port, host)`, `stop()`, `service.on('beforeUserinfo' | 'beforeTokenSigning')`, `service.removeAllListeners()`, `issuer.url`) is unchanged from v7. Strict-bump-only contract per D-04 honored.
- **All 4 active CI jobs green.** `lint-typecheck`, `unit-integration`, `browser-tests`, `smoke`. 5th job `docker-build-publish` skipped as expected for non-master pushes.
- **Lockfile delta is a net shrink:** 293 lines rewritten, 130 insertions / 165 deletions = **-35 net lines**. v8's transitive dep tree is smaller than v7's.

## Task Commits

1. **Task 1: Bump oauth2-mock-server 7→8 and commit** — `bcdd610` (chore)

**Versions:**

| | Before | After |
|---|---|---|
| `package.json` pin | `^7.2.0` | `^8.2.2` |
| `pnpm-lock.yaml` resolution | `oauth2-mock-server@7.2.1` | `oauth2-mock-server@8.2.2` |
| Net lockfile delta | — | -35 lines (smaller transitive tree) |

**CI run on the resulting commit:** https://github.com/d954mas/neotolis-game-promotion-diary/actions/runs/25652696679 — **conclusion: success** (all 4 active jobs).

**Smoke job (D-07 load-bearing):** https://github.com/d954mas/neotolis-game-promotion-diary/actions/runs/25652696679/job/75294321920 — duration 1m 24s, conclusion success.

## Files Created/Modified

- `package.json` — `devDependencies['oauth2-mock-server']` pin: `^7.2.0` → `^8.2.2` (one-line change, line 72).
- `pnpm-lock.yaml` — `oauth2-mock-server` resolution updated at both occurrence sites; transitive tree pruned (-35 net lines).

## API Surface Verification

The four caller files import the same `OAuth2Server` class and use the same lifecycle / event surface in v8 as in v7. No source edits needed:

| Caller | Usage |
|---|---|
| `tests/smoke/lib/oauth-mock-driver.ts` | `new OAuth2Server()`, `issuer.keys.generate('RS256')`, `start(port, '127.0.0.1')`, `service.removeAllListeners('beforeUserinfo'/'beforeTokenSigning')`, `service.on('beforeUserinfo'/'beforeTokenSigning', ...)`, `stop()` |
| `tests/setup/oauth.ts` | `new OAuth2Server()`, `issuer.keys.generate('RS256')`, `issuer.url`, `start/stop`, same `service.on(...)` events |
| `scripts/dev-mock-oauth.mjs` | same shape as the above two |
| `dev-mock-oauth.mjs` (root, legacy) | same shape as the above two |

Smoke job exercising all of these surfaces end-to-end is green on v8.

## Decisions Made

See `key-decisions` in frontmatter. The two load-bearing decisions:

1. **No source edits required** — strict-bump-only contract per D-04 honored. v8's release notes are explicit that only Node 18 support was dropped (we are on Node 22) and packaging switched to Universal ESM. Wire-level OAuth/OIDC contract unchanged.
2. **Smoke is the load-bearing assertion** per D-07 — and it passed. The cheaper `pnpm typecheck` + `pnpm test:unit` are necessary but not sufficient signals for a mock-server bump; smoke is the actual contract test.

## Deviations from Plan

**None — plan executed exactly as written.**

The two Windows-host workarounds from Plan 01 (`NODE_OPTIONS="--use-system-ca"` for pnpm registry access, `pnpm exec prettier --write` before staging for CRLF normalization) were applied as documented in plan 01's SUMMARY; they are operating procedure for the remaining 8 commits in this phase and not deviations from the plan.

Two environmental notes carried over from Plan 01 (not deviations, not blockers):

1. **Local `pnpm test:integration` failed with `ECONNREFUSED 127.0.0.1:5432`** — no local Postgres service running on this Windows host. Identical failure pattern across all integration test files; zero oauth2-mock-server-specific errors in the output (only DB connection errors). CI on Linux uses the Postgres service container in `.github/workflows/ci.yml` and ran the same suite green (`unit-integration` job: success). Per Plan 01's pattern, "Linux CI is the load-bearing assertion per D-02; local lint/integration on Windows is informational."
2. **Local `pnpm lint` failure (~128 files pre-existing CRLF mismatch)** — out of scope per SCOPE BOUNDARY rule, already logged to `.planning/phases/03.0.2-dependency-refresh-inserted/deferred-items.md` by Plan 01. The two files this commit touches (`package.json`, `pnpm-lock.yaml`) both pass `prettier --check` individually after the targeted `prettier --write` touch-up.

## Issues Encountered

None. The plan executed in the order specified, all verifications passed, CI green on first push.

## User Setup Required

None. Same as Plan 01 — no external service configuration required for this commit.

## Next Phase Readiness

- **CI pipeline continues to validate cleanly.** All 4 active jobs green on `bcdd610`. Smoke job specifically — the load-bearing trust signal — passed in 1m 24s.
- **Phase PR (#26) auto-runs CI on every new commit.** No further setup needed for plans 03–10.
- **Operational learnings re-validated:** the two Windows-host workarounds (TLS + Prettier) from Plan 01 applied cleanly with no new surprises. Remaining 8 commits should reuse them without rediscovery.
- **No blockers for plan 03** (next planned: `dotenv` 16 → 17 per RESEARCH.md ordering — another type-only / config-shape-unchanged bump).

## Self-Check: PASSED

- [x] `grep -q '"oauth2-mock-server": "\^8\.' package.json` exits 0 — verified.
- [x] `grep -q "oauth2-mock-server@8\." pnpm-lock.yaml` exits 0 — verified at lines 2455 and 5151.
- [x] `pnpm install --frozen-lockfile` exit 0, no lockfile diff — verified locally AND in CI run 25652696679 step `Run pnpm install --frozen-lockfile`.
- [x] `pnpm typecheck` exit 0 — verified locally (0 errors, 54 pre-existing Svelte warnings unrelated to oauth2-mock-server) AND in CI.
- [x] `pnpm test:unit` exit 0 — verified locally (324/324 tests green) AND in CI.
- [x] `pnpm lint` exit 0 — verified in CI (Linux runner, LF line endings). Local Windows-CRLF failure is pre-existing and out of scope per Plan 01's `deferred-items.md`.
- [x] `pnpm test:integration` — green in CI (Linux, Postgres service container); local Windows host has no Postgres running so all integration suites fail with `ECONNREFUSED 127.0.0.1:5432` — environmental, not caused by this bump.
- [x] `git log -1 --format=%s` matches `^chore\(deps\): bump oauth2-mock-server` — verified.
- [x] `git diff HEAD~1 HEAD --stat` shows exactly 2 files: `package.json` (2 lines) + `pnpm-lock.yaml` (293 lines) — verified.
- [x] CI on the resulting commit: conclusion=success — https://github.com/d954mas/neotolis-game-promotion-diary/actions/runs/25652696679
- [x] Smoke job (D-07 load-bearing): success in 1m 24s — https://github.com/d954mas/neotolis-game-promotion-diary/actions/runs/25652696679/job/75294321920
- [x] Commit on branch: `git log feat/phase-03.0.2-dependency-refresh ^master --oneline` shows `bcdd610` on top of plan 01's `4ec4c6a` — verified.

---

*Phase: 03.0.2-dependency-refresh-inserted*
*Completed: 2026-05-11*
