---
phase: 03.0.2-dependency-refresh-inserted
plan: 10
type: execute
wave: 1
depends_on:
  - "03.0.2-09"
files_modified:
  - package.json
  - pnpm-lock.yaml
  - .planning/phases/03.0.2-dependency-refresh-inserted/PR-BODY-DRAFT.md
autonomous: false
requirements:
  - 03.0.2-C10
commit_label: "Commit 10 (per RESEARCH.md ordering — D-04 row #10, better-auth) — D-07 LOAD-BEARING (final OAuth-touching commit) + PHASE PR FINALIZATION"
must_haves:
  truths:
    - "better-auth@latest re-verified at commit-time via `npm view better-auth dist-tags`"
    - "If better-auth@latest is still 1.6.x patch range, normal Path applies: bump to the latest patch, exact-pin preserved"
    - "If better-auth@latest has flipped to 1.7+ stable, STOP and surface to user (1.7 is a minor with surface area beyond a patch)"
    - "package.json declares better-auth at exact-pin 1.6.10 (or whatever today's latest is, exact pin preserved)"
    - "pnpm-lock.yaml resolves better-auth to a 1.6.x version"
    - "src/lib/auth.ts (genericOAuth integration) is UNCHANGED — the 1.6.x line preserves the API contract"
    - "smoke CI job passes — full OAuth dance against the production image via oauth2-mock-server still works"
    - "tests/integration/auth.test.ts green; tests/integration/anonymous-401.test.ts sweep green"
    - "git history contains one final bump commit AND the phase PR is opened against master with body listing every commit's one-line rationale per D-03"
  artifacts:
    - path: "package.json"
      provides: "Updated dependency pin for better-auth (exact, no caret)"
      contains: "better-auth"
    - path: "pnpm-lock.yaml"
      provides: "Lockfile snapshot for better-auth 1.6.x"
    - path: ".planning/phases/03.0.2-dependency-refresh-inserted/PR-BODY-DRAFT.md"
      provides: "Drafted PR body following D-03 shape, surfaced to user for review before `gh pr create`"
  key_links:
    - from: "src/lib/auth.ts"
      to: "better-auth 1.6.x genericOAuth plugin"
      via: "import { betterAuth } from 'better-auth'; auth.use(genericOAuth(...))"
      pattern: "from ['\"]better-auth['\"]"
    - from: "tests/smoke/self-host.sh"
      to: "better-auth + oauth2-mock-server full dance"
      via: "smoke harness: discovery → authorize → callback → session"
      pattern: "better-auth"
---

<objective>
Land the tenth and FINAL atomic commit of Phase 03.0.2: bump `better-auth` from `1.6.9` to `1.6.10` (or today's latest 1.6.x patch), then draft and open the phase PR with a D-03-compliant body.

**This is a D-07 LOAD-BEARING commit + PR FINALIZATION plan.** Per CONTEXT.md §D-07, better-auth touches the OAuth dance, and the smoke test (production image + oauth2-mock-server + full genericOAuth flow) is the load-bearing assertion. This is the LAST commit on the branch by design (RESEARCH.md Pattern 1: "riskiest at the END so earlier green commits validate the pipeline before risk concentrates").

**Scope finding (RESEARCH.md Key Finding #2, verified 2026-05-11):**
- `better-auth@latest = 1.6.10` (we are on `1.6.9` exact-pin). Single-patch bump.
- `beta` tag is at `1.7.0-beta.3` — NOT `latest`.
- 1.6.10 changelog: bug fixes including exposed `refreshUserSessions`, OpenAPI schema fix for social sign-in, captcha plugin email-OTP fix, OAuth/SSO fixes, eliminated duplicate `Set-Cookie` on redirects.
- `genericOAuth` plugin API: unchanged in 1.6.x. Session config, cookie config, Drizzle adapter: unchanged.

**The executor's first action MUST be re-verifying `npm view better-auth dist-tags`. If `latest` has moved beyond 1.6.x (e.g. 1.7.0 stable shipped), STOP and surface — 1.7 is a minor with larger surface and warrants explicit user decision.**

After the bump commit lands and CI is green, Task 2 drafts the phase PR body per CONTEXT.md §D-03 and PAUSES for user review/approval before opening the PR. PR title: `chore(03.0.2): dependency refresh — N major bumps`.

This plan is **autonomous: false** because the PR opening is a final-state step the user typically wants to inspect before submission.
Output: One commit `chore(deps): bump better-auth from 1.6.9 to 1.6.10` on `feat/phase-03.0.2-dependency-refresh`; CI green; drafted PR body file + PR opened against master with D-03-compliant body (subject to user approval at checkpoint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-CONTEXT.md
@.planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-RESEARCH.md
@.planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-VALIDATION.md
@AGENTS.md
@package.json
@src/lib/auth.ts
@src/lib/server/auth-adapter.ts
@tests/smoke/self-host.sh

<interfaces>
<!-- Current pin (from package.json line 39):
"better-auth": "1.6.9"  (EXACT pin in dependencies — load-bearing OAuth runtime)

Target pin (RESEARCH.md table line 144):
"better-auth": "1.6.10"  (EXACT pin preserved)

Breaking changes (RESEARCH.md §"Commit 10"):
- ZERO. 1.6.10 is a patch with bug fixes only. No API changes in 1.6.x.
- genericOAuth, session/cookie config, Drizzle adapter: all unchanged.

Integration points (must NOT need code changes):
- src/lib/auth.ts: betterAuth() construction + genericOAuth plugin
- src/lib/server/auth-adapter.ts: encryption wrapper around drizzleAdapter

Load-bearing assertions (D-07):
- tests/smoke/self-host.sh: production image boot + full OAuth dance (discovery → authorize → callback → /api/auth/session). This is THE assertion that must be green.
- tests/integration/auth.test.ts: better-auth integration paths.
- tests/integration/anonymous-401.test.ts: anonymous-401 sweep covers all /api/* routes.

CONTEXT.md §D-03 — phase PR shape:
- Title: "chore(03.0.2): dependency refresh — N major bumps"
- Body: list every bump (10 commits in this branch + pg-boss "done outside the phase" note) with one-line rationale each, plus the smoke-CI link.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify better-auth dist-tag, bump 1.6.9→1.6.10 (preserve exact pin), run full local verification</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>
    - package.json (current pin: `"better-auth": "1.6.9"` — line 39, EXACT, no caret)
    - src/lib/auth.ts (FULL FILE — confirm genericOAuth setup; the bump MUST NOT touch this file)
    - src/lib/server/auth-adapter.ts (the encryption wrapper around drizzleAdapter; MUST NOT change)
    - tests/smoke/self-host.sh + tests/smoke/lib/oauth-mock-driver.ts (the load-bearing OAuth-dance harness)
    - .planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-RESEARCH.md §"Commit 10 — better-auth 1.6.9 → 1.6.10" (full breaking-change verification)
    - .planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-CONTEXT.md §D-07 (better-auth vigilance — smoke is load-bearing) and §D-03 (PR title + body shape)
    - AGENTS.md §"Practices" → "Locked stack versions" and §"Privacy & multi-tenancy" item 8 (self-host parity — smoke is the CI gate)
  </read_first>
  <action>
    Re-verification (MANDATORY first action — RESEARCH.md valid-until 2026-05-25):
    1. Confirm on branch + previous commit landed: `git log --oneline -1` MUST show the Plan 09 drizzle commit.
    2. Re-verify better-auth latest dist-tag at commit time:
       ```bash
       npm view better-auth dist-tags
       ```
       Expected per RESEARCH.md: `latest: 1.6.10`, `beta: 1.7.0-beta.X`.
       - **If `latest` is still a `1.6.x` value:** proceed to step 4 with that exact version.
       - **If `latest` has flipped to a `1.7.x` stable** (1.7.0+ shipped): STOP. 1.7 is a minor with surface area beyond a patch — emit a CHECKPOINT and wait for user direction (land 1.7 in this phase with extended verification scope, OR cap this phase at the last 1.6.x patch and schedule 1.7 for a follow-up phase). Do NOT land 1.7 unilaterally.
    3. Capture the resolved target version (e.g. `1.6.10`) and use it verbatim in the commands below.

    Perform the bump (EXACT pin — drop the caret):
    4. ```bash
       pnpm add better-auth@1.6.10
       ```
       (Substitute the version from step 3 if different. No caret — `better-auth` is exact-pinned per the existing load-bearing-runtime-deps convention.)
    5. Verify the pin landed exact:
       ```bash
       grep -E '"better-auth":\s*"1\.6\.[0-9]+"' package.json
       grep -E '"better-auth":\s*"\^' package.json | wc -l  # MUST output "0"
       ```
    6. `pnpm install --frozen-lockfile` MUST exit 0.

    Run local verification:
    7. ```bash
       pnpm lint
       pnpm typecheck
       pnpm test:unit
       pnpm test:integration
       pnpm build
       ```
       All MUST exit 0. `test:integration` specifically must pass `tests/integration/auth.test.ts` and `tests/integration/anonymous-401.test.ts` (the sweep).
    8. If Docker is available locally, run smoke harness (recommended — it's the D-07 load-bearing assertion):
       ```bash
       bash tests/smoke/self-host.sh
       ```
       MUST exit 0. If Docker is unavailable locally, rely on the CI smoke job (push triggers it automatically).

    Commit:
    9. `git add package.json pnpm-lock.yaml`. NO source files should be touched — confirm via `git status --porcelain`.
    10. Commit:
       ```bash
       git commit -m "$(cat <<'EOF'
chore(deps): bump better-auth from 1.6.9 to 1.6.10

Re-verified at commit time: `npm view better-auth dist-tags` →
latest: [INSERT today's latest 1.6.x value]
beta tag is 1.7.0-beta.X — NOT latest; phase scope says "latest stable."

1.6.10 is a patch release with bug fixes only — no API changes in the
1.6.x line. genericOAuth plugin, session/cookie config, Drizzle adapter
integration: all preserved. src/lib/auth.ts and src/lib/server/auth-adapter.ts
need no edits.

Notable fixes landing in 1.6.10:
- exposed refreshUserSessions
- OpenAPI schema fix for social sign-in
- captcha plugin email-OTP fix
- OAuth/SSO fixes
- eliminated duplicate Set-Cookie on redirects

D-07 LOAD-BEARING VERIFICATION: smoke CI job (tests/smoke/self-host.sh)
boots the production image, drives the full OAuth dance through
oauth2-mock-server (Plan 02's bumped version), and asserts the user
lands authenticated. tests/integration/auth.test.ts and the
anonymous-401 sweep (tests/integration/anonymous-401.test.ts) also pass —
preserving the multi-tenant auth invariants per AGENTS.md "Privacy &
multi-tenancy" items 3 + 8.

Verification: pnpm lint + typecheck + test:unit + test:integration +
build all pass locally; smoke runs in CI.
EOF
       )"
       ```
    11. `git push origin feat/phase-03.0.2-dependency-refresh`. WAIT for the full CI matrix (all three jobs) to be green before proceeding to Task 2. The smoke job is the D-07 load-bearing assertion — until smoke is green, do NOT proceed to the PR-finalization checkpoint.
  </action>
  <verify>
    <automated>
      # Pin landed EXACT (no caret):
      grep -E '"better-auth":\s*"1\.6\.[0-9]+"' package.json
      grep -E '"better-auth":\s*"\^' package.json | wc -l  # MUST output "0"
      grep -q "better-auth@1\.6\." pnpm-lock.yaml
      # No source changes leaked:
      git diff HEAD~1 HEAD -- src/lib/auth.ts | wc -l  # MUST output "0"
      git diff HEAD~1 HEAD -- src/lib/server/auth-adapter.ts | wc -l  # MUST output "0"
      pnpm install --frozen-lockfile
      pnpm lint && pnpm typecheck && pnpm test:unit && pnpm test:integration && pnpm build
      git log -1 --format=%s | grep -qE '^chore\(deps\): bump better-auth'
      # After push, CI smoke must be green:
      # gh run list --branch feat/phase-03.0.2-dependency-refresh --limit 1 --json conclusion,jobs --jq '.[0].jobs[] | select(.name == "smoke") | .conclusion'
      # MUST output: "success"
    </automated>
  </verify>
  <acceptance_criteria>
    - First action `npm view better-auth dist-tags` confirmed `latest` is still in the `1.6.x` line. If it returned `1.7.x` stable, plan halted with a checkpoint.
    - `grep -E '"better-auth":\s*"1\.6\.[0-9]+"' package.json` exits 0 (EXACT pin).
    - `grep -E '"better-auth":\s*"\^' package.json | wc -l` outputs `0` (no caret).
    - `grep -q "better-auth@1\.6\." pnpm-lock.yaml` exits 0.
    - `pnpm install --frozen-lockfile` exits 0.
    - `pnpm test:integration` exits 0 (passes `tests/integration/auth.test.ts` and `tests/integration/anonymous-401.test.ts`).
    - `pnpm build` exits 0.
    - `git diff HEAD~1 HEAD -- src/` is empty (no source-code changes leaked).
    - `git log -1 --format=%s` matches `^chore\(deps\): bump better-auth`.
    - After push: smoke CI job `success` — D-07 LOAD-BEARING assertion. Until smoke is green, Task 2 (PR opening) does NOT start.
  </acceptance_criteria>
  <done>
    Branch has commit `chore(deps): bump better-auth from 1.6.9 to 1.6.10`. Zero source changes. All three CI jobs green on the commit, smoke in particular. Branch is ready for PR submission via Task 2.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Draft phase PR body, present to user, open PR on approval</name>
  <files>.planning/phases/03.0.2-dependency-refresh-inserted/PR-BODY-DRAFT.md</files>
  <read_first>
    - All ten `.planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-{01..10}-SUMMARY.md` files (each prior plan's recorded commit SHA, version delta, and CI link)
    - .planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-CONTEXT.md §D-03 (PR title + body shape: title `chore(03.0.2): dependency refresh — N major bumps`; body lists every bump with one-line rationale + smoke link)
    - AGENTS.md §"Validation" (Self-review checklist — Constraints / Philosophy / Practices / CI gate honesty / Documentation drift)
    - AGENTS.md §"Workflow" (squash-merge model; PR title becomes the squash commit subject; PR body becomes the commit message body)
    - tests/smoke/self-host.sh (so the executor knows which CI run URL to cite as the load-bearing smoke link)
  </read_first>
  <action>
    Task 2 is the ONE checkpoint in the phase. The executor performs three sub-steps:

    Step A — Draft the PR body locally to a file (does NOT push to GitHub):
    1. Gather inputs:
       - For each plan 01-10, read its SUMMARY.md and extract: commit SHA, resolved version pre/post, any fix-up notes.
       - Identify the latest GREEN smoke CI run URL on the final commit. Use:
         ```bash
         gh run list --branch feat/phase-03.0.2-dependency-refresh --limit 5 --json databaseId,conclusion,headSha,jobs,url \
           --jq '.[] | select(.conclusion == "success") | {sha: .headSha, url: .url}' | head -1
         ```
    2. Write the drafted body to `.planning/phases/03.0.2-dependency-refresh-inserted/PR-BODY-DRAFT.md` using the EXACT structure below (substitute bracketed placeholders with real values gathered in step 1):
       ```markdown
       ## Summary

       Phase 03.0.2 — dependency refresh — N major bumps. Closes #23.

       Per CONTEXT.md D-01: one phase-PR onto master, with per-major atomic
       commits on the branch (preserving AGENTS.md "Locked stack versions"
       practice in spirit; the per-major-PR shape becomes per-major-commit
       shape for batched major refresh).

       ## Per-bump rationale (one line each, in commit order)

       1. `@types/supertest` 6→7 — type-only dev dep, zero callers (stranded
          from earlier phases; consider follow-up `chore(deps): drop unused
          supertest`).
       2. `oauth2-mock-server` 7→8 — Universal ESM; smoke catches it.
       3. `dotenv` 16→17 — silenced new `{ quiet: false }` advertising line
          with `{ quiet: true }` in env.ts (preserves Pino JSON log discipline
          in production).
       4. ESLint core family 9→10 (+ @eslint/js 10, eslint-config-prettier 10,
          globals 17) — three new recommended rules vetted; internal
          eslint-plugin-tenant-scope was audited and was already on the
          ESLint-10-compatible property-accessor form (`context.sourceCode`).
       5. eslint-plugin-svelte 2→3 + svelte-eslint-parser 0.43→1.6 — peer-dep
          pair; rune-aware parser for Svelte 5 surfaces is a net positive.
       6. pino 9→10 + pino-pretty 11→13 — peer-dep pair; @pinojs/redact engine
          swap; tests/unit/logger.test.ts (D-07 load-bearing redact tripwire)
          PASSES — privacy floor invariant intact.
       7. @hono/node-server 1→2 — public API unchanged; smoke catches it.
       8. typescript 5.6→6.0 — tsconfig.json pre-immunized; D-06 honored:
          [insert "zero source fix-ups" OR "N source fix-ups for
          noUncheckedSideEffectImports — see commit body"].
       9. drizzle pair refresh — no major on `latest` dist-tag as of [DATE];
          Path A applied: drizzle-orm re-pinned at 0.45.2, drizzle-kit caret
          bumped to ^0.31.10; pnpm db:check no-diff. Trigger for follow-up
          when drizzle-orm 1.0 stable releases.
       10. better-auth 1.6.9→1.6.10 — patch only; smoke CI green; D-07
           load-bearing OAuth dance verified.

       **Done outside the phase:** pg-boss 12.18.2 was already in
       `package.json` pre-phase per the ROADMAP forecast — no commit in this
       phase, but noted here for completeness.

       ## Smoke link

       The load-bearing trust signal per CONTEXT.md D-03:
       [URL from step 1's gh run query]

       ## Self-review

       Per AGENTS.md §Validation:
       - Constraints: ✓ no new env reads outside config/env.ts; ✓ envelope
         encryption invariants untouched; ✓ self-host parity preserved
         (smoke green with minimal env).
       - Philosophy: ✓ no SaaS/self-host branching; no premature
         abstraction; security as floor (D-06 + D-07 honored, no
         constraints relaxed).
       - Practices: ✓ atomic PRs (1 PR, 10 commits, each green); ✓ tests
         land with the feature (where source touched); ✓ migrations
         forward-only (drizzle commit no-op); ✓ env reads centralized;
         ✓ no secrets in logs (D-07 verified via logger.test.ts);
         ✓ Conventional Commits.
       - CI gate honesty: every assertion in this phase rides through an
         existing load-bearing test (no soft-passes; no skip-flags
         introduced).
       - Documentation drift: ROADMAP.md Phase 03.0.2 entry updated to
         reflect the 10 plans landed; CONTEXT.md "Deferred Ideas" item
         re: AGENTS.md practice-text update [is/isn't] addressed in this PR.

       ## Self-review (second pass)

       [Reserved for /gsd:check-phase reviewer output. Filled in before
       merge.]
       ```

    Step B — Surface to user (THIS IS THE CHECKPOINT — wait for user signal):
    3. Show the user the drafted body. The user may respond with:
       - **"approved — open PR"** → proceed to Step C.
       - Inline edits (e.g. "change line 3 to X", "add note about Y") → amend `PR-BODY-DRAFT.md` and re-present.
       - **"defer — I'll open the PR myself"** → halt this task; the user opens the PR manually outside the executor's loop. Mark Task 2 done with the deferral noted.

    Step C — Open the PR (only after user signal "approved — open PR"):
    4. Count the number of major bumps (typically 10, but may be 9 if drizzle Path A is documented as no-op-only and the planner decided not to count it as a "major bump" for the title). The title uses N = count of bumps:
       ```bash
       gh pr create \
         --base master \
         --head feat/phase-03.0.2-dependency-refresh \
         --title "chore(03.0.2): dependency refresh — N major bumps" \
         --body-file .planning/phases/03.0.2-dependency-refresh-inserted/PR-BODY-DRAFT.md
       ```
       (Substitute the actual N value in the title.)
    5. Verify the PR was created:
       ```bash
       gh pr view --json number,title,url --jq '.url'
       ```
       Returns the PR URL. Record it in the plan summary.
  </action>
  <verify>
    <automated>
      # Step A artifact exists:
      test -f .planning/phases/03.0.2-dependency-refresh-inserted/PR-BODY-DRAFT.md
      # Body contains the D-03-mandated sections:
      grep -q "^## Summary" .planning/phases/03.0.2-dependency-refresh-inserted/PR-BODY-DRAFT.md
      grep -q "^## Per-bump rationale" .planning/phases/03.0.2-dependency-refresh-inserted/PR-BODY-DRAFT.md
      grep -q "^## Smoke link" .planning/phases/03.0.2-dependency-refresh-inserted/PR-BODY-DRAFT.md
      grep -q "^## Self-review" .planning/phases/03.0.2-dependency-refresh-inserted/PR-BODY-DRAFT.md
      # At least 10 rationale bullet/numbered lines in the per-bump section:
      awk '/^## Per-bump rationale/,/^## Smoke link/' .planning/phases/03.0.2-dependency-refresh-inserted/PR-BODY-DRAFT.md | grep -cE '^\s*[0-9]+\.|^\s*-\s'  # MUST output >= 10
      # If user approved + Step C ran, the PR exists:
      # gh pr list --head feat/phase-03.0.2-dependency-refresh --json number,title --jq '.[0].title' MUST match '^chore\(03\.0\.2\): dependency refresh — [0-9]+ major bumps$'
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/03.0.2-dependency-refresh-inserted/PR-BODY-DRAFT.md` exists.
    - Drafted body contains all four required sections: `## Summary`, `## Per-bump rationale`, `## Smoke link`, `## Self-review` (regex anchored to start of line).
    - Per-bump rationale section has at least 10 enumerated lines (one per atomic unit).
    - Smoke link is a valid GitHub Actions run URL pointing to a `success` conclusion (verifiable via `gh run view <RUN_ID> --json conclusion --jq '.conclusion'` returning `success`).
    - User signal received: one of "approved — open PR" / inline edits / "defer".
    - **If approved:** `gh pr list --head feat/phase-03.0.2-dependency-refresh --json title --jq '.[0].title'` matches regex `^chore\(03\.0\.2\): dependency refresh — [0-9]+ major bumps$`. PR body matches the file content of PR-BODY-DRAFT.md.
    - **If deferred:** PR-BODY-DRAFT.md still exists for the user to consume; no `gh pr create` was invoked.
    - **If edits requested:** The amended PR-BODY-DRAFT.md is re-presented to the user; loop until approval or deferral.
  </acceptance_criteria>
  <done>
    PR-BODY-DRAFT.md is on disk with all four mandated sections + ≥10 rationale lines + the load-bearing smoke link. EITHER (a) the phase PR is open on GitHub with title `chore(03.0.2): dependency refresh — N major bumps` and body matching the drafted file, OR (b) the user explicitly deferred PR opening and the draft is left on disk for manual submission. The phase planning loop is complete.
  </done>
  <resume-signal>
    After the executor presents PR-BODY-DRAFT.md, user types ONE of:
    - "approved — open PR" → executor runs `gh pr create` and reports the URL.
    - "[inline edits]" → executor amends and re-presents.
    - "defer — I'll open the PR myself" → executor halts; draft stays on disk.
  </resume-signal>
</task>

</tasks>

<verification>
1. `git log feat/phase-03.0.2-dependency-refresh --oneline | head -11` shows the new better-auth commit on top of Plans 01-09 (10 atomic commits total + any fix-ups).
2. CI on the latest commit: all three jobs `success`. Smoke is the D-07 load-bearing assertion.
3. PR-BODY-DRAFT.md exists in the phase folder with D-03 shape.
4. If approved + opened: `gh pr list --head feat/phase-03.0.2-dependency-refresh --json number,title,url` returns one PR with the right title.
5. ROADMAP.md Phase 03.0.2 entry now reflects 10 plans landed (handled by the orchestrator's `update_roadmap` step).
</verification>

<success_criteria>
- better-auth bumped on exact-pin to 1.6.10 (or today's latest 1.6.x).
- Smoke CI green on the final commit.
- 10 of 10 atomic units complete.
- Phase PR opened against master with D-03-compliant body (or user explicitly deferred PR opening with draft preserved on disk).
- Phase is mergeable: 1 long-lived branch, 1 tracking issue (#23), 1 final PR per memory rule.
</success_criteria>

<output>
After completion, create `.planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-10-SUMMARY.md` recording: commit SHA, the verbatim output of `npm view better-auth dist-tags` at commit time, CI run URL (smoke job link), PR URL (or "deferred — user opens PR manually"), and the final tally of commits on the branch (10 + fix-ups count).

Then create `.planning/phases/03.0.2-dependency-refresh-inserted/PHASE-SUMMARY.md` (phase-level summary) recording: all 10 commit SHAs in landed order, each bump's resolved version, total branch size (lines + files touched), and a phase retrospective note for STATE.md.
</output>
