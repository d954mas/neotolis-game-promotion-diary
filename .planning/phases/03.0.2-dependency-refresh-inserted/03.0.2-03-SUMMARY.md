---
phase: 03.0.2-dependency-refresh-inserted
plan: 03
subsystem: dependency-refresh
tags: [chore, deps, dotenv, env-config, privacy-floor]
requirements:
  - 03.0.2-C3
provides:
  - dotenv@17.4.2 on the phase branch (commit 3 of 10 atomic units)
  - Suppressed dotenv 17 boot-time advertising line at both env.ts call sites
  - Verified D-07 invariant: env.ts is still the SOLE process.env reader; ESLint no-restricted-properties carve-out unchanged
requires:
  - 03.0.2-01 (commit `4ec4c6a`) and 03.0.2-02 (commit `bcdd610`) landed and CI-green
  - eslint.config.js env.ts carve-out at lines 130-134 (no change required)
affects:
  - src/lib/server/config/env.ts (sole process.env reader — gained { quiet: true } at both loadDotenv() calls)
  - package.json (dotenv pin: ^16.4.5 → ^17.4.2)
  - pnpm-lock.yaml (lockfile resolved dotenv 16.6.1 → 17.4.2)
tech-stack:
  added:
    - dotenv 17.4.2 (replacing 16.6.1)
  patterns:
    - Code-level quiet-flag opt-out (per RESEARCH.md Pattern 1 — preserves self-host parity vs. DOTENV_CONFIG_QUIET env var)
key-files:
  modified:
    - package.json
    - pnpm-lock.yaml
    - src/lib/server/config/env.ts
decisions:
  - "D-07 honored: dotenv 17 still exports the same `config()` shape; { quiet: true } is the single integration point; no second process.env reader introduced"
  - "Atomic-unit shape: ONE commit (bump + env.ts edit combined), not 2 (per CONTEXT.md D-02 option — the 2-line env.ts edit is inseparable from the bump)"
metrics:
  duration: ~12 min
  tasks: 1
  files: 3
  completed: 2026-05-11
---

# Phase 03.0.2 Plan 03: dotenv 16 → 17 Summary

**One-liner:** Bumped dotenv from ^16.4.5 to ^17.4.2 and added `{ quiet: true }` to both `loadDotenv()` call sites in `src/lib/server/config/env.ts` to suppress dotenv 17's new boot-time advertising line, preserving Pino's structured-JSON log discipline in production containers.

## Commits

| SHA | Type | Subject |
|-----|------|---------|
| `46a8c1e` | chore(deps) | bump dotenv from ^16.4.5 to ^17.4.2 |

## What Changed

### `package.json`
Single-line dependency pin update:
```diff
-    "dotenv": "^16.4.5",
+    "dotenv": "^17.4.2",
```

### `pnpm-lock.yaml`
Lockfile resolves dotenv to `17.4.2` (was `16.6.1`).

### `src/lib/server/config/env.ts`
Exact two-line surgical edit — surrounding comments, schema, KEK scrubbing, and every other code path untouched:

```diff
-loadDotenv();
+loadDotenv({ quiet: true });
 // dotenv probe BEFORE we parse env; this is the boot-time gate that
 // decides whether to layer .env.local (D-24). env.ts is the single
 // legitimate process.env reader, so the no-restricted-properties rule
 // does not apply here.
 if (process.env.NODE_ENV !== "test") {
-  loadDotenv({ path: ".env.local", override: true });
+  loadDotenv({ path: ".env.local", override: true, quiet: true });
 }
```

`git diff HEAD~1 HEAD -- src/lib/server/config/env.ts` shows exactly 2 changed lines — both `loadDotenv(...)` calls.

## D-07 Privacy Invariant Verification

The bump touches the file AGENTS.md calls out as load-bearing: `src/lib/server/config/env.ts` is the SOLE legitimate `process.env` reader. The ESLint `no-restricted-properties` rule and its env.ts carve-out (`eslint.config.js` lines 130-134) are unchanged.

Verified:
- `pnpm exec eslint .` exits 0 → no-restricted-properties rule still green
- `grep -rEn "process\.env" src/ --include="*.ts"` returns the same 6 files as before the bump (5 with `eslint-disable` lines + env.ts itself) — **no new `process.env` reader introduced by the bump**
- `eslint.config.js` lines 130-134 unchanged (carve-out still scopes ONLY env.ts)

## CI Verification

**Run:** https://github.com/d954mas/neotolis-game-promotion-diary/actions/runs/25653146599

All four jobs **success** on commit `46a8c1e`:

| Job | Duration | Result |
|-----|----------|--------|
| lint-typecheck | 52s | ✓ ESLint + Prettier + typecheck all green |
| unit-integration | 2m10s | ✓ All unit + integration tests pass with dotenv 17 |
| browser-tests | 1m8s | ✓ Playwright smoke passes |
| smoke | 2m21s | ✓ **Load-bearing trust signal**: production Docker image boots clean with dotenv 17 + quiet flag; self-host smoke test passes; SaaS-leak grep clean |

## Local Verification (pre-push)

| Gate | Command | Result |
|------|---------|--------|
| package.json pin | `grep -q '"dotenv": "\^17\.' package.json` | exit 0 |
| Lockfile resolution | `grep -q "dotenv@17\." pnpm-lock.yaml` | exit 0 |
| Both call sites quieted | `grep -cE 'loadDotenv\(\{[^}]*quiet:\s*true' src/lib/server/config/env.ts` | `2` (expected) |
| Frozen-lockfile install | `pnpm install --frozen-lockfile` | exit 0 |
| ESLint (no-restricted-properties guard) | `pnpm exec eslint .` | exit 0 |
| Typecheck | `pnpm typecheck` | exit 0 (0 errors; 54 pre-existing svelte warnings unchanged) |
| Unit tests | `pnpm test:unit` | 324/324 pass |
| **Advertising-line suppression** | `pnpm test:unit 2>&1 \| grep -c '^\[dotenv@'` | `0` (line absent — fix works) |
| Conventional commit format | `git log -1 --format=%s \| grep -qE '^chore\(deps\): bump dotenv'` | exit 0 |

Local integration tests not run on this machine (no local Postgres; `ECONNREFUSED ::1:5432` — environmental, not a regression). CI's `unit-integration` job exercised the integration suite against the Postgres service container and passed (2m10s).

## Pre-existing CRLF/Prettier baseline (unchanged)

`pnpm lint` (which composes ESLint + Prettier) fails locally on Windows with `Code style issues found in 127 files` due to a pre-existing CRLF line-ending mismatch — the same baseline carried forward from Plans 01 and 02 (per `<prior_plan_context>`). None of this plan's modified files (package.json, pnpm-lock.yaml, env.ts) appear in the 127-file list. CI runs on Linux with LF endings and reports `lint-typecheck` green.

## Deviations from Plan

None — plan executed exactly as written:
- Single atomic commit (per CONTEXT.md D-02 option A; the env.ts edit is 2 lines and inseparable from the bump).
- Both edits applied verbatim from the PLAN's `<action>` block.
- No new `process.env` reads added; ESLint passes.

## Manual Verification Reminder

Per CONTEXT.md §"Manual-Only Verifications", on the next production deploy the operator should run:

```bash
ssh neotolis-diary "docker logs diary-app-1 2>&1 | head -50"
```

The output MUST NOT contain a line matching `^\[dotenv@` — that line's absence is the production-side proof the fix took effect.

## Self-Check: PASSED

- File exists: `.planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-03-SUMMARY.md` ✓
- Commit exists: `46a8c1e` ✓ (verified via `git log --oneline -3`)
- CI conclusion: success ✓ (verified via `gh run view 25653146599 --json conclusion`)
- D-07 invariant intact ✓ (env.ts is still sole process.env reader, ESLint green)
- Advertising line suppressed ✓ (`grep -c '^\[dotenv@'` returns 0)

## State Update

- Phase 03.0.2 Plan 3 of 10 complete (✓)
- Next: Plan 04 — ESLint core family bump (eslint 9→10, @eslint/js, eslint-config-prettier 9→10, globals 15→17)
- Branch `feat/phase-03.0.2-dependency-refresh` has 3 atomic-unit commits: `4ec4c6a` (@types/supertest), `bcdd610` (oauth2-mock-server), `46a8c1e` (dotenv)
- Draft PR #26 picked up the new commit; CI green on all four jobs
