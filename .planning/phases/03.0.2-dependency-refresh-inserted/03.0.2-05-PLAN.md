---
phase: 03.0.2-dependency-refresh-inserted
plan: 05
type: execute
wave: 1
depends_on:
  - "03.0.2-04"
files_modified:
  - package.json
  - pnpm-lock.yaml
autonomous: true
requirements:
  - 03.0.2-C5
commit_label: "Commit 5 (per RESEARCH.md ordering — was D-04 row #2, Svelte-lint pair)"
must_haves:
  truths:
    - "package.json declares eslint-plugin-svelte at ^3.x AND svelte-eslint-parser at ^1.x (peer-dep pair landed together per D-05)"
    - "pnpm-lock.yaml resolves both to declared ranges"
    - "pnpm lint exits 0 on every .svelte file with the new svelte-eslint-parser 1.6 rune-aware AST"
    - "tests/integration green (Svelte components still compile + render)"
    - "git history contains one commit (or one + fix-up) for the Svelte-lint pair"
  artifacts:
    - path: "package.json"
      provides: "Updated devDependency pins for eslint-plugin-svelte + svelte-eslint-parser"
      contains: "eslint-plugin-svelte"
    - path: "pnpm-lock.yaml"
      provides: "Lockfile snapshot for the peer-dep pair"
  key_links:
    - from: "eslint.config.js block 2 (files: ['**/*.svelte'])"
      to: "eslint-plugin-svelte v3 + svelte-eslint-parser v1"
      via: "plugins: { svelte: sveltePlugin }; languageOptions.parser: svelteParser"
      pattern: "svelte-eslint-parser"
---

<objective>
Land the fifth atomic commit of Phase 03.0.2: bump the Svelte-lint pair — `eslint-plugin-svelte` 2→3 + `svelte-eslint-parser` 0.43→1.6 — together as one atomic unit per CONTEXT.md §D-05 (peer-dep: plugin v3 requires parser ≥1.0, MUST land together).

Per RESEARCH.md §"Commit 2", this is a TRIVIAL bump for our codebase because:
- We do NOT pull in `sveltePlugin.configs.recommended` — we declare `plugins: { svelte: sveltePlugin }` and configure rules manually (eslint.config.js lines 73-92). So the ~20 newly-enabled "recommended" rules in v3 are a NO-OP for us.
- We don't reference `no-deprecated-raw-special-elements` or any deprecated rules being removed/renamed.
- We're already on Svelte 5.55.5 — `svelte-eslint-parser` v1 introduces rune-aware parsing (`$state`, `$derived`, `$props`, `$bindable`, `{#snippet}`, `{@render}`, `<svelte:boundary>`) which is a NET POSITIVE; our existing .svelte rune usage parses correctly. v1 may surface true rune-related lint hits that the older 0.43 parser missed — these are real issues and get fixed in the same atomic unit per D-06.

Purpose: Bring Svelte linting to latest stable; benefit from rune-aware parsing on Svelte 5 surfaces; preserve the lint gate's effectiveness.
Output: One commit `chore(deps): bump eslint-plugin-svelte + svelte-eslint-parser to v3 + v1.6` (with optional fix-up commit if rune-aware parser surfaces new hits); CI green on all three jobs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-CONTEXT.md
@.planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-RESEARCH.md
@.planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-VALIDATION.md
@AGENTS.md
@package.json
@eslint.config.js

<interfaces>
<!-- Current pins (from package.json):
"eslint-plugin-svelte": "^2.46.0"        (line 68)
"svelte-eslint-parser": "^0.43.0"        (line 79)

Target pins (RESEARCH.md table lines 133-134):
"eslint-plugin-svelte": "^3.17.1"
"svelte-eslint-parser": "^1.6.1"

D-05 peer-dep rule: MUST land together — plugin v3 requires parser ≥1.0.

Verified config shape (RESEARCH.md §"Commit 2"):
- We DON'T spread sveltePlugin.configs.recommended — only declare plugins
- Lint rules for .svelte files: no-undef:off, tsUnusedVarsRule, noProcessEnv
- Parser pipeline: svelteParser with tsParser as embedded parser

Breaking changes that affect us:
- eslint-plugin-svelte 3 is ESM-only — our eslint.config.js is already ESM (verified by ESM-only ESLint 10 in Plan 04). Safe.
- svelte-eslint-parser 1.0 is ESM-only — same. Safe.
- svelte-eslint-parser 1.0+ parses Svelte 5 runes properly — NET POSITIVE.

Breaking changes that do NOT affect us:
- ~20 rules newly enabled in recommended preset — we don't use recommended preset.
- Removed `svelte/valid-compile` from recommended — same reason.
- Renamed rule `no-deprecated-raw-special-elements` — we don't reference it.
- AST internal change `declaration` → `declarations` (plural) on SvelteConstTag — internal AST consumer change, not exposed to rule users. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump eslint-plugin-svelte 2→3 + svelte-eslint-parser 0.43→1.6 together</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>
    - package.json (current pins: lines 68 and 79)
    - eslint.config.js block 2 (lines 73-92 — the `files: ["**/*.svelte"]` flat-config block; verify we do NOT spread `sveltePlugin.configs.recommended`)
    - .planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-RESEARCH.md §"Commit 2 — Svelte-lint pair"
    - .planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-CONTEXT.md §D-05 (peer-dep pairing rule for these two packages)
    - AGENTS.md §"Practices" → "Locked stack versions" + "Conventional Commits"
  </read_first>
  <action>
    Pre-bump audit:
    1. Confirm on branch + previous commit landed: `git log --oneline -1` MUST show the Plan 04 ESLint family commit.
    2. Re-verify eslint.config.js block 2 (lines 73-92) does NOT spread `sveltePlugin.configs.recommended`:
       ```bash
       grep -n 'sveltePlugin\.configs\.recommended\|configs\.recommended\|\.\.\.sveltePlugin' eslint.config.js
       ```
       Expected: zero matches. If non-zero (e.g. config drifted after Plan 04), the bump's surface is larger — re-read RESEARCH.md §"Commit 2 — Code-touch surface area" and adjust the action plan to account for the ~20 newly-enabled rules.

    Perform the bump (single pnpm command, both packages, per D-05):
    3. ```bash
       pnpm add -D eslint-plugin-svelte@^3.17.1 svelte-eslint-parser@^1.6.1
       ```
    4. `pnpm install --frozen-lockfile` MUST exit 0.

    Run local verification:
    5. ```bash
       pnpm lint
       ```
       Outcomes:
       (a) GREEN — proceed to commit.
       (b) Rune-aware parser surfaces real lint issues in .svelte files (e.g. unused rune-declared props, accidentally-shadowed runes) — fix each per the rule's intent in the SAME atomic unit per D-06. Do NOT suppress with `// eslint-disable-next-line`.
       (c) Parser errors / crashes — re-read RESEARCH.md §"Commit 2"; if not addressable trivially, surface to user (rare; per RESEARCH.md, expected hit rate is zero or very low).
    6. ```bash
       pnpm typecheck     # svelte-check must still parse all .svelte files
       pnpm test:unit
       pnpm test:integration
       ```
       All MUST exit 0.

    Commit:
    7. `git add package.json pnpm-lock.yaml` (+ any .svelte fix-ups from step 5b).
    8. Commit:
       ```bash
       git commit -m "$(cat <<'EOF'
chore(deps): bump eslint-plugin-svelte 2→3 and svelte-eslint-parser 0.43→1.6

Peer-dep pair landed together per CONTEXT.md D-05 — plugin v3 requires
parser ≥1.0. Both packages are now ESM-only, which already matches our
eslint.config.js (ESM module) and is consistent with ESLint 10 (Plan 04).

eslint-plugin-svelte v3 newly enables ~20 rules in its `recommended`
preset — NO-OP for us because we don't spread sveltePlugin.configs.recommended
(eslint.config.js block 2 declares only `plugins: { svelte: sveltePlugin }`
and configures rules manually). svelte-eslint-parser v1.6 introduces
rune-aware parsing for Svelte 5 ($state, $derived, $props, $bindable,
{#snippet}, {@render}, <svelte:boundary>) — net positive for our Svelte
5.55.5 codebase.

[List any fix-ups applied if the rune-aware parser surfaced real issues.]

Verification: pnpm lint passes against the rune-aware parser on all
.svelte files; pnpm typecheck (svelte-check) and test:unit + test:integration
also pass.
EOF
       )"
       ```
    9. `git push origin feat/phase-03.0.2-dependency-refresh`. Wait for CI green before Plan 06.
  </action>
  <verify>
    <automated>
      grep -q '"eslint-plugin-svelte": "\^3\.' package.json
      grep -q '"svelte-eslint-parser": "\^1\.' package.json
      grep -q "eslint-plugin-svelte@3\." pnpm-lock.yaml
      grep -q "svelte-eslint-parser@1\." pnpm-lock.yaml
      pnpm install --frozen-lockfile
      pnpm lint
      pnpm typecheck
      pnpm test:unit
      pnpm test:integration
      git log -1 --format=%s | grep -qE '^chore\(deps\): bump eslint-plugin-svelte'
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"eslint-plugin-svelte": "\^3\.' package.json` exits 0.
    - `grep -q '"svelte-eslint-parser": "\^1\.' package.json` exits 0.
    - `grep -q "eslint-plugin-svelte@3\." pnpm-lock.yaml` exits 0.
    - `grep -q "svelte-eslint-parser@1\." pnpm-lock.yaml` exits 0.
    - `pnpm install --frozen-lockfile` exits 0.
    - `pnpm lint` exits 0 (rune-aware parser doesn't crash; no unresolved violations).
    - `pnpm typecheck` exits 0.
    - `pnpm test:integration` exits 0.
    - `git log -1 --format=%s` matches `^chore\(deps\): bump eslint-plugin-svelte`.
    - `git diff HEAD~1 HEAD --stat` shows changes in `package.json` + `pnpm-lock.yaml` (+ optionally one or more `.svelte` files IF rune-parser fix-ups landed — those edits MUST be the minimum needed to clear lint, per D-06).
    - After push: all three CI jobs `success` on the commit.
  </acceptance_criteria>
  <done>
    Branch has commit `chore(deps): bump eslint-plugin-svelte 2→3 and svelte-eslint-parser 0.43→1.6`. Any rune-aware fix-ups landed in the same atomic unit. CI green on push.
  </done>
</task>

</tasks>

<verification>
1. `git log feat/phase-03.0.2-dependency-refresh --oneline | head -5` shows the new Svelte-lint commit on top of Plans 01-04.
2. CI on the latest commit: all three jobs green.
3. Manual sanity check: open one .svelte file using runes and confirm `pnpm lint <that-file>` still reports correctly (no parser crash, no false-positive on legitimate `$state` / `$derived`).
</verification>

<success_criteria>
- Both Svelte-lint packages on their target majors.
- D-05 peer-dep pairing honored (both bumped together).
- 5 of 10 atomic units complete.
</success_criteria>

<output>
After completion, create `.planning/phases/03.0.2-dependency-refresh-inserted/03.0.2-05-SUMMARY.md` recording: commit SHA(s), any .svelte fix-ups required by the rune-aware parser (file:line + nature), CI run URL.
</output>
