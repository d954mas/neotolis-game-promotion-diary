---
phase: 03.0.1-source-plugin-architecture
plan: 11
type: execute
wave: 10
depends_on: ["03.0.1-02", "03.0.1-03", "03.0.1-04", "03.0.1-05", "03.0.1-06", "03.0.1-07", "03.0.1-08", "03.0.1-09", "03.0.1-10"]
files_modified:
  - SOURCE-REFERENCE.md
  - .planning/ROADMAP.md
autonomous: true
requirements_addressed: []
must_haves:
  truths:
    - "SOURCE-REFERENCE.md exists at repo root (or under docs/ if that's the convention — check existing repo) and has 7 sections per CONTEXT.md D-17: Overview, Folder structure, Step-by-step Adding a new source, Common patterns, Anti-patterns, Testing, Phase 6 future"
    - "Doc references the SHIPPED YouTube tree (sources/youtube/) verbatim — file paths in the doc point to actually-existing files in the repo. NO `_template/` scaffolding; canonical YouTube IS the live reference (D-17)."
    - "Section 3 (Adding a new source) is a numbered checklist a contributor can follow end-to-end. Each step references concrete files (e.g., 'Create sources/<kind>/server/schema/<table>.ts following the pattern in sources/youtube/server/schema/videos.ts'). Step verbs are imperative, not descriptive."
    - "Section 4 (Common patterns) covers chargedFetch + token bucket reservoir, OAuth refresh (forward-only — Phase 6 placeholder for user-creds branch), snapshot UPSERT, URL parsing host-first, AdapterError taxonomy."
    - "Section 5 (Anti-patterns) lists kind-switch in cross-source code, duplicating fetchWithTimeout, skipping observability, ignoring rate-limit reserve, throwing bare Error in adapter code, reading process.env outside config/env.ts."
    - "Section 6 (Testing) covers unit (HTTP wrapper, parseUrl, mappers), integration (full adapter against real DB + mocked HTTP), smoke (production image vs mock API)."
    - "Section 7 (Phase 6 future) describes per-user credentials override entry point: pickCredentials(ctx) extension in adapter — no contract change required. References the deferred-ideas list in CONTEXT.md."
    - "ROADMAP.md updated: Phase 03.0.1 entry transitions from 'Goal: [Urgent work - to be planned]' / 'Plans: 0' / 'Plans: TBD' placeholder to the actual goal narrative + plan count (11) + plan list (11 plan filenames)."
  artifacts:
    - path: "SOURCE-REFERENCE.md"
      provides: "Sections 1-7 contributor guide for adding a new source plugin. Canonical YouTube tree as the live reference."
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 03.0.1 entry with goal + plan list (11 plans)"
  key_links:
    - from: "SOURCE-REFERENCE.md"
      to: "src/lib/sources/youtube/"
      via: "doc references concrete files in the YouTube tree"
      pattern: 'src/lib/sources/youtube/'
---

<objective>
Wave 10 — Final wave. Land SOURCE-REFERENCE.md (D-17 deliverable: sections 1-7 contributor guide pointing at the canonical YouTube tree as the live reference). Update ROADMAP.md so Phase 03.0.1's placeholder values (Goal "Urgent work - to be planned" / Plans 0 / Plans TBD) are replaced with the actual goal narrative + plan count + plan list.

This plan is the LAST wave in Phase 03.0.1 because the doc must describe the SHIPPED tree, not the planned tree (per CONTEXT.md "wave_strategy_guidance" — "doc landed last so it describes the ACTUAL shipped tree, not the planned one").

NO source code changes. NO test changes. Pure documentation + planning-artifact updates.

Purpose: D-17 contract surface lands here. Phase 03.1 (Reddit Adapter) reads this doc as the entry point for adding the new source.

Output: 1 NEW doc + 1 ROADMAP update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.0.1-source-plugin-architecture/03.0.1-CONTEXT.md
@.planning/phases/03.0.1-source-plugin-architecture/03.0.1-RESEARCH.md
@.planning/ROADMAP.md
@CLAUDE.md
@AGENTS.md

<interfaces>
SOURCE-REFERENCE.md sections 1-7 per CONTEXT.md D-17:
1. **Overview** — what is a source plugin, why this architecture (D-01..D-04 thick adapter rationale)
2. **Folder structure** — where files live (server / ui / schema / handlers; cross-source vs per-source split)
3. **Step-by-step "Adding a new source"** walkthrough (numbered list a contributor follows end-to-end)
4. **Common patterns** — chargedFetch with token bucket, OAuth refresh, snapshot UPSERT, URL parsing
5. **Anti-patterns** — kind switch in cross-source code, duplicating fetchWithTimeout, skipping observability, ignoring rate-limit reserve
6. **Testing** — unit (HTTP wrapper, parseUrl, mappers), integration (full adapter against real DB, mocked HTTP), smoke (production image vs mock API)
7. **Phase 6 future** — placeholder section describing how user-credentials will plug in

The doc location: SOURCE-REFERENCE.md at repo root (precedent: README.md at root). Verify via:
```bash
ls README.md AGENTS.md CLAUDE.md 2>&1
```
If existing reference docs (e.g. SECURITY.md) live at root, follow that convention. Otherwise place at root. CONTEXT.md mentions "SOURCE-REFERENCE.md" without a path — root is the natural choice.

ROADMAP.md Phase 03.0.1 entry update: replace the placeholder lines:
```
### Phase 03.0.1: source plugin architecture (INSERTED)

**Goal:** [Urgent work - to be planned]
**Requirements**: TBD
**Depends on:** Phase 03.0
**Plans:** 0 plans

Plans:
- [ ] TBD (run /gsd:plan-phase 03.0.1 to break down)
```
WITH a real entry mirroring the structure of Phase 02.2 / Phase 03.0:
```
### Phase 03.0.1: Source Plugin Architecture (INSERTED)
*INSERTED 2026-05-08 — restructure Phase 03.0 YouTube polling pipeline into per-source plugin folders so future sources (Reddit/Twitter/Telegram/Discord/DTF/Pikabu) drop in as new sub-folders without editing cross-source plumbing. Behavioral identity preserved (existing 03.0 tests + smoke pass unchanged); structural cleanup + standardized contract.*

**Goal**: All YouTube-prefixed code moved into src/lib/sources/youtube/{server,ui}/ with co-located schema/http/quota/snapshot/handlers/route/UI; SourceRegistry dispatches by source.kind via getAdapter / getAdapterUI; widened DataSourceAdapter contract carries credentials/quota/observability mechanics inside the adapter; standardized 5-category AdapterError taxonomy + observability API on every adapter; generic POST /api/sources/:id/refresh-content + Pull-new-content UI button on /sources/[id]; SOURCE-REFERENCE.md sections 1-7 contributor guide. Per-kind queue topology (youtube.poll.cron with key-based schedules + youtube.poll.user + youtube.backfill.user). Operator-only credentials everywhere (Phase 6 trigger for per-user override).
**Depends on**: Phase 03.0
**Requirements**: No NEW requirements. Preserves POLL-01..06, INGEST-02..04, EVENTS-01..03, SOURCES-01..02, FEED-01 BEHAVIORALLY. New artifacts (refresh-content endpoint, SOURCE-REFERENCE.md, AdapterError taxonomy, observability API) are CONTEXT-driven deliverables (D-01..D-17).
**Success Criteria** (what must be TRUE):
  1. Every existing 03.0 integration + smoke test passes UNCHANGED — behavioral identity is the load-bearing gate.
  2. src/lib/sources/youtube/{server,ui}/ tree exists with all YouTube-specific schema, services, http, handlers, route, and UI mappers; cross-source plumbing (events.ts, data-sources.ts, ingest.ts, refresh-poll.ts, tier-resolver.ts, poll-eligibility.ts, audit.ts, audit-read.ts, purge-account.ts) STAYS at src/lib/server/.
  3. SourceRegistry dispatches via getAdapter(kind) / getAdapterUI(kind); cross-source code never switches on source.kind.
  4. Widened DataSourceAdapter interface (parseUrl + observability + registerQueues + scheduleCronTicks + backfillSource + canRefreshPoll) implemented for youtube_channel; Reddit/Twitter/Telegram/Discord adapters land in Phase 03.1+.
  5. AdapterError 5-category taxonomy (transient / rate-limited / not-found / permanent / operator-issue) wired into chargedFetch error mapping; data_sources.needs_reconnect / last_error_at / last_error_kind columns updated on operator-issue / permanent / not-found.
  6. Observability API (auth + getDailyStats + getRecentAudit) per adapter; /admin/quota reads via the API instead of querying tables directly.
  7. Per-kind queues live: youtube.poll.cron (Active+Cold via pg-boss v11+ key-based schedules), youtube.poll.user, youtube.backfill.user, youtube.quota_reset, youtube.rehab, youtube.channel_context_backfill. Old kind-agnostic queues (poll.active / poll.cold / poll.user / scheduler.tick.*) RETIRED via forward-only migration.
  8. POST /api/sources/:id/refresh-content endpoint exists; AGENTS.md invariants 1-4 honored (tenant scope, cross-tenant 404, anonymous-401 sweep, audit INSERT).
  9. SOURCE-REFERENCE.md describes the SHIPPED tree (not planned); sections 1-7 per D-17.
**Plans**: 11 plans (the actual count from this phase)
**Plan list**:
- [x] 03.0.1-01-PLAN.md — Wave 0: Test scaffolding + foundation interfaces (sources/adapter.ts widened, errors.ts, registry skeleton, 5 NEW test files)
- [x] 03.0.1-02-PLAN.md — Wave 1: Move 5 YouTube schemas to sources/youtube/server/schema/ + drizzle.config glob + barrel re-export
- [x] 03.0.1-03-PLAN.md — Wave 2: Move adapter + http to sources/youtube/server/; delete legacy data-source-adapter.ts; wire registry
- [x] 03.0.1-04-PLAN.md — Wave 3: Move 4 YouTube services (quota / snapshots / url / metadata) into sources/youtube/server/
- [x] 03.0.1-05-PLAN.md — Wave 4: Move 6 worker handlers into sources/youtube/server/handlers/; refactor worker bootstrap to iterate adapter.registerQueues
- [x] 03.0.1-06-PLAN.md — Wave 5: URL detection iterate-registry refactor; youtubeAdapter.parseUrl real impl; parseAnyUrl router; preserve Reddit deferral; relocate youtube-metadata route
- [x] 03.0.1-07-PLAN.md — Wave 6: Per-kind queue topology rename (youtube.poll.cron with key-based schedules); retire orphan pg-boss state via 0021 migration; scheduler iterates adapters
- [x] 03.0.1-08-PLAN.md — Wave 7: Observability surface + rate-budget reservoir + AdapterError taxonomy in chargedFetch; data_sources error columns via 0022 migration
- [x] 03.0.1-09-PLAN.md — Wave 8: UI dual-tree (sources/youtube/ui/card-props.ts + universal EventCard.svelte shell + registry-ui wiring)
- [x] 03.0.1-10-PLAN.md — Wave 9: POST /api/sources/:id/refresh-content endpoint + youtube.backfill.user handler + RefreshContentButton.svelte + /sources/[id] route + 0023 migration (audit_action enum extension) + 7 integration tests flip live
- [x] 03.0.1-11-PLAN.md — Wave 10: SOURCE-REFERENCE.md sections 1-7 + ROADMAP placeholder fill-in
**UI hint**: yes
```

Adjust the goal narrative + completed-checkboxes to reflect actual implementation outcomes. The plan filenames in the bullet list MUST match the actual filenames written by Plans 01-10 (which were assigned in this planning session). The completion checkbox states `[x]` are populated WHEN this Phase 03.0.1 has been executed; for the planning artifact, use `[ ]` placeholders. The plan-phase output (the user-facing run of this planner) writes `[ ]` since execute-phase has not yet run; the post-execute SUMMARY would flip them to `[x]`.

For the planner output of THIS plan (Plan 11), use `[ ]` for all 11 entries — execute-phase is downstream.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Land SOURCE-REFERENCE.md sections 1-7</name>
  <read_first>
    - .planning/phases/03.0.1-source-plugin-architecture/03.0.1-CONTEXT.md § D-17 (sections list)
    - .planning/phases/03.0.1-source-plugin-architecture/03.0.1-RESEARCH.md § Architecture Patterns (Patterns 1-8) + § Common Pitfalls + § Don't Hand-Roll
    - src/lib/sources/youtube/server/index.ts (the actual barrel — referenced verbatim)
    - src/lib/sources/youtube/server/adapter.ts (the actual adapter implementation — referenced verbatim)
    - src/lib/sources/adapter.ts (the widened interface — referenced verbatim)
    - src/lib/sources/errors.ts (AdapterError taxonomy — referenced verbatim)
    - src/lib/sources/registry.ts (the registry — referenced verbatim)
    - src/lib/server/http/routes/sources.ts (POST /api/sources/:id/refresh-content — referenced)
    - README.md (or root docs convention — for placement)
  </read_first>

  <action>
    1. **Determine doc placement**. Check repo root for existing docs:
       ```bash
       ls *.md
       ```
       If README.md / AGENTS.md / CLAUDE.md / SECURITY.md / etc. live at root, place SOURCE-REFERENCE.md at root. If a `docs/` subdir exists with conventions, place under docs/. Default: repo root.

    2. **Create `SOURCE-REFERENCE.md`** with the following structure (verbatim — fill in concrete file paths after verifying the actual repo layout):

       ```markdown
       # Source Plugin Reference

       Adding a new source — Reddit, Twitter, Telegram, DTF, Pikabu, your-favorite-platform — should be a folder, not a refactor. This document describes how source plugins are organized, what contract each one implements, and the step-by-step path for landing a new one.

       The canonical YouTube implementation under `src/lib/sources/youtube/` is the live reference. Read it alongside this document; the patterns shown here are extracted from there.

       ## 1. Overview

       A **source plugin** is a self-contained module under `src/lib/sources/<kind>/` that owns everything a single platform needs:

       - HTTP wrapper with rate-limit reservoir (cron / user pools)
       - Credential resolution (operator keys today; per-user override post-Phase-6)
       - URL parsing (paste flow → externalId)
       - Polling logic (active tier, cold tier, user-initiated refresh)
       - Schema (per-source DB tables — channels metadata, video snapshots, quota counters)
       - Worker handlers + queue topology
       - UI props mapper (CardProps for /feed; admin/quota tab)
       - Observability surface (daily stats + recent audit)
       - AdapterError taxonomy mapping for HTTP error paths

       Cross-source plumbing — events table, data_sources table, audit_log, the /feed view, the events service, the ingest orchestrator — STAYS in `src/lib/server/` and is import-once / source-agnostic. The Thick Adapter principle: cross-source code never switches on `source.kind`. Adapters do.

       ### Why per-source plugins

       1. **Isolation.** A bug in YouTube's quota counter cannot affect Reddit's OAuth refresh.
       2. **Replaceability.** Swapping out the YouTube path for a new vendor / new API version is one folder of edits, not a tour through the codebase.
       3. **Discoverability.** "Where does Reddit's URL parser live?" → `sources/reddit/server/url.ts`. Always.
       4. **Onboarding.** A new contributor reads `sources/youtube/` once and can ship `sources/reddit/` from there.

       ## 2. Folder Structure

       \`\`\`
       src/lib/sources/
       ├── adapter.ts              ← Widened DataSourceAdapter interface; SourceKind / EventKind unions; AdapterContext
       ├── card-props.ts           ← Universal CardProps interface
       ├── errors.ts               ← AdapterError 5-category taxonomy
       ├── event-to-source-kind.ts ← EventKind → SourceKind helper for /feed dispatch
       ├── future-kinds.ts         ← FUTURE_KIND_HOSTS for friendly deferral messages (e.g. Reddit before Phase 03.1)
       ├── index.ts                ← Cross-source barrel
       ├── registry.ts             ← getAdapter(kind); allAdapters[]
       ├── registry-ui.ts          ← getAdapterUI(kind)
       ├── url.ts                  ← parseAnyUrl(input) — first-match-wins iteration
       └── youtube/                ← Per-source plugin
           ├── server/             ← Server-only code
           │   ├── adapter.ts      ← Implements DataSourceAdapter
           │   ├── credentials.ts  ← pickCredentials(ctx)
           │   ├── http.ts         ← chargedFetch + RateLimiterMemory reservoirs + AdapterError mapping
           │   ├── url.ts          ← youtubeParseUrl + parseYoutubeUrl
           │   ├── observability.ts ← getDailyStats + getRecentAudit
           │   ├── quota.ts        ← pickKeyForJob, incrementUsage, getThrottleState, todayPacific
           │   ├── snapshots.ts    ← writeSnapshot
           │   ├── metadata.ts     ← Channel resolution helpers
           │   ├── route-metadata.ts ← Hono sub-router for /api/youtube-metadata/*
           │   ├── index.ts        ← Barrel: { youtubeAdapter, registerQueues, scheduleCronTicks }
           │   ├── schema/         ← Drizzle pgTable definitions for YouTube-specific tables
           │   │   ├── channels.ts
           │   │   ├── videos.ts
           │   │   ├── video-snapshots.ts
           │   │   ├── service-quota-usage.ts
           │   │   ├── metadata-fetch-log.ts
           │   │   └── index.ts    ← Barrel re-export
           │   └── handlers/       ← pg-boss queue handlers
           │       ├── poll-cron.ts            ← Combined Active+Cold via job.data.tier dispatch
           │       ├── poll-active.ts          ← Active-tier batch logic
           │       ├── poll-cold.ts            ← Cold-tier batch logic
           │       ├── poll-user.ts            ← User-initiated refresh-poll (per-event)
           │       ├── backfill-user.ts        ← User-initiated "Pull new content" (per-source)
           │       ├── channel-context-backfill.ts
           │       ├── quota-reset.ts
           │       └── rehab-unavailable.ts
           └── ui/                 ← Client-importable code
               ├── card-props.ts    ← toCardProps(event) → CardProps mapper
               ├── server.ts        ← Server-safe re-export (no Svelte component imports — Pitfall 7 mitigation)
               └── index.ts         ← Client-safe entry (re-exports + future Svelte components)
       \`\`\`

       Cross-source schema (events, data_sources, audit_log, games, event_games, game_steam_listings, api_keys_steam, Better Auth tables) STAYS in `src/lib/server/db/schema/`. Drizzle config glob covers both:

       \`\`\`typescript
       schema: [
         "./src/lib/server/db/schema/*.ts",
         "./src/lib/sources/*/server/schema/*.ts",
       ],
       \`\`\`

       ## 3. Step-by-Step: Adding a New Source (e.g. Reddit)

       Walking through Reddit. Substitute `reddit_account` / `reddit_post` / `reddit` etc. for any new source.

       ### 3.1 Extend the SourceKind / EventKind unions

       In `src/lib/sources/adapter.ts`, the unions already include `reddit_account` and `reddit_post`. New kinds beyond the v1 set need adding here. The `event-to-source-kind.ts` helper also maps the new pair.

       ### 3.2 Create the per-source folder

       \`\`\`bash
       mkdir -p src/lib/sources/reddit/server/{schema,handlers}
       mkdir -p src/lib/sources/reddit/ui
       \`\`\`

       ### 3.3 Define the per-source schemas

       Create one file per table under `src/lib/sources/reddit/server/schema/`. Reddit examples:
       - `accounts.ts` — operator-OAuth-refreshed account credentials (envelope-encrypted; mirror api_keys_steam shape)
       - `posts.ts` — post metadata cache (subreddit, post id, permalink) keyed by post id (public-data, no user_id)
       - `post-snapshots.ts` — immutable time-series of (post_id, polled_at, upvotes, comment_count, awards)

       Re-export from a sibling `index.ts` barrel; the cross-source `src/lib/server/db/schema/index.ts` already pulls from the glob. Run `pnpm db:generate` to produce the migration; review the SQL; commit.

       ### 3.4 Implement the HTTP wrapper

       Copy the YouTube `http.ts` pattern. Key points:
       - Reservoirs: `RateLimiterMemory` cron pool ~80%, user pool ~20%. Reddit's free tier is 60 req/min OAuth-bearer; default reservoir 48 cron / 12 user per minute.
       - AdapterError mapping: 429 → rate-limited; 401/403 → operator-issue (OAuth refresh); 404 → not-found; 5xx → transient.
       - OAuth refresh: detect 401 once, refresh the bearer via `client_credentials` grant, retry once. Phase 6 placeholder for per-user refresh-token override.

       ### 3.5 Implement the adapter

       In `src/lib/sources/reddit/server/adapter.ts`, export `redditAccountAdapter: DataSourceAdapter`. Implement the contract methods:
       - `kind: "reddit_account"`
       - `parseUrl(url)` — host check first (`reddit.com` / `redd.it` / `old.reddit.com`); extract subreddit + post-id from `/r/<sub>/comments/<id>/...`
       - `pollContent(source, since, ctx)` — list latest /user/<account>/submitted with `?after=` for incremental
       - `pollStats(events, source, ctx)` — batch /by_id/t3_xxx,t3_yyy
       - `pollStatsByVideoId` (rename `pollStatsByExternalId` if signature awkward — refactor opportunity for v0.2)
       - `observability` — auth.kind === "operator-oauth-app-only"; quota.getDailyStats reads a Reddit-specific quota counter
       - `registerQueues(boss)` + `scheduleCronTicks(boss)` — declare reddit.poll.cron / reddit.poll.user / reddit.backfill.user; schedule cron ticks
       - `backfillSource(source, ctx)` — boss.send to reddit.backfill.user with singletonKey

       ### 3.6 Wire the registry

       In `src/lib/sources/registry.ts`:
       \`\`\`typescript
       import { redditAccountAdapter } from "./reddit/server/index.js";
       const registry = new Map<SourceKind, DataSourceAdapter>([
         ["youtube_channel", youtubeAdapter],
         ["reddit_account",   redditAccountAdapter],
       ]);
       \`\`\`
       Same for `registry-ui.ts`. That is the ENTIRE bootstrap edit. Cross-source code (refresh-poll, ingest, /feed loader, /admin/quota) automatically picks up the new adapter via getAdapter / allAdapters.

       ### 3.7 UI props mapper

       Create `src/lib/sources/reddit/ui/card-props.ts` exporting `toCardProps(event) → CardProps` for reddit_post events. Re-export via `ui/server.ts` (server-safe) and `ui/index.ts` (client-safe — adds Svelte components if any).

       ### 3.8 Tests

       Mirror the YouTube test files under `tests/unit/sources/reddit/` and `tests/integration/`. The cross-source tests (anonymous-401 sweep, tenant-scope, refresh-content endpoint) AUTOMATICALLY cover the new kind once the registry has the entry — no edit needed for the new endpoint behavior.

       ## 4. Common Patterns

       ### 4.1 chargedFetch with token bucket reservoir

       Every adapter HTTP wrapper consumes from a per-origin reservoir BEFORE the request:

       \`\`\`typescript
       const cronReservoir = new RateLimiterMemory({ points: BUDGET * 0.8, duration: WINDOW_SECONDS });
       const userReservoir = new RateLimiterMemory({ points: BUDGET * 0.2, duration: WINDOW_SECONDS });

       export async function chargedFetch(url, picked, units, ctx) {
         const reservoir = ctx.origin === "cron" ? cronReservoir : userReservoir;
         try {
           await reservoir.consume(picked.apiKeyId, units);
         } catch (rejRes) {
           throw new AdapterError("Reservoir exhausted", { category: "rate-limited", retryAfterMs: rejRes.msBeforeNext });
         }
         // ... fetch + status mapping ...
       }
       \`\`\`

       Reconcile reservoirs on worker boot from the persistent counter table (sources/<kind>/server/quota.ts).

       ### 4.2 OAuth refresh (Phase 6 future-shape — Reddit / Twitter)

       The OAuth-refresh path is internal to the credentials wrapper:

       \`\`\`typescript
       // sources/reddit/server/credentials.ts
       export async function pickCredentials(ctx: AdapterContext): Promise<RedditCredentials> {
         // 1) Operator app-only bearer (refreshed by a sidecar cron job)
         // 2) Phase 6: if ctx.userId is set AND the user has a per-user OAuth row, use that
         //    Until Phase 6, ctx.userId is informational only.
         return loadOperatorBearer();
       }
       \`\`\`

       ### 4.3 Snapshot UPSERT (idempotent)

       \`\`\`typescript
       // sources/reddit/server/snapshots.ts
       export async function writeSnapshot(s: StatsSnapshot): Promise<void> {
         await db.insert(redditPostSnapshots).values({
           postId: s.eventId,
           polledAt: roundToMinute(s.polledAt),
           upvotes: s.metrics?.upvotes ?? 0,
           // ... other metrics ...
         }).onConflictDoNothing();
       }
       \`\`\`

       The unique constraint `(post_id, polled_at, metric_key)` (with polled_at truncated to the minute) makes worker retries within the same minute a no-op. See sources/youtube/server/snapshots.ts for the canonical pattern.

       ### 4.4 URL parsing — host-first

       \`\`\`typescript
       export function redditParseUrl(input: string): ParsedUrl | null {
         let url: URL;
         try { url = new URL(input.trim()); } catch { return null; }
         const host = url.hostname.toLowerCase();
         if (!RD_HOSTS.has(host)) return null; // Pitfall 2: host check FIRST
         const m = url.pathname.match(/^\\/r\\/([^/]+)\\/comments\\/([a-z0-9]+)/i);
         if (!m) return null;
         return { kind: "reddit_post", externalId: m[2], metadata: { subreddit: m[1] } };
       }
       \`\`\`

       Order in the registry = priority (first-match-wins). Adapters with overlapping host claims (rare) must NOT happen — host claims are exclusive by convention.

       ### 4.5 AdapterError taxonomy

       Every non-2xx response THROWS an `AdapterError` with one of 5 categories. The cross-source worker-handler wrapper routes:

       \`\`\`typescript
       try { /* ... */ } catch (err) {
         if (err instanceof AdapterError) {
           if (err.category === "rate-limited" || err.category === "transient") throw err; // pg-boss retries
           if (err.category === "operator-issue") { await markSourceNeedsReconnect(...); return; }
           if (err.category === "permanent")     { await markSourceNeedsReconnect(...); return; }
           if (err.category === "not-found")     { /* mark event unavailable */; return; }
         }
         throw err;
       }
       \`\`\`

       ## 5. Anti-Patterns

       - **Switch on `source.kind` outside `src/lib/sources/`.** Cross-source code calls `getAdapter(kind)` for any per-kind logic. The temptation is real (refresh-poll's "is this kind pollable?" check is the canonical example) — resist via an adapter-contract method like `canRefreshPoll(eventKind)`.
       - **Duplicating fetchWithTimeout** across adapters. Each adapter has ITS OWN HTTP wrapper because rate-limit policy is per-source — but the timeout + AbortController shape is identical. Resist the urge to extract; prefer copy + adapt.
       - **Skipping observability.** Every adapter MUST implement `observability.auth` + `observability.quota.{getDailyStats, getRecentAudit}`. /admin/quota's per-kind tab depends on it.
       - **Ignoring the rate-limit reserve.** Don't let cron-heavy days starve user-initiated calls. The reservoir split (D-09) is the floor; UI clicks that hit "rate-limited" because cron used 100% of the daily budget = breaks UX promise.
       - **Throwing bare `new Error("...")`** in adapter HTTP wrapper. Always wrap in `AdapterError({ category, ... })`. Cross-source can't otherwise distinguish retry-able from operator-action-required.
       - **Reading `process.env`** outside `src/lib/server/config/env.ts`. ESLint enforces. Adapters import env via `$lib/server/config/env.js`.
       - **Hand-rolling rate-limit logic.** Use `rate-limiter-flexible` (already in package.json). YouTube's reservoir is in-memory `RateLimiterMemory`; if multi-process, swap to `RateLimiterPostgres`.
       - **Using `<svelte:component this={X}>`** — deprecated in Svelte 5 runes mode. Use `{@const C = X}; <C ... />`.

       ## 6. Testing

       ### 6.1 Unit tests (`tests/unit/sources/<kind>/...`)

       - HTTP wrapper: reservoir consume, AdapterError mapping by status code (RESEARCH.md Example 2)
       - parseUrl: host-first claim, externalId extraction, returns null on non-host
       - card-props mapper: variants (ok / no-stats / authorIsMe), thumbnail rule, formatStat formatting

       Mock at the module level via `vi.mock(...)`; the YouTube tests are the reference (`tests/unit/sources/youtube/card-props.test.ts`).

       ### 6.2 Integration tests (`tests/integration/...`)

       - Adapter against real DB + mocked HTTP: enqueue a poll job, assert snapshot row written.
       - Cross-tenant 404: any new endpoint follows the `tests/integration/tenant-scope.test.ts` matrix (body MUST NOT contain "forbidden" / "permission").
       - Anonymous-401 sweep: every new `/api/*` route added to `MUST_BE_PROTECTED` allowlist.

       ### 6.3 Smoke (`tests/smoke/run.sh`)

       Self-host parity gate. Boots the production image with mock APIs (e.g. `tests/smoke/lib/youtube-mock.sh`); asserts the polling pipeline writes a snapshot, cron registrations happen, cross-tenant + anonymous-401 invariants hold. NEW sources extend the smoke gate IF they introduce new cron registrations or new mock APIs. Reddit's smoke addition: a Reddit-mock reverse-proxy + assertion that a reddit_post snapshot is written end-to-end.

       ## 7. Phase 6 Future: Per-User Credentials

       v0.1 (today) ships **operator-only credentials everywhere** (D-05). Phase 6 trigger: "≥1 power user trips 95% of operator quota for one source." When that fires, the adapter's `pickCredentials(ctx)` extends:

       \`\`\`typescript
       export async function pickCredentials(ctx: AdapterContext): Promise<Credentials> {
         if (ctx.userId !== null) {
           const userKey = await loadUserKey(ctx.userId);
           if (userKey !== null) return userKey;
         }
         return loadOperatorKey();
       }
       \`\`\`

       NO contract change required. The widened DataSourceAdapter interface already passes `ctx.userId` through every method. Phase 6 adds a 6th AdapterError category `user-auth` (when the per-user key is invalid; revoked; missing) which the cross-source handler routes to "user must reconnect their key" UI.

       Other Phase 6 items:
       - Per-user QUOTA-01 dashboards (re-uses `observability.quota.getDailyStats` API contract — no adapter edit)
       - Per-user 429 surfacing UI (toast + badge; reads adapter.observability per-user when ctx.userId is set)
       - Reddit per-user OAuth (phase trigger; replaces operator app-only with per-user refresh-token flow)

       ---

       **Maintenance.** This document describes the SHIPPED architecture as of Phase 03.0.1 (2026-05-08). When Phase 03.1 (Reddit) lands, the Reddit-specific adapter sections may surface refinements that update sections 4-6. Updates land via PR; squash-merge per AGENTS.md Workflow.

       **Canonical reference.** The YouTube tree (`src/lib/sources/youtube/`) IS the live reference. When this doc and the code disagree, the code wins; file a doc-update PR.
       ```

       NOTE: this is a LONG document. Land it as one file. Verify the file paths it references actually exist by running `ls` on each one cited (e.g., `ls src/lib/sources/youtube/server/handlers/poll-cron.ts`) AFTER Plan 07's commit lands. If a path doesn't match the shipped tree, ADJUST the doc to match — the doc must describe what shipped.

    2. **Run sanity checks**:
       ```bash
       # Verify every file path the doc references actually exists
       grep -oE "src/lib/sources/youtube/[a-z/.-]+\\.[tj]s" SOURCE-REFERENCE.md | sort -u | while read p; do test -f "$p" || echo "MISSING: $p"; done
       grep -oE "src/lib/sources/[a-z/.-]+\\.[tj]s" SOURCE-REFERENCE.md | sort -u | while read p; do test -f "$p" || echo "MISSING: $p"; done
       ```
       Each should produce ZERO "MISSING:" lines. If any path is reported missing, either the doc has a typo or the shipped tree differs — adjust the doc.
  </action>

  <verify>
    <automated>test -f SOURCE-REFERENCE.md && pnpm exec eslint . && pnpm test</automated>
  </verify>

  <acceptance_criteria>
    - File `SOURCE-REFERENCE.md` exists at repo root
    - File contains the literal section headers `## 1. Overview`, `## 2. Folder Structure`, `## 3. Step-by-Step: Adding a New Source`, `## 4. Common Patterns`, `## 5. Anti-Patterns`, `## 6. Testing`, `## 7. Phase 6 Future`: `grep -cE "^## [1-7]\\." SOURCE-REFERENCE.md` returns 7
    - File contains the literal `src/lib/sources/youtube/` (proof it references the canonical tree): `grep -c "src/lib/sources/youtube/" SOURCE-REFERENCE.md` returns at least 5
    - File DOES NOT contain `_template/` (D-17: no scaffolding files; canonical YouTube IS the live reference): `grep -c "_template/" SOURCE-REFERENCE.md` returns 0
    - All file paths referenced in the doc that match the pattern `src/lib/sources/youtube/...\\.ts` actually exist on disk: the verification grep loop produces 0 "MISSING:" lines
    - File contains at least 5 references to AdapterError-related concepts: `grep -cE "AdapterError|rate-limited|operator-issue|not-found|transient|permanent" SOURCE-REFERENCE.md` returns at least 5
    - File contains the literal `getAdapter(kind)` AND `getAdapterUI(kind)` (proof the registry contract is documented)
    - File contains the literal `pickCredentials` (proof the Phase 6 contract surface is documented)
    - File contains the literal `RateLimiterMemory` AND `cron pool` (proof the rate-budget reservoir is documented)
    - File contains the literal `host check FIRST` (proof Pitfall 2 is documented)
    - `pnpm exec eslint .` exits 0 (no source code changes, but verify nothing broke)
    - `pnpm test` exits 0 (no test changes, but verify the suite still passes)
  </acceptance_criteria>

  <done>
    SOURCE-REFERENCE.md sections 1-7 land at repo root; doc accurately describes the shipped tree; all referenced file paths verified to exist.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md Phase 03.0.1 entry</name>
  <read_first>
    - .planning/ROADMAP.md (the existing Phase 03.0.1 placeholder + the Phase 02.2 / Phase 03.0 entries that demonstrate the format)
    - All 11 plan filenames in .planning/phases/03.0.1-source-plugin-architecture/*-PLAN.md (confirm exact filenames for the bullet list)
  </read_first>

  <action>
    1. **List the actual plan filenames**:
       ```bash
       ls .planning/phases/03.0.1-source-plugin-architecture/*-PLAN.md
       ```
       Expected: 11 entries (03.0.1-01-... through 03.0.1-11-...). Capture the EXACT filenames for the bullet list.

    2. **Update `.planning/ROADMAP.md`**. Find the Phase 03.0.1 entry (search for `### Phase 03.0.1:`). Replace its placeholder content with the verbatim entry from <interfaces> above. Adjust the Plan list bullets to match the actual filenames from step 1.

       Each bullet uses checkbox `[ ]` because execute-phase has not yet run for 03.0.1. The post-execute SUMMARY would change them to `[x]` after each plan completes; for THIS planning artifact, use `[ ]`.

    3. **Update the Progress table** at the bottom of ROADMAP.md. The row for "Phase 03.0.1" probably reads `0/TBD` or `0/0`. Update to `0/11 | Plan list ready (run /gsd:execute-phase 03.0.1) | -`. Match the format of other in-progress phases.

    4. **Verify ROADMAP.md still parses** (no broken markdown). Eye-check for: heading levels consistent, bullet lists indented, code blocks closed, no orphan markdown.

    5. **Run sanity check**:
       ```bash
       grep -c "03.0.1-" .planning/ROADMAP.md
       ```
       Should return at least 11 (one match per plan filename in the bullet list) PLUS some matches in the Progress table / overview text.
  </action>

  <verify>
    <automated>test -f .planning/ROADMAP.md && grep -c "Phase 03.0.1" .planning/ROADMAP.md
</automated>
  </verify>

  <acceptance_criteria>
    - File `.planning/ROADMAP.md` Phase 03.0.1 section no longer contains the literal `[Urgent work - to be planned]`: `grep -c "Urgent work - to be planned" .planning/ROADMAP.md` returns 0
    - File `.planning/ROADMAP.md` contains the literal `**Plans**: 11 plans` AND `03.0.1-01-PLAN.md` AND `03.0.1-11-PLAN.md` (the bullet list — first and last entries)
    - File `.planning/ROADMAP.md` Phase 03.0.1 entry has a real Goal description (not the placeholder): `grep -A2 "### Phase 03.0.1:" .planning/ROADMAP.md | grep -c "TBD"` returns 0
    - File `.planning/ROADMAP.md` Progress table has the Phase 03.0.1 row updated from `0/TBD` to `0/11`
    - `grep -c "03.0.1-" .planning/ROADMAP.md` returns at least 11 (the bullet-list filenames)
  </acceptance_criteria>

  <done>
    ROADMAP.md Phase 03.0.1 entry no longer placeholder; 11-plan bullet list reflects actual phase output.
  </done>
</task>

</tasks>

<verification>
- SOURCE-REFERENCE.md exists at repo root with 7 sections per D-17.
- All file paths in the doc verified to exist on disk (post-Plan-10 shipped tree).
- ROADMAP.md Phase 03.0.1 placeholder replaced with real entry.
- 11-plan bullet list with the actual filenames.
- Progress table updated.
- No source code changes; full suite + smoke unaffected.
</verification>

<success_criteria>
1. SOURCE-REFERENCE.md sections 1-7 land per D-17.
2. Doc references the SHIPPED tree (no _template/ scaffolding).
3. ROADMAP.md Phase 03.0.1 entry filled in.
4. 11-plan bullet list matches actual filenames.
5. Progress table updated.
</success_criteria>

<output>
After completion, create `.planning/phases/03.0.1-source-plugin-architecture/03.0.1-11-SUMMARY.md` recording: doc location, section count verification, ROADMAP diff, file-path verification result.
</output>
