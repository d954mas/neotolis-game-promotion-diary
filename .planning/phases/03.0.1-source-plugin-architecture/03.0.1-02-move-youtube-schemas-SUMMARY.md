---
phase: 03.0.1-source-plugin-architecture
plan: 02
subsystem: database
tags: [drizzle, schema, source-plugin-architecture, youtube, refactor]

# Dependency graph
requires:
  - phase: 03.0.1-01
    provides: src/lib/sources/ scaffold + per-source schema folder (files moved via git mv)
provides:
  - relative-import fixes inside the moved YouTube schema files (compiled-clean state)
  - per-source schema barrel src/lib/sources/youtube/server/schema/index.ts
  - cross-source barrel re-export from the new path (existing call sites compile via $lib/server/db/schema/index.js)
  - drizzle.config.ts schema glob extension covering src/lib/sources/*/server/schema/*.ts
  - 14 consumer call-site updates routing per-file imports through the cross-source barrel
affects: [03.0.1-03 (adapter+http move), 03.0.1-04 (services move), 03.0.1-05 (handlers move), 03.1+ (Reddit / future per-source schemas use the same glob)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Drizzle schema glob array — discovers cross-source + per-source schemas without a hand-maintained list (Pattern 6 from RESEARCH.md)"
    - "Cross-source barrel re-export from per-source barrel — single line keeps consumers compiling without per-file path edits"

key-files:
  created:
    - src/lib/sources/youtube/server/schema/index.ts (per-source barrel; 5 re-exports)
  modified:
    - drizzle.config.ts (schema field: string → array of two globs)
    - src/lib/server/db/schema/index.ts (5 youtube-* re-exports replaced by 1 re-export from new barrel)
    - src/lib/sources/youtube/server/schema/video-snapshots.ts (uuidv7 import: ../../ids.js → $lib/server/ids.js)
    - src/lib/sources/youtube/server/schema/metadata-fetch-log.ts (user import: ./auth.js → $lib/server/db/schema/auth.js)
    - 14 consumer files redirected from per-file youtube-*.js imports to the cross-source barrel (services, routes, worker handlers, scripts, tests)

key-decisions:
  - "Route per-file consumers through the cross-source barrel ($lib/server/db/schema/index.js) rather than the new per-source path. Minimal churn; preserves the plan's must-have 'every existing consumer of `import { youtubeVideos } from \"$lib/server/db/schema/index.js\"` continues to compile' contract; later plans (Plan 03 adapter move) will tighten as needed."
  - "Use SvelteKit alias paths ($lib/...) for the broken relative imports inside moved files instead of new relative paths. Independent of folder depth; survives further moves; matches the project's existing alias convention."

patterns-established:
  - "Per-source schema folder under src/lib/sources/<kind>/server/schema/. Discovered by drizzle-kit via glob; Reddit/Twitter/Telegram follow the same shape."
  - "Cross-source barrel as the stable consumer surface for tenant + public-data schemas, regardless of where the underlying files live on disk."

requirements-completed: []  # POLL-01 and POLL-04 were closed by Phase 03.0; this plan is a structural relocation that preserves behavioral identity rather than adding new requirements coverage.

# Metrics
duration: ~12 min
completed: 2026-05-08
---

# Phase 03.0.1 Plan 02: Move YouTube Schemas Summary

**Relocated 5 YouTube schema files into the per-source folder (src/lib/sources/youtube/server/schema/), fixed two relative imports broken by the move, and routed every per-file consumer through the cross-source barrel — drizzle-kit sees identical pgTable identities, db:check exits 0, no SQL drift.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-05-08T15:11:34Z
- **Completed:** 2026-05-08T15:23:45Z
- **Tasks:** 1 of 1
- **Files modified:** 32 (5 schema files at new path + 1 new per-source barrel + 1 cross-source barrel + 1 drizzle config + 14 consumer redirects + 10 test redirects)

## Accomplishments

- 5 YouTube schema files now live at `src/lib/sources/youtube/server/schema/{channels,videos,video-snapshots,service-quota-usage,metadata-fetch-log}.ts` with their pgTable("youtube_*") identities preserved verbatim — drizzle-kit's diff is empty.
- Cross-source barrel `src/lib/server/db/schema/index.ts` re-exports from the new per-source barrel via `export * from "$lib/sources/youtube/server/schema/index.js"`. Existing imports of the form `import { youtubeVideos } from "$lib/server/db/schema/index.js"` continue to compile without edit.
- Drizzle config extended to the array form `["./src/lib/server/db/schema/*.ts", "./src/lib/sources/*/server/schema/*.ts"]` so future Reddit / Twitter / Telegram schemas auto-discover.
- Two relative imports inside the moved files (broken by the move) repointed to SvelteKit alias paths: `video-snapshots.ts` uses `$lib/server/ids.js` for uuidv7; `metadata-fetch-log.ts` uses `$lib/server/db/schema/auth.js` for the user FK reference.
- 14 consumer call-sites that imported from per-file `db/schema/youtube-*.js` paths were redirected to the cross-source barrel — preserves behavioral identity without touching the schema files themselves.

## Task Commits

1. **Task 1: relocate YouTube schemas + redirect imports through barrel** — `3b5bb98` (feat)

_Plan metadata commit will follow this Summary._

## Files Created / Modified

### Created
- `src/lib/sources/youtube/server/schema/index.ts` — per-source barrel; 5 re-exports.

### Modified (schema layer)
- `drizzle.config.ts` — `schema: string` → `schema: string[]` covering both cross-source and per-source globs.
- `src/lib/server/db/schema/index.ts` — 5 per-file `youtube-*` exports replaced by single re-export from the new per-source barrel.
- `src/lib/sources/youtube/server/schema/video-snapshots.ts` — `import { uuidv7 } from "../../ids.js"` → `from "$lib/server/ids.js"`.
- `src/lib/sources/youtube/server/schema/metadata-fetch-log.ts` — `import { user } from "./auth.js"` → `from "$lib/server/db/schema/auth.js"`.

### Modified (consumer redirects — per-file → cross-source barrel)
- `src/lib/server/dto.ts`
- `src/lib/server/services/admin-quota-read.ts`
- `src/lib/server/services/data-sources.ts`
- `src/lib/server/services/ingest.ts`
- `src/lib/server/services/poll-eligibility.ts`
- `src/lib/server/services/quota.ts`
- `src/lib/server/services/refresh-poll.ts`
- `src/lib/server/services/youtube-metadata.ts`
- `src/lib/server/services/youtube-quota-tracker.ts`
- `src/lib/server/services/youtube-snapshot-writer.ts`
- `src/routes/feed/+page.server.ts`
- `src/routes/sources/+page.server.ts`
- `src/worker/handlers/rehab-unavailable.ts`
- `src/worker/handlers/youtube-channel-context-backfill.ts`
- `src/worker/handlers/youtube-quota-reset.ts`
- `scripts/seed-design-data.ts`
- `tests/integration/admin-quota.test.ts`
- `tests/integration/ingest-paste-then-poll.test.ts`
- `tests/integration/ingest.test.ts`
- `tests/integration/poll-worker-tx-boundary.test.ts`
- `tests/integration/refresh-now.test.ts`
- `tests/integration/refresh-poll-cooldown.test.ts`
- `tests/integration/scheduler-enqueue.test.ts`
- `tests/integration/youtube-quota-throttle.test.ts`
- `tests/integration/youtube-quota-tracker.test.ts`
- `tests/integration/youtube-snapshot-read.test.ts`
- `tests/unit/youtube-snapshot-writer.test.ts`

## drizzle.config.ts Diff (substantive)

```diff
-  schema: "./src/lib/server/db/schema/*.ts",
+  schema: [
+    "./src/lib/server/db/schema/*.ts",
+    "./src/lib/sources/*/server/schema/*.ts",
+  ],
```

## Verification Results

| Gate | Command | Result |
|------|---------|--------|
| Schema drift | `pnpm db:check` | exit 0 — "Everything's fine" |
| Type-check | `pnpm exec tsc --noEmit` | exit 0 — no errors |
| Lint | `pnpm exec eslint .` | exit 0 — no warnings |
| Unit tests | `pnpm test:unit` | 270 passed / 19 todo, 30 files passed / 1 skipped |
| Integration tests | `pnpm test` | environmental fail — local Windows env has no Postgres running. Every failure is `ECONNREFUSED 127.0.0.1:5432` from `seedUserDirectly`; **0 module-resolution errors** (`grep -c "Cannot find module" = 0`). Behavioral identity is therefore preserved up to the test code path; CI Postgres-equipped job will run these. |
| Smoke test | `bash tests/smoke/run.sh` | not runnable locally — the actual smoke entrypoint is `tests/smoke/self-host.sh` which is CI-only (requires Docker + Postgres + ALLOW_LOCAL_SMOKE flag). The plan referenced a non-existent `run.sh` (deferred to CI). |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Blocking issue] Plan 01 (parallel agent) committed the schema move without updating the cross-source barrel or fixing relative imports**

- **Found during:** Task 1, after running `pnpm exec tsc --noEmit`.
- **Issue:** The parallel agent's commit `2173217 feat(03.0.1-01): land foundation interfaces in src/lib/sources/` moved the 5 schema files via `git mv` but left:
  (a) the cross-source barrel `src/lib/server/db/schema/index.ts` still re-exporting from `./youtube-*.js` paths that no longer exist (HEAD was uncompilable);
  (b) two relative imports inside the moved files broken (`../../ids.js` and `./auth.js` resolved against the new folder pointed at non-existent files).
- **Fix:**
  - Replaced the 5 per-file `youtube-*` exports in the cross-source barrel with one re-export from the new per-source barrel (the plan's intended Step 5).
  - Repointed broken imports to SvelteKit alias paths (`$lib/server/ids.js`, `$lib/server/db/schema/auth.js`) — independent of folder depth; survives any future move.
- **Files modified:** `src/lib/server/db/schema/index.ts`, `src/lib/sources/youtube/server/schema/{video-snapshots,metadata-fetch-log}.ts`.
- **Commit:** 3b5bb98.

**2. [Rule 3 — Blocking issue] Many consumers imported from per-file paths the plan did not anticipate**

- **Found during:** Task 1, second `pnpm exec tsc --noEmit` run.
- **Issue:** The plan's must-have stated "every existing consumer of `import { youtubeVideos } from '$lib/server/db/schema/index.js'` continues to compile". 27 call-sites (services, routes, worker handlers, scripts, tests) instead imported per-file paths like `import { youtubeVideos } from "../db/schema/youtube-videos.js"`. After the move, all of them errored.
- **Fix:** Redirected each to the cross-source barrel (`../db/schema/index.js` or `$lib/server/db/schema/index.js`). Plan 03 anticipates a similar pattern (its line 127: "Update any `import { ... } from "../db/schema/youtube-videos.js"` to `import { ... } from "$lib/server/db/schema/index.js"`") so this is consistent with phase intent.
- **Files modified:** 14 source files + 10 test files + 1 script (listed above).
- **Commit:** 3b5bb98.

**3. [Rule 3 — Plan acceptance criteria mismatch] Smoke runner path**

- **Found during:** Verification step.
- **Issue:** Plan referenced `bash tests/smoke/run.sh`; that file does not exist in this repo. The actual smoke runner is `tests/smoke/self-host.sh` and is CI-only (Docker + Postgres required; gated by `ALLOW_LOCAL_SMOKE=1`).
- **Fix:** Did not run the smoke runner locally. Documented as deferred-to-CI; the next PR's CI smoke job is the actual gate.
- **Files modified:** none.

**4. [Rule 3 — Plan acceptance criteria mismatch] grep verification literal-pattern mismatch**

- **Found during:** Verification step.
- **Issue:** The plan's acceptance criteria included single-line greps like `grep -c 'pgTable("youtube_channels"' src/lib/sources/youtube/server/schema/channels.ts returns 1`. The actual code uses a multi-line form: `pgTable(\n  "youtube_channels",\n...`. Single-line grep returns 0; multi-line grep returns 1. The pgTable IDENTITY (the first-arg string) is preserved — confirmed via multi-line `Grep multiline=true` against all 5 files. The acceptance criterion's grep wording was overly literal; the underlying intent (identity preserved) is satisfied.
- **Fix:** None needed — pgTable identities are unchanged. Plan acceptance criterion grep wording captured below for next-plan editor's awareness.

### Notes (no fix required)

- The plan's "verbatim 'public external data, no tenant scope' comment ≥5 times" criterion was overly strict — only 3 of the 5 files contain that exact phrase in HEAD; one says "operator-side counter, no tenant scope" (apt for the operator-quota counter); `metadata-fetch-log.ts` is tenant-OWNED and never had the phrase. The intent (each public-data file documents its no-tenant-scope rationale verbatim) is fully preserved across all 5 files; only the specific phrase wording varies per the underlying file's domain.

### Authentication Gates

None.

## Self-Check

Verified after writing this Summary:

- [x] `src/lib/sources/youtube/server/schema/channels.ts` exists; `pgTable("youtube_channels", ...)` identity preserved.
- [x] `src/lib/sources/youtube/server/schema/videos.ts` exists; `pgTable("youtube_videos", ...)` preserved.
- [x] `src/lib/sources/youtube/server/schema/video-snapshots.ts` exists; `pgTable("youtube_video_snapshots", ...)` preserved; relative import fixed.
- [x] `src/lib/sources/youtube/server/schema/service-quota-usage.ts` exists; `pgTable("youtube_service_quota_usage", ...)` preserved.
- [x] `src/lib/sources/youtube/server/schema/metadata-fetch-log.ts` exists; `pgTable("youtube_metadata_fetch_log", ...)` preserved; relative import fixed.
- [x] `src/lib/sources/youtube/server/schema/index.ts` exists; 5 re-exports.
- [x] Old paths `src/lib/server/db/schema/youtube-*.ts` no longer exist.
- [x] Cross-source barrel `src/lib/server/db/schema/index.ts` contains `export * from "$lib/sources/youtube/server/schema/index.js"` and no `export * from "./youtube-*.js"`.
- [x] `drizzle.config.ts` contains `"./src/lib/sources/*/server/schema/*.ts"`.
- [x] Commit `3b5bb98` exists in `git log`.
- [x] `eslint-plugin-tenant-scope/` unchanged: `git diff HEAD~1 eslint-plugin-tenant-scope/` empty.
- [x] `pnpm db:check` exit 0.
- [x] `pnpm exec tsc --noEmit` exit 0.
- [x] `pnpm exec eslint .` exit 0.
- [x] `pnpm test:unit` 270/270 pass.

## Self-Check: PASSED
