---
phase: 03.0.1-source-plugin-architecture
plan: 04
type: execute
wave: 3
depends_on: ["03.0.1-03"]
files_modified:
  - src/lib/sources/youtube/server/quota.ts
  - src/lib/sources/youtube/server/snapshots.ts
  - src/lib/sources/youtube/server/url.ts
  - src/lib/sources/youtube/server/metadata.ts
  - src/lib/server/services/youtube-quota-tracker.ts
  - src/lib/server/services/youtube-snapshot-writer.ts
  - src/lib/server/services/youtube-url.ts
  - src/lib/server/services/youtube-metadata.ts
  - src/lib/sources/youtube/server/index.ts
autonomous: true
requirements_addressed:
  - POLL-01
  - POLL-04
must_haves:
  truths:
    - "Four YouTube-specific service files moved into src/lib/sources/youtube/server/ via git mv: quota-tracker → quota.ts; snapshot-writer → snapshots.ts; youtube-url → url.ts; youtube-metadata → metadata.ts"
    - "Cross-source services (refresh-poll.ts, ingest.ts, tier-resolver.ts, poll-eligibility.ts, data-sources.ts, events.ts, purge-account.ts) STAY in src/lib/server/services/ — these orchestrate across kinds, not within YouTube"
    - "All importers updated; no stale imports in src/ or tests/"
    - "Behavioral identity gate: existing 03.0 integration + smoke tests pass UNCHANGED"
  artifacts:
    - path: "src/lib/sources/youtube/server/quota.ts"
      provides: "youtube-quota-tracker (pickKeyForJob, hashApiKeyId, incrementUsage, getThrottleState, todayPacific) — relocated"
    - path: "src/lib/sources/youtube/server/snapshots.ts"
      provides: "youtube-snapshot-writer (writeSnapshot two-phase tx) — relocated"
    - path: "src/lib/sources/youtube/server/url.ts"
      provides: "parseYoutubeUrl YouTube-specific URL → channelId/handle/videoId decomposition — relocated"
    - path: "src/lib/sources/youtube/server/metadata.ts"
      provides: "youtube-metadata channel resolution helpers — relocated"
  key_links:
    - from: "src/lib/sources/youtube/server/adapter.ts"
      to: "src/lib/sources/youtube/server/quota.ts"
      via: "import (intra-folder relative import)"
      pattern: 'from "\\./quota\\.js"'
    - from: "src/lib/sources/youtube/server/adapter.ts"
      to: "src/lib/sources/youtube/server/snapshots.ts"
      via: "import"
      pattern: 'from "\\./snapshots\\.js"'
---

<objective>
Wave 3 — Move YouTube-specific services (quota tracker, snapshot writer, URL helper, metadata helper) into per-source folder. Cross-source services (refresh-poll, ingest, tier-resolver, poll-eligibility, data-sources, events, purge-account) STAY in src/lib/server/services/ per CONTEXT.md D-14 + Open Question #1 in RESEARCH.md.

After this plan, the per-source `sources/youtube/server/` folder contains: adapter.ts, http.ts, schema/, quota.ts, snapshots.ts, url.ts, metadata.ts. Handlers move in Plan 05.

Purpose: Continue D-16's phased migration. Services are the next logical move after adapter+http because the adapter and handlers both consume them. Moving them now means by Plan 05 (handlers), each handler imports its services via clean relative paths within the same per-source folder.

Output: 4 services moved (8 files modified counting old paths).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.0.1-source-plugin-architecture/03.0.1-CONTEXT.md
@.planning/phases/03.0.1-source-plugin-architecture/03.0.1-RESEARCH.md
@CLAUDE.md
@AGENTS.md

<interfaces>
Files being moved:
- src/lib/server/services/youtube-quota-tracker.ts → src/lib/sources/youtube/server/quota.ts
- src/lib/server/services/youtube-snapshot-writer.ts → src/lib/sources/youtube/server/snapshots.ts
- src/lib/server/services/youtube-url.ts → src/lib/sources/youtube/server/url.ts (Note: URL HOST classification stays in services/url-parser.ts — that's cross-source. youtube-url.ts is YOUTUBE-INTERNAL: parses /watch /shorts /channel /handle etc. once host is known to be YouTube)
- src/lib/server/services/youtube-metadata.ts → src/lib/sources/youtube/server/metadata.ts

Files staying (cross-source orchestrators per CONTEXT.md D-14):
- src/lib/server/services/refresh-poll.ts (handles youtube_video AND future reddit_post via getAdapter dispatch)
- src/lib/server/services/ingest.ts (orchestrates URL detection across kinds via parseAnyUrl in Plan 06)
- src/lib/server/services/tier-resolver.ts (Phase 03.1 may relocate to sources/youtube/server/ — RESEARCH.md OQ#1; staying for 03.0.1)
- src/lib/server/services/poll-eligibility.ts (same as tier-resolver)
- src/lib/server/services/data-sources.ts (cross-source CRUD over data_sources table)
- src/lib/server/services/events.ts (cross-source CRUD over events table)
- src/lib/server/services/purge-account.ts (cross-source FK-cascade purge)
- src/lib/server/services/url-parser.ts (cross-source — refactored in Plan 06 to iterate registry)
- src/lib/server/services/quota.ts (events_per_day per-user quota — cross-source)
- src/lib/server/services/audit-read.ts, account.ts, api-keys-steam.ts, ... (all cross-source)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv 4 YouTube services + refactor all importers</name>
  <read_first>
    - src/lib/server/services/youtube-quota-tracker.ts (full file — understand exports + internal pickKeyForJob, hashApiKeyId, incrementUsage, getThrottleState, todayPacific helpers)
    - src/lib/server/services/youtube-snapshot-writer.ts (full file — understand writeSnapshot two-phase tx + idempotent INSERT ON CONFLICT pattern)
    - src/lib/server/services/youtube-url.ts (full file — parseYoutubeUrl YouTube-internal URL decomposition)
    - src/lib/server/services/youtube-metadata.ts (full file — channel resolution + handle resolution)
    - src/lib/sources/youtube/server/adapter.ts (currently imports from `../services/...` — paths become `./quota.js`, `./snapshots.js` etc. after this move)
    - src/lib/sources/youtube/server/http.ts (currently imports `incrementUsage` from quota tracker — path becomes `./quota.js`)
    - .planning/phases/03.0.1-source-plugin-architecture/03.0.1-RESEARCH.md § "Code Insights → Reusable Assets"
  </read_first>

  <action>
    1. **`git mv` 4 files**:
       ```bash
       git mv src/lib/server/services/youtube-quota-tracker.ts   src/lib/sources/youtube/server/quota.ts
       git mv src/lib/server/services/youtube-snapshot-writer.ts src/lib/sources/youtube/server/snapshots.ts
       git mv src/lib/server/services/youtube-url.ts             src/lib/sources/youtube/server/url.ts
       git mv src/lib/server/services/youtube-metadata.ts        src/lib/sources/youtube/server/metadata.ts
       ```

    2. **Update relative imports inside each moved file** — these files were at `src/lib/server/services/`. After move to `src/lib/sources/youtube/server/`:
       - `from "../db/client.js"` → `from "$lib/server/db/client.js"`
       - `from "../db/schema/index.js"` → `from "$lib/server/db/schema/index.js"`
       - `from "../db/schema/youtube-*.js"` (any direct imports) → `from "$lib/server/db/schema/index.js"` (use barrel)
       - `from "../config/env.js"` → `from "$lib/server/config/env.js"`
       - `from "../audit.js"` → `from "$lib/server/audit.js"`
       - `from "../logger.js"` → `from "$lib/server/logger.js"`
       - `from "./errors.js"` (if importing from services/errors.ts) → `from "$lib/server/services/errors.js"`
       - `from "./url-parser.js"` (if any) → `from "$lib/server/services/url-parser.js"` — but verify; YouTube-specific files probably don't import the cross-source URL parser (they may import each other's helpers — those become relative within the new folder, e.g., `from "./url.js"` inside metadata.ts if it imports parseYoutubeUrl)
       - `from "./youtube-*.js"` (sibling YouTube services) → `from "./<new-name>.js"`:
         - `youtube-quota-tracker.js` → `./quota.js`
         - `youtube-snapshot-writer.js` → `./snapshots.js`
         - `youtube-url.js` → `./url.js`
         - `youtube-metadata.js` → `./metadata.js`

       Run after each edit:
       ```bash
       grep -n "from \"\\./\\|from \"\\.\\.\\/" src/lib/sources/youtube/server/{quota,snapshots,url,metadata}.ts
       ```
       Confirm no `../services/...` or `../db/...` paths remain (they should all be `$lib/server/...` aliases or local `./` siblings).

    3. **Update `src/lib/sources/youtube/server/adapter.ts`** to import services via local relative paths:
       - `from "$lib/server/services/youtube-quota-tracker.js"` → `from "./quota.js"`
       - `from "$lib/server/services/youtube-snapshot-writer.js"` → `from "./snapshots.js"`
       - `from "$lib/server/services/youtube-metadata.js"` → `from "./metadata.js"` (if used directly)
       - `from "$lib/server/services/youtube-url.js"` → `from "./url.js"` (if used directly)

    4. **Update `src/lib/sources/youtube/server/http.ts`** similarly — any `import { incrementUsage } from "$lib/server/services/youtube-quota-tracker.js"` becomes `from "./quota.js"`.

    5. **Refactor every cross-source importer** of the moved files. Use grep to enumerate:
       ```bash
       git grep -l "youtube-quota-tracker\\|youtube-snapshot-writer\\|youtube-url\\|youtube-metadata" -- 'src/' 'tests/'
       ```
       For each match, update the import path to the new per-source location:
       - `from "$lib/server/services/youtube-quota-tracker.js"` → `from "$lib/sources/youtube/server/quota.js"`
       - `from "$lib/server/services/youtube-snapshot-writer.js"` → `from "$lib/sources/youtube/server/snapshots.js"`
       - `from "$lib/server/services/youtube-url.js"` → `from "$lib/sources/youtube/server/url.js"`
       - `from "$lib/server/services/youtube-metadata.js"` → `from "$lib/sources/youtube/server/metadata.js"`
       - Relative variants (`from "../services/youtube-*"` or `from "../../lib/server/services/youtube-*"`) — same retargeting

       PARTICULAR FILES likely to need updates:
       - src/lib/server/http/routes/youtube-metadata.ts (Hono route — uses youtube-metadata service)
       - src/worker/handlers/poll-active.ts, poll-cold.ts, poll-user.ts, rehab-unavailable.ts (use snapshot-writer, quota-tracker)
       - src/worker/handlers/youtube-channel-context-backfill.ts (uses metadata + url + quota-tracker)
       - src/worker/handlers/youtube-quota-reset.ts (uses quota-tracker)
       - src/scheduler/enqueue.ts (uses quota-tracker for getThrottleState)
       - src/lib/server/services/ingest.ts (may use youtube-url for canonicalization; verify)
       - src/lib/server/services/admin-quota-read.ts (likely reads youtube_service_quota_usage via quota-tracker helpers)
       - tests/unit/* and tests/integration/* — any test file mocking or importing these

       AFTER UPDATES, re-run the grep and confirm zero remaining matches under `src/lib/server/services/youtube-*`:
       ```bash
       git grep "from \"[^\"]*services/youtube-quota-tracker\\|from \"[^\"]*services/youtube-snapshot-writer\\|from \"[^\"]*services/youtube-url\\|from \"[^\"]*services/youtube-metadata\"" -- src/ tests/
       ```
       Expected: no output.

    6. **Update vi.mock calls in tests** (Pitfall 9 from RESEARCH.md). Search:
       ```bash
       git grep "vi.mock.*services/youtube-" -- tests/
       ```
       Each match needs the path updated to the new location.

    7. **Update Plan 02 schema barrel comment** (cosmetic — confirm Plan 02 didn't preserve a stale comment about "services live alongside schemas"; usually unrelated).

    8. **Verify behavioral identity**:
       ```bash
       pnpm exec tsc --noEmit
       pnpm exec eslint .
       pnpm test
       bash tests/smoke/run.sh
       ```
       All MUST exit 0.
  </action>

  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm exec eslint . && pnpm test && bash tests/smoke/run.sh</automated>
  </verify>

  <acceptance_criteria>
    - File `src/lib/sources/youtube/server/quota.ts` exists; `grep -c "pickKeyForJob\\|incrementUsage\\|getThrottleState" src/lib/sources/youtube/server/quota.ts` returns at least 3
    - File `src/lib/sources/youtube/server/snapshots.ts` exists; `grep -c "writeSnapshot" src/lib/sources/youtube/server/snapshots.ts` returns at least 2
    - File `src/lib/sources/youtube/server/url.ts` exists; `grep -c "parseYoutubeUrl" src/lib/sources/youtube/server/url.ts` returns at least 2
    - File `src/lib/sources/youtube/server/metadata.ts` exists
    - File `src/lib/server/services/youtube-quota-tracker.ts` DOES NOT EXIST
    - File `src/lib/server/services/youtube-snapshot-writer.ts` DOES NOT EXIST
    - File `src/lib/server/services/youtube-url.ts` DOES NOT EXIST
    - File `src/lib/server/services/youtube-metadata.ts` DOES NOT EXIST
    - `git log --follow --oneline -- src/lib/sources/youtube/server/quota.ts` shows pre-move commits (history preserved)
    - `git grep -l "from \"[^\"]*services/youtube-quota-tracker\"\\|from \"[^\"]*services/youtube-snapshot-writer\"\\|from \"[^\"]*services/youtube-url\"\\|from \"[^\"]*services/youtube-metadata\"" -- src/ tests/` returns NO output (no stale imports)
    - Adapter file confirms intra-folder imports: `grep -c 'from "\\./quota\\.js"\\|from "\\./snapshots\\.js"\\|from "\\./url\\.js"\\|from "\\./metadata\\.js"' src/lib/sources/youtube/server/adapter.ts` returns at least 1 (depending on what adapter actually consumes)
    - Cross-source services preserved: `test -f src/lib/server/services/refresh-poll.ts && test -f src/lib/server/services/ingest.ts && test -f src/lib/server/services/tier-resolver.ts && test -f src/lib/server/services/poll-eligibility.ts && test -f src/lib/server/services/data-sources.ts && test -f src/lib/server/services/events.ts && test -f src/lib/server/services/purge-account.ts && test -f src/lib/server/services/url-parser.ts && echo OK`
    - `pnpm exec tsc --noEmit` exits 0
    - `pnpm exec eslint .` exits 0
    - `pnpm test` exits 0 (full suite — behavioral identity)
    - `pnpm test -- tests/integration/poll-active.test.ts tests/integration/poll-cold.test.ts tests/integration/poll-user.test.ts` exits 0
    - `bash tests/smoke/run.sh` exits 0
  </acceptance_criteria>

  <done>
    4 YouTube services moved into per-source folder; all importers updated; no stale imports; full suite + smoke green.
  </done>
</task>

</tasks>

<verification>
- 4 services moved via git mv (history preserved).
- Adapter + HTTP wrapper now consume services via local relative paths.
- Cross-source services (8 files) confirmed remaining at src/lib/server/services/.
- Behavioral identity gate: full suite + smoke green unchanged.
</verification>

<success_criteria>
1. youtube-quota-tracker → quota.ts; youtube-snapshot-writer → snapshots.ts; youtube-url → url.ts; youtube-metadata → metadata.ts.
2. All importers retargeted; zero stale paths.
3. Cross-source services explicitly preserved.
4. Behavioral identity preserved via existing tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03.0.1-source-plugin-architecture/03.0.1-04-SUMMARY.md` recording: file moves, importer-migration count, full-suite + smoke exit codes.
</output>
