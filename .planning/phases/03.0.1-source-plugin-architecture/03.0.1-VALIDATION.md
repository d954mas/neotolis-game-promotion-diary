---
phase: 03.0.1
slug: source-plugin-architecture
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-08
---

# Phase 03.0.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.1.5 + @vitest/browser-playwright |
| **Config file** | `vitest.config.ts` (existing) |
| **Quick run command** | `pnpm test:unit` |
| **Full suite command** | `pnpm test` (unit + integration; integration uses Postgres service container) |
| **Smoke command** | `bash tests/smoke/run.sh` (boots production image; OAuth dance via oauth2-mock-server; cross-tenant + anonymous-401 invariants) |
| **Estimated runtime** | unit ~30s · full ~3-5 min · smoke ~3-5 min |

---

## Sampling Rate

- **After every task commit:** Run `pnpm test:unit`
- **After every plan wave:** Run `pnpm test`
- **Before `/gsd:verify-work`:** Full suite + smoke must be green
- **Max feedback latency:** 30 seconds (unit), 5 minutes (full suite)

---

## Per-Task Verification Map

> Filled in by gsd-planner. Each task with new behavior maps to at least one row below; each row's `Automated Command` MUST reference a real test file (created in Wave 0 or pre-existing).

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| TBD by planner | | | | | | | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Test files seeded in Wave 0 (placeholder pattern from Phase 03.0 — `test.todo(...)` / `it.skip(...)` cases that flip live as later waves land):

- [ ] `tests/unit/sources/registry.test.ts` — covers SourceRegistry + getAdapter contract (kind set, methods present)
- [ ] `tests/unit/sources/url-routing.test.ts` — covers parseAnyUrl first-match-wins + future-kinds hint (e.g. reddit deferral message preserved)
- [ ] `tests/unit/sources/errors.test.ts` — covers AdapterError 5 categories (`transient`, `rate-limited`, `not-found`, `permanent`, `operator-issue`) + `Error.cause` chain + `categoryToSnapshotStatus` mapping
- [ ] `tests/unit/sources/youtube/card-props.test.ts` — covers `toCardProps(event)` mapper variants (ok / no-stats / authorIsMe)
- [ ] `tests/integration/api-sources-refresh-content.test.ts` — covers new endpoint (202 happy-path with jobId; 404 cross-tenant; 401 anonymous; tenant-scope body never contains "forbidden"/"permission")
- [ ] `tests/integration/anonymous-401.test.ts` — extend `MUST_BE_PROTECTED` allowlist with `POST /api/sources/:id/refresh-content`
- [ ] `tests/integration/tenant-scope.test.ts` — extend cross-tenant cases to cover the new endpoint

*Existing infrastructure remains the floor:* `tests/integration/poll-*.test.ts`, smoke (`tests/smoke/run.sh`), drizzle `pnpm db:check` — these run unchanged after every refactor commit and act as the behavioral-identity gate (D-16: each commit independently CI-green).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| "Pull new content" button on `/sources/[id]` enqueues backfill job | new UI behavior | Click → confirm → 202 toast → poll job; visually verifying the button surface lives in the SourceDetailHeader header | 1. Sign in as test user. 2. Navigate to `/sources/<youtube-source-id>`. 3. Click "Pull new content". 4. Observe optimistic toast + spinner. 5. After ~5s, refresh — new events from registered channel appear. |
| `SOURCE-REFERENCE.md` walkthrough is followable end-to-end | D-17 doc deliverable | Doc-readability is qualitative; reviewers must walk through sections 1-7 against the canonical YouTube tree | Reviewer reads SOURCE-REFERENCE.md alongside `src/lib/sources/youtube/` and verifies each section's claims against the live code. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s (unit) / < 5min (full)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
