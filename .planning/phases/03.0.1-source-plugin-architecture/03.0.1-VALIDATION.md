---
phase: 03.0.1
slug: source-plugin-architecture
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-08
revised: 2026-05-08
---

# Phase 03.0.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.1.5 + @vitest/browser-playwright |
| **Config file** | `vitest.config.ts` (existing) |
| **Quick run command** | `pnpm test:unit` |
| **Full suite command** | `pnpm test` (unit + integration; integration uses Postgres service container) |
| **Smoke command** | `bash tests/smoke/run.sh` (boots production image; OAuth dance via oauth2-mock-server; cross-tenant + anonymous-401 invariants) |
| **Estimated runtime** | unit ~30s · full ~3-5 min · smoke ~3-5 min |

---

## Sampling Rate

- **After every task commit:** Run `pnpm test:unit`
- **After every plan wave:** Run `pnpm test`
- **Before `/gsd:verify-work`:** Full suite + smoke must be green
- **Max feedback latency:** 30 seconds (unit), 5 minutes (full suite)

---

## Per-Task Verification Map

> Filled in by gsd-planner. Each task with new behavior maps to at least one row below; each row's `Automated Command` references a real test file (created in Wave 0 or pre-existing).
>
> **Wave numbering:** Plan 01 = wave 0 (test scaffolding + foundation interfaces). Plans 02-11 = waves 1-10.

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 03.0.1-01-01 | 01 | 0 | (foundation, no requirement; supports all 03.0.1) | unit + tooling | `pnpm exec tsc --noEmit` | ✅ (creates src/lib/sources/) | ⬜ pending |
| 03.0.1-01-02 | 01 | 0 | (foundation, supports all 03.0.1) | unit | `pnpm test:unit -- tests/unit/sources/` | ❌ W0 (this task SEEDS the 5 NEW test files) | ⬜ pending |
| 03.0.1-02-01 | 02 | 1 | POLL-01, POLL-04 | integration + tooling | `pnpm db:check && pnpm exec tsc --noEmit && pnpm exec eslint . && pnpm test` | ✅ (existing 03.0 integration tests + smoke) | ⬜ pending |
| 03.0.1-03-01 | 03 | 2 | POLL-01, POLL-02, POLL-04 | integration + tooling | `pnpm exec tsc --noEmit && pnpm exec eslint . && pnpm test && bash tests/smoke/run.sh` | ✅ (existing 03.0 integration tests + smoke + Plan 01 registry.test.ts) | ⬜ pending |
| 03.0.1-04-01 | 04 | 3 | POLL-01, POLL-04 | integration + tooling | `pnpm exec tsc --noEmit && pnpm exec eslint . && pnpm test && bash tests/smoke/run.sh` | ✅ (existing 03.0 integration tests + smoke) | ⬜ pending |
| 03.0.1-05-01 | 05 | 4 | POLL-01, POLL-02, POLL-03, POLL-04, POLL-05, POLL-06 | integration + smoke | `pnpm exec tsc --noEmit && pnpm exec eslint . && pnpm test && bash tests/smoke/run.sh` | ✅ (existing 03.0 integration tests + smoke — load-bearing behavioral identity gate for handler relocation) | ⬜ pending |
| 03.0.1-06-01 | 06 | 5 | INGEST-02, INGEST-03, INGEST-04 | unit + integration + smoke | `pnpm exec tsc --noEmit && pnpm exec eslint . && pnpm test && bash tests/smoke/run.sh` | ✅ (Plan 01 url-routing.test.ts + registry.test.ts flip live; existing paste-flow integration tests preserve Telegram/Twitter/YouTube + Reddit-deferred messaging) | ⬜ pending |
| 03.0.1-07-01 | 07 | 6 | POLL-01, POLL-02, POLL-06 | tooling | `pnpm db:migrate && pnpm db:check` | ✅ (drizzle migration runner) | ⬜ pending |
| 03.0.1-07-02 | 07 | 6 | POLL-01, POLL-02, POLL-06 | integration + smoke | `pnpm exec tsc --noEmit && pnpm exec eslint . && pnpm test && bash tests/smoke/run.sh` | ✅ (existing tests/integration/poll-worker-tx-boundary.test.ts pins Phase A/B contract; smoke gate pins cron + snapshot writes) | ⬜ pending |
| 03.0.1-08-01 | 08 | 7 | POLL-03 | tooling | `pnpm db:migrate && pnpm db:check` | ✅ (drizzle migration runner) | ⬜ pending |
| 03.0.1-08-02 | 08 | 7 | POLL-03 | unit + integration + smoke | `pnpm exec tsc --noEmit && pnpm exec eslint . && pnpm test && bash tests/smoke/run.sh` | ✅ (Plan 01 registry.test.ts observability test.todo cases flip live; existing /admin/quota integration test pins behavioral identity through observability refactor) | ⬜ pending |
| 03.0.1-09-01 | 09 | 8 | FEED-01, EVENTS-01, EVENTS-02, EVENTS-03 | unit + smoke | `pnpm exec tsc --noEmit && pnpm exec eslint . && pnpm test && bash tests/smoke/run.sh` | ✅ (Plan 01 card-props.test.ts test.todo cases flip live; existing /feed browser tests pin behavioral identity) | ⬜ pending |
| 03.0.1-10-01 | 10 | 9 | SOURCES-01, SOURCES-02, INGEST-02 | tooling | `pnpm db:migrate && pnpm db:check && pnpm exec tsc --noEmit && pnpm exec eslint .` | ✅ (drizzle migration + tsc + eslint) | ⬜ pending |
| 03.0.1-10-02 | 10 | 9 | SOURCES-01, SOURCES-02 | integration + smoke | `pnpm test -- tests/integration/api-sources-refresh-content.test.ts tests/integration/anonymous-401.test.ts tests/integration/tenant-scope.test.ts && pnpm test && bash tests/smoke/run.sh` | ✅ (Plan 01 api-sources-refresh-content.test.ts it.skip cases flip live; anonymous-401 + tenant-scope sweeps extended) | ⬜ pending |
| 03.0.1-10-03 | 10 | 9 | SOURCES-01 | unit + tooling | `pnpm exec tsc --noEmit && pnpm exec eslint . && pnpm test` | ✅ (existing Paraglide expected-keys test + browser tests cover RefreshContentButton.svelte + /sources/[id] route) | ⬜ pending |
| 03.0.1-11-01 | 11 | 10 | (documentation, no code-level requirement) | manual review | (manual: reviewer walks SOURCE-REFERENCE.md sections 1-7 against `src/lib/sources/youtube/` tree; ROADMAP.md update is a docs-only diff) | N/A (pure documentation) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Test files seeded in Wave 0 (placeholder pattern from Phase 03.0 — `test.todo(...)` / `it.skip(...)` cases that flip live as later waves land):

- [x] `tests/unit/sources/registry.test.ts` — covers SourceRegistry + getAdapter contract (kind set, methods present). **Plan 01** seeds; **Plan 03** flips kind/registration cases live; **Plan 06** flips parseUrl cases live; **Plan 08** flips observability cases live.
- [x] `tests/unit/sources/url-routing.test.ts` — covers parseAnyUrl first-match-wins + future-kinds hint (Reddit deferral message preserved). **Plan 01** seeds detectFutureKind active assertions + parseAnyUrl test.todo; **Plan 06** flips parseAnyUrl cases live.
- [x] `tests/unit/sources/errors.test.ts` — covers AdapterError 5 categories (`transient`, `rate-limited`, `not-found`, `permanent`, `operator-issue`) + `Error.cause` chain + `categoryToSnapshotStatus` mapping. **Plan 01** seeds AND flips live (errors.ts lands in Plan 01).
- [x] `tests/unit/sources/youtube/card-props.test.ts` — covers `toCardProps(event)` mapper variants (ok / no-stats / authorIsMe). **Plan 01** seeds; **Plan 09** flips live.
- [x] `tests/integration/api-sources-refresh-content.test.ts` — covers new endpoint (202 happy-path with jobId; 404 cross-tenant; 401 anonymous; tenant-scope body never contains "forbidden"/"permission"). **Plan 01** seeds; **Plan 10** flips all 7 it.skip cases live.
- [x] `tests/integration/anonymous-401.test.ts` — extend `MUST_BE_PROTECTED` allowlist with `POST /api/sources/:id/refresh-content`. **Plan 10** extends.
- [x] `tests/integration/tenant-scope.test.ts` — extend cross-tenant cases to cover the new endpoint. **Plan 10** extends.

*Existing infrastructure remains the floor:* `tests/integration/poll-worker-tx-boundary.test.ts`, smoke (`tests/smoke/run.sh`), drizzle `pnpm db:check` — these run unchanged after every refactor commit and act as the behavioral-identity gate (D-16: each commit independently CI-green). NOTE: `tests/integration/poll-active.test.ts` and `tests/integration/poll-cold.test.ts` DO NOT EXIST in this repo — earlier plan drafts referenced them in error.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| "Pull new content" button on `/sources/[id]` enqueues backfill job | new UI behavior | Click → confirm → 202 toast → poll job; visually verifying the button surface lives in the SourceDetailHeader header | 1. Sign in as test user. 2. Navigate to `/sources/<youtube-source-id>`. 3. Click "Pull new content". 4. Observe optimistic toast + spinner. 5. After ~5s, refresh — new events from registered channel appear. |
| `SOURCE-REFERENCE.md` walkthrough is followable end-to-end | D-17 doc deliverable | Doc-readability is qualitative; reviewers must walk through sections 1-7 against the canonical YouTube tree | Reviewer reads SOURCE-REFERENCE.md alongside `src/lib/sources/youtube/` and verifies each section's claims against the live code. |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies — every row in the Per-Task Verification Map names an automated command; Plan 11's documentation-only task is the sole manual-review entry (justified: docs-only diff)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify — every task either runs `pnpm test`/`pnpm exec tsc`/`pnpm db:migrate` or relies on the smoke gate; consecutive-tasks-without-automation count = 0
- [x] Wave 0 covers all MISSING references — Plan 01 Task 2 seeds all 5 NEW test files; the 2 existing test files (anonymous-401, tenant-scope) are extended in Plan 10
- [x] No watch-mode flags — `grep -rE "(--watch|test:watch|vitest --watch)" .planning/phases/03.0.1-source-plugin-architecture/*-PLAN.md` returns 0 matches; all `pnpm test` invocations use the default one-shot runner
- [x] Feedback latency < 30s (unit) / < 5min (full) — already documented in Test Infrastructure table; estimates inherited from Phase 03.0
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** ready for execute-phase
