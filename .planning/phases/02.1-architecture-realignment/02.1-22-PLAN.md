---
phase: 02.1-architecture-realignment
plan: 22
type: execute
wave: 1
depends_on: ["02.1-21"]
files_modified:
  - src/routes/+layout.svelte
  - src/lib/components/AppHeader.svelte
  - src/lib/components/FiltersSheet.svelte
  - src/routes/sources/+page.svelte
  - src/routes/settings/+page.svelte
  - src/lib/components/SourceRow.svelte
  - tests/browser/responsive-360.test.ts
  - tests/browser/feed-360.test.ts
  - tests/integration/empty-states.test.ts
  - messages/en.json
  - tests/unit/paraglide.test.ts
autonomous: true
gap_closure: true
requirements: ["FEED-01", "SOURCES-01", "UX-02"]

must_haves:
  truths:
    - "Root +layout.svelte establishes a flex column with min-height: 100vh — fixes UAT-NOTES.md §2.2-bug 'Sticky positioning broken globally'. The layout root sets `display: flex; flex-direction: column; min-height: 100vh` so AppHeader sticky-top and per-page sticky CTAs (e.g. /sources `+ Add data source`) anchor correctly. Without this scaffold neither `position: sticky; top: 0` on AppHeader nor `position: sticky; bottom: 0` on per-page CTAs work."
    - "AppHeader.svelte renders with `position: sticky; top: 0; z-index: 10` so it stays anchored when content scrolls. Per UAT-NOTES.md §2.2-bug user quote: 'кнопка не стики. Она осталась на самом верху. И табы и header тоже не стики' / 'да везде не стики'."
    - "/sources `+ Add data source` CTA gains `position: sticky; bottom: var(--space-md); z-index: 5` (or `position: fixed; bottom: 0` at ≤600px per UAT-NOTES.md §2.2-bug suggested fix) so the affordance stays reachable while the user scrolls a long source list. The page-header is restructured to use the inline-CTA pattern (Plan 02.1-25 will apply the same restructure to /feed and /games)."
    - "FiltersSheet.svelte locks body scroll while the dialog is open — fixes UAT-NOTES.md §1.4-bug 'Background feed scrolls behind FiltersSheet modal'. Implementation: in the `$effect` that calls `dialogEl.showModal()`, also set `document.body.style.overflow = 'hidden'`; on dialog close (onClose / oncancel) restore `document.body.style.overflow = ''`. The restore path runs in onDialogCancel + onClose + the apply-then-close flow."
    - "/settings sign-out-this-session form invalidates session list reactively — fixes UAT-NOTES.md §6.3-bug 'Sign-out on /settings does not invalidate sessions list'. Implementation: the form (or button) that POSTs to /api/me/sessions/:id (or equivalent) calls `await invalidateAll()` after the response so the +page.server.ts loader re-runs and the SessionsList re-renders with the destroyed session removed."
    - "/settings has a visible link to /keys/steam — fixes UAT-NOTES.md §8.1-bug '/keys/steam unreachable from any nav'. Per round-3 scope this is the MINIMAL FIX ONLY (the unified /settings/credentials hub is DEFERRED to Phase 3+). The link sits in a new 'Credentials' block on /settings with copy 'Steam Web API key — Manage' and a TODO comment in the source noting Phase 3+ will replace this with the unified /settings/credentials hub. New Paraglide keys: `settings_credentials_heading` ('Credentials'), `settings_credentials_steam_link_label` ('Steam Web API key')."
    - "SourceRow.svelte makes `auto_import` READ-ONLY display only — fixes UAT-NOTES.md §2.4-decision 'auto_import edit-gate UX'. Per option A confirmed during UAT, we KEEP the field but FIX the UX. The inline checkbox toggle is REMOVED from SourceRow's read-mode rendering. In its place we render a non-interactive status pill: 'Auto-import: On' or 'Auto-import: Off' (Paraglide keys `sources_auto_import_on` / `sources_auto_import_off`). The actual toggle moves into the row's edit form (the existing `editing = true` state). Inside the edit form we add a checkbox bound to a local `editAutoImport` state; on save, the form sends BOTH `displayName` AND `autoImport` in the PATCH /api/sources/:id payload. The previous standalone `toggleAutoImport()` function on SourceRow is DELETED."
    - "Privacy & multi-tenancy invariants preserved (CLAUDE.md/AGENTS.md): no new env reads outside config/env.ts; no new audit verbs; no schema changes; PATCH /api/sources/:id contract unchanged (already accepts displayName + autoImport in same payload); the moved auto_import toggle still flows through the same userId-scoped service function `updateSource(userId, ...)`; no new client-only state can leak across tenants."
    - "Paraglide invariant D-41: every new message key (settings_credentials_heading, settings_credentials_steam_link_label, sources_auto_import_on, sources_auto_import_off) added to messages/en.json AND tests/unit/paraglide.test.ts EXPECTED_KEYS in the SAME commit. Per CLAUDE.md/AGENTS.md Practices."
    - "Browser regression test (tests/browser/responsive-360.test.ts) gains: (a) AppHeader sticky behavior at 360px (scroll page → header stays at top); (b) /sources CTA sticky-bottom behavior at 360px; (c) FiltersSheet open → document.body.style.overflow === 'hidden'; close → restored. Assertion strategy: use the existing browser test pattern; verify computed styles via window.getComputedStyle."
    - "Manual UAT row added to 02.1-VALIDATION.md Per-Task Verification Map: '02.1-22 T1' for sticky / body-scroll-lock / sign-out invalidate / /keys/steam link / auto-import edit-gate (visual + interactive — manual)."
  artifacts:
    - path: "src/routes/+layout.svelte"
      provides: "Root flex column with min-height: 100vh; AppHeader sticky positioning anchor; <main> gets flex: 1 + min-width: 0."
      contains: "min-height: 100vh"
    - path: "src/lib/components/AppHeader.svelte"
      provides: "position: sticky; top: 0; z-index: 10; background still --color-surface so content scrolling underneath does not bleed through."
      contains: "position: sticky"
    - path: "src/lib/components/FiltersSheet.svelte"
      provides: "Body-scroll-lock on open; restore on close. Implementation in the existing $effect that calls dialogEl.showModal()."
      contains: "document.body.style.overflow"
    - path: "src/routes/sources/+page.svelte"
      provides: "+ Add data source CTA gains sticky bottom positioning (z-index: 5; padding around so it doesn't overlap last row). Page-header restructured to inline-CTA-next-to-title pattern (3.1-polish lite preview — full pattern lands in Plan 02.1-25)."
      contains: "position: sticky"
    - path: "src/routes/settings/+page.svelte"
      provides: "Adds a 'Credentials' block with link to /keys/steam. invalidateAll() called after sign-out-this-session POST (8.1-bug minimal fix)."
      contains: "/keys/steam"
    - path: "src/lib/components/SourceRow.svelte"
      provides: "auto_import becomes read-only pill in row-display mode; toggle moves into edit form alongside displayName; PATCH payload sends both fields together; standalone toggleAutoImport() function deleted."
      contains: "sources_auto_import_on"
    - path: "tests/browser/responsive-360.test.ts"
      provides: "Plan 02.1-22 describe block: AppHeader sticky top assertion, /sources CTA sticky bottom assertion, FiltersSheet body-scroll-lock assertion."
      contains: "Plan 02.1-22"
    - path: "tests/browser/feed-360.test.ts"
      provides: "Existing tests pass after FiltersSheet body-scroll-lock change (regression check); no Plan 02.1-22 specific block needed unless body-scroll-lock semantics surface in feed-360 too."
      contains: ""
    - path: "tests/integration/empty-states.test.ts"
      provides: "Existing /keys/steam empty-state test continues to pass (no copy change in this plan; only access path)."
      contains: ""
    - path: "messages/en.json"
      provides: "ADD: settings_credentials_heading, settings_credentials_steam_link_label, sources_auto_import_on, sources_auto_import_off."
      contains: "settings_credentials_heading"
    - path: "tests/unit/paraglide.test.ts"
      provides: "EXPECTED_KEYS snapshot includes the 4 new keys in alphabetical position."
      contains: "settings_credentials_heading"
  key_links:
    - from: "Root +layout.svelte flex column"
      to: "AppHeader sticky-top + per-page sticky CTAs"
      via: "Without min-height: 100vh + flex container, position: sticky has no scrolling parent to anchor to (browser quirk per UAT-NOTES.md §2.2-bug 'Likely root cause' analysis)"
      pattern: "min-height: 100vh"
    - from: "FiltersSheet showModal()"
      to: "document.body overflow lock"
      via: "Set in same $effect that opens the dialog; restored in onClose/onDialogCancel; covers /feed and /audit reuse via Plan 02.1-21 schema unification"
      pattern: "document\\.body\\.style\\.overflow"
    - from: "/settings sign-out-this-session form"
      to: "Sessions list refresh"
      via: "await invalidateAll() after fetch DELETE response so +page.server.ts loader re-runs and SessionsList re-renders without the destroyed session"
      pattern: "invalidateAll"
    - from: "/settings → /keys/steam"
      to: "Phase 3+ /settings/credentials hub TODO marker"
      via: "Inline TODO comment + Paraglide keys named with `_credentials_` prefix so Phase 3+ rebuild is a search-and-restructure, not a from-scratch rewrite"
      pattern: "TODO.*credentials"
    - from: "SourceRow read-mode auto_import pill"
      to: "Edit form auto_import checkbox"
      via: "editing flag toggles between read pill and edit form; saveDisplayName function renamed to saveSourceEdit and sends both displayName + autoImport fields in PATCH payload"
      pattern: "autoImport"
---

<objective>
Bug-batch plan: closes 5 round-3 bug fixes that share a layout/global UX theme. They batch together because they touch overlapping shells (root layout, AppHeader, FiltersSheet) AND because their fixes are individually small (sticky position rules, body-scroll-lock CSS, invalidateAll(), one new link, edit-gate refactor) — no single bug warrants its own plan.

Findings closed:
1. **2.2-bug** sticky positioning broken globally (UAT-NOTES.md §2.2-bug). Root layout gains the flex-column scaffold; AppHeader gains sticky-top; /sources CTA gains sticky-bottom.
2. **1.4-bug** background feed scrolls behind FiltersSheet modal (UAT-NOTES.md §1.4-bug). FiltersSheet sets document.body overflow on open + restores on close.
3. **6.3-bug** sign-out on /settings does not invalidate sessions list (UAT-NOTES.md §6.3-bug). The form action calls invalidateAll() after success.
4. **8.1-bug** /keys/steam unreachable from any nav (UAT-NOTES.md §8.1-bug). MINIMAL FIX: add a single 'Credentials' block on /settings with a link to /keys/steam. The unified /settings/credentials hub (8.2-redesign) is DEFERRED to Phase 3+.
5. **2.4-decision** auto_import edit-gate UX (UAT-NOTES.md §2.4-decision, option A confirmed). The inline toggle on SourceRow is REMOVED from read-mode (read-only pill takes its place); the actual toggle moves into the row's edit form.

Purpose: Round-3 user direction "Фиксим баги" — these 5 are the bug fixes the user explicitly flagged. Each is a small change; the plan groups them so the round-3 review surface is one clean PR per concern (bug batch separate from card-redesign cluster separate from inbox-triage redesign). Wave 1 because they depend on Plan 02.1-21 (FiltersSheet schema-prop) — touching the FiltersSheet body-scroll-lock right after that change avoids merge friction.

Output:
- Root +layout.svelte gains flex-column scaffold with min-height: 100vh.
- AppHeader.svelte gains position: sticky; top: 0.
- FiltersSheet.svelte body-scroll-lock on open; restore on close.
- /sources CTA gains sticky-bottom positioning.
- /settings adds Credentials block with /keys/steam link; sign-out form invalidates session list.
- SourceRow.svelte auto_import becomes read-only pill in display mode; editable in edit form.
- Test coverage: browser regression for sticky / body-scroll-lock; existing /keys/steam test extended (the Sign-In link path is covered by manual UAT; the existence of the link is tested via grep on the rendered HTML in the empty-states test).
- 4 new Paraglide keys added with EXPECTED_KEYS snapshot updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-architecture-realignment/02.1-CONTEXT.md
@.planning/phases/02.1-architecture-realignment/02.1-10-UAT-NOTES.md
@.planning/phases/02.1-architecture-realignment/02.1-21-PLAN.md
@.planning/phases/02.1-architecture-realignment/02.1-VALIDATION.md
@CLAUDE.md
@AGENTS.md

<interfaces>
<!-- Existing surfaces this plan touches. Executor uses these directly. -->

From src/routes/+layout.svelte (current — surgical edit):

```svelte
<main>
  {@render children()}
</main>

<style>
  main {
    max-width: 1024px;
    margin: 0 auto;
    padding: var(--space-md);
    min-width: 0;
  }
</style>
```

Plan 02.1-22 reshapes — adds a wrapping flex container (or applies `display: flex; flex-direction: column; min-height: 100vh` to a `:root` selector / body via :global, executor picks). The simplest path is to add a class on the body via a top-level `<svelte:body>` style or add a wrapping `<div class="layout-root">` around AppHeader/Nav/main. Either works as long as the sticky-anchor scaffold lands.

From src/lib/components/AppHeader.svelte (current — surgical edit):

```svelte
<header class="header">
  ...
</header>

<style>
  .header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-md);
    padding: var(--space-md);
    background: var(--color-surface);
    border-bottom: 1px solid var(--color-border);
    min-width: 0;
  }
</style>
```

Plan 02.1-22 adds:
- `position: sticky; top: 0; z-index: 10;` to `.header`.

From src/lib/components/FiltersSheet.svelte (current after Plan 02.1-21 — surgical edit):

The existing `$effect` that calls `dialogEl.showModal()`:

```typescript
$effect(() => {
  if (dialogEl && !dialogEl.open) {
    dialogEl.showModal();
    if (focusAxis) { /* ... */ }
  }
});
```

Plan 02.1-22 extends this to set `document.body.style.overflow = 'hidden'` AND returns a cleanup function (Svelte 5 $effect pattern):

```typescript
$effect(() => {
  if (dialogEl && !dialogEl.open) {
    dialogEl.showModal();
    document.body.style.overflow = 'hidden';
    if (focusAxis) { /* ... */ }
    return () => {
      document.body.style.overflow = '';
    };
  }
});
```

Note: also restore in `onDialogCancel` and `onClose` so closing via Esc / backdrop / button paths all restore body scroll.

From src/routes/settings/+page.svelte (current — surgical edit):

Currently has an "Activity log" block linking to /audit. Plan 02.1-22 ADDS a new "Credentials" block ABOVE the Activity log block:

```svelte
<article class="block">
  <h2>{m.settings_credentials_heading()}</h2>
  <p class="muted">
    <!-- TODO Phase 3+: replace this with the unified /settings/credentials
         hub per UAT-NOTES.md §8.2-redesign (deferred — minimal fix in 02.1-22). -->
    Manage API keys for external services.
  </p>
  <a href="/keys/steam" class="audit-link">
    {m.settings_credentials_steam_link_label()} →
  </a>
</article>
```

Sign-out behavior — Plan 02.1-22 changes the sign-out-this-session UX. Today /settings hosts a SessionsList component which presumably has a per-row "Sign out" button. The fix is in /settings/+page.svelte (or the SessionsList component file). Investigate which file owns the per-session sign-out call (likely SessionsList.svelte) and ensure that after the DELETE/POST response, the page calls `await invalidateAll()`. If the existing button calls `goto()` or just refetches manually, switch to `await invalidateAll()`.

From src/lib/components/SourceRow.svelte (current Plan 02.1-08 — surgical edit):

Read-mode currently:

```svelte
<div class="status">
  <span class="polling-status" role="status">...</span>
  <label class="toggle">
    <input type="checkbox" checked={source.autoImport} ... />
    <span>Auto-import</span>
  </label>
</div>
```

Plan 02.1-22 reshapes:
- Read-mode: replace the toggle <label> with a non-interactive pill `<span class="auto-pill">{source.autoImport ? m.sources_auto_import_on() : m.sources_auto_import_off()}</span>`.
- Edit-mode: add a checkbox alongside the rename input, bound to local `editAutoImport` state (initialized from `source.autoImport` when openEdit() runs).
- saveDisplayName renamed to saveSourceEdit; PATCH body becomes `{ displayName, autoImport }`.
- Standalone `toggleAutoImport` function DELETED.

From src/routes/sources/+page.svelte (current Plan 02.1-08 — surgical edit):

```svelte
<header class="head">
  <h1>Data sources</h1>
  <a class="cta" href="/sources/new">{m.sources_cta_new_source()}</a>
</header>
```

Plan 02.1-22 adds sticky-bottom CTA. Approach: ADD a new `.cta-sticky` class (kept as `<a class="cta cta-sticky" href="/sources/new">`) — `position: sticky; bottom: var(--space-md); z-index: 5;` PLUS rearrange the page so the CTA renders at the BOTTOM of the section (not in the header). Alternative: keep the header CTA AND add a new floating CTA at the bottom. Executor picks; the user-facing requirement is "the + Add data source CTA stays reachable while the user scrolls a long source list" (UAT-NOTES.md §2.2-bug observation '/sources: "+ Add data source" CTA stays at top of content and scrolls away').

Recommended path (saves UI surface): KEEP the header CTA (matches the inline-CTA-next-to-title pattern Plan 02.1-25 will roll out broadly) AND add a separate sticky floating CTA at the bottom-right corner of the viewport when the page has 5+ sources. For simplicity in 02.1-22, do the simplest thing that passes the UAT spec: make the header CTA sticky to the page (not the viewport — i.e. `position: sticky; top: 80px;` so it stays under AppHeader). If user feedback in re-UAT prefers a floating bottom CTA, file it for Phase 6.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Layout scaffold + AppHeader sticky + FiltersSheet body-scroll-lock + browser tests</name>
  <files>
    src/routes/+layout.svelte,
    src/lib/components/AppHeader.svelte,
    src/lib/components/FiltersSheet.svelte,
    src/routes/sources/+page.svelte,
    tests/browser/responsive-360.test.ts
  </files>
  <read_first>
    - .planning/phases/02.1-architecture-realignment/02.1-10-UAT-NOTES.md (§2.2-bug + §1.4-bug — user quotes are load-bearing)
    - src/routes/+layout.svelte (current shape — surgical edit only)
    - src/lib/components/AppHeader.svelte (current shape)
    - src/lib/components/FiltersSheet.svelte (Plan 02.1-21 schema-prop version — apply body-scroll-lock on TOP of the schema-prop change)
    - src/routes/sources/+page.svelte (current shape)
    - tests/browser/responsive-360.test.ts (existing test pattern — append, don't replace)
    - .planning/phases/02.1-architecture-realignment/02.1-21-PLAN.md (this plan's hard predecessor; the FiltersSheet $effect was modified there)
    - CLAUDE.md (Privacy & multi-tenancy invariants 6 — no env reads)
  </read_first>
  <action>
    1. Edit `src/routes/+layout.svelte`:
       - Wrap the existing AppHeader + Nav + main in a top-level `<div class="layout-root">` (or apply :global(body) styles via a `<svelte:head>` style block — pick the simpler path that works).
       - Add CSS: `.layout-root { display: flex; flex-direction: column; min-height: 100vh; }` and `main { flex: 1; min-width: 0; }` (the existing main rule keeps max-width and padding).
    2. Edit `src/lib/components/AppHeader.svelte`:
       - Add to `.header` CSS rule: `position: sticky; top: 0; z-index: 10;`.
    3. Edit `src/lib/components/FiltersSheet.svelte`:
       - In the existing `$effect(() => { if (dialogEl && !dialogEl.open) { dialogEl.showModal(); ... } });` block, ADD `document.body.style.overflow = 'hidden';` immediately after `dialogEl.showModal()`.
       - Return a cleanup function from the $effect: `return () => { document.body.style.overflow = ''; };`
       - Add the same restore call (`document.body.style.overflow = '';`) at the top of `onDialogCancel`. (The existing onClose path is wired by /feed and /audit setting `sheetOpen = false`; the cleanup function in $effect handles that case automatically when the if-condition becomes false on re-run.)
    4. Edit `src/routes/sources/+page.svelte`:
       - Make the header `.head` `position: sticky; top: 80px;` (so it stays under AppHeader sticky-top).
       - Add `background: var(--color-bg);` to .head so scrolled content does not bleed through.
       - z-index: 5.
    5. Edit `tests/browser/responsive-360.test.ts`:
       - APPEND a `describe('Plan 02.1-22 — sticky + body-scroll-lock at 360px', ...)` block:
         (a) After visiting any page (e.g., /login or /sources via local dev), scroll the body 200px → assert `document.querySelector('header.header')` has its bounding rect's top still equal to 0 (or whatever the AppHeader natural height position is).
         (b) On a page with FiltersSheet (e.g., /sources or /audit if seeded; use the test's existing harness), open the sheet → assert `document.body.style.overflow === 'hidden'`. Close → assert it's restored to ''.
         (c) On /sources, scroll body → assert the page-header `.head` element's top is at the AppHeader-relative offset (~80px), not -200 (i.e., it's sticky to the page).
  </action>
  <verify>
    <automated>pnpm test:browser -- tests/browser/responsive-360.test.ts -t "Plan 02.1-22"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "min-height: 100vh" src/routes/+layout.svelte` returns ≥ 1
    - `grep -c "position: sticky" src/lib/components/AppHeader.svelte` returns ≥ 1
    - `grep -c "document.body.style.overflow" src/lib/components/FiltersSheet.svelte` returns ≥ 2 (set + restore)
    - `grep -c "position: sticky" src/routes/sources/+page.svelte` returns ≥ 1
    - `grep "Plan 02.1-22" tests/browser/responsive-360.test.ts` returns ≥ 1 line
    - `pnpm test:browser` passes
    - `pnpm test:integration` (full suite — no integration regression)
  </acceptance_criteria>
  <done>
    Sticky positioning works on AppHeader and /sources page-header at 360px; FiltersSheet locks body scroll while open; browser tests assert all three behaviors; no integration test regression.
  </done>
</task>

<task type="auto">
  <name>Task 2: /settings credentials block + sign-out invalidate + SourceRow auto_import edit-gate + paraglide keys</name>
  <files>
    src/routes/settings/+page.svelte,
    src/lib/components/SessionsList.svelte,
    src/lib/components/SourceRow.svelte,
    messages/en.json,
    tests/unit/paraglide.test.ts,
    tests/integration/empty-states.test.ts
  </files>
  <read_first>
    - .planning/phases/02.1-architecture-realignment/02.1-10-UAT-NOTES.md (§6.3-bug + §8.1-bug + §2.4-decision — user quotes load-bearing)
    - src/routes/settings/+page.svelte (current Plan 02.1-09 shape — surgical edit; ADD credentials block ABOVE activity log block)
    - src/lib/components/SessionsList.svelte (current shape — find where per-session sign-out is wired; that's where invalidateAll() goes)
    - src/lib/components/SourceRow.svelte (current Plan 02.1-08 shape — surgical edit; toggle moves from read mode to edit form)
    - src/routes/sources/+page.svelte (uses SourceRow — confirm no page-level wiring needs adjustment)
    - messages/en.json (existing keys — alphabetical insertion of 4 new keys)
    - tests/unit/paraglide.test.ts EXPECTED_KEYS (alphabetical; 4 new keys must be inserted at correct positions)
    - tests/integration/empty-states.test.ts (existing /keys/steam empty-state test — confirm it still works; this plan adds a navigation path, not a copy change)
    - CLAUDE.md (Practices §"Tests land with the feature"; Paraglide invariant D-41)
    - AGENTS.md (Privacy invariant 6 — no new env reads)
  </read_first>
  <action>
    1. Edit `src/routes/settings/+page.svelte`:
       - ADD a new `<article class="block">` ABOVE the existing "Activity log" block:
         ```svelte
         <article class="block">
           <h2>{m.settings_credentials_heading()}</h2>
           <!-- TODO Phase 3+: replace with unified /settings/credentials hub per
                UAT-NOTES.md §8.2-redesign (deferred from round-3). -->
           <p class="muted">Manage API keys for external services.</p>
           <a href="/keys/steam" class="audit-link">
             {m.settings_credentials_steam_link_label()} →
           </a>
         </article>
         ```
    2. Edit `src/lib/components/SessionsList.svelte`:
       - Locate the per-session sign-out handler (button or form action that POSTs/DELETEs to /api/me/sessions/:id or equivalent — examine the existing code).
       - After `await fetch(...)` success, add `await invalidateAll();` (import `invalidateAll` from `$app/navigation` if not already imported).
       - If the component currently relies on a manual reload or no refresh, switch it to invalidateAll().
    3. Edit `src/lib/components/SourceRow.svelte`:
       - REMOVE the inline `<label class="toggle"><input type="checkbox" checked={source.autoImport} ... /></label>` from the `.status` block. Replace with a non-interactive pill `<span class="auto-pill">{source.autoImport ? m.sources_auto_import_on() : m.sources_auto_import_off()}</span>`.
       - Add `let editAutoImport = $state(false);` near the existing `editName` state.
       - In the existing `editing = !editing` toggle (the pencil button), when transitioning false → true, ALSO seed `editAutoImport = source.autoImport;` (mirror the editName seeding pattern).
       - Inside the `{#if editing}` block, ADD a new `<label class="toggle"><input type="checkbox" bind:checked={editAutoImport} /> Auto-import</label>` (or use Paraglide key m.sources_form_auto_import_label if you add one — for round-3 keep using the existing inline string).
       - Rename function `saveDisplayName` to `saveSourceEdit`. PATCH body becomes `{ displayName: editName.trim() || null, autoImport: editAutoImport }`.
       - DELETE the standalone `toggleAutoImport` function and its references in the row markup.
       - Remove `disabled={mutating}` from the deleted toggle (no longer needed).
    4. Edit `messages/en.json`:
       - ADD 4 keys (alphabetical insertion):
         - `settings_credentials_heading`: "Credentials"
         - `settings_credentials_steam_link_label`: "Steam Web API key"
         - `sources_auto_import_off`: "Auto-import: Off"
         - `sources_auto_import_on`: "Auto-import: On"
       - Run `pnpm paraglide:compile` so compiled message bundles regenerate.
    5. Edit `tests/unit/paraglide.test.ts`:
       - Add the 4 new keys to EXPECTED_KEYS in alphabetical order.
    6. Edit `tests/integration/empty-states.test.ts`:
       - APPEND a small assertion confirming /settings page (when rendered with a logged-in user fixture) contains the literal `/keys/steam` href (use the existing rendering test pattern in this file). This guards UAT-NOTES.md §8.1-bug closure.
    7. Update `.planning/phases/02.1-architecture-realignment/02.1-VALIDATION.md`:
       - APPEND a new row to the Per-Task Verification Map: `| 02.1-22 T2 | 02.1-22 | round-3 | UX | manual + integration | (paraglide test + empty-states test) | (existing files extended) | ⬜ manual |` for the visual sticky / body-scroll-lock / sessions-list-refresh / auto-import-edit-gate UAT.
       - Do NOT flip sign-off (per orchestrator guidance).
  </action>
  <verify>
    <automated>pnpm test:unit -- tests/unit/paraglide.test.ts && pnpm test:integration -- tests/integration/empty-states.test.ts && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/keys/steam" src/routes/settings/+page.svelte` returns ≥ 1
    - `grep -c "settings_credentials_heading" src/routes/settings/+page.svelte` returns ≥ 1
    - `grep -c "invalidateAll" src/lib/components/SessionsList.svelte` returns ≥ 1
    - `grep "toggleAutoImport" src/lib/components/SourceRow.svelte` returns NOTHING (function deleted)
    - `grep -c "editAutoImport" src/lib/components/SourceRow.svelte` returns ≥ 3 (declaration + seed + bind)
    - `grep -c "auto-pill\|sources_auto_import_on" src/lib/components/SourceRow.svelte` returns ≥ 1
    - `grep -E "settings_credentials_heading|settings_credentials_steam_link_label|sources_auto_import_on|sources_auto_import_off" messages/en.json | wc -l` returns 4
    - `grep -E "settings_credentials_heading|settings_credentials_steam_link_label|sources_auto_import_on|sources_auto_import_off" tests/unit/paraglide.test.ts | wc -l` returns 4
    - `pnpm test:unit -- tests/unit/paraglide.test.ts` passes
    - `pnpm test:integration` ALL pass (no regression)
    - `pnpm lint` passes (Svelte 5 + TypeScript)
  </acceptance_criteria>
  <done>
    /settings has a Credentials block with link to /keys/steam (UAT-NOTES.md §8.1-bug closed); sign-out-this-session refreshes the list reactively (§6.3-bug closed); SourceRow auto_import is read-only in display mode and editable in edit form (§2.4-decision option A implemented); 4 new Paraglide keys added with EXPECTED_KEYS update; tests/integration/empty-states.test.ts gains /keys/steam-link-on-/settings assertion.
  </done>
</task>

</tasks>

<verification>
- `pnpm test` passes (paraglide compile + svelte-kit sync + every project)
- `pnpm test:browser` passes (vite preview on :5173)
- `pnpm lint` passes
- `pnpm db:check` passes (no schema changes in this plan)
- Manual UAT walkthrough (in Russian, per user-MEMORY profile) covers:
  - Sticky AppHeader on every page (/feed, /sources, /games/[id], /settings, /audit) at 360px
  - Sticky page CTA on /sources at 360px
  - FiltersSheet open → background does NOT scroll
  - /settings → click "Steam Web API key" → arrives at /keys/steam
  - /settings → with two sessions, click sign-out on the non-current one → list updates without F5
  - /sources → SourceRow auto_import shows as a pill in display mode; click Edit → toggle is editable; save → both fields persist
</verification>

<success_criteria>
- All 5 round-3 bug findings (2.2-bug + 1.4-bug + 6.3-bug + 8.1-bug + 2.4-decision) closed.
- Privacy & multi-tenancy invariants intact (no schema, no env, no audit verb additions).
- Paraglide invariant D-41 honored (4 new keys + EXPECTED_KEYS update in same commit).
- /settings/credentials hub deferral marker (TODO) in place so Phase 3+ rebuild is mechanical.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-architecture-realignment/02.1-22-SUMMARY.md` per template.
</output>
