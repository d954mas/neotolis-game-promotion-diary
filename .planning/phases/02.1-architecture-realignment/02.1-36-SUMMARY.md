---
phase: 02.1-architecture-realignment
plan: 36
subsystem: privacy-floor
tags: [ci-blocker, privacy-floor, redact-paths, locale-lock, env-discipline, jsdoc-aware-scanner, schema-introspection, gap-closure, round-6]

# Dependency graph
requires:
  - phase: 02.1-architecture-realignment
    provides: Plan 02.1-16 formatFeedDate helper (4-bucket compact date format); Plan 01-01 src/lib/server/logger.ts D-24 redact paths foundation; Plan 02-10 src/routes/settings/+page.server.ts JSDoc commentary on env-discipline; Plan 02.1-04 data_sources schema + Plan 02-05 api_keys_steam ciphertext columns
provides:
  - "src/lib/util/format-feed-date.ts: same-year + prior-year branches now use fixed `\"en\"` locale instead of host locale (`undefined`). Date format conventions (Apr 15, Dec 25, 2025) are a brand choice, NOT a translation target — Paraglide handles all user-facing copy elsewhere. Locale-dependent assertions in tests/unit/format-feed-date.test.ts now pass on any host machine (a Russian-locale dev box was producing `15 апр.` and tripping CI)."
  - "src/lib/server/logger.ts: REDACT_PATHS extended with 12 entries covering Phase 2.1 ciphertext column shapes (camel + snake): secret_ct/secretCt, secret_iv/secretIv, secret_tag/secretTag, dek_iv/dekIv, dek_tag/dekTag, kek_version/kekVersion. Both shapes listed because row dumps surface in either form depending on the call site (Drizzle returns camel; raw pg returns snake)."
  - "src/lib/server/logger.ts: REDACT_PATHS now `export const` (was `const`) so the schema-introspection coverage test can import the canonical list directly instead of grep'ing the source. Visibility-only change — no runtime behavior shift."
  - "tests/unit/logger.test.ts: env-discipline scanner refactored to strip multi-line block comments (including JSDoc /** ... */) at the FILE level before grep'ing for `process.env`. The strip preserves newlines so per-line indices stay aligned with the original file (so reported line numbers for actual offenders remain accurate). JSDoc bodies that document the env-discipline rule no longer trigger false positives."
  - "tests/unit/logger.test.ts: NEW `Plan 02.1-36: env-discipline scanner skips JSDoc /** ... */ blocks` regression test — asserts a synthetic JSDoc fixture containing `process.env` does NOT trip the scanner pipeline. Independent of the live scanner implementation; a future scanner refactor that drops the strip will fail this test."
  - "tests/unit/logger.test.ts: NEW `Plan 02.1-36: REDACT_PATHS covers every ciphertext column in src/lib/server/db/schema/` schema-introspection coverage test — scans every src/lib/server/db/schema/*.ts for `bytea(\"...\")` declarations + the `kek_version` smallint, builds the EXPECTED set as the union of camelCase Drizzle field names and snake_case DB column names, asserts every entry is present in REDACT_PATHS. Once-and-done invariant per AGENTS.md item 6 — any future ciphertext column added without a corresponding redact path fails this test loudly. Defense-in-depth: also asserts the explicit Phase 2.1 floor list (12 entries) is present even if the schema regex misses something."
affects:
  - "Future ciphertext column additions: any new `bytea(\"...\")` column added to src/lib/server/db/schema/*.ts WITHOUT a corresponding REDACT_PATHS entry now fails `pnpm test:unit` on PR. Privacy floor (AGENTS.md item 6) is enforced by construction, not by manual upkeep."
  - "Future JSDoc that mentions process.env / env-discipline rule: the env-scanner now ignores all multi-line block comment bodies. Project documentation can reference the rule freely."
  - "Future date format polish (Phase 6+): if per-user date-format preference ships, the `\"en\"` literal in formatFeedDate becomes a user setting. Documented as forward-pointer in the helper's header comment."

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Schema-introspection-derived test invariants — instead of asserting against a hand-maintained list, derive the EXPECTED set programmatically from source-of-truth declarations (schema files, route definitions, etc.) and assert the runtime config matches. Future schema additions without a corresponding config update fail the test loudly. Generalizes the Phase 1 D-24 redact-test pattern from 'verify listed paths get redacted' to 'verify every ciphertext column has a redact path'."
    - "File-level block-comment strip with newline preservation — when a regex-based source scanner needs to ignore comment bodies that span multiple lines, replace each `/* ... */` match with a same-line-count newline-only string: `src.replace(/\\/\\*[\\s\\S]*?\\*\\//g, (m) => m.replace(/[^\\n]/g, \"\"))`. Reported line numbers for actual offenders stay accurate because the index pipeline still sees the same number of lines."

key-files:
  created: []
  modified:
    - "src/lib/util/format-feed-date.ts (Task 1: locale lock — `undefined` → `\"en\"` on lines 49 + 51; header comment updated to document the deliberate brand choice)"
    - "src/lib/server/logger.ts (Task 1: REDACT_PATHS extended with 12 ciphertext entries; export keyword added; header comment updated to document the schema-introspection load-bearing guarantee)"
    - "tests/unit/logger.test.ts (Task 1: env-scanner refactor + 2 new tests — JSDoc-aware regression + schema-introspection redact-coverage)"
    - ".planning/phases/02.1-architecture-realignment/deferred-items.md (Task 1 logging: pre-existing dev-mock-oauth.mjs lint error noted as out-of-scope)"

key-decisions:
  - "formatFeedDate locale strategy — fixed `\"en\"` over locale-tolerant assertions. Two paths existed: (A) lock the helper to `\"en\"` and keep test assertions strict; (B) keep host locale and rewrite test assertions to be locale-tolerant (e.g. assert via Intl.DateTimeFormat with month index, not literal text). Path A chosen because date format is a deliberate brand choice (Apr 15, Dec 25, 2025) — it's not a translation target. Paraglide handles all user-facing copy. Path B would have made the test logic more fragile (locale-tolerant regex over getMonth+getFullYear) AND would have produced inconsistent UI across locales. Forward-pointer in the helper's header comment notes that Phase 6+ user-date-format-preference work makes the literal a user setting."
  - "Env-scanner JSDoc handling — extend the scanner over rephrasing the JSDoc. The Plan 02-10 precedent rewrote a JSDoc body to drop the literal `process.env` substring (commit history confirms). Plan 02.1-36 extends the scanner instead because (a) JSDoc IS canonical project documentation per AGENTS.md commenting policy, (b) future plans will mirror the env-discipline JSDoc on other server-side loaders, and (c) the rule should target code, not comments. The newline-preserving strip pattern keeps reported line numbers for real offenders accurate."
  - "Schema-introspection coverage test over hand-maintained explicit list — the comment at logger.ts:12-14 already promised 'a typo here will fail that test loudly instead of silently disabling the protection'. The pre-existing redaction test covered the SOUNDNESS direction (every listed path is redacted) but not the COMPLETENESS direction (every ciphertext column has a listed path). Schema introspection closes the completeness gap. Defense-in-depth: the test also asserts the explicit Phase 2.1 floor list (12 entries) is present even if the schema regex misses a future declaration that uses an alias type or some other shape the regex can't see."
  - "REDACT_PATHS visibility (`const` → `export const`) — the schema-introspection test imports the canonical list directly instead of grep'ing the source. Two paths existed: (A) export the constant; (B) keep grep'ing logger.ts for the array literal. Path A chosen because (a) it's a visibility-only change (no behavior shift; production runtime unchanged), (b) it removes a fragile regex from the test (a future logger.ts refactor that splits the array across multiple lines would break the grep), and (c) it makes the canonical list discoverable to future tests / instrumentation. The pre-existing 'redacts every D-24 path' test still grep's the source — left unchanged because rewriting it is out of scope for this plan."

patterns-established:
  - "Schema-derived REDACT_PATHS coverage — every `bytea(\"<snake>\")` column in src/lib/server/db/schema/*.ts MUST have both `*.<camel>` and `*.<snake>` entries in REDACT_PATHS. Enforced by tests/unit/logger.test.ts schema-introspection test. Adding a ciphertext column is a one-step change: add the bytea column, add the redact paths, ship. The test catches drift on PR."
  - "File-level block-comment strip in source scanners — when a regex tripwire scans .ts source files for forbidden patterns, strip multi-line block comments (`/\\/\\*[\\s\\S]*?\\*\\//g`) at the file level FIRST (preserving newlines), then split into lines for per-line `//` strip. JSDoc bodies are canonical project documentation; they MUST NOT trip code-targeting tripwires. Pattern documented inline at tests/unit/logger.test.ts."
  - "Brand-choice locale lock for UI primitives — when a string is a brand choice (date format, currency separator, etc.) NOT a translation target, lock the locale at the helper level (`toLocaleDateString(\"en\", ...)`) instead of inheriting host locale. Paraglide handles user-facing copy elsewhere. Documented as forward-pointer in src/lib/util/format-feed-date.ts header comment."

requirements-completed: ["FEED-01", "PRIV-02", "AUTH-03"]

# Metrics
duration: ~5 min
completed: 2026-04-30
---

# Phase 02.1 Plan 36: CI-blocker + privacy-floor gap closure (round 6) Summary

**Closes UAT-NOTES.md §5.9 (P0, CI-blocker) and §5.10 (P1, privacy floor) in a single Wave 0 commit. `pnpm test:unit` exits 0 on any host locale by locking `formatFeedDate` to a fixed `"en"` locale and extending the env-discipline scanner to strip multi-line JSDoc bodies (`/** ... */`) at the file level before grep'ing for `process.env`. `REDACT_PATHS` in `src/lib/server/logger.ts` extended with the 12 Phase 2.1 ciphertext column shapes (secret_ct/secretCt, secret_iv/secretIv, secret_tag/secretTag, dek_iv/dekIv, dek_tag/dekTag, kek_version/kekVersion); a new schema-introspection test scans `src/lib/server/db/schema/*.ts` for every `bytea("...")` declaration plus the `kek_version` smallint and asserts both shapes are in `REDACT_PATHS` — privacy floor (AGENTS.md item 6) now enforced by construction, not by manual upkeep. NO schema migration. NO service refactor. NO HTTP route change. Pure utility / config / test edits.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-04-30T10:22:30Z
- **Completed:** 2026-04-30T10:27:30Z
- **Tasks:** 1
- **Files modified:** 4 (3 production: src/lib/util/format-feed-date.ts, src/lib/server/logger.ts, tests/unit/logger.test.ts; 1 docs: deferred-items.md)

## Accomplishments

- **§5.9 root cause A closed** — `formatFeedDate` switched from host-locale `toLocaleDateString(undefined, ...)` to fixed `toLocaleDateString("en", ...)` on both branches (same-year + prior-year). Russian-locale dev boxes (and any non-English-locale CI runner) now produce `Apr 15` / `Dec 25, 2025` consistently. Test assertions in `tests/unit/format-feed-date.test.ts` pass unchanged. Header comment documents the deliberate brand choice (date format ≠ translation target; Paraglide handles user-facing copy) and forward-points to Phase 6+ user-date-format-preference work.
- **§5.9 root cause B closed** — env-discipline scanner in `tests/unit/logger.test.ts` extended to strip multi-line block comments at the FILE level (preserving newlines) before splitting into lines. The previous per-line strip handled `// ...` and same-line `/* ... */` but not multi-line JSDoc bodies. Now `src/routes/settings/+page.server.ts:14-15` (and any future file with a JSDoc that documents the env-discipline rule) no longer trips the tripwire. New regression test asserts the strip stays in place — independent of the scanner implementation; a future refactor that drops the strip fails the test.
- **§5.10 closed** — `REDACT_PATHS` in `src/lib/server/logger.ts` extended with 12 entries covering the Phase 2.1 ciphertext column shapes (camel + snake): `secret_ct`/`secretCt`, `secret_iv`/`secretIv`, `secret_tag`/`secretTag`, `dek_iv`/`dekIv`, `dek_tag`/`dekTag`, `kek_version`/`kekVersion`. Both shapes listed because row dumps surface in either form depending on the call site (Drizzle returns camel; raw pg returns snake). REDACT_PATHS is now `export const` so the new schema-introspection test can import the canonical list directly.
- **§5.10 enforcement** — NEW schema-introspection test scans `src/lib/server/db/schema/*.ts` for every `bytea("<snake>")` declaration plus the `kek_version` smallint, builds the EXPECTED set as the union of camelCase Drizzle field names and snake_case DB column names, and asserts every entry is present in `REDACT_PATHS`. Adding a new ciphertext column WITHOUT a corresponding redact path now fails `pnpm test:unit` on PR. Privacy floor (AGENTS.md item 6) enforced by construction, not by manual upkeep. Defense-in-depth: also asserts the explicit Phase 2.1 floor list (12 entries) is present even if the schema regex misses a future declaration that uses an alias type or some other shape the regex can't see.
- **Privacy & multi-tenancy invariants intact** — NO schema, NO service, NO env, NO audit verb, NO HTTP route changes. The fix is pure-test (env-scanner refactor + 2 new tests) + pure-config (REDACT_PATHS list + helper locale lock). Tenant scoping, anonymous-401 sweep, DTO discipline, audit log INSERT-only, cross-tenant 404 — all unchanged. Self-host parity preserved (no APP_MODE branch introduced).
- **`src/routes/settings/+page.server.ts` intentionally NOT modified** — the scanner refactor obviates the false positive and the JSDoc body is canonical project documentation. `git diff --stat` shows zero line changes to this file. The Plan 02-10 precedent (rewriting the JSDoc to drop the literal substring) is superseded by extending the scanner instead — JSDoc IS canonical project documentation per AGENTS.md commenting policy.

## Task Commits

| Task | Name                                                                                                                                                                                                | Commit  | Files                                                                                                       |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ----------------------------------------------------------------------------------------------------------- |
| 1    | Fix formatFeedDate locale-lock; extend env-scanner JSDoc strip; expand REDACT_PATHS; add schema-introspection redact-coverage test                                                                  | 26702d3 | src/lib/util/format-feed-date.ts, src/lib/server/logger.ts, tests/unit/logger.test.ts                       |

## Verification Evidence

```
$ pnpm test:unit
 ✓ |unit| tests/unit/format-feed-date.test.ts (6 tests) 31ms
 ✓ |unit| tests/unit/logger.test.ts (5 tests) 215ms
 ... (14 test files total, 97 tests)
 Test Files  14 passed (14)
      Tests  97 passed (97)
```

Test count: was 92 (with 3 failing) before plan; now 97 (all passing) — +2 new tests (JSDoc-regression + schema-introspection redact-coverage) + 3 previously-failing tests now passing.

Acceptance-criteria grep checks:

```
$ grep -c 'toLocaleDateString("en"' src/lib/util/format-feed-date.ts
2                                                            # ✓ both branches locked
$ grep -c 'toLocaleDateString(undefined' src/lib/util/format-feed-date.ts
0                                                            # ✓ no host-locale references
$ grep -c "secretCt" src/lib/server/logger.ts                # ✓ camel covered
1
$ grep -c "secret_ct" src/lib/server/logger.ts               # ✓ snake covered
1
$ grep -cE "secretIv|secret_iv" src/lib/server/logger.ts     # ✓ both shapes
2
$ grep -cE "secretTag|secret_tag" src/lib/server/logger.ts   # ✓
2
$ grep -cE "dekIv|dek_iv" src/lib/server/logger.ts           # ✓
2
$ grep -cE "dekTag|dek_tag" src/lib/server/logger.ts         # ✓
2
$ grep -c "export const REDACT_PATHS" src/lib/server/logger.ts
1                                                            # ✓ visibility tweak
$ grep -c "Plan 02.1-36" tests/unit/logger.test.ts
3                                                            # ✓ env-scanner comment + JSDoc test + redact-coverage test
$ git diff --stat src/routes/settings/+page.server.ts
                                                             # ✓ zero changes (negative assertion)
```

`pnpm typecheck` exits with **0 ERRORS** (29 pre-existing Svelte warnings unrelated to this plan; logged out-of-scope).

`pnpm lint` reports **1 pre-existing error** in `dev-mock-oauth.mjs:26` — `'process.env' is restricted from being used`. Pre-existing on master (commit 9a0792e); NOT a regression. Logged in `deferred-items.md` under "Plan 02.1-36 — out-of-scope lint error in dev-mock-oauth.mjs". Per SCOPE BOUNDARY rule (executors do not auto-fix issues NOT directly caused by the current task), tracked for separate follow-up.

## Deviations from Plan

None. The plan was executed exactly as written:

- Step 1 (formatFeedDate locale lock): applied verbatim. Both `toLocaleDateString(undefined, ...)` calls replaced with `"en"`; header comment updated to document the brand-choice rationale + Phase 6+ forward-pointer.
- Step 2 (env-scanner JSDoc strip): file-level strip with newline-preserving replacement applied verbatim per <interfaces> "Fix path (preferred)". JSDoc regression test added per <interfaces> "JSDoc-aware env-scanner regression assertion".
- Step 3 (REDACT_PATHS expansion): all 12 entries appended in the expected camel-then-snake ordering; export keyword added; header comment updated.
- Step 4 (schema-introspection coverage test): added per <interfaces> spec, with the additional defense-in-depth floor-list assertion mentioned in the plan's action step.
- Step 5 (existing format-feed-date test verification): zero changes — source-side locale lock was the entire fix as predicted.
- Step 6 (settings/+page.server.ts NOT modified): zero changes — `git diff --stat` confirms; scanner refactor handled the false positive cleanly.
- Step 7 (full unit suite): `pnpm test:unit` exits 0; 14 test files, 97 tests, all passing.
- Step 8 (typecheck + lint): typecheck 0 errors; lint pre-existing error in dev-mock-oauth.mjs logged as out-of-scope.

## Authentication Gates

None.

## Self-review

- **Constraints:** ✓ no new env reads outside `src/lib/server/config/env.ts`; no new at-rest secret added; no SaaS/self-host divergence (changes are environment-agnostic — locale lock is a literal, REDACT_PATHS is a constant, scanner refactor is test-only). The privacy floor (AGENTS.md item 6) is now ENFORCED by construction.
- **Philosophy:** ✓ simplicity for an outsider — the schema-introspection test reads source-of-truth schema files and produces an actionable error message ("REDACT_PATHS is missing entries for ciphertext columns: ..."); SaaS/self-host parity preserved (no env / mode branches); modularity preserved (logger.ts owns redact paths; logger.test.ts owns the invariant); no premature abstraction (the brand-choice locale lock is a single literal, the schema regex is inline in the test); security as floor — privacy floor (AGENTS.md item 6) tightened.
- **Practices:** ✓ atomic (single commit, single goal); tests with feature (the new tests land in the same commit as the code change); migrations forward-only (no schema change); env reads centralized (no new reads); self-host parity preserved; secrets redaction TIGHTENED (12 new paths); comments WHY-only (header comment updates document the rationale); Conventional Commits (`fix(02.1-36):`); versions pinned (no dep change).
- **CI gate honesty:** ✓ every assertion in the new tests is load-bearing — JSDoc regression asserts `process.env` doesn't survive the strip pipeline (would fail on a regressed scanner); schema-introspection test asserts every bytea column has a redact path (would fail on a missing entry); defense-in-depth floor-list assertion is the redundant-but-cheap guard against schema-regex misses. No softened existing test.
- **Documentation drift:** ✓ AGENTS.md item 6 (privacy floor / Pino redact coverage) is now ENFORCED by construction in tests/unit/logger.test.ts — text and code now match. `src/lib/util/format-feed-date.ts` header comment updated to reflect the locked locale. `src/lib/server/logger.ts` header comment updated to reference the schema-introspection load-bearing guarantee. UAT-NOTES.md §5.9 + §5.10 are CLOSED by this plan; the round-6 closure record will be reflected in STATE.md and ROADMAP.md.

## Self-Check: PASSED

Created files:
- `.planning/phases/02.1-architecture-realignment/02.1-36-SUMMARY.md` ← FOUND

Commit verified:
- `26702d3` (`fix(02.1-36): close round-5 P0 CI-blocker + P1 privacy-floor gaps`) ← FOUND in `git log`

Modified files (plan-scoped only):
- `src/lib/util/format-feed-date.ts` ← FOUND (commit 26702d3)
- `src/lib/server/logger.ts` ← FOUND (commit 26702d3)
- `tests/unit/logger.test.ts` ← FOUND (commit 26702d3)
- `.planning/phases/02.1-architecture-realignment/deferred-items.md` ← FOUND (uncommitted; will land in final docs commit)

Out-of-scope items (parallel agent 02.1-35):
- `src/lib/server/services/events.ts`, `tests/integration/events.test.ts`, `tests/integration/events-attach.test.ts` ← INTENTIONALLY excluded from this commit (parallel-execution scope per Plan 02.1-02 commit-hygiene precedent).
