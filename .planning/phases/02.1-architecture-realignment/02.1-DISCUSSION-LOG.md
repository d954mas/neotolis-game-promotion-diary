# Phase 2.1: Architecture Realignment - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-28
**Phase:** 02.1-architecture-realignment
**Discussion language:** Russian (user request: "на русском")
**Areas discussed:** Phase shape & wave strategy, Dev DB migration behavior, PollingBadge default for manual paste + /events/[id] stub, AttachToGamePicker mobile UX + "+ New event" location, Polish bundle scope, Smoke test scope, /sources YouTube key warning, /feed filter persistence, 0 games + source registration UX

---

## Phase shape & wave strategy

### Question 1: Phase 2.1 — одна фаза или сплит на 2.1a + 2.1b?

| Option | Description | Selected |
|--------|-------------|----------|
| Одна фаза 2.1 (рекомендую) | ~10–12 планов в 5 волнах. Один атомарный пакет; никаких промежуточных «полу-мигрированных» состояний на master. Соответствует Phase 2 прецеденту. | ✓ |
| Сплит 2.1a (data) + 2.1b (UI) | Меньшие фазы, поэтапный релиз. Минус: между ними master в состоянии «новые routes без UI» — нужны заглушки, и пользователь видит частично сломанный продукт. | |

**User's choice:** Одна фаза 2.1 (рекомендую)
**Notes:** Confirmed по recommendation.

### Question 2: Как структурировать миграцию + удаление старого кода между волнами?

| Option | Description | Selected |
|--------|-------------|----------|
| Wave 0 = миграции + scaffolding; Wave 1+ переписывает поверх (рекомендую) | Wave 0 лендит 0002+0003 SQL, обновляет ESLint TENANT_TABLES, лендит 5 новых test placeholder файлов. Wave 1 удаляет старые services + строит новые. Прецедент: Phase 2 Wave 0. | |
| Большой план: миграция + удаление старого + новые services вместе | Один тяжёлый план обновляет схему, удаляет старые services/routes, лендит новые. Меньше промежуточных сломанных состояний, но трудно ревьюить. | |

**User's choice:** Other (free text): "Миграции не нужно, это все в разработке, старую базу можно просто удалить"
**Notes:** This answer pivoted the entire migration strategy — instead of forward-only with a hand-written companion (RESEARCH §4 Path B), the user authorized a single-new-baseline approach. Subsequent questions (Question 3 / 4) reflected this pivot. The original recommendation (Wave 0 = migrations + scaffolding) is preserved within the new baseline approach: Wave 0 lands the SINGLE NEW BASELINE migration (not the 2-file split) plus scaffolding.

---

## Dev DB migration behavior

### Question 3: Как реорганизовать drizzle/ для Phase 2.1?

| Option | Description | Selected |
|--------|-------------|----------|
| Single new baseline (рекомендую) | Удалить 0000 + 0001, сгенерировать один 0000_phase02_1_baseline.sql с финальной схемой (data_sources, events в финальном виде, audit_action в финальной vocabulary, без youtube_channels/tracked_youtube_videos/game_youtube_channels). Чистый audit trail от ребазы. CI и dev DB стартуют с чистого листа. | ✓ |
| 0002 = DROP + CREATE поверх 0000 + 0001 | Сохранить Phase 1 + Phase 2 миграции; добавить один крупный 0002 с DROP старых таблиц + CREATE новой схемы. Audit trail сохраняется в истории файлов, но финальную схему нужно собирать из 3 файлов. | |

**User's choice:** Single new baseline (рекомендую)
**Notes:** Eliminates RESEARCH §4 hand-written `0003_phase02_1_audit_enum_rename.sql` companion entirely. Captured as DV-1 + DV-2 in CONTEXT.md.

### Question 4: Удаляем ли dev DB пользовательских UAT-данных при первой загрузке Phase 2.1?

| Option | Description | Selected |
|--------|-------------|----------|
| Да, снести dev DB полностью (рекомендую) | Executor в plan README пишет инструкцию: 'pnpm db:drop && pnpm db:migrate'. Чистая база, первый сигнин создаёт user, дальше manual UAT с нуля. Подходит к 'pre-production, всё в разработке'. | ✓ |
| Нет, писать скрипт backfill (UAT данные сохраняются) | Переживаем существующие games + youtube_channels + events. Больше работы в миграции; ломает вариант 'Single new baseline' выше и возвращает нас к 0002 + INSERT по новым таблицам. | |

**User's choice:** Да, снести dev DB полностью (рекомендую)
**Notes:** Captured as D-04 in CONTEXT.md.

---

## PollingBadge default for manual paste + /events/[id] stub

### Question 5: PollingBadge default для событий с source_id=NULL (manual paste)?

| Option | Description | Selected |
|--------|-------------|----------|
| 'Manual entry — no polling' (рекомендую) | Muted text в badge. Честно объясняет почему нет статуса. Парирует с визуальным языком PollingBadge для auto-import (Phase 3 будет 'Hot/Warm/Cold/Stale'). | |
| Скрыть PollingBadge для этих событий | Нет источника = нет статуса = не рендерится. Меньше визуального шума. Минус: row для manual events выглядит «неполным» рядом с auto-import rows в Phase 3. | |

**User's choice:** Other (free text): "Так подожди. Я вставил сам видео, я же могу вставить видео со своего канала? Или не со своего? И я хочу чтобы вот это конкретное видео потом получало информацию и обновляло просмотры"
**Notes:** The user's clarification revealed that the original options were too narrow — manually pasted YouTube videos must ALSO get stats history. The discussion pivoted to clarifying the polling architecture and confirming that `pollStats(event, source: DataSourceRow | null)` (RESEARCH §2.1, already nullable) handles this naturally. Re-asked as Question 6 with the corrected framing.

### Question 6: Подтверждаешь PollingBadge UX (с учётом того что Phase 3 будет поллить manual-paste видео тоже)?

| Option | Description | Selected |
|--------|-------------|----------|
| Да: единый 'Phase 3 will start polling' для pollable kinds | Для kind ∈ {youtube_video, reddit_post} (любых, с source_id или без) — PollingBadge: 'Phase 3 will start polling' (muted). Для kind ∈ {conference, talk, press, other, twitter_post, telegram_post, discord_drop} — PollingBadge скрыт в 2.1. Phase 3 worker scope: pollStats вызывается для всех youtube_video events независимо от source_id. | ✓ |
| Нет, я имел в виду другое — объясню в следующем сообщении | Спросить уточняющий вопрос перед фиксацией решения. | |

**User's choice:** Да: единый 'Phase 3 will start polling' для pollable kinds
**Notes:** Captured as D-05 + D-06 in CONTEXT.md. DV-3 records the override of UI-SPEC's 3-option FLAG.

### Question 7: /events/[id] — ship stub в 2.1 или скрыть 'Open' кнопку до Phase 4?

| Option | Description | Selected |
|--------|-------------|----------|
| Ship stub (рекомендую) | Phase 4 placeholder page: 'Detail page lands in Phase 4' + 'Back to feed' link. Affordance в row видима, IA полная, нет dead-end 404. | ✓ |
| Скрыть 'Open' кнопку до Phase 4 | Меньше кода. FeedRow в 2.1 без 'Open'. Плюс: нет «пустых» страниц. Минус: row эволюционирует между фазами — юзер видит новый affordance только в Phase 4. | |

**User's choice:** Ship stub (рекомендую)
**Notes:** Captured as D-07 in CONTEXT.md.

---

## AttachToGamePicker mobile UX + "+ New event" location

### Question 8: AttachToGamePicker open-state на мобиле (360px viewport)?

| Option | Description | Selected |
|--------|-------------|----------|
| Inline anchored dropdown (рекомендую) | Выпадает под кнопкой row. Просто, подходит для типичных indie devs (1–5 игр). Bottom-sheet вариант ждёт Phase 6 если clipping появится. | ✓ |
| Bottom-sheet <dialog> сразу | Поднимается снизу, full-width. Скейлится для любого числа игр. Больше кода сразу; UX-риск 'context switch' на каждый attach. | |

**User's choice:** Inline anchored dropdown (рекомендую)
**Notes:** Captured as D-08 in CONTEXT.md.

### Question 9: '+ New event' (free-form: conference / talk / press / other) — где создавать?

| Option | Description | Selected |
|--------|-------------|----------|
| Отдельный route /events/new (рекомендую) | Full-page форма, консистентно с /sources/new. Один паттерн 'new ресурс = отдельная страница' во всём codebase. URL можно расшарить и отладить. | ✓ |
| Inline <dialog> на /feed | Modal поверх /feed, не выходим из контекста. Похоже на PasteBox flow. Минус: два паттерна создания resource (форма-роут vs dialog) в соседних фичах. | |

**User's choice:** Отдельный route /events/new (рекомендую)
**Notes:** Captured as D-09 in CONTEXT.md.

---

## Polish bundle scope

### Question 10: Todo `audit-ux-improvements` (group checkbox filters + table headers на /audit) — включаем в Phase 2.1 polish bundle или деферим к Phase 6?

| Option | Description | Selected |
|--------|-------------|----------|
| Defer к Phase 6 polish (рекомендую) | Phase 2.1 уже большая (миграция + 3 страницы + 5 polish items). /audit в UI-SPEC трогается только для copy delta (5 новых audit-action labels). Phase 6 polish именно про это — date-range filter, page-size selector уже планируются там. | ✓ |
| Включить в Phase 2.1 polish bundle | +1 polish item к 5 уже запланированным. Расширяет wave 4. | |

**User's choice:** Defer к Phase 6 polish (рекомендую)
**Notes:** Captured as D-10 in CONTEXT.md and added to Reviewed Todos (not folded).

---

## Smoke test scope, /sources YouTube key warning, /feed filter persistence, 0 games UX (BATCH)

### Question 11: Smoke test: включать PATCH /api/events/:id/dismiss-inbox в Phase 2.1 smoke?

| Option | Description | Selected |
|--------|-------------|----------|
| Нет — defer к Phase 3 (рекомендую) | Phase 2.1 smoke: register source → paste → /feed → attach → /games/[id]. Dismissal покрывает tests/integration/inbox.test.ts. Phase 3 smoke добавляет dismissal когда auto-import реально наполняет inbox. | ✓ |
| Да — включить в 2.1 | +1 шаг в smoke. Плюс: end-to-end покрытие нового endpoint. Минус: smoke setup сложнее (нужно имитировать inbox вручную: вставить URL → не attach → dismiss). | |

**User's choice:** Нет — defer к Phase 3 (рекомендую)
**Notes:** Captured as D-11 in CONTEXT.md.

### Question 12: /sources warning о необходимости YouTube API key (KEYS-01) в 2.1?

| Option | Description | Selected |
|--------|-------------|----------|
| PollingBadge достаточно (рекомендую) | 'Phase 3 will start polling' — детали (нужен API key) лендит Phase 3 вместе с KEYS-01 UI. 2.1 не дублирует Phase 3 onboarding. | ✓ |
| Добавить inline-info в SourceRow | 'Polling needs a YouTube API key in Phase 3 — add it on /keys when ready'. +1 Paraglide key, +1 элемент UI. | |

**User's choice:** PollingBadge достаточно (рекомендую)
**Notes:** Captured as D-12 in CONTEXT.md.

### Question 13: /feed filter persistence между сессиями?

| Option | Description | Selected |
|--------|-------------|----------|
| URL-only — clean slate каждый раз (рекомендую) | Без cookie/localStorage. Букмарк желаемый view. Меньше кода. Cookie серверу не нужен — self-host parity чище. | ✓ |
| Cookie + URL (URL wins) | Авто-восстановление последних фильтров при возврате на /feed. URL params перебивают cookie. +1 cookie name + reconciliation логика. | |

**User's choice:** URL-only — clean slate каждый раз (рекомендую)
**Notes:** Captured as D-13 in CONTEXT.md.

### Question 14: 0 games + регистрация source: разрешаем или onboarding gate?

| Option | Description | Selected |
|--------|-------------|----------|
| Разрешить (рекомендую) | Source и game — независимые сущности (PROJECT.md 'три независимых top-level lists'). AttachToGamePicker покажет inline 'No games yet — + Add a game' линк. Юзер свободен в порядке. | ✓ |
| Onboarding gate: сначала игра | /sources/new при 0 games редиректит на /games/new с inline-info 'Создай игру для attach'. Плюс: явный happy path. Минус: forcing порядка против PROJECT.md. | |

**User's choice:** Разрешить (рекомендую)
**Notes:** Captured as D-14 in CONTEXT.md.

---

## Closing question

### Question 15: 13 решений зафиксированы. Готов к CONTEXT.md, или есть ещё вопросы?

| Option | Description | Selected |
|--------|-------------|----------|
| Готов к CONTEXT.md | Записываем всё в 02.1-CONTEXT.md + 02.1-DISCUSSION-LOG.md, коммитим, выходим на /gsd:plan-phase 2.1. | ✓ |
| Обсудим ещё | Назови область или я предложу ещё кандидаты. | |

**User's choice:** Готов к CONTEXT.md

---

## Claude's Discretion

The following were noted in CONTEXT.md as planner-discretion (no user decision required, but listed for traceability):

- Wave 0 task split: one big "land migration + ESLint + scaffolding" plan vs three small plans.
- Wave 1 service-deletion ordering: delete-then-rewrite vs rewrite-then-delete.
- Whether `<RenameInline>` and `<AddSteamListingForm>` ship as separate plans in Wave 3 or bundled with `/games/[id]` rebuild.
- Per-Paraglide-key timing: lump all ~53 new keys into one Wave 0 commit (with placeholder copy) vs land per-component (with real copy).
- Smoke test extension: shell-script extension vs new dedicated smoke step.
- Whether `/events/new` form's optional game attach uses the same `<AttachToGamePicker>` component or a simpler `<select>`.

## Deferred Ideas

Captured in CONTEXT.md `<deferred>` section:

- Phase 3: event_stats_snapshots, real pollContent/pollStats, KEYS-01/KEYS-02, INGEST-01, /sources YouTube key warning, dismiss-inbox smoke, POLL-04/POLL-05 real surfaces.
- Phase 4: /events/[id] LayerChart detail page, VIZ-02, VIZ-03.
- Phase 6 polish: /audit UX improvements (D-10), /feed filter cookie persistence (D-13 alt), AttachToGamePicker bottom-sheet variant (D-08 alt), m.confirm_item_delete cleanup, /audit date-range filter, GIN index on events.metadata.
- Reviewed-but-not-folded todos: attachments-feature-new-milestone (separate milestone), event-detail-page (Phase 4), phase4-video-detail-cards-with-charts (Phase 4), audit-ux-improvements (Phase 6), redirect-dashboard-to-games (superseded), dashboard-first-step-cta (superseded), games-empty-state-misleading (Phase 2 leftover, possibly Phase 6).
