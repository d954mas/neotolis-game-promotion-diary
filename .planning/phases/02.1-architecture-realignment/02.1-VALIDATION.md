---
phase: 2.1
slug: architecture-realignment
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-28
---

# Phase 2.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
>
> **Authoritative source:** `02.1-RESEARCH.md` § 8 "Validation Architecture" — that section has the full per-REQ-ID test map (27 entries), Wave 0 gaps (5 new + 9 extends), and Manual-Only Verifications table. This file is the planner-/executor-consumed contract; sections below pull from research §8 verbatim or by reference.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest ^4.1.5 (locked Phase 1) — unchanged |
| **Config file** | `vitest.config.ts` (test.projects: `unit` / `integration` / `browser`) |
| **Quick run command** | `pnpm test:unit` |
| **Full suite command** | `pnpm test` (paraglide compile + svelte-kit sync + every project) **plus** `pnpm test:browser` (needs vite preview on :5173) |
| **Estimated runtime** | ~50s unit + integration; ~30s browser (Phase 2.1 adds ~5s for new feed/sources/dismissal tests) |

---

## Sampling Rate

- **After every task commit:** Run `pnpm test:unit` (~5s)
- **After every plan wave:** Run `pnpm test:integration` (~50s; needs Postgres)
- **Before `/gsd:verify-work`:** `pnpm test` + `pnpm test:browser` + smoke job all green
- **Max feedback latency:** ~5s (unit) / ~50s (integration)

---

## Per-Task Verification Map

> Filled in during planning. Each plan task adds a row; planner derives from RESEARCH §8 "Phase Requirements → Test Map" table.

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| _to be filled by planner_ | | | | | | | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

5 new test files MUST land as `it.skip` placeholders (Phase 1+2 Wave 0 pattern):

- [ ] `tests/integration/data-sources.test.ts` — covers SOURCES-01, SOURCES-02 (CRUD + soft-delete + audit + 422 forward-compat)
- [ ] `tests/integration/feed.test.ts` — covers FEED-01 (URL filters + cursor + 7 filter combinations)
- [ ] `tests/integration/inbox.test.ts` — covers INBOX-01 (auto-imported flow placeholder + dismissal endpoint)
- [ ] `tests/integration/events-attach.test.ts` — covers GAMES-04a reframed (PATCH /api/events/:id/attach)
- [ ] `tests/browser/feed-360.test.ts` — covers FEED-01 360px responsive (placeholder; manual UAT for auth-routed part per Phase 2 precedent)

9 existing test files EXTEND in later waves:
- `tests/integration/events.test.ts` — unified-table assertions, kind=youtube_video / kind=reddit_post coverage, source_id=NULL on manual paste
- `tests/integration/ingest.test.ts` — flip kind from `tracked_youtube_videos` to `events` row assertions
- `tests/integration/anonymous-401.test.ts` — add new routes; remove old routes
- `tests/integration/tenant-scope.test.ts` — extend cross-tenant matrix to new routes
- `tests/integration/migrate.test.ts` — assert renamed table has rows, dropped table is gone, audit_action enum has new vocabulary
- `tests/unit/dto.test.ts` — add `toDataSourceDto` behavioural test; assert `toEventDto` includes `author_is_me` + `sourceId`
- `tests/unit/tenant-scope-eslint-rule.test.ts` — assert `dataSources` triggers the rule, `youtubeChannels` does not (table no longer exists)
- `tests/integration/empty-states.test.ts` — fix /keys/steam empty-state copy
- `tests/smoke/self-host.sh` — extend with the Phase 2.1 unified flow

**No new framework install needed** — vitest + @vitest/browser + playwright + Postgres service container are all pinned from Phase 1+2.

---

## Manual-Only Verifications

Same pattern as Phase 2 (UX-02 360px on authenticated pages requires session-cookie injection deferred to Phase 6 polish):

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| /feed at 360px (authenticated) | FEED-01 | Cookie-injection harness deferred to Phase 6 | Sign in via local dev server; resize Chrome devtools to 360x640; visit /feed; verify chips collapse to "Filters (N)" sheet button |
| /sources at 360px (authenticated) | SOURCES-01 | Same | Same pattern, /sources |
| /games/[id] rebuild at 360px | GAMES-04a (reframed) | Same | Same pattern, /games/[id] |
| AppHeader avatar+email visual | UI polish | Token aesthetics resist auto-assertion | Confirm avatar circle + email render; verify legible at 360px |
| ConfirmDialog on event delete | EVENTS-03 (reframed) | Visual flow is the assertion | Click delete on a /feed row; verify ConfirmDialog appears before delete; click "Cancel" — event NOT deleted; click again, click "Delete" — event removed |
| /settings active sessions list | UI polish | New page, visual | Sign in twice (two browsers); /settings shows both sessions |
| Theme toggle moved | UI polish | Visual | ThemeToggle absent from AppHeader; present on /settings |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (5 new files above)
- [ ] No watch-mode flags
- [ ] Feedback latency < ~5s (unit) / ~50s (integration)
- [ ] `nyquist_compliant: true` set in frontmatter (after planner/executor passes)

**Approval:** pending
