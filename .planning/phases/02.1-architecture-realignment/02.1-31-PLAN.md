---
phase: 02.1-architecture-realignment
plan: 31
type: execute
wave: 2
depends_on: []
files_modified:
  - messages/en.json
  - tests/unit/paraglide.test.ts
  - src/lib/components/FeedCard.svelte
  - src/lib/components/FeedQuickNav.svelte
  - src/lib/components/FilterChips.svelte
  - src/lib/components/FiltersSheet.svelte
  - src/routes/feed/+page.svelte
  - src/routes/feed/+page.server.ts
autonomous: true
gap_closure: true
requirements: ["FEED-01", "INBOX-01", "EVENTS-02"]

must_haves:
  truths:
    - "Pure copy/i18n plan. Closes UAT-NOTES.md §4.24.A — user does not parse 'Standalone' as 'not related to any game'. Renames the user-facing copy of every Standalone surface to 'Не про игры' (ru) / 'Not game-related' (en) WITHOUT changing the underlying schema state name (`metadata.triage.standalone` boolean stays — DB column / audit verb / URL param `?show=standalone` / service function names markStandalone / unmarkStandalone all PRESERVED). Only the user-facing rendered text changes."
    - "Surfaces touched (5 components, 1 server file, 1 page file): FeedCard.svelte (overlay pill 'Standalone' + inline 'Mark standalone' button — Plan 02.1-23/24), FeedQuickNav.svelte (Plan 02.1-26 segment), FilterChips.svelte (Plan 02.1-24 chip text), FiltersSheet.svelte (Plan 02.1-24 radio label), /feed/+page.svelte (any inline 'Standalone' label), /feed/+page.server.ts (URL param parsing — `?show=standalone` PRESERVED but the user-facing display text in any breadcrumb / page title is updated)."
    - "Paraglide invariant D-41: NO new keys; CHANGE the VALUES of the 4 existing standalone-display keys (the keys themselves stay so the URL contract / state shape stays). Existing keys to change values:\n  - feed_card_mark_standalone_button: 'Mark standalone' → 'Mark as not game-related' (en) / 'Пометить как не про игры' (ru)\n  - feed_filter_show_standalone: 'Standalone — not related to any game' → 'Not game-related' (en) / 'Не про игры' (ru)\n  - feed_quick_nav_standalone: 'Standalone' → 'Not game-related' (en) / 'Не про игры' (ru)\n  - audit_action_event_marked_standalone + audit_action_event_unmarked_standalone STAY UNCHANGED (audit log uses the technical verb name; users read the audit log in technical context — this matches existing 'event.attached_to_game' style)."
    - "FeedCard.svelte overlay pill text: locate the rendering of 'Standalone' overlay/badge (Plan 02.1-23/24) and confirm it uses m.feed_filter_show_standalone() OR a dedicated key. If a dedicated key like `feed_card_standalone_label` exists, change its VALUE; if it reuses the filter key, no per-component edit needed."
    - "Verification check: NO occurrence of the literal user-facing word 'Standalone' (capitalized, English) in any .svelte file's rendered template (m.* calls only). The technical state name 'standalone' (lowercase, in URL params + service code + metadata.triage.standalone) STAYS — that's the schema-level identifier, not user-facing copy."
    - "Privacy & multi-tenancy invariants preserved: NO schema changes; NO new audit verbs; NO new env reads; NO route additions. Pure i18n key-value updates. ESLint tenant-scope rule unaffected (no DB queries touched). Forward-only migration discipline unaffected."
    - "Independent of Plans 27/28/29/30 — touches disjoint surfaces (only Paraglide keys + Svelte rendering; no service/schema work). Wave 2 (no depends_on) is a parallelism choice — could ship in any wave; placed in Wave 2 alongside the other UI plans for review-surface coherence. The plan does NOT block other plans."
    - "Browser regression test (tests/browser/feed-360.test.ts): assert (a) FeedCard with metadata.triage.standalone=true renders 'Не про игры' (or English equivalent depending on test locale) — NOT 'Standalone'; (b) FeedQuickNav segment text matches 'Не про игры' / 'Not game-related'; (c) FilterChips chip for show=standalone reads 'Не про игры' / 'Not game-related'."
    - "i18n ru locale note (EXPLICIT EN-ONLY ACK FOR ROUND-5 UAT): project ships en.json today; ru.json is NOT yet shipped (Phase 6+ multi-locale per CONTEXT.md). The 'Не про игры' Russian copy is documented in the plan's commit message + AGENTS.md note for the Phase 6 locale-add to consume; en.json takes the English value 'Not game-related'. **Round-5 UAT human will see English text rendered ('Not game-related') NOT Russian ('Не про игры'); this is acceptable because round-4 scope is en-only. If the user maintains a ru.json snapshot for round-4 UAT, the executor adds the same key-value pairs there.** If round-5 UAT user requires Russian copy, scope-back to Phase 6 ru.json shipping or open a fast-fix Plan 02.1-35 for ru.json."
    - "User on /feed sees 'Not game-related' (English) instead of 'Standalone'. Russian copy ('Не про игры') deferred to Phase 6 ru.json shipping. This deferral is INTENTIONAL and surfaced in CONTEXT.md sign-off block before merging Plan 31."
  artifacts:
    - path: "messages/en.json"
      provides: "Updated VALUES for feed_card_mark_standalone_button / feed_filter_show_standalone / feed_quick_nav_standalone (audit verbs unchanged)."
      contains: "Not game-related"
    - path: "tests/unit/paraglide.test.ts"
      provides: "EXPECTED_KEYS unchanged (no new keys); existing snapshot test still passes since key SET is the invariant — values are not asserted."
      contains: ""
    - path: "src/lib/components/FeedCard.svelte"
      provides: "Overlay pill + inline button text via the existing m.* keys (now with renamed values). NO direct literal 'Standalone' string in template."
      contains: ""
    - path: "src/lib/components/FeedQuickNav.svelte"
      provides: "Segment label via m.feed_quick_nav_standalone() (renamed value)."
      contains: ""
    - path: "src/lib/components/FilterChips.svelte"
      provides: "Chip text via m.feed_filter_show_standalone() (renamed value)."
      contains: ""
    - path: "src/lib/components/FiltersSheet.svelte"
      provides: "Radio label via m.feed_filter_show_standalone() (renamed value)."
      contains: ""
    - path: "src/routes/feed/+page.svelte"
      provides: "Any inline 'Standalone' label uses the renamed Paraglide key."
      contains: ""
    - path: "src/routes/feed/+page.server.ts"
      provides: "?show=standalone URL param parsing UNCHANGED (technical state name preserved)."
      contains: "standalone"
    - path: "tests/browser/feed-360.test.ts"
      provides: "Plan 02.1-31 describe block: rendered text assertions for FeedCard / FeedQuickNav / FilterChips."
      contains: "Plan 02.1-31"
  key_links:
    - from: "Existing Paraglide keys (feed_card_mark_standalone_button etc.)"
      to: "User-facing renamed copy"
      via: "VALUE update only — keys stay; URL contract / state shape stays; only rendered text changes."
      pattern: "Not game-related"
    - from: "Technical state name 'standalone'"
      to: "User-facing copy 'Не про игры' / 'Not game-related'"
      via: "URL param ?show=standalone STAYS; service function markStandalone STAYS; metadata.triage.standalone STAYS; audit verb event.marked_standalone STAYS — only m.feed_*_standalone()/m.audit_*() rendering changes its English/Russian text."
      pattern: "feed_filter_show_standalone\\|feed_quick_nav_standalone\\|feed_card_mark_standalone_button"
---

<objective>
Pure i18n plan. Closes UAT-NOTES.md §4.24.A — user does not parse 'Standalone' as 'not related to any game'.

Approach: rename user-facing TEXT only. Underlying schema state, audit verb, URL param, service function — ALL preserved. Only the values of 3 user-facing Paraglide keys change. The audit_action_event_marked_standalone / audit_action_event_unmarked_standalone keys STAY (audit log is technical context, matches 'event.attached_to_game' pattern).

Findings closed:
- **4.24.A (P0)** — Standalone label rename to 'Не про игры' / 'Not game-related'.

Wave 2 with no depends_on — touches disjoint surfaces from Plans 27-30 (no schema, no services, only i18n + Svelte rendering). Could ship in any wave; placed in Wave 2 for review-surface coherence with other UI plans.

Output:
- messages/en.json: 3 keys updated (values only).
- tests/unit/paraglide.test.ts: unchanged (key SET invariant; snapshot test asserts keys not values).
- 4 components verified to use m.* (no literal 'Standalone'): FeedCard, FeedQuickNav, FilterChips, FiltersSheet.
- 2 route files verified.
- tests/browser/feed-360.test.ts: regression assertion that the rendered text matches the new copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-architecture-realignment/02.1-CONTEXT.md
@.planning/phases/02.1-architecture-realignment/02.1-10-UAT-NOTES.md
@.planning/phases/02.1-architecture-realignment/02.1-24-PLAN.md
@.planning/phases/02.1-architecture-realignment/02.1-26-PLAN.md
@CLAUDE.md
@AGENTS.md

<interfaces>
<!-- Existing keys this plan only changes the values of. -->

From messages/en.json (current — change values):

```json
{
  "feed_card_mark_standalone_button": "Mark standalone",        // → "Mark as not game-related"
  "feed_filter_show_standalone": "Standalone — not related to any game",  // → "Not game-related"
  "feed_quick_nav_standalone": "Standalone",                    // → "Not game-related"
  "audit_action_event_marked_standalone": "Event marked standalone",      // STAYS (technical audit log)
  "audit_action_event_unmarked_standalone": "Event unmarked standalone"   // STAYS
}
```

User-facing rendered surfaces using these keys (verified via grep + Plan 02.1-23/24/26):

1. **FeedCard.svelte** — overlay pill (when isStandalone) + inline "Mark standalone" button (when isInboxRow). Uses m.feed_card_mark_standalone_button() for the button label. The overlay pill MAY use a separate key (verify by reading FeedCard.svelte; Plan 02.1-23 added the overlay; if it reuses m.feed_filter_show_standalone() that's covered; if it uses its own key like `feed_card_standalone_overlay_label` THAT key's value also changes).

2. **FeedQuickNav.svelte** (Plan 02.1-26) — segment text. Uses m.feed_quick_nav_standalone().

3. **FilterChips.svelte** (Plan 02.1-24) — chip text "Show: Standalone — not related to any game". Uses m.feed_filter_show_standalone().

4. **FiltersSheet.svelte** (Plan 02.1-24) — radio label. Uses m.feed_filter_show_standalone().

5. **/feed/+page.svelte / +page.server.ts** — URL param ?show=standalone parsing STAYS (technical name). No rendered text uses literal 'Standalone'.

The audit log surface (audit log row text via m.audit_action_event_marked_standalone()) STAYS as 'Event marked standalone' — the audit log displays the technical verb name to match existing entries like 'Event attached to a game'.

Acceptance check post-edit: `rg -i "standalone" src/lib/components/*.svelte src/routes/feed/*.svelte src/routes/feed/*.ts` should show ONLY:
- m.feed_*_standalone() / m.feed_card_mark_standalone_button() / m.audit_action_event_*_standalone() function calls
- Logical state names ('standalone' lowercase as a kind in discriminated unions / URL param values / metadata.triage.standalone references)
- NO occurrence of the literal user-facing capitalized 'Standalone' English string in template HTML (the m.* call returns the new copy)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Paraglide key values + verify components + browser test</name>
  <files>
    messages/en.json,
    tests/unit/paraglide.test.ts,
    src/lib/components/FeedCard.svelte,
    src/lib/components/FeedQuickNav.svelte,
    src/lib/components/FilterChips.svelte,
    src/lib/components/FiltersSheet.svelte,
    src/routes/feed/+page.svelte,
    src/routes/feed/+page.server.ts,
    tests/browser/feed-360.test.ts
  </files>
  <read_first>
    - .planning/phases/02.1-architecture-realignment/02.1-10-UAT-NOTES.md (§4.24.A — load-bearing user quote 'Standalone странный текст. Не очевидно что это вообще не про игры.')
    - messages/en.json (find all keys containing 'standalone' — verify the EXACT 3 keys to update; audit verbs stay)
    - src/lib/components/FeedCard.svelte (Plan 02.1-23/24 — find ALL m.*standalone* calls; the overlay pill rendering)
    - src/lib/components/FeedQuickNav.svelte (Plan 02.1-26 — segment using m.feed_quick_nav_standalone())
    - src/lib/components/FilterChips.svelte (Plan 02.1-24 — chip using m.feed_filter_show_standalone())
    - src/lib/components/FiltersSheet.svelte (Plan 02.1-24 — radio using m.feed_filter_show_standalone())
    - src/routes/feed/+page.svelte (verify no literal 'Standalone' string)
    - src/routes/feed/+page.server.ts (verify ?show=standalone param parsing — STAYS)
    - tests/browser/feed-360.test.ts (existing pattern — APPEND Plan 02.1-31 block)
    - tests/unit/paraglide.test.ts EXPECTED_KEYS (key SET — verify no key additions/deletions)
    - CLAUDE.md (Paraglide invariant D-41 + Practices §"Tests land with the feature")
  </read_first>
  <action>
    1. Edit `messages/en.json` — UPDATE the VALUES of these 3 keys (keys themselves unchanged):
       - `feed_card_mark_standalone_button`: from "Mark standalone" to **"Mark as not game-related"**
       - `feed_filter_show_standalone`: from "Standalone — not related to any game" to **"Not game-related"**
       - `feed_quick_nav_standalone`: from "Standalone" to **"Not game-related"**
       - `audit_action_event_marked_standalone`: STAYS as "Event marked standalone"
       - `audit_action_event_unmarked_standalone`: STAYS as "Event unmarked standalone"
       Run `pnpm paraglide:compile`.
    2. Verify each touched component in <files>:
       - `src/lib/components/FeedCard.svelte`: grep for the literal 'Standalone' string in any rendered template. If present (e.g. as a hard-coded `>Standalone<` outside m.*), REPLACE with m.feed_filter_show_standalone() OR a NEW key (rare — Plan 02.1-23 should already use m.* but verify). The overlay-pill rendering (Plan 02.1-23) may use a dedicated key like `feed_card_overlay_standalone_label` — if such a key exists, ALSO update its value to "Not game-related" (and add it to the value-change list for messages/en.json).
       - `src/lib/components/FeedQuickNav.svelte`: confirms uses m.feed_quick_nav_standalone(); no template literal 'Standalone'.
       - `src/lib/components/FilterChips.svelte`: confirms uses m.feed_filter_show_standalone() in the show.kind === "standalone" branch.
       - `src/lib/components/FiltersSheet.svelte`: confirms uses m.feed_filter_show_standalone() in the radio label.
       - `src/routes/feed/+page.svelte`: grep for the literal 'Standalone' string; remove if any.
       - `src/routes/feed/+page.server.ts`: confirm `'standalone'` (lowercase, technical) is in the URL parsing branch — STAYS unchanged.
    3. tests/unit/paraglide.test.ts: NO change needed — EXPECTED_KEYS asserts the SET of keys; values are not asserted. Verify the test still passes after the values-only update.
    4. Edit `tests/browser/feed-360.test.ts`:
       - APPEND `describe('Plan 02.1-31 — Standalone label rename', ...)` with assertions:
         - On a page rendering FeedCard with metadata.triage.standalone=true, the rendered text equals m.feed_filter_show_standalone() (i.e. "Not game-related" in en locale) and does NOT contain literal 'Standalone' (case-sensitive English word match).
         - On /feed with FeedQuickNav rendered, the segment for the standalone state has textContent === m.feed_quick_nav_standalone() ("Not game-related").
         - On /feed with FiltersSheet open and the show fieldset visible, the radio with value="standalone" has its label text === m.feed_filter_show_standalone().
         - The PATCH /api/events/:id/mark-standalone path STAYS (URL contract) — verify via fetch in the test if practical, else rely on integration tests.
    5. Run a final grep sanity check: `grep -rn "Standalone" src/lib/components/ src/routes/ messages/en.json | grep -v "audit_action_event_.*_standalone"` should return ONLY the keys themselves (e.g. `feed_quick_nav_standalone:` JSON key) and NOT any template literal capital-S Standalone.
  </action>
  <verify>
    <automated>pnpm test:unit -- tests/unit/paraglide.test.ts && pnpm test:browser -- tests/browser/feed-360.test.ts -t "Plan 02.1-31" && pnpm lint && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep "feed_card_mark_standalone_button" messages/en.json` returns line containing "Mark as not game-related"
    - `grep "feed_filter_show_standalone" messages/en.json` returns line containing "Not game-related"
    - `grep "feed_quick_nav_standalone" messages/en.json` returns line containing "Not game-related"
    - `grep "audit_action_event_marked_standalone" messages/en.json` returns line containing "Event marked standalone" (STAYS)
    - `grep "audit_action_event_unmarked_standalone" messages/en.json` returns line containing "Event unmarked standalone" (STAYS)
    - Capital-S "Standalone" string in user-facing template literals: `grep -E ">[^<]*Standalone[^<]*<" src/lib/components/*.svelte src/routes/feed/*.svelte` returns NOTHING (no hard-coded English Standalone in rendered HTML)
    - Lowercase "standalone" technical state name in URL/service code STAYS: `grep -c "show.*standalone\|metadata.triage.standalone" src/routes/feed/+page.server.ts src/lib/components/FiltersSheet.svelte` returns >= 2 (URL param parsing + radio value attribute)
    - `grep -c "Plan 02.1-31" tests/browser/feed-360.test.ts` returns >= 1
    - `pnpm test:unit -- tests/unit/paraglide.test.ts` passes (EXPECTED_KEYS snapshot unchanged)
    - `pnpm test:browser -- tests/browser/feed-360.test.ts -t "Plan 02.1-31"` passes
    - `pnpm test:integration -- tests/integration/feed.test.ts tests/integration/inbox.test.ts` pass (no contract regression — URL param ?show=standalone still works)
    - `pnpm lint` passes
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <done>
    Standalone label renamed in all 3 user-facing Paraglide keys; audit verbs preserved; URL contract / service / schema unchanged; browser regression test asserts new copy renders correctly; no hard-coded literal 'Standalone' English string remains in template HTML.
  </done>
</task>

</tasks>

<verification>
- `pnpm test` passes (full suite)
- `pnpm test:browser` passes
- `pnpm test:integration` passes (URL param contract unchanged)
- `pnpm test:unit -- tests/unit/paraglide.test.ts` passes (EXPECTED_KEYS unchanged)
- `pnpm lint` passes
- `pnpm typecheck` passes
- Manual UAT walkthrough (Russian, per user-MEMORY profile):
  - /feed FeedQuickNav shows segment 'Не про игры' (or 'Not game-related' in en locale) — NOT 'Standalone'
  - /feed FiltersSheet show fieldset has radio labeled 'Не про игры' / 'Not game-related'
  - FeedCard with metadata.triage.standalone=true shows overlay pill text 'Не про игры' / 'Not game-related'
  - Inbox card shows inline button 'Пометить как не про игры' / 'Mark as not game-related' (NOT 'Mark standalone')
  - URL bar after clicking the FeedQuickNav segment contains ?show=standalone (technical state name STAYS — no breakage)
  - /audit log row for the marked-standalone audit verb still reads 'Event marked standalone' (technical context — by design)
- PR self-review present: PR body contains `## Self-review` heading with one line per item from AGENTS.md "Validation" checklist (Constraints, Philosophy, Practices, CI gate, Doc drift). Documentation drift item must mention that audit verb names + URL contract are intentionally preserved while only the user-facing rendered text changed.
- **Locale acknowledgment:** Project ships en.json only as of Phase 2.1; ru.json shipping is deferred to Phase 6. Round-5 UAT human will see "Not game-related" rendered (English). Confirm in CONTEXT.md sign-off block before merging Plan 31. If user requires Russian copy in round-5, scope-back to Phase 6 or open a fast-fix Plan 02.1-35 for ru.json.
</verification>

<success_criteria>
- UAT-NOTES.md §4.24.A closed.
- Privacy & multi-tenancy invariants intact (no schema, no service, no env, no audit verb, no route changes).
- Paraglide invariant D-41 honored — no key additions/deletions; only VALUE updates of 3 existing keys; EXPECTED_KEYS snapshot test unchanged and passing.
- URL contract preserved: `?show=standalone` URL still works; future locale additions only need to update the 3 key values in their locale file.
- Forward-only migration discipline unaffected (no DB changes).
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-architecture-realignment/02.1-31-SUMMARY.md` per template.
</output>
</content>
