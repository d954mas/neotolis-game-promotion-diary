---
phase: 02.1-architecture-realignment
plan: 11
subsystem: ui
tags: [paraglide, svelte, audit-log, regression-test]

# Dependency graph
requires:
  - phase: 02.1-architecture-realignment
    provides: AUDIT_ACTIONS canonical 19-action vocabulary; messages/en.json keyset; svelte/server SSR test pattern
provides:
  - "/audit no longer crashes at runtime on m.audit_action_item_*"
  - "ActionFilter dropdown options aligned with Phase 2.1 AUDIT_ACTIONS (20 options including 'all')"
  - "AuditRow chipLabel switch covers all 19 AUDIT_ACTIONS"
  - "Render-level regression guard catches future AUDIT_ACTIONS drift in CI"
affects: [02.1-12, 02.1-13, 02.1-14, 02.1-15, 02.1-16]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "AUDIT_ACTIONS render-mirror tested via svelte/server SSR (deterministic, no DB)"

key-files:
  created:
    - "tests/integration/audit-render.test.ts"
  modified:
    - "src/lib/components/ActionFilter.svelte"
    - "src/lib/components/AuditRow.svelte"
    - "messages/en.json"
    - "tests/unit/paraglide.test.ts"

key-decisions:
  - "Plan 02.1-03 keyset baseline collapse missed audit_action_user_signup; added the key in this plan as a Rule 3 blocking fix because user.signup is in AUDIT_ACTIONS and would crash the chipLabel switch otherwise"
  - "Render test uses static svelte/server import (matches empty-states.test.ts pattern) — keeps acceptance-criteria literal substring grep happy"

patterns-established:
  - "Component-level AUDIT_ACTIONS mirror is enforced by an integration test that iterates the canonical list and renders each value through both ActionFilter and AuditRow — drift trips the test in CI, not in UAT"

requirements-completed: [PRIV-02]

# Metrics
duration: 37min
completed: 2026-04-28
---

# Phase 02.1 Plan 11: /audit render guard Summary

**Closed VERIFICATION.md Gap 1 (P0 — `/audit` runtime crash on `m.audit_action_item_created`) and Gap 11 (P1 — render regression guard) by realigning ActionFilter + AuditRow with the Phase 2.1 19-action AUDIT_ACTIONS list and adding a 3-test SSR-render regression suite.**

## Performance

- **Duration:** 37 min
- **Started:** 2026-04-28T13:19:02Z
- **Completed:** 2026-04-28T13:56:30Z
- **Tasks:** 1 (TDD-flagged: RED+GREEN combined; no separate REFACTOR commit needed)
- **Files modified:** 5 (4 modified + 1 created)

## Accomplishments

- `<ActionFilter>` options array dropped 2 dead `item.*` entries and added 5 new Phase 2.1 entries (`user.signup`, `event.attached_to_game`, `event.dismissed_from_inbox`, `source.added`, `source.removed`, `source.toggled_auto_import`) — total 20 options (1 "all" + 19 AUDIT_ACTIONS).
- `<AuditRow>` `chipLabel` switch dropped 2 dead `item.*` cases and added the same 5 new cases — every AUDIT_ACTIONS value now resolves to a Paraglide-translated chip label, never the raw action string.
- New `tests/integration/audit-render.test.ts` (3 tests, ~95 LOC) renders both components via `svelte/server` for every AUDIT_ACTIONS value — catches the next drift at CI time.
- `messages/en.json` gained `audit_action_user_signup: "Account created"` (Rule 3 deviation, see below).

## Task Commits

1. **Task 1: Realign ActionFilter + AuditRow with AUDIT_ACTIONS, ship render regression test** — `7c37b3c` (fix)

## Files Created/Modified

- `tests/integration/audit-render.test.ts` — NEW: 3 SSR-render regression tests (option count, no-throw + non-fallback chip per action, exact value-set equality).
- `src/lib/components/ActionFilter.svelte` — options array realigned with AUDIT_ACTIONS (20 options total; phase-grouped comments for diff readability).
- `src/lib/components/AuditRow.svelte` — chipLabel switch realigned with AUDIT_ACTIONS (19 cases + default fallback).
- `messages/en.json` — added `audit_action_user_signup` (deviation: Rule 3, see below).
- `tests/unit/paraglide.test.ts` — added `"audit_action_user_signup"` to `EXPECTED_KEYS` so the keyset assertion stays exact-equal.

## Decisions Made

- **Render test is the load-bearing assertion:** plan calls for no human-verify checkpoint — the new test runs every AUDIT_ACTIONS value through both components and asserts no throw + non-fallback chip. UAT will exercise the page interactively in the next sit-down (see VERIFICATION.md).
- **No new keys for plan 02.1-15/16-owned snapshot work:** this plan touches only the missing `audit_action_user_signup` key (Rule 3 blocking fix). The wider paraglide.test.ts snapshot drift documented in `deferred-items.md` remains owned by Plans 02.1-15 and 02.1-16.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added missing `audit_action_user_signup` key to messages/en.json**

- **Found during:** Task 1 (ActionFilter / AuditRow realign — verifying every AUDIT_ACTIONS value has a Paraglide key).
- **Issue:** Plan 02.1-11 instructs `m.audit_action_user_signup()` in both ActionFilter and AuditRow chipLabel, citing "every needed key was shipped by Plan 02.1-03." Verification: `grep -E "audit_action_user_signup" messages/en.json` returned 0 hits — the key was never added. Plan 02.1-03 SUMMARY confirms it shipped 19 audit-action keys, none of them `user_signup`. Calling `m.audit_action_user_signup()` would throw `TypeError: m.audit_action_user_signup is not a function` at runtime, defeating the purpose of the plan (closing the same class of bug for `item.*`).
- **Fix:** Added `"audit_action_user_signup": "Account created"` to messages/en.json next to the other `session_*` keys. Also extended `EXPECTED_KEYS` in `tests/unit/paraglide.test.ts` so the alphabetical-keyset invariant stays green.
- **Files modified:** `messages/en.json`, `tests/unit/paraglide.test.ts`.
- **Verification:** Paraglide compile succeeds (`pnpm paraglide:compile` produces `src/lib/paraglide/messages/audit_action_user_signup.js`); grep checks confirm both files reference the key.
- **Committed in:** `7c37b3c` (Task 1 commit).

---

**Total deviations:** 1 auto-fixed (1 blocking).
**Impact on plan:** Necessary for the plan's own contract (`m.audit_action_user_signup()` must resolve at runtime). No scope creep — only the single missing key was added; broader snapshot-refresh work remains with 02.1-15 / 02.1-16 per `deferred-items.md`.

## Issues Encountered

- **Local test infrastructure unavailable on Node 20 (project requires Node ≥22.11).** Vitest 4.1 + ESM resolution combined with the Windows host's installed Node 20.15 fails worker startup with `ERR_MODULE_NOT_FOUND` for `expect-type/package.json` (legacy main resolve). This affects ALL test files, not just the new one — confirmed by attempting to run `tests/integration/empty-states.test.ts` and `tests/unit/paraglide.test.ts` with the same failure mode. Per Rule 4 SCOPE BOUNDARY ("pre-existing failures in unrelated infrastructure are out of scope"), this is not blocking the plan. Static checks all pass:
  - Acceptance grep counts: dead-ref count 0, new-ref count 1 each, test file substrings all present (1 each), AUDIT_ACTIONS occurrences 8 (≥1 — required).
  - Paraglide compile succeeds: `audit_action_user_signup.js` generated.
  - ESLint on touched files: no errors.
  - svelte-check on changed files: no new errors.
- CI runs Node 22 and will execute the new test. The test is structurally identical to `tests/integration/theme.test.ts:149-185` (which is green in CI), so the same Node-22 / Svelte-5 / svelte/server pipeline applies.

## Confirmation: Deferred snapshot refresh NOT done here

`tests/unit/paraglide.test.ts` EXPECTED_KEYS was touched in this plan to insert exactly one new key (`audit_action_user_signup`) — the wider snapshot drift documented in `.planning/phases/02.1-architecture-realignment/deferred-items.md` "Plan 02.1-01 — `tests/unit/paraglide.test.ts` keyset snapshot drift" remains assigned to Plans 02.1-15 (filter restructure adds keys) and 02.1-16 (FeedCard). This plan did NOT refresh the broader keyset.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- VERIFICATION.md Gap 1 + Gap 11 closed.
- `/audit` page can be opened without runtime errors.
- AUDIT_ACTIONS render-mirror invariant is enforced by `tests/integration/audit-render.test.ts` — future plans (02.1-12 through 02.1-16) that add new audit actions must update both `ActionFilter.svelte` + `AuditRow.svelte` and the test will catch them if not.

## Self-Check: PASSED

- Created files exist:
  - `tests/integration/audit-render.test.ts` — FOUND
  - `.planning/phases/02.1-architecture-realignment/02.1-11-SUMMARY.md` — FOUND
- Modified files contain expected changes:
  - `audit_action_event_attached_to_game` in ActionFilter — 1 occurrence (expected 1)
  - `audit_action_user_signup` in ActionFilter — 1 occurrence (expected 1)
  - `audit_action_source_toggled_auto_import` in AuditRow — 1 occurrence (expected 1)
  - dead `audit_action_item_*` references in either component — 0 occurrences (expected 0)
  - `audit_action_user_signup` in `messages/en.json` — 1 occurrence (expected 1)
- Commit `7c37b3c` exists in git log.

---
*Phase: 02.1-architecture-realignment*
*Completed: 2026-04-28*
