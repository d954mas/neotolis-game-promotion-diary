---
phase: 02.1-architecture-realignment
plan: 34
subsystem: ui
tags: [css, sticky-positioning, body-scroll-lock, has-selector, filterssheet, audit, gap-closure, regression-fix]

# Dependency graph
requires:
  - phase: 02.1-architecture-realignment
    provides: Plan 02.1-22 layout-root flex scaffold + AppHeader position:sticky + FiltersSheet imperative body-scroll-lock; Plan 02.1-21 FiltersSheet schema-prop API + /audit DateRangeControl above FilterChips; Plan 02.1-19 FiltersSheet $effect.showModal() + focusAxis logic; Plan 02-09 src/app.css single global stylesheet importing once from src/routes/+layout.svelte
provides:
  - src/app.css overflow-x:hidden→clip on html+body — CSS spec compliance: hidden + visible coerces overflow-y to auto and promotes body to a scroll container, breaking position:sticky on descendants. clip crops without promoting.
  - src/app.css NEW body:has(dialog[open]) { overflow: hidden; } declarative scroll-lock — replaces the imperative document.body.style.overflow approach Plan 02.1-22 introduced. Works for ANY <dialog open> in the DOM (FiltersSheet, ConfirmDialog, future modals). Browser engine self-applies + self-restores.
  - FiltersSheet.svelte body-scroll-lock simplified — imperative document.body.style.overflow lines REMOVED from $effect AND onDialogCancel; showModal() + focusAxis logic preserved.
  - /audit AUDIT_SCHEMA = ['action'] (was ['action', 'date']) — page-level <DateRangeControl> stays as the single source of truth for date filtering on /audit; in-sheet date axis dropped to fix UAT-NOTES.md §4.21.A duplication.
  - tests/integration/audit-render.test.ts NEW Plan 02.1-34 describe block (6 tests) — AUDIT_SCHEMA literal grep, /feed schema preservation grep, FiltersSheet code-only document.body.style.overflow absence, body:has(dialog[open]) rule presence, html+body overflow-x:clip presence, FiltersSheet schema=['action'] component render.
  - tests/browser/responsive-360.test.ts NEW Plan 02.1-34 describe blocks — body computed overflow-x === 'clip' (PUBLIC-routed, runs CI; public-route /); 7 it.skip placeholders for /feed-/sources-/games sticky-top assertions + /audit FiltersSheet axis assertion (auth harness deferred to Phase 6).
  - tests/browser/feed-360.test.ts NEW Plan 02.1-34 describe block (3 it.skip placeholders) — FiltersSheet body-scroll-lock end-to-end at 360px (open sheet → body unscrollable → close → body scrollable; all auth-harness-deferred).
  - vitest.config.ts NEW measureBodyOverflowX command — getComputedStyle(document.body).overflowX exposure for the public-routed regression-source guard.
affects:
  - Future modal additions (ConfirmDialog and any new <dialog>-based component) automatically inherit the body-scroll lock via CSS :has() — no JS state to manage per-component.

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "overflow-x: clip (instead of hidden) on html+body — when a page needs to crop horizontal overflow but MUST NOT promote body to a scroll container (because position:sticky descendants anchor against the first scrolling ancestor). Per CSS spec, overflow-x:hidden paired with overflow-y:visible coerces overflow-y to auto and creates a scroll container; clip crops without coercion. Supported in Chrome 90+, Firefox 81+, Safari 16+ — modern-browser baseline."
    - "Declarative body-scroll-lock via CSS :has(dialog[open]) — replaces imperative document.body.style.overflow. The browser engine applies and self-restores the lock based on DOM state (any <dialog open>); no JS state to manage; cross-component-safe (multiple dialogs / nested dialogs all participate). Supported in Chrome 105+, Safari 15.4+, Firefox 121+."

key-files:
  created: []
  modified:
    - "src/app.css (Task 1: overflow-x:clip on body; Task 2: body:has(dialog[open]) rule)"
    - "src/lib/components/FiltersSheet.svelte (Task 2: removed imperative body.style.overflow)"
    - "src/routes/audit/+page.svelte (Task 2: AUDIT_SCHEMA dropped 'date' axis)"
    - "tests/browser/responsive-360.test.ts (Task 1: Plan 02.1-34 describe blocks — sticky regression guard + /audit no-date placeholder)"
    - "tests/browser/feed-360.test.ts (Task 2: Plan 02.1-34 body-scroll-lock placeholders — landed via parallel-execution race in commit 21c7000)"
    - "tests/integration/audit-render.test.ts (Task 2: Plan 02.1-34 describe block — 6 SSR + grep tests; commented-code-strip refinement landed in commit bb3be3c)"
    - "tests/browser/commands.d.ts (Task 1: measureBodyOverflowX command type)"
    - "vitest.config.ts (Task 1: measureBodyOverflowX command implementation)"

key-decisions:
  - "Fix path A chosen for sticky regression — change body's overflow-x from hidden to clip. Diagnosis: the only `overflow-x: hidden` rule across +layout.svelte / AppHeader.svelte / Nav.svelte / FeedQuickNav.svelte / etc. lived on src/app.css line 139 (html+body). Per CSS spec (https://www.w3.org/TR/css-overflow-3/#overflow-properties), when overflow-x is set to a non-visible value AND overflow-y is visible, overflow-y is coerced to `auto` — promoting the element to a scroll container. position:sticky on a descendant anchors to the FIRST scrolling ancestor; with body promoted, AppHeader anchored against body (not viewport), and scrolled away with content. `overflow-x: clip` (CSS Overflow Module Level 3, 2020) crops without promoting because clip doesn't establish scrolling. Path C (sticky-zone wrapper) was the fallback if no rogue overflow was found; Path B (position:fixed) would have required compensating padding-top everywhere — last resort. Path A surgical and minimal."
  - "Fix path declarative for FiltersSheet body-scroll-lock — CSS :has(dialog[open]) over imperative JS state. The Plan 02.1-22 imperative approach regressed because (a) Svelte 5 $effect cleanup timing was unreliable across re-runs and (b) ConfirmDialog instances could overwrite the inline document.body.style.overflow back to ''. CSS :has() is reactive at the DOM level: the browser engine applies overflow:hidden to body the moment ANY <dialog open> exists in the DOM and self-restores when none does. Works for FiltersSheet, ConfirmDialog, and any future modal using <dialog> + showModal(). No JS state to manage per-component; cross-component-safe by construction. Browser support (Chrome 105+, Safari 15.4+, Firefox 121+) is within the project's modern-browser baseline."
  - "/audit FiltersSheet schema narrowed to ['action'] — page-level <DateRangeControl> stays as the SINGLE source of truth for date filtering on /audit. /feed schema is unchanged ('date' axis preserved by design — the in-sheet date axis is the secondary entry the user actively wants on /feed per Plan 02.1-21 design intent: DateRangeControl above the chip strip is primary, sheet axis is fallback for users opening via chip click). The duplication on /audit was the round-4 UAT finding."
  - "Comment-stripping precedent extended to grep-based source assertions (Plan 02.1-10's `the Node env` rewrite precedent applied to test logic instead of source). The audit-render.test.ts Plan 02.1-34 negative-grep tests strip // single-line and /* */ block comments before matching `document.body.style.overflow` and `overflow-x: hidden` — comments referring to the historical (regressed) approach are useful for reviewers and shouldn't trip the regression guard. The Plan 02.1-10 precedent moved the comment to avoid the literal; here we move the test to be smarter."

patterns-established:
  - "CSS-spec-aware overflow choice: when sticky descendants are required AND horizontal-overflow cropping is needed, prefer `overflow-x: clip` over `overflow-x: hidden` on html+body. The hidden+visible combination coerces overflow-y to auto and promotes the element to a scroll container, silently breaking position:sticky anchoring against the viewport."
  - "Declarative body-scroll-lock via body:has(dialog[open]) — the canonical pattern for this codebase. Imperative document.body.style.overflow approaches are deprecated; future modal additions (any <dialog> + showModal()) automatically inherit the lock with no per-component code."
  - "Comment-stripping in negative-grep regression guards — when a test asserts a CODE pattern is absent in a source file, strip // and /* */ comments first. Documentation referring to the historical / regressed approach is valuable; only code-side drift should trip the guard."

requirements-completed: ["FEED-01", "PRIV-02", "UX-02"]

# Metrics
duration: ~14 min
completed: 2026-04-29
---

# Phase 02.1 Plan 34: Layout regression fixes + /audit FiltersSheet schema cleanup Summary

**Sticky AppHeader regression closed by switching html+body's `overflow-x: hidden` to `overflow-x: clip` (CSS spec: hidden+visible coerces overflow-y to auto and promotes body to a scroll container, breaking position:sticky on descendants); FiltersSheet body-scroll-lock regression closed by replacing the imperative document.body.style.overflow approach with a declarative `body:has(dialog[open]) { overflow: hidden; }` rule in src/app.css; /audit FiltersSheet schema narrowed to ['action'] so the page-level <DateRangeControl> stays as the single source of truth for date filtering on /audit. Three round-4 UAT findings (§4.22.A / §4.22.F / §4.21.A) closed in one plan with no schema, no service, no env, no audit verb, no route changes.**

## Performance

- **Duration:** ~14 min
- **Started:** 2026-04-29T17:27:17Z
- **Completed:** 2026-04-29T17:41:01Z
- **Tasks:** 2
- **Files modified:** 7 (4 production: src/app.css, src/lib/components/FiltersSheet.svelte, src/routes/audit/+page.svelte; 4 test/config: tests/browser/responsive-360.test.ts, tests/browser/feed-360.test.ts, tests/browser/commands.d.ts, vitest.config.ts, tests/integration/audit-render.test.ts)

## Accomplishments

- **§4.22.A closed** — sticky AppHeader regression diagnosed and fixed via fix path A. Diagnosis: grep across +layout.svelte / AppHeader.svelte / Nav.svelte / FeedQuickNav.svelte / etc. revealed the only candidate `overflow-x: hidden` rule lived on src/app.css line 139 (html+body, originally for UX-02 360px no-horizontal-scroll). Per CSS spec, overflow-x:hidden paired with overflow-y:visible coerces overflow-y to auto, promoting body to a scroll container. position:sticky on AppHeader anchored against body instead of viewport, so it scrolled away with content. Fix: `overflow-x: clip` crops without promoting (clip doesn't establish scrolling per CSS Overflow Module Level 3). Sticky descendants now anchor to the viewport.
- **§4.22.F closed** — FiltersSheet body-scroll-lock rewritten declaratively via CSS :has(). The Plan 02.1-22 imperative document.body.style.overflow approach regressed (Svelte 5 $effect cleanup timing unreliable; ConfirmDialog could overwrite the inline style). New approach: `body:has(dialog[open]) { overflow: hidden; }` in src/app.css applies the lock the moment ANY <dialog open> exists in the DOM and self-restores when none does. No JS state; cross-component-safe. FiltersSheet's $effect now only calls showModal() + handles focusAxis; the imperative body.style.overflow lines are gone.
- **§4.21.A closed** — /audit FiltersSheet AUDIT_SCHEMA narrowed from ['action', 'date'] to ['action']. Page-level <DateRangeControl> above <FilterChips> stays as the single source of truth for date filtering on /audit. /feed schema unchanged (date axis preserved by design — the in-sheet date axis is the secondary entry users actively want on /feed).
- **Test regression guards** — 6 new Plan 02.1-34 integration tests in tests/integration/audit-render.test.ts cover the source-level invariants (AUDIT_SCHEMA literal, /feed 'date' preservation, FiltersSheet code-side document.body.style.overflow absence, body:has rule presence, html+body overflow-x:clip, FiltersSheet schema=['action'] component render). 1 PUBLIC-routed browser test asserts body computed overflow-x === 'clip' on the dashboard route. 11 it.skip placeholders cover the auth-harness-deferred end-to-end paths for grep discoverability when the harness arrives in Phase 6.
- **Privacy & multi-tenancy invariants intact** — NO schema, NO service, NO env, NO audit verb, NO route changes. Pure CSS + component state + page-level prop refactor. Cross-tenant isolation, anonymous-401 sweep, DTO discipline, audit log INSERT-only, Pino redact paths — all unchanged.

## Task Commits

1. **Task 1: Diagnose + fix sticky AppHeader regression + browser test** — commit `7dc9c12` `fix(02.1-34): sticky AppHeader regression — overflow-x clip on body`. 4 files changed, +121 / −2.
2. **Task 2: FiltersSheet body-scroll-lock CSS :has() rewrite + /audit dateRange schema cleanup + browser test** — commit `bb3be3c` `fix(02.1-34): FiltersSheet body-scroll-lock CSS :has() + /audit dateRange schema cleanup`. 4 files changed, +51 / −23.

**Parallel-execution race:** during Task 2 a sibling agent's commit `21c7000` (Plan 02.1-30) absorbed staged Plan 02.1-34 edits to `tests/integration/audit-render.test.ts` and `tests/browser/feed-360.test.ts` while my changes were on the index. The Plan 02.1-34 describe blocks landed inside that sibling commit; the Plan 02.1-34 attribution remained intact via the commented `Plan 02.1-34` markers, but the original Task 2 atomic-commit scope was reduced to the comment-stripping refinement (the absorbed describe blocks had a buggy version that lacked code/comment separation; my refinement makes the assertions pass). Net effect: code is correct on master; commit hygiene was best-effort under the parallel race.

## Files Created/Modified

- `src/app.css` (modified) — Plan 02.1-34 fix #1 (Task 1): `overflow-x: hidden` → `overflow-x: clip` on html+body block (sticky regression source). Plan 02.1-34 fix #2 (Task 2): NEW `body:has(dialog[open]) { overflow: hidden; }` rule (declarative body-scroll-lock).
- `src/lib/components/FiltersSheet.svelte` (modified) — REMOVED `document.body.style.overflow = "hidden"` from $effect; REMOVED `return () => { document.body.style.overflow = ""; }` cleanup; REMOVED `document.body.style.overflow = ""` from onDialogCancel. PRESERVED dialogEl.showModal() + focusAxis logic.
- `src/routes/audit/+page.svelte` (modified) — `AUDIT_SCHEMA = ["action"] as const` (was `["action", "date"]`).
- `tests/integration/audit-render.test.ts` (modified) — Plan 02.1-34 describe block with 6 SSR + grep regression tests. Code-comment-strip refinement so negative-grep assertions don't trip on comments referring to the historical patterns.
- `tests/browser/responsive-360.test.ts` (modified) — Plan 02.1-34 describe blocks: PUBLIC-routed body computed overflow-x === 'clip' assertion (the regression-source guard) + 4 it.skip placeholders for the /feed-/sources-/games sticky-top end-to-end + 1 it.skip for /audit FiltersSheet axis assertion.
- `tests/browser/feed-360.test.ts` (modified) — Plan 02.1-34 describe block (3 it.skip placeholders) for FiltersSheet body-scroll-lock end-to-end at 360px (auth harness deferred).
- `tests/browser/commands.d.ts` (modified) — `measureBodyOverflowX(): Promise<string>` type augmentation on BrowserCommands.
- `vitest.config.ts` (modified) — `measureBodyOverflowX` command implementation: `ctx.page.evaluate(() => window.getComputedStyle(document.body).overflowX)`.

## Decisions Made

See `key-decisions` in frontmatter — 4 entries: (1) overflow-x:clip path A vs paths B/C; (2) declarative CSS :has() body-scroll-lock vs imperative JS; (3) /audit schema narrowing while /feed schema preserved; (4) comment-stripping in negative-grep regression guards as a Plan 02.1-10 precedent extension.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Comment-stripping in negative-grep regression guards**
- **Found during:** Task 2 verification run — first integration-test execution failed because the FiltersSheet `document.body.style.overflow` regression test matched the literal string in CODE COMMENTS (the `// Plan 02.1-22 imperative approach...` paragraph in FiltersSheet.svelte mentions `document.body.style.overflow = 'hidden'`). Same issue tripped the html+body overflow-x:hidden negative test on src/app.css comments.
- **Issue:** Negative-grep regression tests don't distinguish code from comments. The Plan 02.1-22 historical-explanation comments in both FiltersSheet.svelte and src/app.css legitimately mention the literal patterns being asserted absent — useful documentation for reviewers, but tripped the test.
- **Fix:** Strip // and /* */ comments before applying the negative-grep regex. Plan 02.1-10 set the precedent of moving the COMMENT to avoid the literal; this plan moves the TEST to be smarter (preserves the documentation value).
- **Files modified:** tests/integration/audit-render.test.ts (2 of the 6 Plan 02.1-34 tests refined).
- **Verification:** All 6 Plan 02.1-34 tests pass after the refinement.
- **Committed in:** bb3be3c (Task 2 commit).

---

**Total deviations:** 1 auto-fixed (Rule 1 — bug fix on the test logic, not the production code).
**Impact on plan:** Test refinement made all assertions pass; production code (the actual UAT-NOTES.md §4.22.A / §4.22.F / §4.21.A fixes) was unchanged.

## Issues Encountered

- **Parallel-execution race with sibling Plan 02.1-30 agent.** While Task 2 had `tests/integration/audit-render.test.ts` and `tests/browser/feed-360.test.ts` changes staged, sibling commit `21c7000` absorbed those staged hunks into a Plan 02.1-30 commit. The Plan 02.1-34 describe blocks landed on master with the Plan 02.1-30 commit's authorship; my targeted comment-stripping refinement landed in commit `bb3be3c`. The code is correct on master; the commit attribution is split. STATE.md's parallel-execution pattern (Plan 02.1-02 deviation: "use 'git commit -o <pathspec>...' to scope a plan's commit to its own files when sibling agents have pre-staged unrelated index entries") was followed for `src/app.css` (sibling Plan 02.1-30's --color-mine token landed in the working tree concurrently with my overflow-x:clip change; staged only my hunk via `git apply --cached` of a targeted patch).
- **`pnpm lint` blocked by pre-existing dev-mock-oauth.mjs no-restricted-properties violation.** Out of scope for Plan 02.1-34 (already filed in deferred-items.md by Plan 02.1-28). My touched files lint clean (verified via `pnpm exec eslint <files>`).

## User Setup Required

None — no external service configuration required. Pure CSS + component refactor.

## Next Phase Readiness

- 3 round-4 UAT findings closed (§4.22.A / §4.22.F / §4.21.A) — UAT-NOTES.md sign-off blocker count drops by 3.
- Pattern established: future modal additions automatically inherit body-scroll-lock via CSS :has(), no per-component code needed.
- Pattern established: `overflow-x: clip` over `overflow-x: hidden` for any future container that needs to crop overflow but not promote to a scroll container (sticky compatibility).
- Auth-harness-deferred placeholders (`it.skip` blocks for /feed / /sources / /games / /audit end-to-end at 360px) carry the Plan 02.1-34 marker for grep discoverability when the cookie-injection harness lands in Phase 6.

## Self-Check: PASSED

- src/app.css: overflow-x:clip on body (line 159) — VERIFIED via grep.
- src/app.css: body:has(dialog[open]) { overflow: hidden; } (lines 181-194) — VERIFIED via grep.
- src/lib/components/FiltersSheet.svelte: no `document.body.style.overflow` in code (only in a comment block explaining the removal) — VERIFIED via integration test (with code-comment strip).
- src/routes/audit/+page.svelte: AUDIT_SCHEMA = ["action"] (line 64) — VERIFIED via grep.
- tests/integration/audit-render.test.ts: Plan 02.1-34 describe block with 6 passing tests — VERIFIED via `pnpm exec vitest run -t "Plan 02.1-34"`.
- tests/browser/responsive-360.test.ts: Plan 02.1-34 describe blocks present (6 markers) — VERIFIED via grep.
- tests/browser/feed-360.test.ts: Plan 02.1-34 describe block present (4 markers) — VERIFIED via grep.
- vitest.config.ts: measureBodyOverflowX command — VERIFIED via grep.
- tests/browser/commands.d.ts: measureBodyOverflowX type — VERIFIED via grep.
- Commits 7dc9c12 (Task 1) + bb3be3c (Task 2) — VERIFIED via `git log --oneline -10`.

---
*Phase: 02.1-architecture-realignment*
*Completed: 2026-04-29*
