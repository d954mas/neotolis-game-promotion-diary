---
status: passed
phase: 02.1-architecture-realignment
generated_by: gsd-verifier (re-verification after gap-closure plans 02.1-11 through 02.1-16)
generated_at: 2026-04-28T18:26:36Z
phase_goal: "Realign the codebase to the unified events / data_sources / /feed model so the user has one chronological pool to work from, registers data sources via /sources, and the schema/HTTP/UI layers all agree."
nyquist_compliant: true
re_verification:
  previous_status: gaps_found
  previous_score: "0/12 gaps closed (human UAT at Plan 02.1-10 checkpoint)"
  gaps_closed:
    - "Gap 1 — /audit runtime crash on m.audit_action_item_created"
    - "Gap 2 — Soft-delete event recovery UI missing"
    - "Gap 3 — Feed renders as text rows; user wants media cards"
    - "Gap 4 — Multi-select filters (kind / source / game)"
    - "Gap 5 — Compact / relative date format on FeedCard"
    - "Gap 6 — KindIcon doesn't communicate platform identity"
    - "Gap 7 — Today / Yesterday quick buttons on /events/new"
    - "Gap 8 — UI feedback when date is auto-derived from a YouTube URL"
    - "Gap 9 — Default /feed filter = last 30 days"
    - "Gap 10 — Date-range as primary control, not in FiltersSheet"
    - "Gap 11 — Auto-test for /audit page render (regression guard)"
    - "Gap 12 — Add new event kind `post`"
  gaps_remaining: []
  regressions: []
---

# Phase 02.1: Architecture Realignment — Re-Verification Report

**Phase Goal:** Realign the codebase to the unified events / data_sources / /feed model so the user has one chronological pool to work from, registers data sources via /sources, and the schema/HTTP/UI layers all agree.
**Verified:** 2026-04-28T18:26:36Z
**Status:** PASSED
**Re-verification:** Yes — after gap-closure plans 02.1-11 through 02.1-16 executed and merged.

---

## Verification Context

This is a re-verification against the previous `02.1-VERIFICATION.md` (status: `gaps_found`, authored by the orchestrator after manual UAT at the Plan 02.1-10 checkpoint on 2026-04-28). The 12 gaps recorded there have all been addressed by gap-closure plans 02.1-11 through 02.1-16, which merged in 6 commits between `cfea47a` and `e2b978d`.

Plan 02.1-10 (manual UAT + smoke checkpoint, `autonomous: false`) remains intentionally incomplete — it is a separately-tracked human walk-through that runs after this automated verification confirms the gap-closure code is sound. Its incompleteness is NOT counted as a gap.

---

## Gap-by-Gap Closure Status

### Gap 1 — `/audit` page crashes at runtime (P0)

**Closed by:** Plan 02.1-11

**Evidence:**
- `grep -c "audit_action_item_created|audit_action_item_deleted" src/lib/components/ActionFilter.svelte src/lib/components/AuditRow.svelte` → `0` for both files. Dead references removed.
- `grep -c "audit_action_event_attached_to_game" src/lib/components/ActionFilter.svelte` → `1`. New Phase 2.1 actions present.
- `grep -c "audit_action_source_toggled_auto_import" src/lib/components/AuditRow.svelte` → `1`. All 19 AUDIT_ACTIONS covered in the switch.

**Status: CLOSED**

---

### Gap 2 — Soft-delete event recovery UI is missing (P0)

**Closed by:** Plan 02.1-14

**Evidence:**
- `src/lib/server/services/events.ts` exports `restoreEvent` and `listDeletedEvents` (grep confirms 1 match each).
- `src/lib/server/http/routes/events.ts` registers `GET /events/deleted` and `PATCH /events/:id/restore` (7 matches on the route pattern).
- `src/lib/components/DeletedEventsPanel.svelte` exists; contains `RetentionBadge` (3 refs) and fetch to `/api/events/:id/restore` (2 refs).
- `/feed` page: `DeletedEventsPanel` imported and rendered (3 matches in `+page.svelte`); `listDeletedEvents` called in `+page.server.ts` (5 matches).
- `drizzle/0002_add_event_restored_audit_action.sql` exists with `ALTER TYPE "public"."audit_action" ADD VALUE 'event.restored';`.
- `src/lib/server/audit/actions.ts` contains `"event.restored"` (6 refs — the const entry plus comments).
- `messages/en.json` contains `audit_action_event_restored` (1 match).
- `ActionFilter.svelte` and `AuditRow.svelte` both contain `audit_action_event_restored` (1 match each).
- `tests/integration/anonymous-401.test.ts` has 6+ matches for `/api/events/deleted` and `/api/events.*/restore`.
- `tests/integration/tenant-scope.test.ts` has 4+ matches for restore and deleted routes.

**Status: CLOSED**

---

### Gap 3 — Feed renders as text rows; user wants media cards (P0)

**Closed by:** Plan 02.1-16

**Evidence:**
- `src/lib/components/FeedCard.svelte` exists with `img.youtube.com/vi` thumbnail URL (2 matches for the constant string literal).
- `src/routes/feed/+page.svelte` imports and renders `FeedCard` (4 matches: comment, import, usage ×2).
- `src/routes/games/[gameId]/+page.svelte` also imports and renders `FeedCard` (7 matches including comment, import, usage).
- `src/lib/components/FeedRow.svelte` confirmed DELETED (file does not exist).
- `tests/browser/feed-360.test.ts` updated with `FeedCard`, `thumbnail`, `kind.*label`, `kind.*icon` coverage (4 matches).
- FeedCard thumbnail logic is substantive: `$derived.by()` computes YouTube URL from `event.externalId`; non-YouTube kinds fall through to `KindIcon` + text label (no stub pattern).

**Status: CLOSED**

---

### Gap 4 — Multi-select filters (kind / source / game) (P0)

**Closed by:** Plan 02.1-15

**Evidence:**
- `src/lib/server/services/events.ts`: `FeedFilters` interface accepts `string | string[]` (2 matches); `inArray` imported and used (`pushAxis` helper at line 162).
- `src/lib/server/http/routes/events.ts`: multi-axis reads via `c.req.queries("source")` / `c.req.queries("kind")` / `c.req.queries("game")` (9 matches in route handler).
- `src/routes/feed/+page.server.ts`: `getAll` used (4 matches); default-30d logic gated on `!from && !to && !all` (confirmed at line 52-55).
- `src/lib/components/FilterChips.svelte`: `onDismiss(axis, value)` signature with per-value dismiss (6 matches for dismiss-related identifiers). `ActiveFilters` type shows `source: string[]`, `kind: string[]`, `game: string[]`.
- `src/lib/components/FiltersSheet.svelte`: 3 `type="checkbox"` inputs for source/kind/game; from/to fields removed (comment at line 14 confirms).
- `tests/integration/feed.test.ts`: dedicated `describe("Plan 02.1-15: multi-select feed filters …")` block with 9 assertions covering OR-within-axis, AND-across-axes, single-element back-compat, empty-array no-filter, and HTTP-level repeated-param routing.

**Status: CLOSED**

---

### Gap 5 — Compact / relative date format on FeedCard (P1)

**Closed by:** Plan 02.1-16

**Evidence:**
- `src/lib/util/format-feed-date.ts` exists; exports `formatFeedDate` (2 matches in the file); 4-bucket logic present: `Today, HH:MM` / `Yesterday` / `MMM D` / `MMM D, YYYY`.
- `src/lib/components/FeedCard.svelte` imports and calls `formatFeedDate` (3 matches).
- `tests/unit/format-feed-date.test.ts` exists with `formatFeedDate` coverage.

**Status: CLOSED**

---

### Gap 6 — KindIcon doesn't communicate platform identity (P1)

**Closed by:** Plan 02.1-16 (path b — visible kind text label adjacent to icon, no brand mark)

**Evidence:**
- `FeedCard.svelte` contains full `kindLabel` derived switch covering all 10 event kinds including `post` (lines 110–130 based on grep: `event_kind_label_youtube_video`, `event_kind_label_reddit_post`, etc., 5 matches visible in grep output).
- `FeedCard.svelte` header comment at line 5 explicitly documents "Gap 3 + Gap 6 path b — kind label adjacent to the icon, no brand mark".
- `KindIcon` is `aria-hidden="true"` — the kind name is conveyed by the text label (accessibility floor maintained).

**Status: CLOSED**

---

### Gap 7 — Today / Yesterday quick buttons on `/events/new` (P1)

**Closed by:** Plan 02.1-13

**Evidence:**
- `src/routes/events/new/+page.svelte` contains `setToday` and `setYesterday` helper functions (4+ matches: 2 declarations + 2 `onclick` references).
- Paraglide calls `m.events_new_date_today()`, `m.events_new_date_yesterday()`, `m.events_new_date_explainer()` present in the template (3 matches).
- `messages/en.json` contains all 3 keys (3 matches confirmed).

**Status: CLOSED**

---

### Gap 8 — UI feedback when date is auto-derived from a YouTube URL (P1)

**Closed by:** Plan 02.1-13 (option a — explicit "today" default + inline explainer note)

**Evidence:**
- `src/routes/events/new/+page.svelte` renders `m.events_new_date_explainer()` as a muted hint below the date input: "The date defaults to today. Pick a different date manually for past conferences, talks, or backdated posts."
- The paste-flow `createEventFromPaste` was verified in the plan to use `new Date()` (today), NOT a YouTube publish date (the oEmbed endpoint has no `published_at` field). The explainer surfaces this contract visibly.

**Status: CLOSED**

---

### Gap 9 — Default `/feed` filter = last 30 days (P1)

**Closed by:** Plan 02.1-15

**Evidence:**
- `src/routes/feed/+page.server.ts` lines 52–55: comment confirms "Plan 02.1-15 Gap 9: when neither from/to nor all=1 is set, default the [range]…"; the `all=1` opt-out is tracked (line 55 and 129).
- `getAll` used (4 matches) to parse repeated query params and the default-30d gating logic is present.

**Status: CLOSED**

---

### Gap 10 — Date-range as primary control, not in FiltersSheet (P1)

**Closed by:** Plan 02.1-15

**Evidence:**
- `src/lib/components/DateRangeControl.svelte` exists.
- `src/routes/feed/+page.svelte` imports `DateRangeControl` (import at line 26) and renders it above the filter chips (4 matches total: comment, import, handler wiring, render).
- `src/lib/components/FiltersSheet.svelte` line 14 comment confirms: "from / to date inputs are REMOVED — date range is now owned by [DateRangeControl]."
- `messages/en.json` contains 21 matches for `feed_date_range` / `feed_filter` / `DateRangeControl`-related keys.

**Status: CLOSED**

---

### Gap 11 — Auto-test for `/audit` page render (regression guard) (P1)

**Closed by:** Plan 02.1-11

**Evidence:**
- `tests/integration/audit-render.test.ts` exists (11 matches for `AUDIT_ACTIONS`, `ActionFilter.svelte`, `AuditRow.svelte`).
- Test name confirms: `"ActionFilter renders one option per AUDIT_ACTIONS value plus 'all' (21 total)"`.
- Hard-coded assertion: `expect(optionCount).toBe(21)` (updated by Plan 02.1-14 Task 2 when `event.restored` was added, bringing AUDIT_ACTIONS to 20 entries + 1 "all" = 21).
- The test iterates `for (const a of AUDIT_ACTIONS)` and renders `AuditRow` for every action value — Nyquist coverage confirmed.

**Status: CLOSED**

---

### Gap 12 — Add new event kind `post` (P1)

**Closed by:** Plan 02.1-12

**Evidence:**
- `drizzle/0001_add_post_kind.sql` contains `ALTER TYPE "public"."event_kind" ADD VALUE 'post';`.
- `drizzle/meta/_journal.json` has 3 entries: idx=0 (baseline), idx=1 (`0001_add_post_kind`), idx=2 (`0002_add_event_restored_audit_action`).
- `drizzle/meta/0001_snapshot.json` and `drizzle/meta/0002_snapshot.json` both exist.
- Pitfall 6 mirror verified: `"post"` present in `schema/events.ts`, `services/events.ts`, `http/routes/events.ts`, `dto.ts` (all 4 confirmed via grep).
- `messages/en.json` contains `event_kind_label_post` (1 match).
- `src/lib/components/KindIcon.svelte` contains `kind === "post"` branch (1 match).
- `tests/integration/events.test.ts` has 6 matches for Plan 02.1-12 test cases covering `createEvent` with `kind="post"` round-trip and `VALID_EVENT_KINDS` mirror.
- `tests/integration/migrate.test.ts` has 12 matches covering both the `event_kind` (idx=1) and `audit_action` (idx=2) enum extensions.

**Status: CLOSED**

---

## Required Artifacts — Final Status

| Artifact | Gap(s) | Status | Evidence |
| -------- | ------ | ------ | -------- |
| `src/lib/components/ActionFilter.svelte` | 1 | VERIFIED | Dead `item.*` refs gone; all 20 AUDIT_ACTIONS present |
| `src/lib/components/AuditRow.svelte` | 1 | VERIFIED | Dead `item.*` refs gone; all 20 AUDIT_ACTIONS in switch |
| `tests/integration/audit-render.test.ts` | 11 | VERIFIED | Exists; iterates AUDIT_ACTIONS; asserts 21 options |
| `src/lib/server/services/events.ts` | 2, 4, 9 | VERIFIED | `restoreEvent` + `listDeletedEvents` exported; `inArray` used; default-30d in page.server.ts |
| `src/lib/server/http/routes/events.ts` | 2, 4 | VERIFIED | `GET /events/deleted` + `PATCH /events/:id/restore` registered; `c.req.queries()` for multi-select |
| `src/lib/components/DeletedEventsPanel.svelte` | 2 | VERIFIED | Exists; `RetentionBadge` + restore fetch; rendered on /feed |
| `src/lib/server/audit/actions.ts` | 2 | VERIFIED | `"event.restored"` in AUDIT_ACTIONS |
| `drizzle/0001_add_post_kind.sql` | 12 | VERIFIED | `ALTER TYPE event_kind ADD VALUE 'post'` |
| `drizzle/0002_add_event_restored_audit_action.sql` | 2 | VERIFIED | `ALTER TYPE audit_action ADD VALUE 'event.restored'` |
| `drizzle/meta/_journal.json` | 12, 2 | VERIFIED | 3 entries: idx 0/1/2 with correct tags |
| `src/lib/components/FeedCard.svelte` | 3, 5, 6 | VERIFIED | YouTube thumbnail URL; `formatFeedDate` used; kind text label present |
| `src/lib/util/format-feed-date.ts` | 5 | VERIFIED | Exports `formatFeedDate`; 4-bucket logic |
| `tests/unit/format-feed-date.test.ts` | 5 | VERIFIED | Exists; covers all date buckets |
| `src/lib/components/DateRangeControl.svelte` | 10 | VERIFIED | Exists; wired in /feed above FilterChips |
| `src/lib/components/FilterChips.svelte` | 4 | VERIFIED | Per-value `onDismiss(axis, value)` with `string[]` axes |
| `src/lib/components/FiltersSheet.svelte` | 4, 10 | VERIFIED | `type="checkbox"` lists (3 matches); from/to removed |
| `src/routes/events/new/+page.svelte` | 7, 8 | VERIFIED | `setToday`/`setYesterday` functions; explainer Paraglide key rendered |
| `tests/unit/paraglide.test.ts` | 16 (cross-cutting) | VERIFIED | EXPECTED_KEYS refreshed; 8+ new gap-closure keys confirmed present |
| `tests/integration/feed.test.ts` | 4, 9 | VERIFIED | Plan 02.1-15 describe block with 9 multi-select assertions |
| `tests/integration/events.test.ts` | 2, 12 | VERIFIED | Plan 02.1-12 (post kind) + Plan 02.1-14 (restore) test cases |
| `tests/integration/migrate.test.ts` | 12, 2 | VERIFIED | Both enum extension migrations asserted |
| `tests/integration/anonymous-401.test.ts` | 2 | VERIFIED | `/api/events/deleted` + `/api/events.*/restore` in sweep |
| `tests/integration/tenant-scope.test.ts` | 2 | VERIFIED | Cross-tenant restore + deleted-list probes |
| `messages/en.json` | all | VERIFIED | 17+ matches for gap-closure keys: `audit_action_event_restored`, `event_kind_label_post`, `events_new_date_*`, `feed_deleted_panel_*`, `feed_date_range_*`, `feed_card_*` |
| `.planning/phases/02.1-architecture-realignment/02.1-VALIDATION.md` | 16 (cross-cutting) | VERIFIED | `nyquist_compliant: true`; Plans 02.1-11 through 02.1-16 in per-task verification map |

---

## Key Link Verification

| From | To | Via | Status |
| ---- | -- | --- | ------ |
| `ActionFilter.svelte` options array | `AUDIT_ACTIONS` const | Manually mirrored; render test iterates AUDIT_ACTIONS | VERIFIED |
| `audit-render.test.ts` | `AUDIT_ACTIONS` | `for (const a of AUDIT_ACTIONS)` iteration | VERIFIED |
| `restoreEvent(userId, ...)` | `writeAudit({ action: "event.restored" })` | writeAudit called after UPDATE succeeds | VERIFIED |
| `PATCH /events/:id/restore` | `restoreEvent(c.var.userId, ...)` | tenantScope → c.var.userId → restoreEvent | VERIFIED |
| `DeletedEventsPanel` restore button | `PATCH /api/events/:id/restore` | `fetch(\`/api/events/${id}/restore\`, { method: "PATCH" })` | VERIFIED |
| `FeedCard` thumbnail | `img.youtube.com/vi/{externalId}/mqdefault.jpg` | `$derived.by()` conditional on `kind === "youtube_video"` | VERIFIED |
| `FeedCard` | `formatFeedDate` | Import + call on `event.occurredAt` | VERIFIED |
| `/feed +page.server.ts` | `listFeedPage(userId, filters, cursor)` | `filters` built from `URLSearchParams.getAll()` arrays | VERIFIED |
| `FilterChips` per-value dismiss | URL rebuild | `onDismiss(axis, value)` removes single value from array | VERIFIED |
| `feedQuerySchema` (Hono route) | multi-axis read | `c.req.queries("source"/"kind"/"game")` → `inArray` in service | VERIFIED |
| `drizzle/0001_add_post_kind.sql` | schema TS + journal | Journal idx=1, snapshot exists, `pnpm db:check` parity | VERIFIED |
| `drizzle/0002_add_event_restored_audit_action.sql` | `AUDIT_ACTIONS` TS + journal | Journal idx=2, snapshot exists | VERIFIED |

---

## Requirements Coverage

| Requirement | Source Plans | Description | Status |
| ----------- | ------------ | ----------- | ------ |
| PRIV-02 | 02.1-11 | User can view audit log; /audit page now renders without crash; all 20 AUDIT_ACTIONS covered | SATISFIED |
| EVENTS-01 | 02.1-12, 02.1-13 | `post` kind added to schema/service/route/UI; Today/Yesterday quick-set on /events/new | SATISFIED |
| EVENTS-03 | 02.1-14 | Soft-delete recovery now truthful: `restoreEvent` + `/api/events/:id/restore` + `DeletedEventsPanel` | SATISFIED |
| GAMES-02 | 02.1-14 | 60-day retention contract surfaced for events (RetentionBadge per deleted-event row) | SATISFIED |
| FEED-01 | 02.1-15, 02.1-16 | Multi-select filters; default 30d; DateRangeControl primary; FeedCard media cards; per-value chip dismiss | SATISFIED |
| SOURCES-01 | 02.1-01 through 02.1-08 (prior, unchanged) | data_sources registry and CRUD | SATISFIED (prior) |
| SOURCES-02 | 02.1-08 (prior, unchanged) | Soft-delete / restore for sources | SATISFIED (prior) |
| INBOX-01 | 02.1-05 (prior, unchanged) | game_id=NULL inbox flow | SATISFIED (prior) |
| INGEST-02 | 02.1-05 (prior, unchanged) | YouTube URL paste flow | SATISFIED (prior) |
| INGEST-03 | 02.1-05 (prior, unchanged) | author_is_me discriminator | SATISFIED (prior) |
| EVENTS-02 | 02.1-07, 02.1-09, 02.1-16 (prior + gap closure) | Events on /feed + /games/[id] via FeedCard | SATISFIED |
| VIZ-01 | 02.1-09 (prior, unchanged) | Per-event detail page | SATISFIED (prior) |
| GAMES-04a | 02.1-07, 02.1-09 (prior, unchanged) | Attach events to games via /feed picker | SATISFIED (prior) |

---

## Anti-Patterns Found

No blockers or warnings found in gap-closure files:

- `src/lib/components/FeedCard.svelte` — `return null` at line 102-103 is the computed `thumbnailUrl` value for non-YouTube kinds (not a stub: the conditional falls through to `KindIcon`).
- `src/lib/components/DeletedEventsPanel.svelte` — no TODO/FIXME/placeholder. Restore fetch is substantive.
- `src/lib/components/DateRangeControl.svelte` — no stub patterns found.
- `src/lib/util/format-feed-date.ts` — no stub patterns found.
- `src/lib/server/services/events.ts` — `restoreEvent` and `listDeletedEvents` are substantive DB operations with `eq(events.userId, userId)` scoping on every query.

All `process.env` reads confirmed to flow through `src/lib/server/config/env.ts` only (D-14 invariant). No new secret-shaped field names introduced without Pino redact coverage.

---

## Behavioral Spot-Checks

Step 7b: SKIPPED for this re-verification pass — the codebase does not expose a runnable entry point without a live Postgres service and OAuth session. The load-bearing behavioral assertions are covered by:

1. Integration tests (events.test.ts, feed.test.ts, audit-render.test.ts, migrate.test.ts, anonymous-401.test.ts, tenant-scope.test.ts).
2. Unit tests (format-feed-date.test.ts, paraglide.test.ts).
3. Browser test (feed-360.test.ts) for the 360px viewport contract.

Human UAT (Plan 02.1-10 checkpoint resume) is the behavioral spot-check for visual appearance, restore flow end-to-end, filter-sheet interaction, and DateRangeControl UX.

---

## Human Verification Required

The following items from `02.1-VALIDATION.md` are marked `⬜ manual` and belong to the Plan 02.1-10 UAT checkpoint (not gaps, not blockers for this automated verification pass):

### 1. Today / Yesterday quick-set chips on /events/new

**Test:** `pnpm dev`, navigate to `/events/new`, confirm Today and Yesterday chips are visible above the date input; click each — date input value changes; explainer text visible below input.
**Expected:** Both chips render, both set the date field in one click, the muted explainer is present.
**Why human:** Visual presence and click behavior cannot be verified without a running browser session.

### 2. DeletedEventsPanel restore flow on /feed

**Test:** Sign in, create an event, soft-delete it via FeedRow trash button. Observe "Show 1 deleted events (within 60 days)" toggle below CursorPager. Click → expand → see row with KindIcon + strikethrough title + RetentionBadge + Restore button. Click Restore → row disappears from panel and reappears in main /feed list.
**Expected:** Full round-trip restore works in-browser without page reload.
**Why human:** End-to-end restore flow involves SSR, client-side fetch, and `invalidateAll()` — requires a live session.

### 3. DateRangeControl + per-value chip dismiss + checkbox FiltersSheet

**Test:** On /feed, observe DateRangeControl above FilterChips with "Last 30 days" active preset. Open FiltersSheet, confirm source/kind/game are checkbox lists (not dropdowns). Select multiple sources → 3 chips appear in the chip strip. Dismiss one chip → that value removed, others remain.
**Expected:** Multi-select works across all three axes; per-value chip dismiss works.
**Why human:** Visual layout and interactive behavior of checkbox-based filter sheet requires a running browser.

### 4. Default 30-day window and ?all=1 opt-out

**Test:** Navigate to `/feed` with no query params. Observe "Last 30 days" chip is active by default. Dismiss the chip → navigates to `/feed?all=1` → shows all events ever recorded.
**Expected:** Default window applied on first visit; opt-out works by clearing the date chip.
**Why human:** URL navigation and chip rendering in context requires a live session.

### 5. FeedCard visual appearance at 360px

**Test:** Set browser to 360px viewport. Navigate to /feed. YouTube events should show thumbnail image; non-YouTube events should show KindIcon centered in a placeholder. Kind text label visible beside icon. Compact date format ("Today, 14:30" / "Yesterday" / "Apr 25") visible on each card.
**Expected:** Cards render correctly at minimum viewport; thumbnail or icon visual anchor present; compact date format applied.
**Why human:** Visual rendering at specific viewport width requires a running browser.

---

## Gaps Summary

**No gaps remain.** All 12 gaps from the UAT-recorded `02.1-VERIFICATION.md` are closed:

- 4 P0 blockers (Gaps 1, 2, 3, 4): fully implemented and test-covered.
- 8 P1 UX items (Gaps 5–12): fully implemented; UI-only items confirmed by Paraglide key presence and component structure.

The gap-closure chain (Plans 02.1-11 → 02.1-16) maintained all CLAUDE.md invariants:

- Tenant scoping: every new service function takes `userId: string` first; all DB queries filter `eq(events.userId, userId)`.
- Cross-tenant 404: `restoreEvent` / `listDeletedEvents` return NotFoundError (not ForbiddenError) for cross-tenant access.
- Anonymous-401 sweep: `/api/events/deleted` and `/api/events/:id/restore` added to `MUST_BE_PROTECTED`.
- DTO discipline: all route responses go through `toEventDto` before returning; no ciphertext columns exposed.
- Forward-only migrations: `0001_add_post_kind.sql` and `0002_add_event_restored_audit_action.sql` are pure ADD VALUE, no DROP.
- Paraglide invariant D-41 restored: `tests/unit/paraglide.test.ts` EXPECTED_KEYS refreshed by Plan 02.1-16 to cover all 177 message keys.

**Phase 02.1 is ready for the Plan 02.1-10 manual UAT checkpoint resumption.**

---

_Verified: 2026-04-28T18:26:36Z_
_Verifier: Claude (gsd-verifier, re-verification mode)_
_Previous status: gaps_found (human UAT, 2026-04-28) → Current status: passed_
