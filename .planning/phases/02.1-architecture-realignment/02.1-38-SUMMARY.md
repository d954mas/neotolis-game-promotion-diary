---
phase: 02.1-architecture-realignment
plan: 38
subsystem: ui
tags: [ui, multi-select, m-n-junction, gap-closure, round-6, events-edit, p0]

# Dependency graph
requires:
  - phase: 02.1-architecture-realignment
    provides: "Plan 02.1-28 attachEventToGames set-replacement semantics + PATCH /api/events/:id/attach already accepts {gameIds: [...]} array body; Plan 02.1-32 standalone-conflict UI guard at /events/[id]/edit (predicate updated, semantics unchanged); Plan 02.1-18 /events/[id]/edit form scaffold; Plan 02.1-15/23 SourceRow + FiltersSheet checkbox-row visual pattern (mirrored for game-list)"
provides:
  - "src/routes/events/[id]/edit/+page.svelte: Game picker rebuilt as multi-select. State `let gameId = $state(...)` + `originalGameId` retired in favor of `let gameIds = $state<string[]>(...)` + `originalGameIds = new Set(...)`; `toggleGame(id, checked)` helper added. Submit handler runs an order-agnostic set-difference dirty check (`new Set` + `every((id) => originalGameIds.has(id))`) before firing PATCH /api/events/:id/attach with the canonical `{gameIds: [...]}` body. Standalone-conflict $derived predicate now reads `gameIds.length > 0` (semantics unchanged — `at least one attached game conflicts with standalone`)."
  - "Markup transformation: the single-select `<label class=\"field\"><select bind:value={gameId}>...</select></label>` block becomes `<fieldset class=\"field game-picker\"><legend>...</legend><ul class=\"game-list\"><li><label class=\"checkbox-row\"><input type=\"checkbox\" checked={gameIds.includes(g.id)} onchange={toggleGame}>{g.title}</label></li>...</ul></fieldset>`. Empty-state hint when `games.length === 0` directs the user to /games to create one first."
  - "Style block: minimal CSS for `.game-picker` / `.game-list` / `.checkbox-row` mirrors the SourceRow + FiltersSheet checkbox-row visual pattern. At 360px the list scrolls vertically inside the form (`max-height: 320px; overflow-y: auto`); each `<li>` is full-width with the checkbox left-aligned (`min-height: 44px` for the touch target). Hover border-color matches the chip pattern in the same file."
  - "messages/en.json: +2 keys (`events_edit_games_label`, `events_edit_games_empty`). Inserted alphabetically between `events_edit_delete_button` and `events_edit_heading`."
  - "tests/unit/paraglide.test.ts: EXPECTED_KEYS extended with the 2 new keys + Plan 02.1-38 commentary block (round-6 gap closure — UAT-NOTES.md §5.2 P0). The locale-add invariant (D-41) absorbs the delta in lock-step."
  - "tests/integration/events-attach.test.ts: NEW `Plan 02.1-38 — multi-select gameIds round-trip (UAT-NOTES.md §5.2)` describe block. Asserts attachEventToGames(u, ev, [g1, g2]) writes BOTH junction rows (set equality check; order-agnostic) AND a subsequent attachEventToGames(u, ev, []) detaches both. Labels the backend round-trip the UI fix now exercises — future grep `Plan 02.1-38` surfaces both ends of the §5.2 closure."
  - "tests/browser/responsive-360.test.ts: NEW `Plan 02.1-38 — /events/[id]/edit multi-select Game picker at 360px` describe block with 4 stub-skipped placeholders. Auth harness deferred to Phase 6 (same precedent as Plans 02.1-18..34). Each placeholder carries the manual-UAT recipe in its skip-reason; the Russian-language manual UAT script (per user-MEMORY profile) lives in the describe-block JSDoc."
affects:
  - "Round-5 walkthrough item 4 (multi-game visibility on /games/[gameId]) becomes UAT-verifiable. Backend correctness was already proven by Plan 02.1-28 integration tests; this plan flips the UI gate."
  - "Future Phase 6 polish backlog: AttachToGamePicker rewrite (Path B from UAT-NOTES.md §5.2) — chip removal + game search + dialog-based picker. Path A (this plan) is sufficient for §5.2 closure per the explicit `Path A is enough to close §4.24.G` framing."
  - "Future /events/new multi-select extension: out of scope per UAT-NOTES.md §5.2; the create flow's `pick a game OR drop in inbox` UX is intentionally simple. A future Phase 6 polish item can extend if user feedback warrants."

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Set-difference dirty check (order-agnostic) for array-bound form state — when a `<form>` binds to an array of ids and the submit handler conditionally fires a PATCH only when the set membership changed, build `currentSet = new Set(state)` + compare `size === originalSize && [...currentSet].every((id) => originalSet.has(id))`. Cheaper than sort-and-compare; semantically explicit about set equality. Pattern documented inline at /events/[id]/edit/+page.svelte submit handler."
    - "Checkbox-list multi-select bound to `string[]` $state — `<input type=\"checkbox\" checked={state.includes(id)} onchange={(e) => toggle(id, e.target.checked)}>` with a `toggle` helper that does `state = [...state, id]` on add and `state = state.filter((x) => x !== id)` on remove. Avoids `bind:group` (Svelte 5 type-friction with $state arrays) while preserving the same UX. Reusable for any multi-select that submits the canonical `{ids: [...]}` array shape."

key-files:
  created: []
  modified:
    - "src/routes/events/[id]/edit/+page.svelte (state retire + helper add + standalone-guard predicate update + submit-handler dirty-check + markup swap + minimal CSS for the checkbox-list)"
    - "messages/en.json (+2 keys: events_edit_games_label, events_edit_games_empty)"
    - "tests/unit/paraglide.test.ts (EXPECTED_KEYS +2 entries; Plan 02.1-38 commentary block)"
    - "tests/integration/events-attach.test.ts (Plan 02.1-38 describe block — multi-element gameIds round-trip)"
    - "tests/browser/responsive-360.test.ts (Plan 02.1-38 describe block — 4 stub-skipped browser tests, auth harness deferred to Phase 6)"

key-decisions:
  - "Path A (checkbox-list, ~30 LOC) over Path B (full AttachToGamePicker rewrite). Per UAT-NOTES.md §5.2 explicit framing — `Path A is enough to close §4.24.G`. Path B (chip removal + game search + dialog-based picker) is DEFERRED to Phase 6 polish backlog. Path A unlocks the round-5 walkthrough item 4 (multi-game visibility) without rewriting the existing /events/[id]/edit form scaffold."
  - "/events/new kept single-select (out of scope per §5.2). The create flow's `pick a game OR drop in inbox` UX is intentionally simple. UAT-NOTES.md §5.2 only flags the EDIT path; no user feedback yet on the create path, so YAGNI. Forward-pointer: a future Phase 6 polish item can extend if needed."
  - "Set-difference dirty check over deep-equality. The pre-Plan-02.1-38 code did `newGameId !== originalGameId` — a singular reference-equality check. The post-Plan-02.1-38 code does an order-agnostic set-equality check (`size === originalSize && every(...)`). Cheaper than sort-and-compare; explicit about set semantics; matches the backend (attachEventToGames is set-replacement)."
  - "Round-trip test asserts at SERVICE layer (attachEventToGames + direct eventGames query), not HTTP layer. Two paths existed: (A) call the service directly, (B) drive a fetch through the route. Path A chosen because (a) the HTTP route is a thin adapter — Plan 02.1-28 already covers it; (b) the integration-test suite has no fetchAuthed harness — every test in events-attach.test.ts calls the service directly; (c) the value of the new test is the labeled trace from UI fix → backend round-trip, which Path A captures with less ceremony."
  - "Browser-mode tests stub-skipped, auth harness deferred to Phase 6. Same precedent as Plans 02.1-18..34. The cookie-injection auth harness in vitest-browser cannot mint Better Auth signed session cookies (no in-test Postgres connection). The describe-block JSDoc carries the manual-UAT recipe in Russian (per user-MEMORY profile) so the human reviewer can reproduce the assertion at the keyboard. The component-level regression guard lives in the integration round-trip test + the existing audit-render.test.ts /events/[id]/edit SSR pattern from Plan 02.1-32."
  - "Standalone-conflict guard predicate update is a SEMANTICS-PRESERVING refactor. The pre-Plan-02.1-38 predicate was `gameId.length > 0` (singular non-empty string check). Post-Plan-02.1-38 it's `gameIds.length > 0` (array non-empty check). Both encode the SAME invariant: `at least one attached game conflicts with standalone`. The Plan 02.1-32 conflict-guard tests should pass unchanged (they assert on the rendered conflict-error + Save-disabled, not the predicate shape)."

patterns-established:
  - "Path A vs Path B framing — when UAT surfaces a UX gap, prefer the smallest fix that closes the user-quoted blocker (Path A) and DEFER the full UX rewrite (Path B) to a polish phase. Document the deferral inline (in the plan AND the SUMMARY) so future planners see the explicit forward-pointer instead of re-deriving the trade-off."
  - "Set-difference dirty check on array-bound form state — see tech-stack above."
  - "Checkbox-list pattern over `bind:group` for `string[]` $state — see tech-stack above."

requirements-completed: ["GAMES-04a", "EVENTS-01"]

# Metrics
duration: ~12 min
completed: 2026-04-30
---

# Phase 02.1 Plan 38: /events/[id]/edit multi-select Game picker (Path A) Summary

**Closes UAT-NOTES.md §5.2 (P0) — `/events/[id]/edit` Game picker becomes a multi-select checkbox-list bound to `gameIds: string[]`. The user can finally attach an event to ≥2 games from the UI; round-5 walkthrough item 4 (multi-game visibility on `/games/[gameId]`) becomes UAT-verifiable. Backend / schema / audit / DTO unchanged — pure component refactor on the existing PATCH /api/events/:id/attach endpoint (Plan 02.1-28 already accepts the array body). Path B (full AttachToGamePicker rewrite with chip removal + game search) explicitly DEFERRED to Phase 6 polish backlog per UAT-NOTES.md §5.2 framing.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-04-30T10:50:00Z (approximate)
- **Completed:** 2026-04-30T11:02:00Z (approximate)
- **Tasks:** 1
- **Files modified:** 5

## Accomplishments

- `/events/[id]/edit` Game picker is now a multi-select checkbox-list bound to `gameIds: string[]` $state — the user can attach an event to ≥2 games from the UI.
- Submit handler runs an order-agnostic set-difference dirty check before firing PATCH `/api/events/:id/attach` with the canonical `{gameIds: [...]}` body. Empty array still detaches all games (move to inbox).
- Standalone-conflict UI guard from Plan 02.1-32 preserved with a SEMANTICS-PRESERVING predicate refactor (`gameId.length > 0` → `gameIds.length > 0`).
- Round-5 walkthrough item 4 (`/games/[gameId]/events` shows the same event on G1 AND G2 after multi-attach) becomes UAT-verifiable. Backend correctness was already proven by Plan 02.1-28 integration tests.
- Paraglide keys + EXPECTED_KEYS in lock-step (+2 keys).
- New service-layer integration test labels the multi-element gameIds round-trip; new browser-mode describe block with 4 stub-skipped tests stays grep-discoverable for the future Phase 6 auth harness.

## Task Commits

1. **Task 1: Multi-select checkbox-list on /events/[id]/edit; round-trip integration test; 360px browser test** — `0ab7900` (feat)

## Files Created/Modified

- `src/routes/events/[id]/edit/+page.svelte` — gameId/originalGameId retired; gameIds: string[] + originalGameIds: Set added; toggleGame helper; standalone-conflict predicate updated; submit-handler set-difference dirty check; markup swapped from `<select>` to `<fieldset.game-picker>` with `<input type=checkbox>`; minimal CSS for the new classes.
- `messages/en.json` — +2 keys (`events_edit_games_label`, `events_edit_games_empty`), inserted alphabetically.
- `tests/unit/paraglide.test.ts` — EXPECTED_KEYS extended with the 2 new keys; Plan 02.1-38 commentary block (round-6 gap closure for UAT-NOTES.md §5.2 P0).
- `tests/integration/events-attach.test.ts` — `Plan 02.1-38 — multi-select gameIds round-trip` describe block: attach with `[g1, g2]` writes both junction rows AND empty array detaches both.
- `tests/browser/responsive-360.test.ts` — `Plan 02.1-38 — /events/[id]/edit multi-select Game picker at 360px` describe block with 4 stub-skipped placeholders carrying the Russian manual-UAT recipe in the JSDoc.

## Decisions Made

See `key-decisions` in frontmatter:
1. Path A (checkbox-list, ~30 LOC) over Path B (full rewrite — DEFERRED to Phase 6).
2. /events/new kept single-select (out of scope per §5.2).
3. Set-difference dirty check over deep-equality (order-agnostic, cheap, matches backend set-replacement semantics).
4. Round-trip test asserts at service layer, not HTTP layer (matches existing events-attach.test.ts patterns).
5. Browser tests stub-skipped (auth harness deferred to Phase 6, same precedent as Plans 02.1-18..34).
6. Standalone-conflict guard predicate update is a SEMANTICS-PRESERVING refactor.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

- **Local Postgres test DB unavailable.** `pnpm test:integration` fails locally with `password authentication failed for user "test"` because the project assumes a service-container Postgres (configured in CI). The integration suite (including the new Plan 02.1-38 round-trip test) will run on PR via CI's `unit-integration` job. Local verification was limited to `pnpm test:unit -- tests/unit/paraglide.test.ts` (passed clean: 5 tests / 103 assertions across 15 files) + structural greps confirming the new code shape is correct.
- **Pre-existing typecheck errors at `tests/integration/events-attach.test.ts:585` and `tests/integration/events.test.ts:1855`.** Both are in Plan 02.1-35 transaction-rollback test mocks (Proxy-based DB transaction interception) that pre-date this plan's branch. NOT caused by Plan 02.1-38 changes; verified by re-running typecheck after `git stash` confirmed both errors persist on the baseline. Out-of-scope for this plan.
- **Pre-existing lint error at `dev-mock-oauth.mjs:26`.** Documented in `.planning/phases/02.1-architecture-realignment/deferred-items.md` under the Plan 02.1-36 entry. Out-of-scope.

## User Setup Required

None — no external service configuration required. Pure UI refactor on an existing endpoint.

## Next Phase Readiness

- UAT-NOTES.md §5.2 (P0) closed; round-5 walkthrough item 4 unblocked.
- Manual UAT recipe (Russian, per user-MEMORY profile) embedded in `tests/browser/responsive-360.test.ts` describe-block JSDoc — the human reviewer can reproduce the assertion at the keyboard.
- Round-6 Wave 1 in lock-step with Plan 02.1-37 (parallel executor); Wave 2 + Wave 3 plans follow per the round-6 wave plan.
- Phase 02.1 sign-off can advance once Plans 02.1-35..39 land + the 13 round-5 findings are all closed.

## Self-Check: PASSED

- File `src/routes/events/[id]/edit/+page.svelte` exists and contains: `let gameIds = $state` (line 84), `originalGameIds = new Set` (line 86), `toggleGame` (lines 88, 363), `gameIds.length > 0` (line 116), `Plan 02.1-38` provenance markers (6 occurrences), `<fieldset class="field game-picker">` (line 349), `<input type="checkbox"` for the game-list (line 359). Single-select `let gameId = $state` returns 0 (retired); `<select bind:value={gameId}` returns 0 (markup retired).
- File `messages/en.json` contains both new keys (`events_edit_games_label`, `events_edit_games_empty`).
- File `tests/unit/paraglide.test.ts` EXPECTED_KEYS contains both new keys + Plan 02.1-38 commentary.
- File `tests/integration/events-attach.test.ts` contains `Plan 02.1-38 — multi-select gameIds round-trip` describe block (4 marker hits).
- File `tests/browser/responsive-360.test.ts` contains `Plan 02.1-38 — /events/[id]/edit multi-select Game picker` describe block (4 it.skip placeholders + JSDoc with Russian manual-UAT recipe).
- Commit `0ab7900` exists on branch `feat/phase-02-ingest-secrets-audit`.

---
*Phase: 02.1-architecture-realignment*
*Completed: 2026-04-30*
