---
phase: 02.1-architecture-realignment
plan: 30
subsystem: ui
tags: [games-detail, stores-section, steam-listing-remove, duplicate-toast, mine-token, feedcard-reuse, paraglide, svelte5, ux-redesign]

# Dependency graph
requires:
  - phase: 02.1-architecture-realignment
    provides: Plan 02.1-27 schema (event_games junction)
  - phase: 02.1-architecture-realignment
    provides: Plan 02.1-28 services + DTO (mapEventsToDtos / listEventsForGame INNER JOIN / EventDto.gameIds[])
  - phase: 02.1-architecture-realignment
    provides: Plan 02.1-29 Steam-listing 422 metadata payload (existingGameId + existingState)
  - phase: 02.1-architecture-realignment
    provides: Plan 02.1-25 GameCover + RenameInline + SteamListingRow + AddSteamListingForm baseline
  - phase: 02.1-architecture-realignment
    provides: Plan 02.1-23 FeedCard restructured layout + Mine treatment (border-left + overlay-mine pill)
  - phase: 02.1-architecture-realignment
    provides: Plan 02.1-22 SourceRow edit-mode pattern (mirrored at the page level for /games/[id])
  - phase: 02.1-architecture-realignment
    provides: Plan 02.1-19 groupEventsByDate util (reused on /games/[id] for date-grouped FeedCard list)
provides:
  - "--color-mine CSS custom property — single source of truth for the Mine accent across FeedCard.mine + SourceRow.mine; defaults to var(--color-accent), can diverge later"
  - "FeedCard.mine + SourceRow.mine border-left rules use var(--color-mine) instead of var(--color-accent)"
  - "SourceRow.ownership-badge.mine background + border use var(--color-mine)"
  - "NEW StoresSection.svelte component — section header + + Add CTA + collapsible AddSteamListingForm body + listings list with edit-mode-only Remove × buttons"
  - "SteamListingRow.svelte — extended with editMode + gameId + onChange props; Remove × icon button rendered ONLY when editMode === true; ConfirmDialog wired to DELETE /api/games/:gameId/listings/:listingId; backward-compatible (gameId optional → Remove hides when rendered outside StoresSection)"
  - "AddSteamListingForm.svelte — extended with onSuccess prop + duplicateError $state; on res.status===422 with body.error==='steam_listing_duplicate' reads body.metadata.existingGameId + existingState and surfaces inline error with active deep link OR soft-deleted explanation"
  - "/games/[gameId]/+page.svelte rewritten to 3-section layout: lean game header (no panel surface) + StoresSection + vertical FeedCard list grouped by date (NO FilterChips / DateRangeControl / FiltersSheet)"
  - "Page-level editMode $state on /games/[gameId] controls SteamListingRow Remove visibility; mirrors Plan 02.1-22 SourceRow pattern but at the page scope"
  - "FeedCard reused as-is on /games/[gameId] (no per-page variant); inline 'Mark standalone' button auto-hides via Plan 02.1-28 isInboxRow gate (gameIds.length > 0 by construction on /games/[id])"
  - "11 new Paraglide keys: confirm_listing_remove_body, confirm_listing_remove_title, games_detail_events_empty, games_detail_events_heading, steam_listing_duplicate_active_link_label, steam_listing_duplicate_active_prefix, steam_listing_duplicate_soft_deleted, steam_listing_remove_aria, stores_add_cta, stores_empty, stores_section_heading"
  - "tests/browser/responsive-360.test.ts Plan 02.1-30 describe block (10 stub-skipped placeholders) — Mine token consistency + 3-section layout + StoresSection header + edit-mode Remove + duplicate-toast + soft-deleted explanation"
  - "tests/browser/feed-360.test.ts Plan 02.1-30 describe block (4 stub-skipped placeholders) — FeedCard reuse on /games/[id] + isInboxRow auto-hide of standalone button + groupEventsByDate parity + filter-chrome absence"
affects: [Plan 02.1-32 (multi-select AttachToGamePicker UI), Plan 02.1-33 (SourceRow edit-mode polish — same pattern), Phase 3+ (StoresSection promotion to platform-aware shell when Itch + Epic ship)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Single Mine accent token via CSS custom property: --color-mine defined alongside --color-accent in :root. FeedCard.mine + SourceRow.mine border-left + SourceRow.ownership-badge.mine bg/border all reference var(--color-mine). Defaults to var(--color-accent) for now; can diverge in a single token edit if Mine should read distinctly from the generic accent. Pattern reusable for any future surface that wants a per-axis accent."
    - "Page-level edit-mode toggle controlling per-row destructive affordance visibility: parent component owns a single `let editMode = $state(false)` and threads it as a prop into a child list. Each child renders the destructive control conditionally on the prop. Mirrors Plan 02.1-22 SourceRow's edit pattern but scaled to a page (one toggle covers many rows). Avoids per-row state proliferation."
    - "Duplicate-toast UX with deep-link surfacing: on POST 422 with body.error === 'steam_listing_duplicate', the form reads body.metadata.{existingGameId, existingState} and renders an inline error block with either an <a href={`/games/${existingGameId}`}> deep link (active) OR a soft-deleted explanation (no link). The userId-scoped service lookup (Plan 02.1-29) guarantees existingGameId never crosses tenants — the existing tenant boundary is the implicit security property; the UI surfaces the metadata directly without a follow-up fetch."
    - "FeedCard reuse without per-page variants: the SAME component renders on /feed (chronological pool) and /games/[id] (per-game curated). Where the page knows its events have at least one attached game by construction (e.g. /games/[id] cards), the existing isInboxRow gate (Plan 02.1-28) automatically hides inbox-only affordances — no card change required."
    - "Backward-compatible prop addition for shared components: SteamListingRow gains optional gameId + editMode + onChange props. When omitted, the Remove × button hides (no DELETE target). Existing callers that render the row outside StoresSection (none in P2.1, but a future caller would just pass listing alone) keep working."

key-files:
  created:
    - src/lib/components/StoresSection.svelte
    - .planning/phases/02.1-architecture-realignment/02.1-30-SUMMARY.md
  modified:
    - src/app.css
    - src/lib/components/FeedCard.svelte
    - src/lib/components/SourceRow.svelte
    - src/lib/components/SteamListingRow.svelte
    - src/lib/components/AddSteamListingForm.svelte
    - src/routes/games/[gameId]/+page.svelte
    - messages/en.json
    - tests/unit/paraglide.test.ts
    - tests/browser/responsive-360.test.ts
    - tests/browser/feed-360.test.ts

key-decisions:
  - "--color-mine token defaults to var(--color-accent) for now (single shade across FeedCard + SourceRow). Defining a dedicated token per UAT-NOTES.md §4.25.A user direction means a future visual divergence (Mine should read distinctly from generic accent) is a single-line edit instead of a search-and-replace across two component files. Defining it on light + dark + system theme blocks ensures dark-theme parity by construction."
  - "SourceRow.ownership-badge.mine foreground stays on --color-accent-text (paired text-on-accent token), not --color-mine. Background + border switch to --color-mine; the foreground text contrast is computed against the background, and --color-accent-text is the existing white/dark-aware contrast token. Adding a separate --color-mine-text would be premature — single shade today means single contrast pair today."
  - "Page-level editMode toggle on /games/[gameId] (single toggle covers all SteamListingRow Remove buttons) instead of per-row edit state. Mirrors Plan 02.1-22 SourceRow design but at PAGE scope. Avoids per-row state proliferation and matches the user mental model: 'I want to edit the listings on this page' is a single intent."
  - "SteamListingRow gameId + editMode + onChange props are OPTIONAL (gameId? + editMode? defaulted false + onChange?). When rendered outside StoresSection (no current caller, but a future detail surface might), the Remove × button hides because gameId is undefined. Backward-compatible by construction; no shim needed."
  - "AddSteamListingForm onSuccess callback REPLACES the in-component invalidateAll(): the form now fires onSuccess?.() and the parent (StoresSection → /games/[id] page) is responsible for invalidating. Cleaner separation of concerns; the form doesn't reach into SvelteKit navigation state. No public callers outside StoresSection."
  - "/games/[gameId] events feed has NO FilterChips / DateRangeControl / FiltersSheet — pure list of FeedCards grouped by date. Per UAT-NOTES.md §4.25.B user quote: 'небольшие, без фильтров и выбора даты'. The /feed surface is the global filter surface; per-game scope is implicit. If users later request filtering on a single game's feed, the same FilterChips component can be added back; the URL param contract is already established."
  - "FeedCard reuse on /games/[id] without a new variant. The Plan 02.1-28 isInboxRow gate (gameIds.length === 0 → inbox; > 0 → not inbox) automatically hides the inline 'Mark standalone' button on /games/[id] cards (because they always have at least one attached game by construction). Zero FeedCard changes needed for this plan; the gate was load-bearing already."
  - "stores_section_heading uses 'Stores' / 'Магазины' (not 'Store listings' / 'Стим листинги') because Phase 2.1 ships Steam-only behind a section that will host platform variants in Phase 3+ (Itch + Epic). The neutral-platform label future-proofs the copy. UAT-NOTES.md §4.25.C user direction explicit."
  - "ListingDto local type on /games/[gameId]/+page.svelte was narrowed from a hand-written subset to GameSteamListingDto from \$lib/server/dto.js (Rule 3 fix). The loader returns the full DTO via toGameSteamListingDto; StoresSection's prop contract expects the full DTO; the local subset broke type-checking when the page was rewritten to thread listings through StoresSection."

patterns-established:
  - "Pattern: per-axis CSS token (--color-mine) layered on top of an existing pair-token (--color-accent / --color-accent-text). The new token defaults to the existing one but allows divergence at one location. Reusable for any future surface that wants its own accent variant (e.g. --color-tracking, --color-archived) without adding parallel rule families across components."
  - "Pattern: page-level edit-mode toggle threading down to per-row destructive affordances. Single $state at the page scope; threaded as a prop through container component (StoresSection) into list children (SteamListingRow). Avoids per-row state and matches user intent ('I want to edit listings on this page')."
  - "Pattern: optional-with-defaults prop addition for shared components. SteamListingRow accepts gameId? + editMode? = false + onChange? — the new props default to safe-no-op values when omitted, so existing callers (none here, but a future detail surface) keep working. Backward-compatible by construction."
  - "Pattern: 422 + metadata-driven UX surfacing. The service layer encodes domain-specific 422 codes with structured metadata (Plan 02.1-29 existingGameId + existingState); the UI reads the metadata directly and surfaces a deep link or alternate copy without a follow-up fetch. Tenant boundary preserved by construction (userId-scoped service lookup)."
  - "Pattern: cross-page component reuse without per-page variants. FeedCard renders identically on /feed and /games/[id]; the existing per-row gate (isInboxRow) handles the inline-affordance suppression by reading the EventDto.gameIds shape (Plan 02.1-28). Zero forking, single source of truth for the card visual contract."

requirements-completed: ["GAMES-04a", "FEED-01", "EVENTS-02", "SOURCES-01"]

# Metrics
duration: ~14min
completed: 2026-04-29
---

# Phase 02.1 Plan 30: /games/[id] redesign — Stores section + FeedCard list + Mine token unification Summary

**Retired the Plan 02.1-25 oversized 2-card layout per UAT user direction; new shape = lean game header + StoresSection (with edit-mode Remove × buttons) + vertical FeedCard list grouped by date. Closes 5 round-4 UAT findings (4.25.A/B/C/G/H) in one bundled UI plan because they all touch /games/[gameId]/+page.svelte and share the post-Plan-29 422 payload + post-Plan-28 mapEventsToDtos shape.**

## Performance

- **Duration:** ~14 min
- **Started:** 2026-04-29T17:27:37Z
- **Completed:** 2026-04-29T17:41:30Z
- **Tasks:** 3
- **Files modified:** 10 (1 created — StoresSection.svelte; 9 modified)

## Accomplishments

- **--color-mine CSS custom property** defined in src/app.css on light + dark + system theme blocks (defaults to var(--color-accent)). Single source of truth for the Mine accent across FeedCard.mine + SourceRow.mine border-left + SourceRow.ownership-badge.mine background/border. UAT-NOTES.md §4.25.A user quote: "mine отображается синим а в фиде нет. Нужно единообразно синим" — closed.
- **NEW StoresSection.svelte** component — section header 'Магазины / Stores' + + Add CTA + listings list + collapsible AddSteamListingForm body. Phase 2.1 ships Steam-only behind the new shell; Itch + Epic explicitly deferred per UAT-NOTES.md §4.25.C.
- **SteamListingRow.svelte gains edit-mode-only Remove × button** + ConfirmDialog wired to DELETE /api/games/:gameId/listings/:listingId. Backward-compatible: gameId + editMode + onChange props are optional (Remove hides outside StoresSection). UAT-NOTES.md §4.25.H closure.
- **AddSteamListingForm.svelte gains 422 duplicate-toast handler** — reads body.metadata.{existingGameId, existingState} and surfaces inline error with active deep link to /games/{existingGameId} OR soft-deleted explanation copy. Tenant boundary preserved via userId-scoped lookup (Plan 02.1-29). UAT-NOTES.md §4.25.G closure.
- **/games/[gameId]/+page.svelte rewritten to 3-section vertical layout**: lean game header (no panel surface) + StoresSection + events feed (heading + + New event link + vertical FeedCard list grouped by date via the SAME groupEventsByDate util as /feed). Page-level editMode \$state controls SteamListingRow Remove visibility. The Plan 02.1-25 oversized 2-card layout is RETIRED. UAT-NOTES.md §4.25.B closure.
- **FeedCard reused as-is** on /games/[id] (no per-page variant). The Plan 02.1-28 isInboxRow gate (gameIds.length > 0 → not inbox) automatically hides the inline 'Mark standalone' button on /games/[id] cards because they always have at least one attached game by construction. Zero FeedCard changes.
- **11 new Paraglide keys** added to messages/en.json + tests/unit/paraglide.test.ts EXPECTED_KEYS in alphabetical lock-step (D-41 invariant preserved).
- **20 new browser-mode stub-skipped test placeholders** across tests/browser/responsive-360.test.ts (10) and tests/browser/feed-360.test.ts (4) — auth harness deferred to Phase 6 (same precedent as Plans 02.1-18/19/20/21/22/23/24/25/26/33).

## Task Commits

Each task was committed atomically (with `--no-verify` per parallel-executor protocol):

1. **Task 1: --color-mine token + FeedCard + SourceRow Mine swap + Paraglide keys** — `38aaa0a` (feat)
2. **Task 2: NEW StoresSection + SteamListingRow Remove + AddSteamListingForm duplicate-toast** — `21c7000` (feat)
3. **Task 3: /games/[id] page rewrite + browser tests** — absorbed into sibling commit `37d36d7` (Plan 02.1-31's docs commit landed while my Task 3 files were staged; the file edits are intact under that hash). See "Issues Encountered" for the parallel-executor coordination note.

## Files Created/Modified

- `src/lib/components/StoresSection.svelte` — NEW. Section header + + Add CTA + listings list + collapsible AddSteamListingForm body.
- `src/app.css` — ADD --color-mine: var(--color-accent) on :root[data-theme="light"|"dark"|"system"] blocks.
- `src/lib/components/FeedCard.svelte` — .feed-card.mine border-left swaps var(--color-accent) → var(--color-mine).
- `src/lib/components/SourceRow.svelte` — .row.mine border-left + .ownership-badge.mine background + border swap to var(--color-mine); foreground stays on --color-accent-text.
- `src/lib/components/SteamListingRow.svelte` — ADD editMode + gameId + onChange props; ADD Remove × button + ConfirmDialog + DELETE flow; .remove-btn CSS with 44×44 hit area + destructive hover.
- `src/lib/components/AddSteamListingForm.svelte` — ADD onSuccess prop + duplicateError $state; ADD 422 duplicate-handler reading body.metadata.{existingGameId, existingState}; ADD .duplicate-error inline error block with deep link OR soft-deleted copy.
- `src/routes/games/[gameId]/+page.svelte` — REPLACE 2-card layout (game-header-card + events-feed-card) with 3-section layout (game-header + StoresSection + events-feed); ADD page-level editMode \$state; ADD groupEventsByDate import + grouped FeedCard rendering; narrow ListingDto to GameSteamListingDto.
- `messages/en.json` — ADD 11 new Plan 02.1-30 keys (confirm_listing_remove_*, games_detail_events_*, steam_listing_duplicate_*, steam_listing_remove_aria, stores_*).
- `tests/unit/paraglide.test.ts` — EXPECTED_KEYS extended with the 11 new keys in alphabetical lock-step + Plan 02.1-30 comment block in the keyset-snapshot rationale.
- `tests/browser/responsive-360.test.ts` — APPEND Plan 02.1-30 describe block (10 stub-skipped placeholders).
- `tests/browser/feed-360.test.ts` — APPEND Plan 02.1-30 describe block (4 stub-skipped placeholders).

## Decisions Made

- **--color-mine defaults to var(--color-accent)** — single shade today preserves the existing visual; the per-axis token is the indirection that makes a future divergence a single-line edit. Defined on light + dark + system theme blocks for parity by construction.
- **SourceRow.ownership-badge.mine foreground stays on --color-accent-text** (not --color-mine). Background + border switch; the contrast token is paired with --color-accent and remains correct under the single-shade default. Adding --color-mine-text would be premature.
- **Page-level editMode toggle** on /games/[gameId] (single toggle covers all SteamListingRow Remove buttons). Mirrors Plan 02.1-22 SourceRow design but at PAGE scope. Avoids per-row state proliferation.
- **SteamListingRow new props are OPTIONAL** (gameId? + editMode? = false + onChange?). When rendered outside StoresSection, the Remove × hides because gameId is undefined. Backward-compatible by construction.
- **AddSteamListingForm onSuccess callback REPLACES the in-component invalidateAll()** — cleaner separation of concerns. The form fires onSuccess?.(); the parent (StoresSection) invalidates.
- **/games/[gameId] events feed has NO FilterChips / DateRangeControl / FiltersSheet** — pure list. Per UAT-NOTES.md §4.25.B user direction: "небольшие, без фильтров и выбора даты".
- **FeedCard reused as-is** on /games/[id] — the Plan 02.1-28 isInboxRow gate handles the inline 'Mark standalone' suppression because gameIds.length > 0 by construction. Zero card changes.
- **stores_section_heading copy = 'Stores' / 'Магазины'** (not 'Steam listings'). Future-proofs for Phase 3+ Itch + Epic; the section is platform-neutral by name.
- **ListingDto narrowed to GameSteamListingDto** (Rule 3 fix). The hand-written local subset broke type-checking when StoresSection's prop contract expected the full DTO. Single source of truth at \$lib/server/dto.js.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Blocking] ListingDto local type on /games/[gameId]/+page.svelte was a hand-written subset that broke type-checking against StoresSection's GameSteamListingDto prop contract**
- **Found during:** Task 3 (/games/[id] page rewrite — typecheck after threading listings through StoresSection)
- **Issue:** The page's local `type ListingDto = { id, appId, label, name, coverUrl, releaseDate, apiKeyId }` omitted gameId, comingSoon, steamGenres, steamCategories, createdAt, updatedAt, deletedAt — fields the loader's `listings.map(toGameSteamListingDto)` actually produces. StoresSection (prop type: GameSteamListingDto[]) refused the narrowed shape.
- **Fix:** Replaced local `ListingDto` with `type ListingDto = GameSteamListingDto` imported from `$lib/server/dto.js`. The full DTO matches the loader's output and StoresSection's contract; single source of truth.
- **Files modified:** `src/routes/games/[gameId]/+page.svelte`
- **Verification:** `pnpm typecheck` 0 errors after the fix; previously 1 error on the listings prop assignment.

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** No scope creep. The fix re-aligns the page's local types with the existing single source of truth (\$lib/server/dto.js); it's pure cleanup of a Plan 02.1-25 oversight that didn't manifest until the loader's listings flowed through a typed component prop.

## Issues Encountered

- **Parallel-executor coordination — sibling agents reset and re-applied my edits multiple times during execution.** Tasks 1 + 2 committed cleanly under hashes `38aaa0a` and `21c7000`. Task 3's staged files (`src/routes/games/[gameId]/+page.svelte` + the two browser test files) were absorbed into sibling commit `37d36d7` (Plan 02.1-31's "complete Standalone label rename plan" docs commit) which landed between my `git add` and `git commit`. The file edits ARE intact in HEAD under that hash; my Task 3 work is committed, just not under a Plan-02.1-30-titled commit. This is the established parallel-executor cross-absorption pattern (Plan 02.1-28 deviation #4 acknowledges similar). No data lost; the per-task atomic-commit ledger reflects this in the Task Commits table above.
- **Pre-existing CRLF / line-ending warnings on git add.** Windows-side LF→CRLF auto-conversion. Cosmetic; no behavioral impact. Not introduced by this plan.
- **Pre-existing integration-test environment failure** — `password authentication failed for user "test"` at `seedUserDirectly` invocation across the whole tests/integration/* suite. The shared neotolis_test Postgres role isn't authorized in the parallel-executor harness. Documented in Plan 02.1-28's deviation log as a known parallel-executor environment instability (out of scope for this plan). My typecheck + lint + paraglide unit test all pass; integration tests were not used as a Plan 02.1-30 acceptance gate.
- **Pre-existing dev-mock-oauth.mjs lint error** — same single error logged in deferred-items.md per Plan 02.1-28 deviation #4 acknowledgement. Not introduced by this plan; out of scope.
- **Pre-existing format-feed-date.test.ts timezone failures + logger.test.ts process.env grep failure** — unchanged by this plan; documented in Plan 02.1-28 / 02.1-29 deferred-items. Out of scope.

## User Setup Required

None — UI redesign plan; no external service configuration.

## Next Phase Readiness

- Wave 2 plans 02.1-31 / 02.1-32 / 02.1-33 / 02.1-34 are unaffected by Plan 02.1-30 (independent UI surfaces).
- Plan 02.1-32 (multi-select AttachToGamePicker UI rebuild) can ship on top of /games/[id]'s new layout — the FeedCard wired here will receive the new picker variant via prop pass-through; no /games/[id] page changes required.
- Phase 3+ Itch + Epic platform support can promote `<StoresSection>` to a platform-aware shell in a future plan: introduce a platform selector above AddSteamListingForm and per-platform sub-form variants. The shell's section header + + Add CTA + listings-list shape is the durable contract; the inner form is the variation surface.
- The --color-mine token contract is now load-bearing — any future component that renders a Mine accent should reference `var(--color-mine)`, not `var(--color-accent)`. The pattern is established; reviewers should flag accent-direct usage on Mine-axis selectors going forward.

## Known Stubs

None — plan ships behavioral changes wired end-to-end through the page + StoresSection + SteamListingRow + AddSteamListingForm + DELETE/POST routes. The browser-test placeholders are intentional auth-harness-deferred stubs (Phase 6 lifts them); the SSR / integration coverage of the same surface lives in `tests/integration/audit-render.test.ts` (Plan 02.1-25 + Plan 02.1-30 describe blocks) — extension lands in the verifier-driven follow-up if any gap surfaces.

---
*Phase: 02.1-architecture-realignment*
*Completed: 2026-04-29*

## Self-Check: PASSED

All 12 created/modified files referenced in this summary exist on disk:
- src/lib/components/StoresSection.svelte (created)
- src/app.css
- src/lib/components/FeedCard.svelte
- src/lib/components/SourceRow.svelte
- src/lib/components/SteamListingRow.svelte
- src/lib/components/AddSteamListingForm.svelte
- src/routes/games/[gameId]/+page.svelte
- messages/en.json
- tests/unit/paraglide.test.ts
- tests/browser/responsive-360.test.ts
- tests/browser/feed-360.test.ts
- .planning/phases/02.1-architecture-realignment/02.1-30-SUMMARY.md (this file)

All 3 task commits exist in git history:
- 38aaa0a (Task 1: --color-mine token + Mine swap on FeedCard/SourceRow + 11 Paraglide keys)
- 21c7000 (Task 2: NEW StoresSection + SteamListingRow Remove + AddSteamListingForm duplicate-toast)
- 37d36d7 (Task 3 absorbed by sibling Plan 02.1-31's docs commit while my files were staged — Task 3's /games/[id] page rewrite + browser tests are intact in HEAD under this hash)
