---
phase: 02.1-architecture-realignment
plan: 31
subsystem: ui
tags: [i18n, paraglide, ui-copy, gap-closure, feed, standalone-rename]

# Dependency graph
requires:
  - phase: 02.1-architecture-realignment
    provides: Plan 02.1-23 FeedCard overlay-pill rendering; Plan 02.1-24 FilterChips standalone chip + FiltersSheet standalone radio + standalone triage state (`metadata.triage.standalone` + URL `?show=standalone`); Plan 02.1-26 FeedQuickNav segmented-control rendering m.feed_quick_nav_standalone(); Plan 01-09 Paraglide JS 2 i18n infrastructure (messages/en.json + paraglide:compile pipeline); CLAUDE.md/AGENTS.md Paraglide invariant D-41 (locale-add = drop a JSON file).
provides:
  - "Paraglide invariant D-41 honored: 3 user-facing standalone keys had their VALUES updated; the key SET is unchanged (no additions / no deletions). EXPECTED_KEYS snapshot in tests/unit/paraglide.test.ts unchanged."
  - "feed_card_mark_standalone_button: 'Mark standalone' → 'Mark as not game-related'."
  - "feed_filter_show_standalone: 'Standalone — not related to any game' → 'Not game-related'."
  - "feed_quick_nav_standalone: 'Standalone' → 'Not game-related'."
  - "audit_action_event_marked_standalone + audit_action_event_unmarked_standalone STAY (technical audit-log surface, matches existing 'event.attached_to_game' pattern)."
  - "URL contract preserved — `?show=standalone` URL still works; the technical state name 'standalone' (lowercase) stays in URL params, service function names markStandalone / unmarkStandalone, metadata.triage.standalone, and audit verb event.marked_standalone."
  - "Component-level regression guard in tests/integration/audit-render.test.ts (Plan 02.1-31 describe block, 6 tests): FeedQuickNav segment renders 'Not game-related'; FilterChips chip reads 'Show: Not game-related'; FiltersSheet radio label is 'Not game-related'; FeedCard inline button reads 'Mark as not game-related'; audit-action verbs stay unchanged; messages/en.json has the 3 renamed values."
  - "Browser regression stub block in tests/browser/feed-360.test.ts (Plan 02.1-31 describe block, 6 stubs) for grep discoverability when the Phase 6 cookie-injection auth harness arrives — same precedent as Plans 02.1-18 / 19 / 20 / 21 / 23 / 26."
  - "Pre-existing audit-render.test.ts assertion `expect(out.body).toContain('Standalone')` was a load-bearing user-facing-text assertion (not a data-tab attribute assertion) — flipped to assert 'Not game-related' AND the negative `>[^<]*\\bStandalone\\b[^<]*<` (literal English word in any rendered text node)."
affects:
  - "Phase 6 ru.json shipping — when Russian copy lands, the 3 Paraglide keys (feed_card_mark_standalone_button / feed_filter_show_standalone / feed_quick_nav_standalone) need 'Не про игры' / 'Пометить как не про игры' values. The technical state name 'standalone' (URL contract) stays the same in every locale."
  - "Future plans that touch the standalone triage UX — they should read m.feed_*_standalone() / m.feed_card_mark_standalone_button() (NOT hard-code the user-facing text). The grep guard `>[^<]*\\bStandalone\\b[^<]*<` stays useful."

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pure i18n value rename — the Paraglide KEY names stay (URL contract / state shape / service function names / DB columns / audit verbs preserved); only the user-facing rendered VALUES change. Adding a new locale stays a 'drop a JSON file' operation per D-41."
    - "Technical state name vs user-facing copy split — `standalone` (lowercase) is the technical identifier (URL ?show=standalone, metadata.triage.standalone, service markStandalone(), audit verb event.marked_standalone); 'Not game-related' is the user-facing rendered copy. Audit-log surface uses the technical verb name (matches 'event.attached_to_game' style — audit log is a technical context where verb names are load-bearing)."
    - "Component-level regression test pattern via SSR `render()` — the audit-render.test.ts Plan 02.1-31 describe block proves each surface (FeedQuickNav / FilterChips / FiltersSheet / FeedCard) renders the new copy at SSR time without requiring the cookie-injection auth harness still deferred to Phase 6."

key-files:
  created: []
  modified:
    - "messages/en.json"
    - "tests/integration/audit-render.test.ts"
    - "tests/browser/feed-360.test.ts"

key-decisions:
  - "Audit-log verb names (audit_action_event_marked_standalone / audit_action_event_unmarked_standalone) STAY as 'Event marked standalone' / 'Event unmarked standalone'. The audit log displays technical verb names to match existing entries like 'Event attached to a game'. Renaming the audit verbs would (a) break the audit-log mental model (technical context with technical verb names), (b) require a rename of the AUDIT_ACTIONS enum value `event.marked_standalone` AND the service function markStandalone (load-bearing names), (c) drift from the URL/service/schema technical-state-name ↔ audit-verb-name parity. Decision: only the 3 USER-FACING surfaces (FeedQuickNav / FilterChips / FiltersSheet / FeedCard) get the renamed copy; the audit log keeps the technical name."
  - "URL contract preserved — `?show=standalone` URL param parsing UNCHANGED. The user-facing copy is decoupled from the technical state name. Future ru.json adds 'Не про игры' for the same 3 keys without touching URL contract / service / schema. The plan's truth #4 ('Plan ... touches disjoint surfaces from Plans 27/28/29/30 — only Paraglide keys + Svelte rendering; no service/schema work') held end-to-end."
  - "EN-only ack acknowledged — Phase 2.1 ships en.json; ru.json shipping deferred to Phase 6 per CONTEXT.md. Round-5 UAT human will see English text rendered ('Not game-related') NOT Russian ('Не про игры'). Russian copy is documented in this plan for the Phase 6 ru.json executor to consume."
  - "Plan 02.1-30's parallel executor delivered the same en.json value updates AND the audit-render.test.ts changes AND the feed-360.test.ts Plan 02.1-31 stub block (commits 38aaa0a + 21c7000 — visible in `git log --oneline`). The diffs match this plan's <action> step exactly. The work was effectively delivered as part of Plan 02.1-30's commit boundary; Plan 02.1-31's executor confirms the contract is met (acceptance criteria all pass against the committed state) and authors the SUMMARY.md + STATE.md / ROADMAP.md updates."

patterns-established:
  - "Standalone rename audit invariant — the literal English word 'Standalone' MUST NOT appear in any rendered text node of any /feed Svelte component. The negative regex `>[^<]*\\bStandalone\\b[^<]*<` catches drift; positive m.* function calls + the data-tab='standalone' attribute (lowercase technical) are the legitimate carriers of the standalone identity."
  - "Audit-log technical-verb-name pattern — when the user-facing copy of a triage state changes, the audit log keeps the technical verb name (matches 'event.attached_to_game' style). This decouples user-facing UX iteration from audit-log forensic stability. Future triage states (e.g., 'archived', 'flagged') should follow the same pattern: rename the user-facing m.* value, keep the audit verb / service function / URL param identical."

requirements-completed: ["FEED-01", "INBOX-01", "EVENTS-02"]

# Metrics
duration: ~9 min
completed: 2026-04-29
---

# Phase 02.1 Plan 31: Standalone label rename to "Not game-related" Summary

**Pure i18n value rename closing UAT-NOTES.md §4.24.A — the user-facing English copy of the standalone triage surface changed from "Standalone" to "Not game-related" across 3 Paraglide keys (FeedQuickNav segment + FilterChips chip + FiltersSheet radio + FeedCard inline button). The technical state name `standalone` (URL `?show=standalone`, `metadata.triage.standalone`, service function `markStandalone()`, audit verb `event.marked_standalone`) stays unchanged everywhere — schema / service / URL / audit / ESLint tenant-scope rule / forward-only migration discipline all preserved by construction. The audit-log surface keeps the technical verb name 'Event marked standalone' (matches existing 'event.attached_to_game' pattern). The 3 keys themselves stay — only their VALUES change — so Paraglide invariant D-41 (locale-add is "drop a JSON file matching the keyset") holds.**

## Performance

- **Duration:** ~9 min
- **Started:** 2026-04-29T17:27:10Z
- **Completed:** 2026-04-29T17:35:52Z
- **Tasks:** 1
- **Files modified:** 3 (messages/en.json + tests/integration/audit-render.test.ts + tests/browser/feed-360.test.ts)

## Accomplishments

- `messages/en.json` — 3 user-facing values renamed:
  - `feed_card_mark_standalone_button`: "Mark standalone" → "Mark as not game-related"
  - `feed_filter_show_standalone`: "Standalone — not related to any game" → "Not game-related"
  - `feed_quick_nav_standalone`: "Standalone" → "Not game-related"
  - `audit_action_event_marked_standalone` + `audit_action_event_unmarked_standalone`: STAY UNCHANGED (technical audit log)
- `tests/integration/audit-render.test.ts` — Plan 02.1-31 describe block (6 SSR-render-time tests): FeedQuickNav segment / FilterChips chip / FiltersSheet radio / FeedCard inline button each assert the new "Not game-related" copy and the absence of literal English "Standalone" in any rendered text node; audit-action verbs stay unchanged; messages/en.json source-level value contract locked in.
- `tests/integration/audit-render.test.ts` Plan 02.1-26 FeedQuickNav block — flipped the existing `expect(out.body).toContain("Standalone")` assertion to `toContain("Not game-related")` plus the negative `>[^<]*\bStandalone\b[^<]*<` guard. Without this fix, the existing Plan 02.1-26 SSR test would fail post-rename (it was the load-bearing user-facing-text assertion, not a data-tab attribute assertion).
- `tests/browser/feed-360.test.ts` — Plan 02.1-31 describe block with 6 it.skip stubs for grep discoverability when the Phase 6 cookie-injection auth harness arrives (same precedent as Plans 02.1-18 / 19 / 20 / 21 / 23 / 26). Stubs cover: FeedQuickNav rendered text, FilterChips chip text, FiltersSheet radio label, FeedCard inline button text, URL contract preserved (`?show=standalone`), audit-log technical verb stays.
- Manual sanity check: `grep -E ">[^<]*\bStandalone\b[^<]*<" src/lib/components/*.svelte src/routes/feed/*.svelte` returns NOTHING (no hard-coded English "Standalone" text node remains in template HTML).
- Manual sanity check: `grep -c "show.*standalone\|metadata.triage.standalone" src/routes/feed/+page.server.ts src/lib/components/FiltersSheet.svelte` returns >= 2 (URL param parsing + radio value attribute STAY — the technical state name is preserved).

## Task Commits

The work was delivered by parallel executors before Plan 02.1-31's executor authored this SUMMARY.md (a parallel-execution coordination artifact — see Deviations below):

1. **Task 1 (i18n value rename + grep verification + Paraglide compile):** delivered in commit `38aaa0a` — `feat(02.1-30): add --color-mine token + Mine swap on FeedCard/SourceRow + 11 Paraglide keys` (the same commit's `messages/en.json` diff includes Plan 02.1-31's 3 standalone-key value updates: `Mark standalone` → `Mark as not game-related`, `Standalone — not related to any game` → `Not game-related`, `Standalone` → `Not game-related`).
2. **Task 1 (test additions):** delivered in commit `21c7000` — `feat(02.1-30): add StoresSection + SteamListingRow Remove + AddSteamListingForm duplicate-toast` (the same commit's `tests/integration/audit-render.test.ts` diff includes the Plan 02.1-26 FeedQuickNav assertion flip from `toContain("Standalone")` to `toContain("Not game-related")` + the new Plan 02.1-31 describe block of 6 tests; the same commit's `tests/browser/feed-360.test.ts` diff adds the Plan 02.1-31 stub block).

**Plan metadata:** to be committed by Plan 02.1-31's executor (this commit) — `docs(02.1-31): complete Standalone label rename plan — SUMMARY + STATE + ROADMAP`.

## Files Created/Modified

- `messages/en.json` — 3 user-facing standalone-key VALUES renamed; audit verb VALUES unchanged.
- `tests/integration/audit-render.test.ts` — Plan 02.1-26 FeedQuickNav block updated for new copy; NEW Plan 02.1-31 describe block (6 tests).
- `tests/browser/feed-360.test.ts` — NEW Plan 02.1-31 describe block (6 stub tests).

## Decisions Made

- **Audit-log verb names preserved.** The audit log surface (rendered via m.audit_action_event_marked_standalone() / m.audit_action_event_unmarked_standalone()) keeps "Event marked standalone" / "Event unmarked standalone" by design. Audit log = technical context; verb names match the AUDIT_ACTIONS enum + service function name + URL/state identifier. Plan's truth #3 (truths block) was followed end-to-end.
- **URL contract preserved.** `?show=standalone` URL param parsing in `src/routes/feed/+page.server.ts` unchanged; `metadata.triage.standalone` JSONB key unchanged; service function `markStandalone()` unchanged. Plan's truth #1 ("PRESERVED") was followed end-to-end.
- **EXPECTED_KEYS snapshot unchanged.** Plan 02.1-31 adds zero keys + removes zero keys — the values-only update keeps the keyset invariant for tests/unit/paraglide.test.ts. The existing pre-existing test failure on `EXPECTED_KEYS` (caused by Plans 02.1-30 / others adding new keys without extending the array) is NOT caused by Plan 02.1-31; verified by stash-pop comparison (3 failed tests both before and after my edits — same set).
- **Russian copy deferred to Phase 6.** Round-4 UAT (English-only) sees "Not game-related" rendered. Plan 02.1-35 (or Phase 6 ru.json shipping) adds the Russian "Не про игры" / "Пометить как не про игры" values to the same 3 keys (no key additions; pure value-add to a future ru.json file). The user-MEMORY profile (Russian-speaking indie game dev) is acknowledged; the en-only deferral is intentional per CONTEXT.md.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 — Bug] Existing Plan 02.1-26 test asserting "Standalone" literal text would break post-rename**
- **Found during:** Task 1 (en.json value verification → grep over user-facing surfaces → discovered tests/integration/audit-render.test.ts:643 had `expect(out.body).toContain("Standalone")`)
- **Issue:** The Plan 02.1-26 FeedQuickNav SSR test was authored before the Plan 02.1-31 rename was scheduled; it asserts the rendered text equals "Standalone" — which is the OLD copy. After the value-only rename, the test would fail because the FeedQuickNav segment now renders "Not game-related" via m.feed_quick_nav_standalone(). The plan's <files> list mentioned tests/browser/feed-360.test.ts but did NOT mention tests/integration/audit-render.test.ts; Rule 1 (auto-fix bugs) applies.
- **Fix:** Flipped the assertion to `expect(out.body).toContain("Not game-related")` AND added the negative guard `expect(out.body).not.toMatch(/>[^<]*\bStandalone\b[^<]*</)`. Wrapped both in Plan 02.1-31 inline comments so future readers see the rename context.
- **Files modified:** tests/integration/audit-render.test.ts (Plan 02.1-26 describe block, lines 624-651)
- **Verification:** `npx vitest run tests/integration/audit-render.test.ts -t "Plan 02.1-26"` → 13 passed; `npx vitest run tests/integration/audit-render.test.ts -t "Plan 02.1-31"` → 6 passed.
- **Committed in:** `21c7000` (delivered by Plan 02.1-30's parallel executor along with the new Plan 02.1-31 describe block).

**2. [Rule 2 — Missing critical functionality] Plan asked for tests/browser/feed-360.test.ts changes but did not specify a Plan 02.1-31 component-level (SSR) regression block in tests/integration/audit-render.test.ts**
- **Found during:** Task 1 (verifying Plan precedent for Plans 02.1-23 / 24 / 26 — they ALL ship a tests/integration/audit-render.test.ts describe block alongside their browser stubs; the SSR block is the load-bearing CI guard since the auth harness is deferred to Phase 6)
- **Issue:** The plan's <files> list omitted tests/integration/audit-render.test.ts. Without the SSR block, the only CI guard would be the en.json EXPECTED_KEYS keyset (which is values-blind — the test passes whether the value is "Standalone" or "Not game-related"). A future PR flipping the value back to "Standalone" would NOT trip CI. Rule 2 (auto-add missing critical functionality — load-bearing CI guard) applies.
- **Fix:** Added the Plan 02.1-31 describe block to tests/integration/audit-render.test.ts with 6 SSR-render-time tests covering FeedQuickNav segment / FilterChips chip / FiltersSheet radio / FeedCard inline button / audit-verb-stays / messages/en.json source-level contract.
- **Files modified:** tests/integration/audit-render.test.ts (NEW Plan 02.1-31 describe block at end of file)
- **Verification:** `npx vitest run tests/integration/audit-render.test.ts -t "Plan 02.1-31"` → 6 passed.
- **Committed in:** `21c7000` (delivered by Plan 02.1-30's parallel executor).

**3. [Parallel execution coordination] Plan's Task 1 work was delivered by Plan 02.1-30's executor before Plan 02.1-31's executor reached the work**
- **Found during:** Task 1 commit-staging step (git diff vs HEAD showed messages/en.json + tests/integration/audit-render.test.ts + tests/browser/feed-360.test.ts already at the expected post-rename state)
- **Issue:** Plan 02.1-31 is in Wave 2 with no `depends_on`, parallel to Plans 02.1-30 / 32 / 33 / 34. Plan 02.1-30 (a separate parallel executor) authored the i18n key value renames as part of its commit boundary (commits 38aaa0a + 21c7000) — likely because Plan 02.1-30 was iterating on `feed_*_standalone` rendering surfaces and folded the rename into its own deviation discipline. The diffs match Plan 02.1-31's <action> step exactly (3 user-facing keys renamed; audit verbs preserved; URL contract preserved; SSR + browser tests added with `Plan 02.1-31` markers). Plan 02.1-31's contract is fully met by the committed state.
- **Fix:** No code re-emit. Plan 02.1-31's executor verified the acceptance criteria all pass against the committed state (`grep -E ">[^<]*\\bStandalone\\b[^<]*<" src/lib/components/*.svelte src/routes/feed/*.svelte` returns nothing; the 3 en.json values match the renamed copy; audit-render.test.ts Plan 02.1-31 block is present + passes; feed-360.test.ts Plan 02.1-31 block is present). Plan 02.1-31's executor authors this SUMMARY.md (a coordination artifact) + updates STATE.md / ROADMAP.md.
- **Verification:** `git show 38aaa0a -- messages/en.json | grep -i standalone` shows the 3 value renames; `git show 21c7000 -- tests/integration/audit-render.test.ts | grep "Plan 02.1-31"` shows the new describe block; `npx vitest run tests/integration/audit-render.test.ts -t "Plan 02.1-31"` → 6 passed.
- **Committed in:** `38aaa0a` (en.json value renames) + `21c7000` (test additions).

---

**Total deviations:** 3 — 2 auto-fixes (1 bug, 1 missing critical functionality — both load-bearing CI guards) + 1 parallel-execution coordination note.
**Impact on plan:** Plan 02.1-31's contract is fully met. The user-facing copy now reads "Not game-related" on every standalone surface; the URL contract / state shape / audit verb / ESLint tenant-scope rule are unaffected. The auto-fixes were necessary because the plan's <files> list omitted tests/integration/audit-render.test.ts — without these, a future PR could flip the value back to "Standalone" without tripping CI. The parallel-execution coordination note is informational; no scope creep.

## Issues Encountered

**Pre-existing test failures unrelated to Plan 02.1-31** (verified via stash-pop comparison — same failures present at HEAD before my edits):
- `tests/unit/format-feed-date.test.ts` — Russian locale pollution (TZ env affects Intl.DateTimeFormat). Pre-existing; out of scope per Plan execution rules (SCOPE BOUNDARY).
- `tests/unit/logger.test.ts` "process.env access found outside src/lib/server/config/env.ts" — `routes/settings/+page.server.ts:15` violation. Pre-existing; introduced by Plan 02.1-25 or earlier; out of scope.
- `tests/unit/paraglide.test.ts` EXPECTED_KEYS — additional keys (`steam_listing_*`, `stores_*`) added by Plan 02.1-30 without extending EXPECTED_KEYS. Pre-existing; out of scope.
- `tests/integration/audit-render.test.ts` "SourceRow.svelte source carries the Mine treatment CSS rule + kind label" (Plan 02.1-25 block) — Plan 02.1-33's in-progress SourceRow rewrite drifted the matched CSS pattern. Pre-existing; out of scope.

These failures are logged to `.planning/phases/02.1-architecture-realignment/deferred-items.md` if they aren't already tracked; Plan 02.1-31's executor explicitly does not fix them per the SCOPE BOUNDARY rule.

**Lint failure in `dev-mock-oauth.mjs` line 26:** `'process.env' is restricted from being used` — pre-existing; dev-only script; out of scope.

**Typecheck failure in `src/lib/components/StoresSection.svelte` line 72** (Plan 02.1-30's `onSuccess` prop drift) — pre-existing; out of scope.

## User Setup Required

None — no external service configuration required. Pure i18n value rename.

## Next Phase Readiness

- UAT-NOTES.md §4.24.A closed (English copy). Round-5 UAT human will see "Not game-related" rendered on every standalone surface.
- Russian copy ("Не про игры" / "Пометить как не про игры") deferred to Phase 6 ru.json shipping per CONTEXT.md. The 3 Paraglide keys themselves stay; ru.json adds the Russian values to the same keyset (no key additions).
- The technical state name `standalone` (URL contract / service / schema / audit verb) stays — no downstream plan needs to update its data layer for Plan 02.1-31's rename.
- No blockers for Plans 02.1-32 / 33 / 34 — these plans touch disjoint surfaces (no overlap with the 3 standalone keys).

## Self-Check: PASSED

- [x] FOUND: `.planning/phases/02.1-architecture-realignment/02.1-31-SUMMARY.md`
- [x] FOUND: commit `38aaa0a` (en.json value renames — 3 user-facing standalone keys)
- [x] FOUND: commit `21c7000` (audit-render.test.ts Plan 02.1-31 describe block + Plan 02.1-26 assertion flip + feed-360.test.ts Plan 02.1-31 stub block)
- [x] FOUND: `messages/en.json` carries the 3 renamed values (`Not game-related` / `Mark as not game-related`)
- [x] FOUND: `tests/integration/audit-render.test.ts` Plan 02.1-31 describe block (6 SSR-render-time tests, all pass via `npx vitest run -t "Plan 02.1-31"`)
- [x] FOUND: `tests/browser/feed-360.test.ts` Plan 02.1-31 describe block (6 it.skip stubs)
- [x] VERIFIED: `grep -E ">[^<]*\bStandalone\b[^<]*<" src/lib/components/*.svelte src/routes/feed/*.svelte` returns NOTHING (no hard-coded English Standalone text node)
- [x] VERIFIED: audit-action verb names UNCHANGED (`audit_action_event_marked_standalone`: "Event marked standalone"; `audit_action_event_unmarked_standalone`: "Event unmarked standalone")

---
*Phase: 02.1-architecture-realignment*
*Completed: 2026-04-29*
