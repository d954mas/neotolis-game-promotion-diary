---
phase: 02.1-architecture-realignment
plan: 25
type: execute
wave: 2
depends_on: ["02.1-22", "02.1-23"]
files_modified:
  - src/lib/components/SourceRow.svelte
  - src/lib/components/PageHeader.svelte
  - src/lib/components/GameCover.svelte
  - src/lib/components/SteamListingRow.svelte
  - src/routes/sources/+page.svelte
  - src/routes/feed/+page.svelte
  - src/routes/games/+page.svelte
  - src/routes/games/[gameId]/+page.svelte
  - src/lib/server/services/game-steam-listings.ts
  - src/lib/server/dto.ts
  - src/lib/server/db/schema/game-steam-listings.ts
  - drizzle/0004_add_steam_listing_name.sql
  - drizzle/meta/_journal.json
  - drizzle/meta/0004_snapshot.json
  - tests/integration/game-listings.test.ts
  - tests/browser/responsive-360.test.ts
  - tests/browser/feed-360.test.ts
  - messages/en.json
  - tests/unit/paraglide.test.ts
autonomous: true
gap_closure: true
requirements: ["SOURCES-01", "SOURCES-02", "GAMES-04a", "EVENTS-02"]

must_haves:
  truths:
    - "SourceRow.svelte adopts the same Mine treatment as FeedCard (Plan 02.1-23). When source.isOwnedByMe === true, the row gains border-left: 4px solid var(--color-accent) AND keeps the existing m.sources_owned_by_me() pill (visually upgraded to overlay-style — accent background, white text). UAT-NOTES.md §2.1-redesign user quote: 'Тут нужно как в feed сделать для mine'."
    - "SourceRow.svelte renders kind as ICON + TEXT, not just icon. UAT-NOTES.md §2.1-redesign user quote: 'И еще писать тип и иконку типа'. New Paraglide keys per source kind (5 total: source_kind_label_youtube_channel / reddit_account / twitter_account / telegram_channel / discord_server). The SourceKindIcon and the kind text render together in a .kind-tag span (mirror FeedCard overlay-kind pattern)."
    - "/games/[id] page restructures into 'GAME HEADER CARD' + 'EVENTS FEED' two-section layout per UAT-NOTES.md §3.2-redesign ASCII mockup. GAME HEADER CARD: <GameCover>, <RenameInline>, metadata (releaseDate / releaseTba / tags / notes), Steam listings as <SteamListingRow> children, <AddSteamListingForm>. EVENTS FEED: existing month-grouped <FeedCard> cards. Both wrapped in panel cards with background var(--color-surface), 1px border, 4px radius."
    - "<GameCover> NEW component. Round-3 rule: if any attached Steam listing has coverUrl (already populated by Plan 02-04 fetchSteamAppDetails — header_image), render the FIRST listing's coverUrl as <img> with referrerpolicy='no-referrer'; otherwise render gradient placeholder + game initials. Manual upload + itch fallback DEFERRED to Phase 3+ (UAT-NOTES.md §9.1-backlog) — TODO comment in source documents the deferral."
    - "<SteamListingRow> NEW component replaces inline <li class='listing'> on /games/[gameId]/+page.svelte. Renders Steam name (listing.name OR fallback 'App {appId}'), 'Open on Steam' link to https://store.steampowered.com/app/{appId}/, release date, label. UAT-NOTES.md §3.3-polish user quote: 'Хотелось бы видеть еще название и способ перейти в стим'."
    - "Steam listing.name persisted to DB. fetchSteamAppDetails already returns name (Plan 02-04). Forward-only migration drizzle/0004_add_steam_listing_name.sql adds nullable `name TEXT` column to game_steam_listings; addSteamListing INSERT writes meta?.name; toGameSteamListingDto projects it; existing rows keep NULL (SteamListingRow renders 'App {appId}' fallback). NO new external fetch — name was already in raw_appdetails. NO data backfill (acceptable for indie-budget round-3; user can remove + re-add to refresh)."
    - "<PageHeader> NEW shared component for /feed, /games, /sources. Layout: display: flex; gap: var(--space-md); align-items: center; — title + CTA on the LEFT (no justify-content: space-between). UAT-NOTES.md §3.1-polish user quote: 'Хочется кнопку после заголовка'. Rule of three earns the abstraction (3 list pages share the pattern). Props: title: string, cta?: { href?: string; label: string; onClick?: () => void } — the optional onClick supports /games' inline-form-toggle case while href supports the standard navigation case."
    - "Privacy & multi-tenancy invariants preserved (CLAUDE.md/AGENTS.md): Steam appdetails fetches stay server-side; img tags use referrerpolicy='no-referrer'; new game_steam_listings.name column is non-secret (public game title); toGameSteamListingDto extends to project name; no new env reads; no audit verbs added; no service-layer userId discipline drift (addSteamListing already userId-first); cross-tenant 404 holds; integration test asserts userB cannot read userA's game listings (existing assertion auto-extends)."
    - "Forward-only migration discipline preserved (DV-2): drizzle/0004_add_steam_listing_name.sql is the next idx after Plan 02.1-24's 0003. ALTER TABLE game_steam_listings ADD COLUMN IF NOT EXISTS name TEXT; idempotent."
    - "Paraglide invariant D-41: 6 new keys (5 source_kind_label_* + 1 steam_listing_open_link_label) added to messages/en.json AND tests/unit/paraglide.test.ts EXPECTED_KEYS in same commit."
    - "Browser regression tests cover: PageHeader inline at 360px on all 3 list pages; SourceRow border-left accent when isOwnedByMe; /games/[id] two-card layout; SteamListingRow Steam name + Open-on-Steam link href; /games/[id] GameCover renders Steam header_image when listing has coverUrl OR placeholder otherwise."
  artifacts:
    - path: "src/lib/components/SourceRow.svelte"
      provides: "class:mine={source.isOwnedByMe} on root; border-left CSS rule; kind icon+text label via new Paraglide keys; ownership-badge pill upgraded to overlay-mine visual style."
      contains: "border-left"
    - path: "src/lib/components/PageHeader.svelte"
      provides: "NEW component — flex row title+CTA on left. Props: title: string, cta?: { href?: string; label: string; onClick?: () => void }."
      contains: "flex"
    - path: "src/lib/components/GameCover.svelte"
      provides: "NEW component — Steam coverUrl-based <img> when present, else gradient placeholder + initials. TODO Phase 3+ marker for manual upload + itch."
      contains: "TODO Phase 3+"
    - path: "src/lib/components/SteamListingRow.svelte"
      provides: "NEW component — Steam name + 'Open on Steam' link to https://store.steampowered.com/app/{appId}/."
      contains: "store.steampowered.com/app/"
    - path: "src/routes/sources/+page.svelte"
      provides: "Inline header replaced with <PageHeader title='Data sources' cta={{ href: '/sources/new', label: m.sources_cta_new_source() }} />."
      contains: "PageHeader"
    - path: "src/routes/feed/+page.svelte"
      provides: "Inline header replaced with <PageHeader title='Feed' cta={{ href: '/events/new', label: m.feed_cta_add_event() }} />."
      contains: "PageHeader"
    - path: "src/routes/games/+page.svelte"
      provides: "Inline header replaced with <PageHeader title='Games' cta={{ onClick: () => showForm = true, label: '+ New game' }} />. Inline form behavior preserved."
      contains: "PageHeader"
    - path: "src/routes/games/[gameId]/+page.svelte"
      provides: "Restructured into 2 panels — GAME HEADER CARD (GameCover + RenameInline + metadata + Steam listings as SteamListingRow children + AddSteamListingForm) + EVENTS FEED (existing month-grouped FeedCards). Both styled as panel cards."
      contains: "GameCover"
    - path: "src/lib/server/services/game-steam-listings.ts"
      provides: "addSteamListing INSERT extended with name: meta?.name ?? null."
      contains: "name: meta?.name"
    - path: "src/lib/server/dto.ts"
      provides: "GameSteamListingDto.name: string | null + toGameSteamListingDto projects it."
      contains: "name: string | null"
    - path: "src/lib/server/db/schema/game-steam-listings.ts"
      provides: "Adds `name: text('name')` column (nullable)."
      contains: "name: text"
    - path: "drizzle/0004_add_steam_listing_name.sql"
      provides: "ALTER TABLE game_steam_listings ADD COLUMN IF NOT EXISTS name TEXT;"
      contains: "name TEXT"
    - path: "drizzle/meta/_journal.json"
      provides: "New entry: idx=4, tag=0004_add_steam_listing_name."
      contains: "0004_add_steam_listing_name"
    - path: "drizzle/meta/0004_snapshot.json"
      provides: "Drizzle-generated snapshot with the new name column."
      contains: "name"
    - path: "tests/integration/game-listings.test.ts"
      provides: "Plan 02.1-25 describe block: addSteamListing populates name on success; null on Steam-down; DTO projects it; cross-tenant 404 unchanged."
      contains: "Plan 02.1-25"
    - path: "tests/browser/responsive-360.test.ts"
      provides: "Plan 02.1-25 describe block: PageHeader inline at 360px on /feed, /games, /sources; SourceRow Mine border-left; /games/[id] two-card layout."
      contains: "Plan 02.1-25"
    - path: "tests/browser/feed-360.test.ts"
      provides: "Plan 02.1-25 describe block: SteamListingRow Steam name + Open-on-Steam href; GameCover img vs placeholder."
      contains: "Plan 02.1-25"
    - path: "messages/en.json"
      provides: "ADD 6 keys (alphabetical): source_kind_label_discord_server, source_kind_label_reddit_account, source_kind_label_telegram_channel, source_kind_label_twitter_account, source_kind_label_youtube_channel, steam_listing_open_link_label."
      contains: "source_kind_label_youtube_channel"
    - path: "tests/unit/paraglide.test.ts"
      provides: "EXPECTED_KEYS extended with 6 new keys."
      contains: "source_kind_label_youtube_channel"
  key_links:
    - from: "Steam appdetails fetch (already returns name)"
      to: "game_steam_listings.name persistence"
      via: "addSteamListing INSERT body adds name: meta?.name ?? null; new column added via 0004 migration"
      pattern: "name: meta?.name"
    - from: "GameSteamListingDto.name"
      to: "<SteamListingRow> rendered name"
      via: "listing.name ?? `App ${listing.appId}` — graceful fallback for legacy NULL rows"
      pattern: "listing.name"
    - from: "SteamListingRow appId"
      to: "Open-on-Steam link"
      via: "https://store.steampowered.com/app/${listing.appId}/ — public URL, no auth"
      pattern: "store.steampowered.com/app/"
    - from: "PageHeader on 3 list pages"
      to: "Inline title+CTA on the left"
      via: "Shared component replaces 3 inline header blocks; rule-of-three earns the abstraction"
      pattern: "PageHeader"
    - from: "source.isOwnedByMe"
      to: "SourceRow Mine treatment"
      via: "class:mine on root; border-left CSS rule mirrors FeedCard pattern"
      pattern: "class:mine"
    - from: "First Steam listing with coverUrl"
      to: "GameCover renders <img>"
      via: "$derived coverSrc iterates listings; first non-null coverUrl wins; otherwise gradient placeholder"
      pattern: "coverUrl"
---

<objective>
Card visual language consistency pass. Closes 4 round-3 findings: 2.1-redesign (SourceRow), 3.1-polish (page-header CTA-next-to-title), 3.2-redesign (/games/[id] card layout), 3.3-polish (Steam listing name + Open link). Wave 2 because it depends on Plan 02.1-22 (auto_import edit-gate landed first) AND Plan 02.1-23 (Mine treatment patterns established on FeedCard — this plan reuses border-left accent + overlay-mine pill style).

Output:
- SourceRow Mine treatment + kind icon+text.
- NEW <PageHeader>, <GameCover>, <SteamListingRow> components.
- /games/[id] restructured into 2-card layout.
- Steam listing.name persisted via forward-only 0004 migration; addSteamListing populates; DTO projects; SteamListingRow renders name OR fallback.
- 6 new Paraglide keys with EXPECTED_KEYS update.
- Browser + integration test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-architecture-realignment/02.1-CONTEXT.md
@.planning/phases/02.1-architecture-realignment/02.1-10-UAT-NOTES.md
@.planning/phases/02.1-architecture-realignment/02.1-22-PLAN.md
@.planning/phases/02.1-architecture-realignment/02.1-23-PLAN.md
@CLAUDE.md
@AGENTS.md

<interfaces>
<!-- Existing surfaces this plan reshapes. -->

From src/lib/server/integrations/steam-api.ts (existing — name already returned):
```typescript
export interface SteamAppDetails {
  appId: number;
  name: string;             // ALREADY populated; just needs persistence
  coverUrl: string | null;  // header_image
  releaseDate: string | null;
  comingSoon: boolean;
  // ...
}
```

From src/lib/server/services/game-steam-listings.ts addSteamListing (single-line addition):
```typescript
const [row] = await db.insert(gameSteamListings).values({
  userId,
  gameId: input.gameId,
  appId: input.appId,
  label: input.label ?? "",
  name: meta?.name ?? null,   // NEW Plan 02.1-25
  coverUrl: meta?.coverUrl ?? null,
  // ... rest unchanged
}).returning();
```

From src/lib/server/dto.ts (extend GameSteamListingDto):
```typescript
export interface GameSteamListingDto {
  // ...
  label: string;
  name: string | null;        // NEW Plan 02.1-25
  coverUrl: string | null;
  // ...
}

export function toGameSteamListingDto(r: SteamListingRow): GameSteamListingDto {
  return {
    // ...
    label: r.label,
    name: r.name,             // NEW
    coverUrl: r.coverUrl,
    // ...
  };
}
```

PageHeader.svelte (NEW — small component):
```svelte
<script lang="ts">
  let { title, cta }: {
    title: string;
    cta?: { href?: string; label: string; onClick?: () => void };
  } = $props();
</script>
<header class="page-header">
  <h1>{title}</h1>
  {#if cta}
    {#if cta.onClick}
      <button type="button" class="cta" onclick={cta.onClick}>{cta.label}</button>
    {:else if cta.href}
      <a href={cta.href} class="cta">{cta.label}</a>
    {/if}
  {/if}
</header>
<style>
  .page-header { display: flex; gap: var(--space-md); align-items: center; flex-wrap: wrap; }
  h1 { margin: 0; font-size: var(--font-size-heading); font-weight: var(--font-weight-semibold); }
  .cta {
    display: inline-flex; align-items: center; min-height: 44px; padding: 0 var(--space-md);
    background: var(--color-accent); color: var(--color-accent-text, #fff);
    border: none; border-radius: 4px; font-size: var(--font-size-body);
    font-weight: var(--font-weight-semibold); text-decoration: none; white-space: nowrap; cursor: pointer;
  }
</style>
```

GameCover.svelte (NEW): renders <img> from FIRST listing.coverUrl, OR gradient placeholder with game-title initials. Aspect ratio 460/215 (Steam capsule). TODO comment for Phase 3+ manual upload + itch fallback per UAT-NOTES.md §9.1-backlog.

SteamListingRow.svelte (NEW): renders { listing.name ?? `App ${listing.appId}` } + label + releaseDate + <a href={`https://store.steampowered.com/app/${appId}/`} target="_blank" rel="noopener noreferrer">Open on Steam</a>.

SourceRow Mine treatment (mirrors FeedCard from Plan 02.1-23): class:mine on root .row; CSS .row.mine { border-left: 4px solid var(--color-accent); }; existing ownership-badge pill upgraded to overlay-mine style when isOwnedByMe.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Forward-only migration (game_steam_listings.name) + addSteamListing.name + DTO projection</name>
  <files>
    drizzle/0004_add_steam_listing_name.sql,
    drizzle/meta/_journal.json,
    drizzle/meta/0004_snapshot.json,
    src/lib/server/db/schema/game-steam-listings.ts,
    src/lib/server/services/game-steam-listings.ts,
    src/lib/server/dto.ts,
    tests/integration/game-listings.test.ts
  </files>
  <read_first>
    - .planning/phases/02.1-architecture-realignment/02.1-10-UAT-NOTES.md (§3.3-polish — Steam name + link user quote)
    - src/lib/server/db/schema/game-steam-listings.ts (current schema)
    - src/lib/server/services/game-steam-listings.ts (existing addSteamListing INSERT body)
    - src/lib/server/integrations/steam-api.ts (confirm SteamAppDetails.name already populated)
    - src/lib/server/dto.ts toGameSteamListingDto (existing — extend with name)
    - drizzle/meta/_journal.json (next idx after 0003)
    - tests/integration/game-listings.test.ts (existing pattern — append Plan 02.1-25 block)
    - CLAUDE.md (Privacy invariants 5 + 6 — DTO discipline; no env reads); AGENTS.md (forward-only migrations)
  </read_first>
  <behavior>
    - Test 1 (Plan 02.1-25 game-listings): addSteamListing(userId, { gameId, appId: 620 }) with mocked Steam returning {name: 'Portal 2', ...} → row.name === 'Portal 2'; toGameSteamListingDto projects { name: 'Portal 2', ... }.
    - Test 2: addSteamListing when fetchSteamAppDetails returns null (Steam down) → row.name === null; DTO projects { name: null }.
    - Test 3: existing P3 DTO discipline tests still pass — name projected; userId still NOT in output.
  </behavior>
  <action>
    1. Edit src/lib/server/db/schema/game-steam-listings.ts: add `name: text("name"),` after the `label` column.
    2. Run `pnpm drizzle-kit generate` to produce drizzle/0004_*.sql + 0004_snapshot.json + journal entry. Rename file to drizzle/0004_add_steam_listing_name.sql if drizzle-kit picks a different tag. Verify SQL contains `ALTER TABLE "game_steam_listings" ADD COLUMN IF NOT EXISTS "name" text;`. If drizzle-kit emits without IF NOT EXISTS, hand-edit to add it (idempotency precedent from Plans 02.1-12/14/24).
    3. Update drizzle/meta/_journal.json with the new idx=4 entry.
    4. Edit src/lib/server/services/game-steam-listings.ts addSteamListing INSERT body: add `name: meta?.name ?? null,` after `label`.
    5. Edit src/lib/server/dto.ts: GameSteamListingDto interface gains `name: string | null;` after label; toGameSteamListingDto adds `name: r.name,` after label.
    6. Edit tests/integration/game-listings.test.ts: APPEND `describe('Plan 02.1-25 — Steam listing name persistence', ...)` with Tests 1-3 from <behavior>. Mock fetchSteamAppDetails (vi.mock or test-helper override).
    7. Run `pnpm db:migrate` to apply migration in test DB.
  </action>
  <verify>
    <automated>pnpm db:migrate && pnpm test:integration -- tests/integration/game-listings.test.ts -t "Plan 02.1-25" && pnpm test:unit -- tests/unit/dto.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cat drizzle/0004_add_steam_listing_name.sql | grep -c "ADD COLUMN.*name"` returns ≥ 1
    - `grep "name: text" src/lib/server/db/schema/game-steam-listings.ts` returns ≥ 1
    - `grep "name: meta?.name" src/lib/server/services/game-steam-listings.ts` returns ≥ 1
    - `grep -E "name: string \\| null|name: r\\.name" src/lib/server/dto.ts | wc -l` returns ≥ 2
    - `grep -c "Plan 02.1-25" tests/integration/game-listings.test.ts` returns ≥ 1
    - `cat drizzle/meta/_journal.json | grep -c "0004_add_steam_listing_name"` returns 1
    - `pnpm db:migrate` succeeds (idempotent)
    - `pnpm test:integration -- tests/integration/game-listings.test.ts` ALL pass
    - `pnpm test:unit -- tests/unit/dto.test.ts` ALL pass (DTO discipline preserved)
  </acceptance_criteria>
  <done>
    Forward-only migration adds nullable name column; addSteamListing persists Steam name; DTO projects it; integration tests verify round-trip with mocked Steam responses; CLAUDE.md DTO discipline preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NEW components (PageHeader, GameCover, SteamListingRow) + SourceRow Mine treatment + Paraglide keys</name>
  <files>
    src/lib/components/PageHeader.svelte,
    src/lib/components/GameCover.svelte,
    src/lib/components/SteamListingRow.svelte,
    src/lib/components/SourceRow.svelte,
    messages/en.json,
    tests/unit/paraglide.test.ts
  </files>
  <read_first>
    - .planning/phases/02.1-architecture-realignment/02.1-10-UAT-NOTES.md (§2.1-redesign + §3.1-polish + §3.2-redesign + §3.3-polish — all 4 findings)
    - src/lib/components/FeedCard.svelte (Plan 02.1-23 final shape — Mine treatment pattern to mirror)
    - src/lib/components/SourceRow.svelte (current shape after Plan 02.1-22 auto_import edit-gate change)
    - src/lib/components/SourceKindIcon.svelte (existing — used in SourceRow)
    - src/app.css (var(--color-accent) and tokens)
    - messages/en.json (alphabetical insertion of 6 new keys)
    - tests/unit/paraglide.test.ts EXPECTED_KEYS (alphabetical)
    - CLAUDE.md Practices §"Tests land with the feature"; Paraglide invariant D-41
  </read_first>
  <behavior>
    - PageHeader renders title + optional CTA (href OR onClick variant) inline-on-the-left.
    - GameCover renders <img> when any listing has coverUrl; otherwise gradient placeholder + initials.
    - SteamListingRow renders `listing.name ?? "App " + appId` + label + releaseDate + Open-on-Steam <a> with target="_blank" + rel="noopener noreferrer" + href=https://store.steampowered.com/app/{appId}/.
    - SourceRow with isOwnedByMe=true → class:mine on root + border-left CSS + kind label rendered next to icon.
  </behavior>
  <action>
    1. Create src/lib/components/PageHeader.svelte per <interfaces> code block.
    2. Create src/lib/components/GameCover.svelte: $derived coverSrc iterates listings array picking first non-null coverUrl; $derived initials from title.split(/\\s+/).slice(0,2).map(w=>w[0].toUpperCase()).join(''); render <img> when coverSrc OR `<div class="cover placeholder"><span class="initials">{initials}</span></div>`. CSS: aspect-ratio 460/215 + linear-gradient(135deg, var(--color-accent), #6366f1) placeholder. ADD top-of-file comment with TODO Phase 3+: manual upload + itch fallback per UAT-NOTES.md §9.1-backlog.
    3. Create src/lib/components/SteamListingRow.svelte: takes `listing` prop with shape `{ id, appId, label, name, coverUrl, releaseDate, apiKeyId }`. $derived displayName = listing.name ?? `App ${listing.appId}`. $derived steamUrl = `https://store.steampowered.com/app/${listing.appId}/`. Render flex row with name (semibold) + label pill (when non-empty) + release-date pill + Open-on-Steam link with `m.steam_listing_open_link_label()`.
    4. Edit src/lib/components/SourceRow.svelte:
       - Add `class:mine={source.isOwnedByMe}` on root .row div.
       - Add CSS .row.mine { border-left: 4px solid var(--color-accent); }.
       - In .primary block, REPLACE `<SourceKindIcon kind={source.kind} />` with a wrapping `<span class="kind-tag"><SourceKindIcon kind={source.kind} /> {kindLabel(source.kind)}</span>`.
       - Add a top-level function `function kindLabel(k: SourceKind): string` that switches across the 5 kinds returning the appropriate `m.source_kind_label_*()` call.
       - Add CSS rule .kind-tag { display: inline-flex; align-items: center; gap: var(--space-xs); font-weight: var(--font-weight-semibold); color: var(--color-text); }.
       - Optionally upgrade .ownership-badge.mine CSS rule for stronger visual: { background: var(--color-accent); color: var(--color-accent-text, #fff); border-color: var(--color-accent); }.
    5. Edit messages/en.json: ADD 6 keys (alphabetical insertion):
       - source_kind_label_discord_server: "Discord server"
       - source_kind_label_reddit_account: "Reddit account"
       - source_kind_label_telegram_channel: "Telegram channel"
       - source_kind_label_twitter_account: "Twitter account"
       - source_kind_label_youtube_channel: "YouTube channel"
       - steam_listing_open_link_label: "Open on Steam"
       Run `pnpm paraglide:compile`.
    6. Edit tests/unit/paraglide.test.ts: add the 6 keys to EXPECTED_KEYS in alphabetical order.
  </action>
  <verify>
    <automated>pnpm test:unit -- tests/unit/paraglide.test.ts && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - 3 NEW component files exist: PageHeader.svelte, GameCover.svelte, SteamListingRow.svelte
    - `grep "class:mine" src/lib/components/SourceRow.svelte` returns ≥ 1
    - `grep "border-left" src/lib/components/SourceRow.svelte` returns ≥ 1
    - `grep "source_kind_label_" src/lib/components/SourceRow.svelte` returns ≥ 5
    - `grep "store.steampowered.com/app/" src/lib/components/SteamListingRow.svelte` returns ≥ 1
    - `grep -c "TODO Phase 3+" src/lib/components/GameCover.svelte` returns ≥ 1
    - `grep -E "source_kind_label_(discord_server|reddit_account|telegram_channel|twitter_account|youtube_channel)|steam_listing_open_link_label" messages/en.json | wc -l` returns 6
    - `grep -E "source_kind_label_(discord_server|reddit_account|telegram_channel|twitter_account|youtube_channel)|steam_listing_open_link_label" tests/unit/paraglide.test.ts | wc -l` returns 6
    - `pnpm test:unit -- tests/unit/paraglide.test.ts` passes
    - `pnpm lint` passes
  </acceptance_criteria>
  <done>
    3 new components ship; SourceRow gains Mine treatment + kind label; 6 new Paraglide keys with EXPECTED_KEYS update.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire PageHeader + new components into /feed, /games, /sources, /games/[gameId] + browser tests</name>
  <files>
    src/routes/sources/+page.svelte,
    src/routes/feed/+page.svelte,
    src/routes/games/+page.svelte,
    src/routes/games/[gameId]/+page.svelte,
    tests/browser/responsive-360.test.ts,
    tests/browser/feed-360.test.ts
  </files>
  <read_first>
    - .planning/phases/02.1-architecture-realignment/02.1-10-UAT-NOTES.md (§3.1-polish + §3.2-redesign — page-header pattern + /games/[id] mockup)
    - src/routes/sources/+page.svelte (current after Plan 02.1-22 sticky)
    - src/routes/feed/+page.svelte (current after Plan 02.1-21 schema-prop)
    - src/routes/games/+page.svelte (current — has inline new-game form)
    - src/routes/games/[gameId]/+page.svelte (current — restructure to 2-card)
    - src/lib/components/PageHeader.svelte / GameCover.svelte / SteamListingRow.svelte (Task 2 products)
    - tests/browser/responsive-360.test.ts (existing pattern)
    - tests/browser/feed-360.test.ts (existing pattern)
  </read_first>
  <behavior>
    - Test 1 (responsive-360 Plan 02.1-25): /feed at 360px renders <header class="page-header"> with title 'Feed' followed by CTA inline (CTA bounding rect.left < 50% of viewport width).
    - Test 2: /games at 360px — same pattern; CTA toggles inline form (button variant).
    - Test 3: /sources at 360px — same pattern with link variant.
    - Test 4: /games/[id] renders <section class="game-header-card"> AND <section class="events-feed-card">; both have computed border + background.
    - Test 5 (feed-360 Plan 02.1-25): /games/[id] with Steam listing populated → SteamListingRow displayName === listing.name (or 'App {appId}' fallback); Open-on-Steam <a> href ends with `/app/{appId}/`; target === '_blank'.
  </behavior>
  <action>
    1. Edit src/routes/sources/+page.svelte:
       - REPLACE inline `<header class="head">...` block with `<PageHeader title="Data sources" cta={{ href: "/sources/new", label: m.sources_cta_new_source() }} />`.
       - PRESERVE Plan 02.1-22's sticky positioning: move sticky CSS into PageHeader via :global selector OR add a `sticky?: boolean` prop on PageHeader (executor picks; document choice in SUMMARY).
       - Remove the old inline .head CSS rule.
    2. Edit src/routes/feed/+page.svelte:
       - REPLACE inline `<header class="head">` block with `<PageHeader title="Feed" cta={{ href: "/events/new", label: m.feed_cta_add_event() }} />`.
       - Remove the old inline .head CSS rule.
    3. Edit src/routes/games/+page.svelte:
       - REPLACE inline header with `<PageHeader title="Games" cta={{ onClick: () => showForm = true, label: '+ New game' }} />` (uses the onClick variant since /games has the inline form toggle pattern).
       - Preserve the existing inline new-game form behavior.
    4. Edit src/routes/games/[gameId]/+page.svelte — restructure:
       - Wrap GAME HEADER content in `<section class="game-header-card">`. Place: <GameCover title={game.title} listings={listings} /> at top, then <RenameInline>, then meta (releaseDate badge / tags / notes), then `<ul class="listings">{#each listings}<li><SteamListingRow {listing} /></li>{/each}</ul>`, then <AddSteamListingForm gameId={game.id} />.
       - Wrap EVENTS FEED content in `<section class="events-feed-card">`. Place: panel-head (h2 + count + + New event link) and the existing month-grouped FeedCards.
       - CSS: both sections .game-header-card / .events-feed-card { background: var(--color-surface); border: 1px solid var(--color-border); border-radius: 4px; padding: var(--space-md); margin-bottom: var(--space-lg); }.
       - REMOVE old `.panels` flex container if no longer needed.
       - Existing breadcrumb stays at top.
    5. Edit tests/browser/responsive-360.test.ts: APPEND `describe('Plan 02.1-25 — PageHeader inline + /games/[id] two-card layout', ...)` with Tests 1-4.
    6. Edit tests/browser/feed-360.test.ts: APPEND `describe('Plan 02.1-25 — SteamListingRow on /games/[id]', ...)` with Test 5.
  </action>
  <verify>
    <automated>pnpm test:browser -- tests/browser/responsive-360.test.ts -t "Plan 02.1-25" && pnpm test:browser -- tests/browser/feed-360.test.ts -t "Plan 02.1-25" && pnpm lint && pnpm test:integration</automated>
  </verify>
  <acceptance_criteria>
    - `grep "PageHeader" src/routes/sources/+page.svelte src/routes/feed/+page.svelte src/routes/games/+page.svelte | wc -l` returns ≥ 6 (import + usage in each)
    - `grep "GameCover\\|SteamListingRow" src/routes/games/[gameId]/+page.svelte | wc -l` returns ≥ 4 (import + usage for both)
    - `grep "game-header-card\\|events-feed-card" src/routes/games/[gameId]/+page.svelte | wc -l` returns ≥ 2
    - `grep "Plan 02.1-25" tests/browser/responsive-360.test.ts` returns ≥ 1 line
    - `grep "Plan 02.1-25" tests/browser/feed-360.test.ts` returns ≥ 1 line
    - `pnpm test:browser` ALL pass
    - `pnpm test:integration` ALL pass (no regression — /games/[id] loader output unchanged)
    - `pnpm lint` passes
  </acceptance_criteria>
  <done>
    PageHeader wired into 3 list pages with inline title-CTA; /games/[id] restructured into 2 cards using GameCover + SteamListingRow; browser tests assert layout + Open-on-Steam link; integration tests still pass.
  </done>
</task>

</tasks>

<verification>
- `pnpm test` passes (full suite)
- `pnpm test:browser` passes
- `pnpm db:migrate` runs idempotently
- `pnpm db:check` passes (schema in sync)
- `pnpm lint` passes
- Manual UAT walkthrough (Russian, per user-MEMORY profile):
  - /feed, /games, /sources: title + CTA inline on the left (no big gap to right edge)
  - /sources: a Mine source has accent left border + bold "Mine" pill; kind icon + text label both visible
  - /games/[id]: two clearly separated card sections (header + events feed); cover image renders Steam header_image when listing present, else gradient placeholder + initials
  - Steam listing renders game name (e.g. "Portal 2") + Open-on-Steam button; clicking the button opens steamcommunity in new tab
</verification>

<success_criteria>
- UAT-NOTES.md §2.1-redesign + §3.1-polish + §3.2-redesign + §3.3-polish closed.
- Forward-only migration discipline preserved (DV-2): 0004 is next idx after 0003.
- Privacy & multi-tenancy invariants intact (no schema scoping change; DTO projection extended; no new env reads).
- Plan 02.1-22 (auto_import edit-gate) preserved; Plan 02.1-23 Mine treatment pattern reused (border-left accent + overlay-style pill).
- Manual upload + itch fallback DEFERRED with TODO marker for Phase 3+ pickup.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-architecture-realignment/02.1-25-SUMMARY.md` per template.
</output>
