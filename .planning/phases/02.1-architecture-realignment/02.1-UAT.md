---
status: partial
phase: 02.1-architecture-realignment
source:
  - 02.1-11-SUMMARY.md
  - 02.1-12-SUMMARY.md
  - 02.1-13-SUMMARY.md
  - 02.1-14-SUMMARY.md
  - 02.1-15-SUMMARY.md
  - 02.1-16-SUMMARY.md
started: "2026-04-28T21:10:00Z"
updated: "2026-04-29T00:30:00Z"
---

## Current Test

[testing complete — 11/12 pass, 1 issue, 1 pass_but_reverted; 17 follow-up UX gaps recorded]

## Tests

### 1. Cold Start Smoke Test
expected: Kill the dev server. Drop & recreate the Postgres dev DB (or run a fresh container). Start `pnpm dev`. Server boots without errors. Drizzle runs both new migrations on first connection (0001_add_post_kind, 0002_add_event_restored_audit_action). Visiting /feed loads without 500.
result: pass
note: "Soft cold-start: Node upgraded 20.15 → 24.15 (project requires ≥22.11). DB not dropped (kept dev data); both migrations confirmed present via psql (event_kind has 'post' as 10th value; audit_action has 'event.restored' as 20th value). vite dev boots in 3.4s with no Node-engine warnings. /feed → 303 → /login?next=%2Ffeed; /login → 200; / → 200."

### 2. Gap 1 (P0) — /audit page loads without crash
expected: Visit `/audit`. Page renders without runtime error (previously crashed on `m.audit_action_item_created is not a function`). The action filter dropdown shows ~20 options, including the new Phase 2.1 verbs (`event.attached_to_game`, `source.added`, `event.restored`, `user.signup`, etc.). Audit rows render translated chip labels, not raw action strings.
result: pass
note: "Page loads without crash. Created a game via UI, audit row appeared as 'Game created' (translated chip label, not raw `game.created`). Dropdown shows all options — verified count is 'many' (~20 expected). Gap 1 (P0) closed."
followup_issue: "DEFERRED-UX: /audit action-filter UX is unusable as a single-select dropdown — needs the multi-select chip + sheet pattern from /feed (FilterChips + FiltersSheet). Out of scope for Gap 1 (which was the runtime crash). Tracked as a new UAT-discovered issue, severity=minor (works but unusable for power users)."

### 3. Gap 2 (P0) — Soft-delete event restore flow
expected: From `/feed`, delete any event via the row menu (ConfirmDialog says "You can restore it within 60 days"). Event disappears from the feed. Scroll below the feed pager — a "Recently deleted" panel (`<DeletedEventsPanel>`) lists the just-deleted event with a Restore button. Click Restore. Event returns to the feed (deletedAt cleared). The /audit page shows a new `event.restored` chip for the action.
result: pass
note: "User confirmed: works."

### 4. Gap 3 (P0) — Feed renders as media cards
expected: `/feed` no longer shows text rows. YouTube events render as `<FeedCard>` with a 16:9 thumbnail (https://img.youtube.com/vi/{externalId}/mqdefault.jpg). Non-thumbnail kinds (post, conference, talk, press, etc.) render with a centered `<KindIcon>` anchor. Each card shows the title, a kind text label adjacent to the icon, and a compact date (Today HH:MM / Yesterday / Mon D / Mon D, YYYY).
result: issue
reported: "Так не вижу картинки. Так как будто fallback показана иконка видео. Пропал важный флаг — раньше мог отметить автора, я или не я. И про ленту — дату можно не показывать в карточке. Дату можно показывать сверху, а потом карточки. Типа как в гугл фото. Дата сверху, а потом карточки. И можно карточки делать не на всю линию, а как карточки, рилсы, несколько в линию."
severity: blocker
diagnosis: "DB inspection (events for user sf7n5...): kind=youtube_video event has external_id=NULL. <FeedCard>'s thumbnailUrl $derived returns null when externalId is null → falls back to <KindIcon>. Root cause: /events/new form posts to POST /api/events → createEvent(), which does NOT parse the url field into external_id. URL → external_id parsing lives only in createEventFromPaste(). Manual-create path is broken end-to-end for thumbnail generation. This is a regression introduced by Plan 02.1-05 (unified events service); Plan 02.1-16 (FeedCard) assumes externalId is populated for kind=youtube_video — without that, thumbnails never render via the manual-create flow."

### 5. Gap 4 (P0) — Multi-select filters
expected: On `/feed`, open the Filters sheet. Source/Kind/Game sections render as checkbox lists (not single-select dropdowns). Pick two kinds (e.g. `youtube_video` + `post`). Apply. URL becomes `?kind=youtube_video&kind=post` (repeated params, NOT comma-split). Feed shows events of both kinds. Each selected value renders as a separate dismissable chip in `<FilterChips>`. Click an X on one chip — only that value drops off; the other stays.
result: pass
note: "Multi-select works at the basic level (checkboxes, repeated query params, feed filters correctly). UX issues (per-axis chip grouping, Game/Attached merge) tracked separately in ## Gaps."

### 6. Gap 5 (P1) — Compact relative date format
expected: On `/feed`, recent event timestamps show as: events from today → "Today HH:MM", yesterday → "Yesterday", same calendar year → "Mon D" (e.g. "Apr 12"), prior years → "Mon D, YYYY" (e.g. "Dec 3, 2025"). Verbose absolute "2026-04-28 14:23:11"-style stamps no longer appear in the feed.
result: pass
note: "Format confirmed by user. Placement (card vs group header) tracked separately in ## Gaps as 'date-group header' UX item from Test 4."

### 7. Gap 6 (P1) — KindIcon platform identity
expected: Each non-YouTube card on `/feed` shows the `<KindIcon>` plus a visible kind text label adjacent (e.g. "Post", "Reddit post", "Conference"). User can identify the platform/kind at a glance without clicking through.
result: pass
note: "User confirmed: works. Side issue raised during this test — see ## Gaps entry 'feed loader stale after POST /api/events' (UAT-discovered, recorded as major regression)."

### 8. Gap 7 (P1) — Today/Yesterday quick-set buttons
expected: Visit `/events/new`. Above the date input, two chip buttons labeled "Today" and "Yesterday" appear. Click "Today" — date field fills with today's date. Click "Yesterday" — fills yesterday. One click, no calendar interaction needed.
result: pass
note: "User confirmed: works."

### 9. Gap 8 (P1) — Date-origin explainer note
expected: On `/events/new`, an inline note near the date input explains the form's date semantics — that the date defaults to today (not auto-derived from any URL paste) and the user can override it.
result: pass_but_reverted
reported: "The date defaults to today. Pick a different date manually for past conferences, talks, or backdated posts. — Она не нужна."
note: "Note IS shipped (Gap 8 technically closed). User reviewed it during UAT and decided it's redundant — date input is self-explanatory. Tracked as new UAT-discovered gap to REMOVE the note in the next gap-closure cycle."

### 10. Gap 9 (P1) — Default last-30-days filter
expected: Land on `/feed` with no query params. The URL effectively scopes the feed to the last 30 days (or the date range UI shows "last 30 days" as the active default). To see everything, the user can use `?all=1` or pick a different range — but the unfiltered default is no longer "show every event ever".
result: pass
note: "User confirmed default behavior. UI shape (presets + chip) refactor tracked separately in ## Gaps as 'date-filter UX rewrite'."

### 11. Gap 10 (P1) — Date-range as primary control
expected: On `/feed`, a `<DateRangeControl>` is rendered prominently (before the kind/source/game filter sections — not buried inside a sheet). User can pick a from/to date range directly. URL reflects the range. Feed updates accordingly.
result: pass
note: "User confirmed: control is on the page, not in the sheet. UI rewrite (always-visible inputs + 4 presets) tracked in ## Gaps."

### 12. Gap 12 (P1) — `post` event kind picker
expected: Visit `/events/new`. The kind picker dropdown now includes "Post" as a selectable option (alongside YouTube video, Twitter post, Telegram post, etc.). Submit an event with kind=post. It appears on `/feed` rendered as a card with the new "post" KindIcon (paper/document silhouette) and "Post" text label. The Filters sheet kind list and FilterChips also include the new "post" option.
result: pass
note: "User confirmed: works."

## Summary

total: 12
passed: 11
issues: 1
pass_but_reverted: 1
pending: 0
skipped: 0
followup_ux_issues: 17

## Gaps

- truth: "/audit action-filter UX should match the /feed multi-select chip + sheet pattern (FilterChips + FiltersSheet) — single-select dropdown with ~20 options is unusable for power users."
  status: failed
  reason: "User reported during Gap 1 verification: 'Много, только таким фильтром пользоваться невозможно. Нужен фильт как в feed.' Pattern exists already in FilterChips.svelte + FiltersSheet.svelte (Plan 02.1-15)."
  severity: minor
  test: 2
  surface: "/audit"
  artifacts:
    - "src/routes/audit/+page.svelte (replace ActionFilter dropdown with FilterChips + FiltersSheet)"
    - "src/lib/components/ActionFilter.svelte (delete after migration)"
  missing:
    - "Multi-select action filter on /audit (URL: ?action=A&action=B repeated params, mirrors /feed convention)"
    - "Per-value chip dismiss"
  source_test: "Test 2 / Gap 1 verification"
  parent_phase: 02.1
  scope_note: "OUT OF SCOPE for Gap 1 closure (which was the runtime crash). New UAT-discovered issue surfaced during this verification round."

- truth: "Date filter on /feed renders as two always-visible from/to inputs with 4 quick presets (Today / Week / Month / Year). Clicking a preset autofills both fields. No chip is emitted in FilterChips for the date — visible inputs ARE the indicator. Small × button beside inputs clears them (replaces 'All time' preset)."
  status: failed
  reason: "User reported during /feed UAT: 'Не хочу видеть даты как фильтр. Хочу видеть два поля начало и конец. И кнопки сегодня, неделя, месяц, год.' Current DateRangeControl shows 5 preset buttons (Today / 7d / 30d / All time / Custom) with from/to inputs hidden behind a 'Custom' button. FilterChips ALSO emits a chip for the active preset, doubling the indicator."
  severity: major
  test: "feed-filters discussion (after Test 2)"
  surface: "/feed (date range as primary control)"
  artifacts:
    - "src/lib/components/DateRangeControl.svelte (rewrite — always-visible from/to inputs + 4 quick-set buttons)"
    - "src/lib/components/FilterChips.svelte (drop dateRange chip emission entirely; lines 131-140)"
    - "src/routes/feed/+page.server.ts (defaults: 'Месяц' = last 30 days; no preset means show last 30 days)"
    - "messages/en.json (replace feed_date_range_7d/30d/all/custom keys with feed_date_range_week/month/year/clear)"
  missing:
    - "Always-visible from / to date input fields"
    - "Quick-set buttons: Today (today→today) / Week (7d→today) / Month (30d→today) / Year (365d→today)"
    - "Click preset → autofills both inputs; preset highlight reflects current range"
    - "Small × button near inputs to clear from/to (replaces 'All time' preset semantically)"
    - "FilterChips MUST NOT emit any chip for date range (the inputs are visible — duplication is confusing)"
  source_test: "feed-filters design discussion (after Test 2 / before Test 3)"
  parent_phase: 02.1
  scope_note: "Refines Gap 10 (P1) closure from Plan 02.1-15. The plan shipped DateRangeControl but kept the 'preset behind Custom' pattern from the planner spec; UAT showed users expect inputs always visible. Default = 'Месяц' (last 30 days) preserved per Gap 9."

- truth: "Game and Attached filters merge into one 'Show' axis with three states: Any (default) / Inbox only (no game) / Attached to specific games (reveals multi-select). Selecting Inbox hides the games list; selecting Specific games reveals the multi-select picker."
  status: failed
  reason: "User reported during /feed UAT: 'Я могу выбрать игру. Но потом выбрать без игры? ... вот это как будто конфликтует.' Current FiltersSheet has Game multi-select AND Attached radio (Any / Attached / Inbox). User can pick Game=X AND Attached=Inbox simultaneously, which is logically impossible. Constraint must be enforced in UI — not at submit-time."
  severity: major
  test: "feed-filters discussion (after Test 2)"
  surface: "/feed (FiltersSheet)"
  artifacts:
    - "src/lib/components/FiltersSheet.svelte (replace Game checkbox-list + Attached radio with one 'Show' radio that conditionally reveals game multi-select)"
    - "src/routes/feed/+page.server.ts (URL parsing: ?show=any|inbox|specific&game=A&game=B — drop ?attached=true/false)"
    - "src/lib/server/services/events.ts (FeedFilters: collapse `attached?: boolean` + `game?: string[]` into a single discriminated union or document the implicit constraint)"
    - "src/lib/components/FilterChips.svelte (chip emission updated for new shape: one chip 'Show: Inbox' or 'Show: Game1, Game2, Game3' replacing two)"
    - "messages/en.json (new feed_filter_show_any / feed_filter_show_inbox / feed_filter_show_specific)"
  missing:
    - "Single 'Show' radio control with three states (Any / Inbox / Specific games)"
    - "Conditional reveal of game multi-select picker when 'Specific games' selected"
    - "Multi-select games via checkbox + typeahead (when Specific games)"
    - "Backend FeedFilters reflects the merged axis (no more attached?: boolean + game?: string[] independently)"
    - "Conflict prevention: cannot pick a game when 'Inbox' is selected (UI guard, not server-side)"
  source_test: "feed-filters design discussion (after Test 2 / before Test 3)"
  parent_phase: 02.1
  scope_note: "Refines Gap 4 (P0) closure from Plan 02.1-15. Multi-select shipped, but the Game/Attached coupling was not addressed. UAT surfaced the contradiction (Game=X + Attached=Inbox) immediately. Backend filter shape changes — schema/DB unchanged."

- truth: "FilterChips groups values per axis instead of one chip per value. Chip format: 'Kind: YouTube video, Post' (all values shown, comma-separated, no '+N more' truncation). Click chip → opens sheet on that axis. Click × → clears entire axis. Long chips wrap text inside the chip; the strip uses flex-wrap so very wide chips move to a new row."
  status: failed
  reason: "User reported during /feed UAT: 'Плашки фильтров когда я выбрал занимают много места. Выбрал 11 разных и треть экрана уже занято.' Current per-value chip rendering produces N chips for N selected values. With 3-4 axes and several values each, the strip eats ~1/3 of viewport vertically. Per-axis grouping reduces chip count to ≤3 (kind/source/show)."
  severity: major
  test: "feed-filters discussion (after Test 2)"
  surface: "/feed (FilterChips)"
  artifacts:
    - "src/lib/components/FilterChips.svelte (rewrite chips() $derived: emit one entry per axis with comma-joined value labels instead of one entry per value)"
    - "src/lib/components/FilterChips.svelte (CSS: chip max-width 100%; word-break: break-word; min-width 0 so flex-wrap pushes wide chips to new row)"
    - "src/routes/feed/+page.svelte (chip click → onOpenSheet now needs to pass axis hint so sheet scrolls/focuses to that axis; minor wiring)"
  missing:
    - "Per-axis chip grouping: one chip per active axis (kind / source / show), label = 'AxisName: V1, V2, V3'"
    - "Click chip opens FiltersSheet (existing behavior); ideally focus-jump to the chip's axis"
    - "Click × clears the entire axis (drops all values for that axis)"
    - "Internal text wrap inside the chip: wide chips grow vertically rather than horizontally; flex-wrap moves whole chip to new row when natural width > remaining strip width"
    - "No '+N more' truncation — all values always visible"
  source_test: "feed-filters design discussion (after Test 2 / before Test 3)"
  parent_phase: 02.1
  scope_note: "Refines Gap 4 (P0) closure from Plan 02.1-15. Multi-select shipped, but per-value chip rendering was the planner-spec default. UAT showed the strip eats too much vertical space. Plan-level refactor — no schema/route change, FilterChips component only."

- truth: "POST /api/events parses the url field for kind=youtube_video and derives external_id (canonical YouTube videoId) the same way createEventFromPaste does. /events/new manual-create produces events whose external_id is populated — so <FeedCard> can render the thumbnail."
  status: failed
  reason: "User reported during /feed UAT (Test 4): YouTube events created via /events/new fall back to KindIcon — no thumbnail. DB inspection confirmed external_id=NULL for the user's youtube_video event. Root cause: /events/new POSTs to /api/events → createEvent(), which doesn't parse url → external_id. URL parsing lives only in createEventFromPaste. Manual-create path bypasses parsing → external_id never gets populated → <FeedCard>'s thumbnailUrl=null → fallback. Regression from Plan 02.1-05; Plan 02.1-16 (FeedCard) assumes external_id exists."
  severity: blocker
  test: 4
  surface: "POST /api/events; /events/new; createEvent service"
  artifacts:
    - "src/lib/server/services/events.ts (createEvent: extract URL → external_id parsing from createEventFromPaste; share helper or inline for kind=youtube_video; idempotent — if input.externalId already set, do not overwrite)"
    - "src/lib/server/http/routes/events.ts (createEventSchema validates url format; ensure 422 on invalid YouTube URL when kind=youtube_video and url is present)"
    - "tests/integration/events.test.ts (regression test: POST /api/events with kind=youtube_video + url=https://www.youtube.com/watch?v=ABC populates external_id=ABC in resulting row)"
  missing:
    - "URL → external_id derivation in createEvent (not just createEventFromPaste)"
    - "Regression test asserting external_id is populated end-to-end for manual-create YouTube events"
  source_test: "Test 4 / Gap 3 verification"
  parent_phase: 02.1
  scope_note: "BLOCKER for Gap 3 (P0) closure — without this, FeedCard thumbnails never render via manual-create path, defeating Plan 02.1-16's primary deliverable."

- truth: "Event creation form (/events/new) and event API (POST /api/events) accept and persist `author_is_me` boolean toggle. Default = false. UI exposes a checkbox or toggle 'Author is me' on /events/new. <FeedCard> shows a visible badge/indicator when author_is_me=true (anchor for the user's own promo posts vs third-party coverage)."
  status: failed
  reason: "User reported during /feed UAT (Test 4): 'Пропал важный флаг. Я раньше мог отметить автора, я или не я.' The author_is_me column exists in events schema (per RESEARCH §3 + ROADMAP success criterion 2: author_is_me bool, inherited from data_source on auto-import or false on manual paste). But the manual-create path on /events/new has no UI affordance to set it; the kind picker also has no per-event toggle. Phase 2 had this; the unified-events rewrite dropped the affordance."
  severity: major
  test: 4
  surface: "/events/new; <FeedCard>; createEvent service"
  artifacts:
    - "src/routes/events/new/+page.svelte (add 'Author is me' checkbox with default false; pass authorIsMe field in POST body)"
    - "src/lib/server/services/events.ts (createEvent input shape includes authorIsMe; persists to events.author_is_me)"
    - "src/lib/server/http/routes/events.ts (createEventSchema accepts authorIsMe: boolean optional)"
    - "src/lib/components/FeedCard.svelte (visible badge/icon when event.authorIsMe=true)"
    - "src/lib/server/dto.ts (toEventDto already projects authorIsMe; verify)"
    - "messages/en.json (events_new_author_is_me, feed_card_author_is_me_badge)"
  missing:
    - "UI toggle on /events/new for author_is_me"
    - "Schema accepts authorIsMe in createEventSchema"
    - "<FeedCard> renders a visible 'mine' badge when authorIsMe=true"
  source_test: "Test 4 / Gap 3 verification"
  parent_phase: 02.1
  scope_note: "Phase 2 → 2.1 regression. The author_is_me concept is documented in ROADMAP and PROJECT.md as a core distinguisher; UI lost the affordance during the unified-events rewrite. Restore + surface in feed."

- truth: "/feed groups events by date (Today / Yesterday / Apr 27 / Apr 26 / ...) with a sticky-or-inline date HEADER above each group's cards. Cards within a group do NOT show their own date — the group header IS the date. Mirrors Google Photos / Apple Photos timeline grouping."
  status: failed
  reason: "User reported during /feed UAT (Test 4): 'Дату можно не показывать в карточке. Дату можно показывать сверху а потом карточки. Типа как в гугл фото. Дата сверху, а потом карточки.' Current FeedCard renders the compact relative date inline on every card (formatFeedDate from Plan 02.1-16). Repetition is visual noise; user wants timeline-style grouping where the date is asserted ONCE per group."
  severity: major
  test: 4
  surface: "/feed (page) + <FeedCard> + new <FeedDateGroupHeader> component"
  artifacts:
    - "src/routes/feed/+page.svelte (group rows by occurredAt date, render <FeedDateGroupHeader> + <FeedCard>[] per group)"
    - "src/lib/components/FeedCard.svelte (drop the inline date display; date stays in metadata for accessibility but is not visually rendered on the card)"
    - "src/lib/components/FeedDateGroupHeader.svelte (NEW: date label using formatFeedDate; sticky positioning optional but visually balanced)"
    - "src/lib/util/format-feed-date.ts (no change — same formatter for header)"
    - "tests/browser/feed-360.test.ts (assert at least one date header renders + no per-card date duplication)"
    - "messages/en.json (no new keys — formatFeedDate already covers labels)"
  missing:
    - "<FeedDateGroupHeader> component"
    - "Grouping logic in /feed/+page.svelte (group consecutive cards by occurredAt date)"
    - "Per-card date display removed from <FeedCard>"
  source_test: "Test 4 / Gap 3 verification"
  parent_phase: 02.1
  scope_note: "Refines Plan 02.1-16's compact-date deliverable. Date format stays; placement moves from card to group header. Aligns with Gap 5 closure (compact format) but takes it further: relative-only date is now ALSO timeline-grouped."

- truth: "/feed renders cards in a responsive grid: multiple cards per row on >=640px, single column on <640px (mobile). Card width auto-fits the grid track (min ~280px, max 1fr) — like Instagram Reels feed or YouTube channel grid view, not full-width list rows."
  status: failed
  reason: "User reported during /feed UAT (Test 4): 'Можно карточки делать не на всю линию, а как карточки, рилсы, несколько в линию.' Current /feed renders <FeedCard>[] in a vertical list (one card per row, full width). On desktop this wastes horizontal space; the user wants grid layout that takes advantage of wider viewports."
  severity: minor
  test: 4
  surface: "/feed (page) + <FeedCard>"
  artifacts:
    - "src/routes/feed/+page.svelte (wrap card list in CSS grid: `grid-template-columns: repeat(auto-fill, minmax(280px, 1fr))` with gap)"
    - "src/lib/components/FeedCard.svelte (review CSS — current FeedCard is sized assuming row context; needs to behave as a self-contained card in a grid cell. Probably remove desktop horizontal flexbox layout described in component comment)"
    - "tests/browser/feed-360.test.ts (assert single-column on 360px; pseudo-test for grid on 1024px is OK but optional)"
  missing:
    - "Grid layout on /feed"
    - "<FeedCard> CSS that works as a grid cell (vertical stack on all viewports)"
  source_test: "Test 4 / Gap 3 verification"
  parent_phase: 02.1
  scope_note: "UX polish on top of Gap 3 (P0). Independent of thumbnail/author_is_me/date-grouping fixes; can ship in same plan or split. CSS-only change once <FeedCard> is decoupled from row-context layout."

- truth: "After successful POST /api/events from /events/new, navigation to /feed shows the new event WITHOUT requiring a manual page refresh. Either goto('/feed') invalidates the /feed loader, or the form uses SvelteKit's progressive enhancement (`use:enhance` + `invalidate`/`invalidateAll`)."
  status: failed
  reason: "User reported during /feed UAT (Test 7): 'Заметил что после добавления нового события, мне нужно сделать обновления окна чтобы его увидеть.' /events/new currently does `await goto('/feed')` after a successful POST — but the /feed loader returns stale data; user must hard-refresh (F5) to see the new event. Likely cause: SvelteKit goto is treating /feed as already-mounted and not re-running the +page.server.ts load, OR a cache header on the underlying /api/events GET prevents revalidation."
  severity: major
  test: 7
  surface: "/events/new (post-submit navigation); /feed/+page.server.ts (loader); /events/new/+page.svelte (submit handler)"
  artifacts:
    - "src/routes/events/new/+page.svelte (replace `await goto('/feed')` with `await invalidateAll(); await goto('/feed')` OR convert form to native form action with `use:enhance` so SvelteKit invalidates by default)"
    - "src/routes/feed/+page.server.ts (verify loader has no cache headers blocking revalidation; emit no-cache or short-cache for HTML loader response)"
    - "tests/integration/events.test.ts (regression: assert that after POST /api/events, GET /api/events lists the new event in the next call)"
    - "tests/browser/feed-360.test.ts (browser flow: create event via /events/new → expect /feed to render the new event without a manual refresh)"
  missing:
    - "Loader invalidation after POST in /events/new submit handler"
    - "Manual-refresh requirement is a UX regression — must be eliminated"
    - "Browser regression test asserting feed updates after create"
  source_test: "Test 7 / KindIcon verification (side observation)"
  parent_phase: 02.1
  scope_note: "UAT-discovered regression. Likely a SvelteKit invalidation gap, not a service/schema bug. Ships with the broader feed-rebuild gap-closure plan since it shares files (/events/new/+page.svelte and /feed/+page.svelte/+page.server.ts)."

- truth: "/events/new no longer shows the date-origin explainer note ('The date defaults to today. Pick a different date manually for past conferences, talks, or backdated posts.'). The date input is self-explanatory; the note is visual noise."
  status: failed
  reason: "User reported during /events/new UAT (Test 9): note IS shipped (Plan 02.1-13 closed Gap 8), but on review they decided the inline explainer is unnecessary — the date input + the Today/Yesterday quick-set buttons make the semantics obvious. This is a scope-back, not a bug; closing it requires reverting Plan 02.1-13's note + removing the now-orphan Paraglide key."
  severity: minor
  test: 9
  surface: "/events/new (form layout)"
  artifacts:
    - "src/routes/events/new/+page.svelte (remove the explainer note element above/below the date input)"
    - "messages/en.json (remove key `events_new_date_explainer` — orphaned after note removal)"
    - "tests/unit/paraglide.test.ts (drop the corresponding EXPECTED_KEYS entry)"
    - "src/lib/paraglide/messages/events_new_date_explainer.js (regenerated by paraglide:compile after key removal)"
  missing:
    - "Removal of explainer note element"
    - "Removal of `events_new_date_explainer` Paraglide key"
    - "Snapshot test update to drop the key"
  source_test: "Test 9 / Gap 8 verification"
  parent_phase: 02.1
  scope_note: "Reverts the SECOND half of Plan 02.1-13. The Today/Yesterday quick-set buttons (FIRST half, Gap 7) stay — those passed UAT cleanly."

- truth: "/events/new kind picker and /feed FiltersSheet kind checkbox-list show ONLY functional event kinds in Phase 2.1: youtube_video, post, conference, talk, press, other. Non-functional kinds (reddit_post, twitter_post, telegram_post, discord_drop) are hidden until their respective Phase 3+ adapters ship."
  status: failed
  reason: "User reported during /feed UAT (Test 10): 'Так и давай пока из списка уберем то чего нет. Типа дискорда, твиттера и тд.' The picker currently exposes all 10 event kinds, but adapters for reddit/twitter/telegram/discord ship in Phase 3+. Showing them in the picker creates dead ends — user picks 'Discord drop', creates an event, and there's no auto-import / no platform-specific affordance, just a text row. Hiding them avoids false expectations until adapters land."
  severity: minor
  test: 10
  surface: "/events/new (kind picker); /feed FiltersSheet (kind checkbox list)"
  artifacts:
    - "src/routes/events/new/+page.svelte (KINDS array — drop reddit_post / twitter_post / telegram_post / discord_drop)"
    - "src/lib/components/FiltersSheet.svelte (kind options array — same trim)"
    - "src/lib/components/KindIcon.svelte (KEEP all branches — old data may still carry these kinds; rendering should not break)"
    - "src/lib/components/FilterChips.svelte (KEEP all kindLabel cases — same reason)"
    - "messages/en.json (KEEP event_kind_label_* keys — used by KindIcon / FilterChips for legacy rows)"
    - "src/lib/server/db/schema/events.ts + drizzle migrations (NO change — enum stays full; this is a UI gate only)"
    - "src/lib/server/services/events.ts (createEvent: NO change — backend continues to accept all enum values)"
  missing:
    - "Functional-only kind allowlist on /events/new picker"
    - "Functional-only kind allowlist on /feed FiltersSheet"
    - "Comment in code referencing the gating reason (Phase 3+ adapters) so future devs know why options are hidden"
  source_test: "Test 10 / default-30d verification (side observation)"
  parent_phase: 02.1
  scope_note: "UI-gate only — no backend / schema / migration change. Kinds re-appear automatically when their adapter phase ships (Phase 3+: just add the option back to the picker arrays)."

- truth: "Pasting a YouTube URL on /events/new auto-populates: (a) title from oEmbed `title`, (b) occurredAt from page metadata (`publishedAt` / `og:video:release_date` / scraped meta), (c) thumbnail derived from external_id (already deterministic). User does NOT have to manually type the title or pick a date for YouTube events — paste-once, save."
  status: failed
  reason: "User reported during /events/new UAT: 'Под каждое событие могут быть свои поля, или не быть. Или вот текущие они универсальные, если нужны другие то потом добавим. Просто вот хочется например чтобы ютуб видео само получало дату и обложку.' Currently /events/new is a universal-fields form (title, kind, occurredAt, url, notes, gameId) with NO auto-enrichment when a recognized URL is pasted. Phase 2's INGEST flow had this on /games/[id] paste, but the unified /events/new manual-create form skipped it. UX expectation: kind-aware enrichment for the kinds that have a public oEmbed/metadata endpoint (YouTube definitely; future Mastodon/Bluesky possibly)."
  severity: major
  test: 10
  surface: "/events/new (auto-enrich on paste); createEvent service or new enrichment helper"
  artifacts:
    - "src/routes/events/new/+page.svelte (on paste/blur of url field, debounce → call /api/events/preview-url → fill title/occurredAt/external_id fields if user hasn't already typed; user can override)"
    - "src/lib/server/http/routes/events.ts (NEW endpoint POST /api/events/preview-url { url } → returns { kind, externalId, title, occurredAt, thumbnailUrl } for known platforms; cross-tenant safe — no DB write)"
    - "src/lib/server/services/events.ts (extract enrichment helper from createEventFromPaste; expose enrichFromUrl(url) returning the same shape; reuse for both paste and preview)"
    - "src/lib/server/integrations/youtube-oembed.ts (NEW or already-exists: oEmbed fetch + minimal page scrape for publishedAt). Verify if the publishedAt is reliably fetchable without YouTube Data API key; fall back to NULL if not (user fills manually as today)"
    - "tests/integration/events.test.ts (regression: POST /api/events/preview-url with a real YouTube URL returns title + externalId; with garbage URL returns 422)"
  missing:
    - "Auto-enrich UX on /events/new (debounced paste handler)"
    - "POST /api/events/preview-url endpoint"
    - "Reusable enrichFromUrl helper (extracted from createEventFromPaste)"
    - "YouTube publishedAt fetch (oEmbed doesn't expose it — needs page-scrape or YouTube Data API; pick one with budget constraint in mind)"
  source_test: "Test 10 / default-30d verification (architectural side observation)"
  parent_phase: 02.1
  scope_note: "Builds on the 'external_id parsing in createEvent' gap (BLOCKER). Once that ships, this is the natural follow-up. NOTE: project budget is indie/zero — YouTube Data API has a free tier (10k requests/day) but requires an API key. Page-scrape alternative is free but fragile (HTML structure changes). Decide in plan-phase. Worst-case fallback: skip publishedAt auto-fill (user types date manually), but DO auto-fill title + thumbnail (oEmbed-only path)."

- truth: "POST /api/events validates kind-aware required fields: when kind === 'youtube_video', `url` MUST be present and parseable as a YouTube URL. Validation surfaces both server-side (Zod refine() in createEventSchema → 422 with clear error) AND client-side (/events/new submit blocks with inline error 'YouTube video requires a URL'). Same rule extends naturally to other URL-bearing kinds when their adapters ship in Phase 3+."
  status: failed
  reason: "User reported during /events/new UAT (Test 10 follow-up): 'Как будто если ютуб видео то поле url обязательно?' Current schema treats url as universally optional. Selecting kind=youtube_video without a URL produces an event with external_id=NULL → no thumbnail (already tracked) AND no anchor link from the card → effectively a broken youtube_video row. Make URL required when kind requires it; let UI guide the user."
  severity: minor
  test: 10
  surface: "POST /api/events schema; /events/new client validation; createEvent service"
  artifacts:
    - "src/lib/server/http/routes/events.ts (createEventSchema: add `.refine()` — if kind==='youtube_video' then url must be present + match YouTube URL pattern)"
    - "src/routes/events/new/+page.svelte (submit handler: pre-validate that url is non-empty when kind=youtube_video; show inline error)"
    - "src/lib/server/services/events.ts (createEvent: defense-in-depth, throw 422 `validation_failed` with field='url' if kind requires URL and url is null)"
    - "messages/en.json (events_new_url_required key)"
    - "tests/integration/events.test.ts (regression: POST {kind:'youtube_video', url:null} → 422; with valid URL → 201)"
  missing:
    - "Conditional URL-required validation by kind"
    - "Inline form-level error message"
    - "Server-side 422 with structured error for missing required field"
  source_test: "Test 10 / default-30d verification (architectural side observation)"
  parent_phase: 02.1
  scope_note: "Pure validation rule — fits naturally in the same gap-closure plan as the external_id parsing gap (both touch createEventSchema + createEvent + /events/new). Future-proof: define a small per-kind metadata table (kind → requires_url | requires_external_id | etc.) so adding new URL-bearing kinds is one-line."

- truth: "Event kinds in pickers and filter lists are sorted ALPHABETICALLY by their TRANSLATED label (m.event_kind_label_*() output), not by enum-declaration order. Applies to: /events/new kind dropdown, /feed FiltersSheet kind checkbox-list, /audit ActionFilter (after audit-filter UX rewrite). Sort happens at render time so it works across locales."
  status: failed
  reason: "User reported during /feed UAT: 'И еще мне вот в фильтрах не нравится что там сортировка типов не по алфавиту а как-то само по себе.' Current order follows the eventKindEnum declaration order (youtube_video → reddit_post → twitter_post → ... → post). User expects A→Z sort by visible label. Cognitive cost to scan the list otherwise."
  severity: minor
  test: 12
  surface: "/events/new (kind picker); /feed FiltersSheet (kind list); /audit ActionFilter (after rewrite)"
  artifacts:
    - "src/routes/events/new/+page.svelte (KINDS array: build at render-time from VALID_EVENT_KINDS, then sort by m.event_kind_label_<k>())"
    - "src/lib/components/FiltersSheet.svelte (same — build kind list at render-time + sort by translated label)"
    - "src/lib/components/ActionFilter.svelte (only relevant if /audit keeps the dropdown; after the /feed-pattern rewrite the same sort applies to the new chip-sheet form)"
    - "Helper utility (optional): src/lib/util/sort-by-label.ts — kept tiny, used by both call sites; tests/unit cover it directly"
  missing:
    - "Alphabetical sort by translated label on /events/new kind picker"
    - "Alphabetical sort on /feed FiltersSheet kind list"
    - "Same treatment for /audit action filter list"
  source_test: "Test 12 / post-kind verification (side observation)"
  parent_phase: 02.1
  scope_note: "Render-only — no schema/route/service change. ~5 LOC per call site. Combine with the 'hide non-functional kinds' gap since both are kind-list polishing on the SAME files."

- truth: "Edit flow: pencil REMOVED from FeedCard on /feed. Click on card → /events/[id] (detail page; no longer Phase-4 stub). Detail page has pencil → /events/[id]/edit (full form). Edit form mirrors /events/new fields (kind, occurredAt, title, url, notes, gameId, author_is_me) pre-filled with current values, PATCHes /api/events/:id on submit, navigates back to /events/[id]."
  status: failed
  reason: "User reported during /feed UAT (Test 12 follow-up): pencil in FeedCard → 404 (route /events/[id]/edit doesn't exist). User refined design: 'Как будто он тут не нужен. Хочешь редактировать — открывай карточку и там нажимай на карандаш.' So: pencil belongs on the DETAIL page, not on the card. Phase-4 stub at /events/[id] gets replaced with a useful detail view (event summary + Edit pencil + Delete + Restore-if-soft-deleted + a Phase-4 chart placeholder). Backend ALREADY has PATCH /api/events/:id wired (updateEvent service + schema)."
  severity: major
  test: 12
  surface: "/feed FeedCard (remove pencil); /events/[id] (detail page rebuild); /events/[id]/edit (NEW)"
  artifacts:
    - "src/lib/components/FeedCard.svelte (REMOVE the pencil button + gotoEdit handler; click on card body → goto(`/events/${id}`); delete button stays — see scope_note)"
    - "src/routes/events/[id]/+page.svelte (REPLACE Phase-4 stub: render full detail — KindIcon, title, date, kind label, url link, attached game name, author_is_me badge, notes; Edit pencil → goto(`/events/${id}/edit`); Delete + Restore buttons; small EmptyState mentioning the Phase-4 chart is still acceptable inline at the bottom)"
    - "src/routes/events/[id]/+page.server.ts (no change — already loads event)"
    - "src/routes/events/[id]/edit/+page.server.ts (NEW: loader uses getEventById; throws 401 / 404 same way)"
    - "src/routes/events/[id]/edit/+page.svelte (NEW: edit form, fields mirror /events/new, pre-filled. On submit: PATCH /api/events/:id, invalidateAll, goto(`/events/${id}`))"
    - "src/lib/server/services/events.ts (verify updateEvent supports kind / occurredAt / title / url / notes / gameId / authorIsMe)"
    - "messages/en.json (events_edit_heading, events_edit_save, events_edit_cancel, events_detail_edit_aria, events_detail_delete_aria — reuse /events/new keys where overlap)"
    - "tests/integration/events.test.ts (PATCH /api/events/:id regression — round-trip)"
    - "tests/browser/feed-360.test.ts (flow: click card → detail → click pencil → edit → save → back to detail with updated values)"
  missing:
    - "Pencil REMOVAL from FeedCard"
    - "Click-through behavior on FeedCard (card body → /events/[id])"
    - "Detail page replaces Phase-4 stub with full view + Edit pencil + Delete + Restore actions"
    - "/events/[id]/edit route ships (form + loader)"
    - "Browser regression test for full pencil flow (now via detail page)"
  source_test: "Test 12 / post-kind verification (side observation; pencil 404 caught + design refined)"
  parent_phase: 02.1
  scope_note: "MAJOR phase-level miss in Plan 02.1-09. Original plan shipped FeedCard pencil + Phase-4 stub detail page; the plan didn't connect them, leaving a 404. User's design fix puts the pencil on the detail page (where it belongs UX-wise) and keeps /feed cards as scannable previews. Detail page rebuild is the LARGER half of this gap; /edit form is the SMALLER half (mostly mirrors /events/new). Delete-button placement: keep on FeedCard for fast removal, ALSO on detail page (consistent with detail-as-source-of-truth)."

- truth: "FeedCard becomes a pure preview tile: NO action buttons (no Edit, no Delete, no Open external). Clicking the card body navigates to /events/[id]. All actions live on the detail page (Edit pencil, Delete, Open original external link, Restore if soft-deleted). Soft-delete recovery flow (DeletedEventsPanel below feed) keeps its inline Restore button — that's the recovery UI, distinct from per-event action surfacing."
  status: failed
  reason: "User reported during /feed UAT (Test 12 follow-up; design refinement): 'Open как будто тоже не нужен — это карточка, открываем её по клику.' + 'Удалить тоже [убрать с карточки].' Combined with the pencil → 404 issue, the user's design intent is now clear: FeedCard is a tile (image + title + kind label), all actions on detail. Cleaner discoverability and reduces noise on /feed."
  severity: major
  test: 12
  surface: "/feed FeedCard (strip all action buttons + make card clickable); /events/[id] detail page (gain Open + Delete buttons in addition to Edit pencil)"
  artifacts:
    - "src/lib/components/FeedCard.svelte (REMOVE: pencil/edit button, delete button, 'Open original ↗' external link button. Wrap card body in <a href='/events/${id}'> or use programmatic goto on click. Keep KindIcon + thumbnail + title + kind label + author_is_me badge.)"
    - "src/routes/events/[id]/+page.svelte (Add: 'Open original ↗' link if event.url; Delete button → DELETE /api/events/:id then goto('/feed'); Restore button if event.deletedAt; Edit pencil → /events/[id]/edit. ConfirmDialog for delete with the same '60 days' messaging from Plan 02.1-14.)"
    - "src/routes/feed/+page.svelte (cards now wrap entire content in click target — verify a11y: card has a role/aria-label + the inner <a> is the primary affordance with full title text)"
    - "tests/browser/feed-360.test.ts (flow: click card → detail page → click delete → ConfirmDialog → confirm → /feed without the deleted event)"
  missing:
    - "FeedCard with NO action buttons (purely visual + clickable)"
    - "Detail page Open external link"
    - "Detail page Delete button (with ConfirmDialog reuse)"
    - "Detail page Restore button (when event.deletedAt is set; visible to deleted-events-panel users who navigate from there)"
    - "Detail page Edit pencil (already in scope above)"
  source_test: "Test 12 / post-kind verification (continued design refinement after pencil-404)"
  parent_phase: 02.1
  scope_note: "Combines with the pencil-404 / detail-page-rebuild gap above. Total surface: FeedCard simplification + detail-page full rebuild + /edit route. All three ship together — they share files and the design intent is one coherent flow. Delete-on-card is REMOVED; delete-from-feed is now a 2-click action (open detail → delete) but with proper preview before destructive action — UX upgrade, not regression."

- truth: "/feed switches from cursor-paginated 'Newer / Older' buttons (<CursorPager>) to infinite scroll. As the user scrolls near the bottom of the loaded list, the next page auto-fetches using the existing cursor mechanism and appends to the rendered list. No buttons. Empty state when no more pages. The cursor encoding / service contract (listFeedPage) is UNCHANGED — only the UI changes."
  status: failed
  reason: "User reported during /feed UAT (Test 12 follow-up): 'Внизу есть кнопки newer older — они тоже не нужны, а у нас же скролл и фид.' Cursor pagination produces button-driven page turns; user expects continuous scroll matching the feed mental model (Instagram, Twitter, etc.). Backend already supports cursor-based fetching; the UI layer just needs an IntersectionObserver-driven loader instead of explicit Newer/Older buttons."
  severity: major
  test: 12
  surface: "/feed (replace <CursorPager> with infinite-scroll loader); <DeletedEventsPanel> may also keep its current pagination since recovery is a different mental model"
  artifacts:
    - "src/routes/feed/+page.svelte (replace <CursorPager> with: IntersectionObserver on a sentinel <div ref> near the bottom of the rendered list. When intersecting AND there's a nextCursor AND not already fetching, call fetch /api/events?cursor={nextCursor} + same active filters → append to rendered list, update nextCursor)"
    - "src/lib/components/CursorPager.svelte (DELETE — no longer used by /feed; verify no other consumer)"
    - "src/routes/feed/+page.server.ts (no change — initial load returns first page + nextCursor as today; the page component drives subsequent loads on the client)"
    - "src/lib/server/http/routes/events.ts (already supports ?cursor= multi-page reads; no change)"
    - "messages/en.json (feed_loading_more = 'Loading more…'; feed_no_more = 'No more events.'; drop feed_pager_newer / feed_pager_older keys)"
    - "tests/browser/feed-360.test.ts (browser flow: scroll feed → IntersectionObserver triggers next fetch → cards appended; reach end → 'No more events' rendered)"
    - "src/lib/components/DeletedEventsPanel.svelte (SEPARATE: keep its current behavior since recovery is a one-time browse, not a continuous feed)"
  missing:
    - "IntersectionObserver-driven infinite loader on /feed"
    - "Loading + end-of-feed states"
    - "<CursorPager> deletion"
    - "Browser regression test asserting infinite-scroll flow"
    - "Accessibility: 'Load more' button as fallback when JavaScript is disabled or when prefers-reduced-motion is active (consider — keep simple JS-required default for v1 if needed; document in plan)"
  source_test: "Test 12 / post-kind verification (continued design refinement)"
  parent_phase: 02.1
  scope_note: "Pure UI swap. Backend cursor contract is unchanged; only the trigger mechanism (button click → scroll-into-view) differs. Largest UX impact in this gap-closure cycle. If a11y fallback is needed (e.g. for screen-reader users who scroll less aggressively), keep a small 'Load more' button as a sentinel-adjacent affordance — but default behavior is auto-load on scroll."
