---
phase: 02.1-architecture-realignment
plan: 22
subsystem: ui
tags: [svelte5, paraglide, sticky-positioning, body-scroll-lock, invalidateAll, edit-gate]

# Dependency graph
requires:
  - phase: 02.1-architecture-realignment
    provides: FiltersSheet schema-prop API (Plan 02.1-21), /sources page (Plan 02.1-08), /settings sessions list (Plan 02.1-09), SourceRow (Plan 02.1-08)
provides:
  - Layout flex-column scaffold (.layout-root with min-height: 100vh) — anchors sticky children for AppHeader and per-page sticky CTAs
  - AppHeader position: sticky; top: 0 — header stays visible across page scroll
  - FiltersSheet body-scroll-lock — document.body.style.overflow="hidden" while open; restored on every close path (Esc/backdrop/Apply/Cancel/unmount)
  - /sources page-header sticky positioning — `+ Add data source` CTA reachable while a long source list scrolls
  - /settings → /keys/steam discoverability link (MINIMAL fix — unified /settings/credentials hub deferred to Phase 3+)
  - SessionsList per-session sign-out triggers invalidateAll() so the page-level loader re-runs and the destroyed session disappears
  - SourceRow auto_import becomes a non-interactive pill in row-display mode; the toggle relocated into the inline edit form alongside rename
  - 4 new Paraglide keys (settings_credentials_heading, settings_credentials_steam_link_label, sources_auto_import_on, sources_auto_import_off) with EXPECTED_KEYS snapshot updated in lock-step (D-41)
affects: [02.1-23, 02.1-24, 02.1-25, 02.1-26]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Flex-column layout scaffold (display: flex; flex-direction: column; min-height: 100vh) as a prerequisite for position: sticky on direct children — applied at root +layout.svelte"
    - "Svelte 5 $effect cleanup-function pattern for body-scroll-lock (return () => { document.body.style.overflow = ''; }) — fires on every effect re-run / unmount"
    - "Per-row edit-gate: read-mode renders a non-interactive pill, edit form ships both display-name AND auto-import in one PATCH — eliminates one-click footguns without an extra confirmation modal"

key-files:
  created: []
  modified:
    - "src/routes/+layout.svelte"
    - "src/lib/components/AppHeader.svelte"
    - "src/lib/components/FiltersSheet.svelte"
    - "src/routes/sources/+page.svelte"
    - "src/routes/settings/+page.svelte"
    - "src/lib/components/SessionsList.svelte"
    - "src/lib/components/SourceRow.svelte"
    - "tests/browser/responsive-360.test.ts"
    - "tests/integration/empty-states.test.ts"
    - "messages/en.json"
    - "tests/unit/paraglide.test.ts"
    - ".planning/phases/02.1-architecture-realignment/02.1-VALIDATION.md"

key-decisions:
  - "Layout scaffold lives in src/routes/+layout.svelte (NOT global :body styles) — keeps the sticky-anchor concern visible to anyone who opens the layout file; matches the existing 'main has max-width + padding' pattern that already lives there"
  - "AppHeader sticky-top z-index: 10 + /sources page-header sticky-under-AppHeader z-index: 5 — explicit layering so future sticky elements can choose a tier (10 = chrome, 5 = page-header, lower for content)"
  - "FiltersSheet body-scroll-lock implemented via $effect cleanup function ($effect returns a teardown function that runs on re-run/unmount) — the cleanup path covers every close mode (Esc/backdrop/Apply/Cancel/unmount) without per-handler duplication; onDialogCancel also restores explicitly to keep the intent visible in the close handlers"
  - "/keys/steam discoverability is the MINIMAL fix only — a single link from /settings; the unified /settings/credentials hub described in UAT-NOTES.md §8.2-redesign is deferred to Phase 3+ when KEYS-01 (YouTube) and KEYS-02 (Reddit) become functional. The Paraglide keys are prefixed _credentials_ so the Phase 3+ rebuild is search-and-restructure, not from-scratch"
  - "SessionsList swaps invalidate('/settings') → invalidateAll() — the targeted invalidate did not pull in the +layout.server.ts session loader path that actually populates this list; invalidateAll() is the correct hammer for this case and matches /sources / /feed / /games which already use invalidateAll on mutate-then-refresh"
  - "SourceRow auto_import edit-gate uses option A from UAT-NOTES.md §2.4-decision (KEEP the field, FIX the UX) — read-mode pill replaces the inline checkbox so a misclick can't flip auto-import; toggle ships in the SAME PATCH as rename so the edit-form save is one round-trip"

requirements-completed: []  # FEED-01, SOURCES-01, UX-02 are partially advanced; round-3 closure tracked at phase-completion gate

# Metrics
duration: ~12min
completed: 2026-04-29
---

# Phase 02.1 Plan 22: Bug-batch round-3 (sticky / body-scroll-lock / invalidateAll / /keys/steam link / auto-import edit-gate) Summary

**Closes 5 round-3 UAT bug findings via small, layered fixes across the global UI shell: layout flex-column scaffold + AppHeader sticky-top + FiltersSheet body-scroll-lock + /sources sticky page-header + /settings → /keys/steam link + SessionsList invalidateAll + SourceRow auto_import edit-gate.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-04-29T12:51:09Z
- **Completed:** 2026-04-29T13:03:17Z (approximate)
- **Tasks:** 2 (auto-mode parallel executor)
- **Files modified:** 12 (no new files created)

## Accomplishments

- **UAT-NOTES.md §2.2-bug closure** — Sticky positioning works globally:
  - Root `+layout.svelte` wraps content in `.layout-root` (flex column, min-height: 100vh) so position-sticky descendants anchor against a real scrolling parent.
  - AppHeader `.header` gains `position: sticky; top: 0; z-index: 10`.
  - /sources `.head` gains `position: sticky; top: 72px; z-index: 5; background: var(--color-bg)` so the `+ Add data source` CTA stays reachable while a long source list scrolls.
- **UAT-NOTES.md §1.4-bug closure** — FiltersSheet locks body scroll on open; restores on every close path. Implementation: in the existing `$effect` that calls `dialogEl.showModal()`, also set `document.body.style.overflow = "hidden"` and return a cleanup function that resets it. `onDialogCancel` also restores explicitly so the intent is visible in the close handler. Covers /feed and /audit reuse via Plan 02.1-21's schema-prop unification.
- **UAT-NOTES.md §8.1-bug closure (minimal)** — /settings adds a "Credentials" block with a single link to /keys/steam. The unified /settings/credentials hub described in §8.2-redesign is DEFERRED to Phase 3+ (when KEYS-01 + KEYS-02 become functional); a TODO comment + the `_credentials_` Paraglide-key prefix mark the rebuild surface.
- **UAT-NOTES.md §6.3-bug closure** — SessionsList swaps `invalidate("/settings")` → `invalidateAll()` so the +layout.server.ts session loader re-runs and the destroyed session disappears without F5. The previous targeted invalidate did not pull in the loader path that actually populates the list.
- **UAT-NOTES.md §2.4-decision (option A) closure** — SourceRow auto_import becomes a read-only pill in display mode. The toggle moves into the inline edit form alongside the rename input. `saveDisplayName` is renamed to `saveSourceEdit`; the PATCH body now sends BOTH `displayName` AND `autoImport` in one request (the route already accepted both — Plan 02.1-06). The standalone `toggleAutoImport` function is DELETED.
- **Paraglide invariant D-41 honored** — 4 new keys (`settings_credentials_heading`, `settings_credentials_steam_link_label`, `sources_auto_import_on`, `sources_auto_import_off`) added to `messages/en.json` AND `tests/unit/paraglide.test.ts` EXPECTED_KEYS in lock-step. Snapshot test passes.
- **Browser regression placeholder added** — `tests/browser/responsive-360.test.ts` Plan 02.1-22 describe block documents the three sticky / body-scroll-lock contracts as `it.skip` (auth harness deferred to Phase 6 — same pattern Plans 02-11 / 02.1-16 / 02.1-19 / 02.1-20 / 02.1-21 use).
- **Integration assertion added** — `tests/integration/empty-states.test.ts` Plan 02.1-22 case asserts `/settings/+page.svelte` contains the literal `/keys/steam` href + both new `settings_credentials_*` keys. Guards §8.1-bug closure against future refactors.
- **VALIDATION.md updated** — Per-Task Verification Map flips 02.1-22 T1+T2 from `⬜ pending` to `✅ + ⬜ manual`.

## Task Commits

1. **Task 1: layout sticky scaffold + AppHeader sticky-top + FiltersSheet body-scroll-lock + /sources sticky page-header + browser tests** — `976d49c` (feat)
2. **Task 2: /settings credentials block + sign-out invalidateAll + SourceRow auto_import edit-gate + paraglide keys + integration assertion + VALIDATION.md** — `1bc826a` (feat)

## Files Created/Modified

- `src/routes/+layout.svelte` — Wraps existing AppHeader + Nav + main in `.layout-root` (flex column, min-height: 100vh). `main` gains `flex: 1; width: 100%` so it fills the remaining viewport while keeping `max-width: 1024px` constraint.
- `src/lib/components/AppHeader.svelte` — `.header` rule gains `position: sticky; top: 0; z-index: 10`. Background fill (`--color-surface`) was already present.
- `src/lib/components/FiltersSheet.svelte` — `$effect` body sets `document.body.style.overflow = "hidden"` after `dialogEl.showModal()` and returns a cleanup function that resets it; `onDialogCancel` also restores explicitly. (Sibling Plan 02.1-24's `event.marked_standalone` / `event.unmarked_standalone` audit-action additions were absorbed into this commit per the parallel-staging precedent — see Deviations.)
- `src/routes/sources/+page.svelte` — `.head` rule gains `position: sticky; top: 72px; z-index: 5; padding: var(--space-sm) 0; background: var(--color-bg)` so the page-header (with the `+ Add data source` CTA) stays reachable while content scrolls.
- `src/routes/settings/+page.svelte` — New `<article class="block">` ABOVE the Activity log block: heading `settings_credentials_heading`, paragraph blurb, link to `/keys/steam` with label `settings_credentials_steam_link_label`. Inline TODO comment marks the Phase 3+ /settings/credentials rebuild surface.
- `src/lib/components/SessionsList.svelte` — Imports swapped (`invalidate` → `invalidateAll`); `signOutOne` calls `await invalidateAll()` instead of `await invalidate("/settings")`. Comment cites §6.3-bug closure.
- `src/lib/components/SourceRow.svelte` — Standalone `toggleAutoImport` function DELETED. `saveDisplayName` renamed to `saveSourceEdit`; PATCH body now sends `{ displayName, autoImport }`. New `editAutoImport` `$state` re-seeded each time edit form opens. Read-mode `<label class="toggle">` checkbox replaced by non-interactive `<span class="auto-pill">`. Edit form gains a checkbox bound to `editAutoImport`. New `.auto-pill` CSS rule mirroring `.ownership-badge`.
- `tests/browser/responsive-360.test.ts` — New `describe("Plan 02.1-22 — sticky + body-scroll-lock at 360px", ...)` block with three `it.skip` placeholders documenting the AppHeader sticky-top / /sources page-header sticky / FiltersSheet body-scroll-lock contracts (auth-harness-deferred per Phase 6 precedent).
- `tests/integration/empty-states.test.ts` — New Plan 02.1-22 assertion: file-content grep of `src/routes/settings/+page.svelte` for `/keys/steam` href + both new `settings_credentials_*` keys.
- `messages/en.json` — 4 new keys added (settings_credentials_heading "Credentials", settings_credentials_steam_link_label "Steam Web API key", sources_auto_import_on "Auto-import: On", sources_auto_import_off "Auto-import: Off"). (Sibling Plan 02.1-24's 4 keys absorbed in same commit.)
- `tests/unit/paraglide.test.ts` — 4 new keys appended to EXPECTED_KEYS in alphabetical order. (Sibling Plan 02.1-24's 4 keys absorbed in same commit.)
- `.planning/phases/02.1-architecture-realignment/02.1-VALIDATION.md` — Per-Task Verification Map flips 02.1-22 T1 + T2 to `✅ + ⬜ manual`; T2's automated command extends to `pnpm test:integration -- tests/integration/empty-states.test.ts`.

## Decisions Made

See frontmatter `key-decisions`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking parallel-staging race] Sibling Plan 02.1-24 changes absorbed into Task 1's FiltersSheet commit and Task 2's messages/en.json + paraglide.test.ts commit**
- **Found during:** Both tasks (sibling parallel agents staging into the same working tree)
- **Issue:** Per-plan atomic-commit invariant: a Plan 02.1-22 commit should contain ONLY Plan 22 changes. But sibling Plan 02.1-24 (parallel agent) is editing FiltersSheet.svelte (`event.marked_standalone` / `event.unmarked_standalone` audit-action additions) AND messages/en.json + paraglide.test.ts (4 new keys) at the same time and writing into the same main-repo working tree (worktree path translation pulls every parallel agent's edits into one shared dirty index). Using `git commit -o <pathspec>` scopes the commit to the named files but cannot cherry-pick hunks within those files.
- **Fix:** Used `git commit -o <pathspec> --no-verify` per the orchestrator's parallel-execution guidance. Task 1's FiltersSheet commit absorbed Plan 24's 4 audit-action lines (added before my body-scroll-lock change took effect). Task 2's messages/en.json + paraglide.test.ts commit absorbed Plan 24's 4 keys (the EXPECTED_KEYS snapshot is a single contract surface — committing my keys without the sibling's would break the snapshot test). Wave-level outcome correct; per-plan attribution slightly off for the absorbed hunks.
- **Files modified:** src/lib/components/FiltersSheet.svelte (Task 1), messages/en.json + tests/unit/paraglide.test.ts (Task 2)
- **Verification:** Both commits pass paraglide-test (`pnpm exec vitest run tests/unit/paraglide.test.ts` — 5/5 green). Plan 02.1-09 deviation pattern explicitly accepts this race.
- **Committed in:** 976d49c (Task 1), 1bc826a (Task 2)
- **Precedent:** Plan 02.1-09 deviation note "Task 1 work absorbed by Plan 07's commit e1f8435 due to a parallel-staging race (Plan 02.1-02 deviation pattern). Per-plan atomic-commit invariant broken; wave-level outcome correct."

**2. [Rule 2 - Missing critical] /sources page-header `padding: var(--space-sm) 0` + background fill added to sticky `.head` rule beyond the plan's bare-bones spec**
- **Found during:** Task 1 (visualizing the sticky behavior)
- **Issue:** A bare `position: sticky; top: 72px; z-index: 5` without padding + background fill leaks scrolled content through the gap between the sticky element and the AppHeader. The plan's `<interfaces>` block flagged this risk ("Add `background: var(--color-bg);` to .head so scrolled content does not bleed through") but the action steps in `<task>` only specified the sticky rules; padding was implicit.
- **Fix:** Added `padding: var(--space-sm) 0` (matching common sticky-header convention) and `background: var(--color-bg)` (matching the plan's `<interfaces>` instruction).
- **Files modified:** src/routes/sources/+page.svelte
- **Verification:** Lint clean; the rule sits in the existing `.head` block alongside the existing flex / align-items / wrap rules.
- **Committed in:** 976d49c (Task 1)

---

**Total deviations:** 2 (1 blocking parallel-staging absorption, 1 missing critical CSS).
**Impact on plan:** Both auto-fixes preserve plan intent. The parallel-staging absorption is a known and accepted race pattern (Plan 02.1-09 precedent). The padding + background fill is a plan-anticipated addition surfaced by the `<interfaces>` block.

## Issues Encountered

- **Pre-existing tests failures (3) unrelated to this plan:** `tests/unit/format-feed-date.test.ts` (1 case — Plan 02.1-16 inheritance), `tests/unit/logger.test.ts` (1 case — flags `routes/settings/+page.server.ts:15` comment containing "process.env" verbatim, the env-discipline grep does not distinguish code from comments). Logged in `.planning/phases/02.1-architecture-realignment/deferred-items.md` and not regressed by this plan. Out of scope per Rule "SCOPE BOUNDARY".
- **Pre-existing typecheck errors (5) in tests/integration/audit-render.test.ts (`notes` not on `EventDtoLite`) + game-listings.test.ts + games.test.ts (deleted-during-baseline-collapse module imports).** All pre-existing per Plan 02.1-21 SUMMARY's Issues Encountered; not regressed by this plan.
- **Pre-existing Svelte 5 `state_referenced_locally` warnings on FiltersSheet.svelte initial-state derivations** unchanged by this plan (Plan 02.1-21 inheritance per its SUMMARY).
- **Parallel-agent worktree pollution in pnpm lint scope** — `.claude/worktrees/agent-*/` directories carry test files that read `process.env`. Not my code (sibling agents). The full-suite `pnpm lint` cannot run cleanly under parallel execution; lint scoped to my Task files (`pnpm exec eslint <files>`) passes.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Plans 02.1-23 (FeedCard + SourceRow polish — depends on Plan 21) and 02.1-24 (event standalone-mark schema — depends on Plan 21) can continue parallel execution; their staged changes are visible in the working tree at the time this commit lands.
- Round-3 sticky / body-scroll-lock / sign-out-refresh / /keys/steam link / auto-import edit-gate UAT spec is satisfied at code-review level. Manual UAT round 3 (auth-harness needed) is the final gate — covered by VALIDATION.md "Manual-Only Verifications" until Phase 6 ships the harness.
- Privacy & multi-tenancy invariants (CLAUDE.md / AGENTS.md) preserved: no schema changes, no env reads outside config/env.ts, PATCH /api/sources/:id contract unchanged (already accepts displayName + autoImport in same payload — Plan 02.1-06), the moved auto_import toggle still flows through the userId-scoped service function `updateSource(userId, ...)`, no new client-only state can leak across tenants.
- The /settings/credentials hub deferral marker (TODO comment + `_credentials_` Paraglide-key prefix) is in place so the Phase 3+ rebuild is mechanical.

## Self-Check: PASSED

Verified after writing SUMMARY:

- ✓ src/routes/+layout.svelte exists (modified) — `min-height: 100vh` present
- ✓ src/lib/components/AppHeader.svelte exists (modified) — `position: sticky` present
- ✓ src/lib/components/FiltersSheet.svelte exists (modified) — `document.body.style.overflow` set + restored (3 occurrences)
- ✓ src/routes/sources/+page.svelte exists (modified) — `position: sticky` present
- ✓ src/routes/settings/+page.svelte exists (modified) — `/keys/steam` href + `settings_credentials_heading` present
- ✓ src/lib/components/SessionsList.svelte exists (modified) — `invalidateAll` present, no `invalidate("/settings")`
- ✓ src/lib/components/SourceRow.svelte exists (modified) — `toggleAutoImport` DELETED, `editAutoImport` declared 3× (declaration + seed + bind), `auto-pill` + `sources_auto_import_on` present
- ✓ tests/browser/responsive-360.test.ts exists (modified) — `Plan 02.1-22` describe block present
- ✓ tests/integration/empty-states.test.ts exists (modified) — Plan 02.1-22 assertion present (4 tests pass — verified via `pnpm exec vitest run tests/integration/empty-states.test.ts`)
- ✓ messages/en.json exists (modified) — all 4 new keys present
- ✓ tests/unit/paraglide.test.ts exists (modified) — all 4 new keys in EXPECTED_KEYS (5 tests pass — verified via `pnpm exec vitest run tests/unit/paraglide.test.ts`)
- ✓ .planning/phases/02.1-architecture-realignment/02.1-VALIDATION.md exists (modified) — 02.1-22 T1+T2 status flipped to ✅ + ⬜ manual
- ✓ Commit 976d49c (Task 1) present in `git log`
- ✓ Commit 1bc826a (Task 2) present in `git log`

---
*Phase: 02.1-architecture-realignment*
*Completed: 2026-04-29*
