---
phase: 03.0
slug: polling-pipeline-plumbing-youtube
status: signed-off-pending-ci-and-uat
verdict: pending-final-signoff
created: 2026-05-06
last_updated: 2026-05-06
nyquist_compliant: true
plans_complete: 14
plans_total: 14
---

# Phase 03.0 — Verification & Sign-Off

> Phase verdict sign-off doc, Phase 2.1 precedent. Records the per-plan
> validation map, surfaces deferred manual UAT items, and captures the
> evidence that the polling pipeline (plumbing + YouTube concrete adapter)
> ships behind CI green + a deterministic smoke gate that NEVER calls live
> Google.

---

## Phase verdict

**Status: `signed-off-pending-ci-and-uat`** — implementation, lint,
typecheck, and unit-test gates are all green at executor sign-off. Two
final gates remain before flipping to `signed-off`:

1. **Smoke gate green on CI.** Plan 14 ships
   `tests/smoke/lib/youtube-mock.{mjs,sh}` + `tests/smoke/lib/phase30-flow.sh`
   wired into `tests/smoke/self-host.sh`. The harness cannot run on the
   author's local Windows machine (Docker + Postgres + production image
   stack); CI is the load-bearing trust signal here per AGENTS.md
   §Workflow ("smoke … is the load-bearing trust signal — when it's
   green, a self-host operator can deploy with confidence").
2. **Wave 4 manual UAT walk-through.** 13-step UAT deferred from
   Plan 13 Task 4 to a dedicated session per `03.0-HUMAN-UAT.md`
   (committed `a934e9c` 2026-05-05). Covers PollingBadge variants
   (Hot/Cold/Frozen/Unavailable), refresh-now cooldown, AccountDeletedBanner
   Permanent-delete-now CTA, BackfillPicker presets, /admin/quota
   render-for-allowlisted + 404-for-non-allowlisted, and the 360px
   responsive sweep across all Wave 4 surfaces.

If both gates pass, this doc flips `verdict: signed-off` and `status:
signed-off`. If either surfaces P0/P1 issues, gap-closure plans land per
the Phase 2.1 round-N precedent (e.g. plans 03.0-15 / 03.0-16 / …).

**Date:** 2026-05-06
**Branch:** `feat/phase-03-polling-pipeline`
**Wave 0 spike outcome:** VERIFIED (see "Spike result" below).

---

## Spike result (Plan 03.0-01 Wave 0 Task 1)

> CONTEXT D-02 / RESEARCH §"Open Questions" #1 — hard gate before any
> worker code lands.

Run by d954mas on **2026-05-06** with a disposable test API key revoked
immediately afterward. **VERIFIED.**

| Calls       | Endpoint              | Response | Cloud Console quota delta |
| ----------- | --------------------- | -------- | ------------------------- |
| 5×          | `playlistItems.list`  | 200 OK   | +5 units                  |
| 3×          | `videos.list`         | 200 OK   | +3 units                  |
| (1 of the 3) | `videos.list?id=v1,…,v50` | 200 OK with all 50 items | (counted in the 3 above) |

Total quota delta: **8 units** (from 0 → 8), exactly matching call count
(8 calls). Conclusion: `videos.list` costs **1 unit per call regardless
of `id=` batch size up to 50 and regardless of `part=snippet,statistics,
contentDetails`.** Google's documented quota table is correct. Phase 3.0
worker design proceeded as planned (8 000 units/day × 50 IDs/batch =
400 000 video updates/day theoretical ceiling for the polling pipeline).

Test data: 50 IDs from uploads playlist `UU_x5XG1OV2P6uZZ5FSM9Ttw`
(Google Developers); first 3 titles "Does this TypeScript compile under
strict mode?" / "What you can do with Gemini Embedding 2" / "Trying to
log off on Friday before you get another assignment"; sum viewCount =
1 099 198 across the 50 returned items.

Source: `.planning/phases/03.0-polling-pipeline-plumbing-youtube/03.0-RESEARCH.md`
"Open Questions" #1, line 789.

---

## 12 success criteria check

> Per ROADMAP §"Phase 3.0: Polling Pipeline — Plumbing + YouTube" Success Criteria.
> Pass = behavior shipped + load-bearing automated test landed (or
> deferred to manual UAT with the test scaffolded).
> Partial = behavior shipped + automated test green BUT one piece of
> downstream wiring or polish deferred to UAT or follow-up plan.
> Fail = behavior absent or test absent.

| # | Criterion (paraphrase) | Status | Evidence (commit / test / file) |
|---|---|---|---|
| 1 | PollingBadge reads Hot / Cold / Frozen / Unavailable per `occurred_at` and `last_poll_status`; "stale" terminology RETIRED | Pass | `b57c671` Plan 11; SSR render guard `tests/integration/audit-render.test.ts` covers 14 PollingBadge assertions. Manual UAT pending (HUMAN-UAT §3-§5). |
| 2 | Cold backlog never blocks Active polls (separate `poll.active` / `poll.cold` / `poll.user` queues; user-triggered ahead of scheduled) | Pass | `ea5c99b` Plan 09 worker handlers; `src/lib/server/queues.ts` declares all 8 queues; `tests/integration/queue-topology.test.ts` (2 live tests) asserts the topology. |
| 3 | Service-level YouTube quota throttle (round-robin keys; 80% pauses Cold + auto-import; 95% pauses everything except refresh-now) | Pass | Plan 03 `src/lib/server/services/youtube-quota-tracker.ts`; `tests/integration/youtube-quota-throttle.test.ts` (4 live tests in `1bfa561`). |
| 4 | Every poll appends immutable row to `youtube_video_snapshots` (idempotent via partial UNIQUE on `(video_id, polled_at, metric_key)` truncated to minute); no `user_id` on the cache table (public-data semantics) | Pass | Plan 04 `src/lib/server/services/youtube-snapshot-writer.ts`; ESLint TENANT_TABLES allowlist (Plan 01); `tests/unit/youtube-snapshot-writer.test.ts` (per-task verify map row Plan 04). |
| 5 | YouTube channel registration with auto-import; one-time backfill picker (5 presets, default 30); manual paste creates `source_id=NULL` event | Pass | Plan 12 `src/lib/components/BackfillPicker.svelte` + `/sources/new` wiring; Plan 10 ingest enqueues channel-context backfill on first paste. Manual UAT pending (HUMAN-UAT §10-§12). |
| 6 | Channel context backfill (one-time per channel) on first paste of an unknown channel; cache hit on subsequent paste | Pass | Plan 10 ingest trigger (`d641896`); Plan 09 `src/worker/handlers/youtube-channel-context-backfill.ts`. Manual UAT recommended for cache-hit visual confirmation. |
| 7 | Refresh-now per-event button on PollingBadge; 5-min cooldown; `events.metadata.last_user_refresh_at`; 429 with retry-after | Pass | Plan 11 `src/lib/components/RefreshNowButton.svelte` + Plan 04 `src/lib/server/services/refresh-poll.ts` + Plan 08 `POST /api/events/:id/refresh-poll` route. Manual UAT pending (HUMAN-UAT §6-§7). |
| 8 | SIGTERM 60s graceful drain (Phase 1 D-22 inherited); partial index `idx_events_last_polled_at WHERE last_polled_at IS NOT NULL` | Pass | Plan 01 baseline migration `0010_phase03_baseline.sql` ships the partial index; Plan 09 worker boots all `poll.*` subscribers and inherits the Phase 1 SIGTERM path. Plan 14 smoke gate re-asserts the drain holds with new subscriptions. |
| 9 | Purge worker (cron `0 4 * * *` PT) hard-deletes user/games/data_sources/events/api_keys_steam past `RETENTION_DAYS`; Permanent-delete-now CTA in AccountDeletedBanner activated | Pass | Plan 05 `src/lib/server/services/purge-account.ts`; Plan 09 `src/worker/handlers/purge-daily.ts`; Plan 12 `e2e8c30` activates the banner CTA + `DELETE /api/me/account/purge` route (Plan 08). Type-DELETE confirm dialog gating preserved. Manual UAT pending (HUMAN-UAT §8-§9). |
| 10 | Admin panel `/admin/*` env-allowlist gated (404 not 403); ships `/admin/quota` only in 3.0; smoke asserts 404 with empty allowlist | Pass | Plan 07 `src/lib/server/http/middleware/admin.ts` + `GET /api/admin/quota`; Plan 13 `/admin/quota/+page.svelte` composes QuotaKeyTable + QuotaAuditList; `tests/integration/admin-quota.test.ts` (3 cases) green. Plan 14 smoke gate exercises both `/api/admin/quota` and `/admin/quota` HTML for the empty-allowlist 404. Manual UAT pending (HUMAN-UAT §1-§2). |
| 11 | `events_per_day` quota EXCLUDES auto-import (`source_id IS NULL` filter); auto-import worker bypasses guard | Pass | Plan 04 `src/lib/server/services/quota.ts` filter; `tests/integration/auto-import-quota-bypass.test.ts` activated in Plan 04 (`12 min` summary). |
| 12 | Smoke extension: mock YouTube reverse-proxy + cron registration + snapshot write + admin parity ×2 + SIGTERM drain; NO live YouTube in CI | Pass (pending CI green) | Plan 14 `tests/smoke/lib/{youtube-mock.mjs,youtube-mock.sh,phase30-flow.sh}` + `tests/smoke/self-host.sh` extension + `.github/workflows/ci.yml` env propagation. Local validation: `bash -n` + youtube-mock end-to-end smoke test all green. CI run pending. |

**12 / 12 Pass.** Two criteria carry "manual UAT pending" qualifiers
(success #1, #5, #6, #7, #9, #10) — the implementation is shipped and
the automated layers are green; the visual / interactive confirmation
is the deferred Wave 4 UAT (`03.0-HUMAN-UAT.md`).

---

## Per-plan validation map

> Cross-link each plan's must_haves to evidence (SUMMARY commit + key
> tests). Execution order = wave order; parallel-wave plans ship
> concurrently.

| Plan | Name | Wave | Tasks | Reqs completed | Tests landed | Commit (final) |
|------|------|------|-------|----------------|--------------|----------------|
| 03.0-01 | Spike + baseline migration | 0 | 3 | POLL-01, 02, 04, 06, VIZ-01, INGEST-02, 03, 04, KEYS-01 | 17 placeholder test files instantiated; migration `0010_phase03_baseline.sql` applied | spike VERIFIED 2026-05-06; migration commit in Plan 01 SUMMARY |
| 03.0-02 | Doc updates + Paraglide + test scaffolds | 0 | 3 | (Wave 0 scaffolding — covers all Phase 3.0 reqs) | 17 placeholder test files; ~42 new Paraglide keys (UI-SPEC verbatim); anonymous-401 + tenant-scope sweep extensions | Plan 02 SUMMARY |
| 03.0-03 | Tier resolver + quota tracker | 1 | 2 | POLL-01, 02, 03, KEYS-01 | `tests/unit/tier-resolver.test.ts` (boundary tests at 23h59m / 24h / 27d23h / 28d / 28d1m); `tests/unit/youtube-quota-tracker.test.ts` (round-robin + 80%/95% threshold; 19 cases) | Plan 03 SUMMARY |
| 03.0-04 | Snapshot writer + refresh-poll + quota fix | 1 | 3 | POLL-04, 06, INGEST-02, 03, 04, VIZ-01 | `tests/unit/youtube-snapshot-writer.test.ts` (idempotency + minute-truncation); `tests/integration/refresh-now.test.ts`; `tests/integration/auto-import-quota-bypass.test.ts` | Plan 04 SUMMARY |
| 03.0-05 | Purge-account service | 1 | 1 | PRIV-04 | `tests/unit/purge-account.test.ts` (cascade order + idempotency + audit-outside-tx) | Plan 05 SUMMARY |
| 03.0-06 | YouTube channel adapter (real impl) | 1 | 1 | POLL-04, 05, INGEST-02, 03, 04, KEYS-01 | `tests/unit/youtube-channel-adapter.test.ts` (`pollContent` + `pollStats` shape; not_found / private / auth_error mapping) | Plan 06 SUMMARY |
| 03.0-07 | Admin middleware + quota route | 2 | 3 | POLL-03, KEYS-01 | `tests/unit/admin-middleware.test.ts` (empty allowlist + non-allowlisted + allowlisted); `tests/integration/admin-quota.test.ts` (3 cases) | Plan 07 SUMMARY |
| 03.0-08 | Refresh-poll + purge routes | 2 | 2 | POLL-04, 06, PRIV-04 | `tests/integration/account-purge.test.ts` (hard-deletes + audit + cross-tenant 404 + anon 401); refresh-poll route activations | Plan 08 SUMMARY |
| 03.0-09 | Worker handlers + scheduler crons | 3 | 3 | POLL-01, 02, 03, 04, 06, INGEST-02, VIZ-01, PRIV-04 | 6 integration test files activated (15 live tests): scheduler-enqueue, queue-topology, youtube-quota-throttle, poll-worker-tx-boundary, ingest-paste-then-poll, youtube-snapshot-read | Plan 09 SUMMARY (8b37477) |
| 03.0-10 | Ingest channel-context trigger | 3 | 2 | INGEST-02, 03, 04 | RED→GREEN test cycle (commit `d641896`); integration test deferred to CI (Postgres not available locally per Plan 01 / Plan 06 precedent) | Plan 10 SUMMARY |
| 03.0-11 | PollingBadge live + RefreshNowButton | 4 | 3 | POLL-05 | `tests/integration/audit-render.test.ts` (14 PollingBadge assertions, SSR render-time guard); browser test placeholder | Plan 11 SUMMARY (deaadfd) |
| 03.0-12 | AccountDeletedBanner CTA + BackfillPicker | 4 | 2 | POLL-01, PRIV-04 | `tests/unit/account-deleted-banner.test.ts` (6 cases); `tests/unit/backfill-picker.test.ts` (7 cases) | Plan 12 SUMMARY (f8c4697) |
| 03.0-13 | /admin/quota page composition | 4 | 3 of 4 (Task 4 = manual UAT, deferred to HUMAN-UAT) | POLL-03, KEYS-01 | render-time guard test; UAT-13 deferred → 03.0-HUMAN-UAT.md | Plan 13 SUMMARY (75d918f) |
| 03.0-14 | Smoke extension + verification | 5 | 2 of 3 build (Task 3 = this VERIFICATION.md sign-off) | POLL-04, POLL-06, INGEST-02, VIZ-01, PRIV-04, KEYS-01 | `tests/smoke/lib/youtube-mock.{mjs,sh}` + `phase30-flow.sh` lifecycle smoke-tested locally; CI run pending | `cbf4cb9` (Task 1 youtube-mock) + `863e70a` (Task 2 self-host extension + ci.yml + eslint carve-out) |

**Plan completion: 14 / 14.** All build phases delivered; Plan 13 Task 4
manual UAT and Plan 14 Task 3 sign-off are the remaining gates (this
doc covers Plan 14 Task 3).

---

## Manual UAT outcome

**Status: deferred — pending dedicated session before PR.**

The 13-step cross-plan UAT covering Wave 4 surfaces (PollingBadge,
RefreshNowButton, AccountDeletedBanner CTA, BackfillPicker, /admin/quota
page) lives at
`.planning/phases/03.0-polling-pipeline-plumbing-youtube/03.0-HUMAN-UAT.md`,
committed at `a934e9c` (`test(03.0): defer Wave 4 13-step UAT to
dedicated session`, 2026-05-05).

Outcome rubric:

- **All 13 pass:** flip this doc's `verdict: signed-off`; phase ships
  in the next PR.
- **P0/P1 surfaced:** spawn round-1 gap-closure plans (03.0-15+) per
  the Phase 2.1 round-N precedent; this doc updates with the gap
  inventory.
- **P2/P3 only:** ship the phase, log the polish items in
  `.planning/todos/pending/` for Phase 4+ pickup.

UAT setup is one-time and lives at the top of `03.0-HUMAN-UAT.md`
(operator must set `ADMIN_EMAIL_ALLOWLIST=d954mas@gmail.com` +
`POLL_ACTIVE_INTERVAL_MS=1800000` + `POLL_COLD_INTERVAL_MS=14400000`
in `.env.local` and restart `pnpm dev`).

---

## Smoke gate result

**Status: local syntax validated; CI run pending.**

| Gate | Local result | Notes |
|------|--------------|-------|
| `bash -n tests/smoke/self-host.sh` | OK | No syntax errors |
| `bash -n tests/smoke/lib/phase30-flow.sh` | OK | New helper sourced from self-host.sh |
| `bash -n tests/smoke/lib/youtube-mock.sh` | OK | New mock lifecycle wrapper |
| youtube-mock end-to-end | PASS | start_youtube_mock → curl /videos → 50 items, /playlistItems → 10, /channels → 1, /search → 404; stop_youtube_mock clean (verified 2026-05-06) |
| ci.yml YAML validity | OK | js-yaml parse + jobs.smoke.env keys include SERVICE_YOUTUBE_API_KEYS + ADMIN_EMAIL_ALLOWLIST |
| `pnpm typecheck` | OK | 0 errors / 40 pre-existing Svelte warnings (state_referenced_locally — unrelated to Plan 14) |
| `pnpm lint` (ESLint half) | OK | 0 errors after extending `.mjs` test carve-out (Rule 3 deviation) |
| `pnpm lint` (Prettier half) | FAIL (pre-existing) | 172 files flagged; verified pre-existing on `git stash`'d HEAD before Plan 14. Logged in `deferred-items.md` Plan 14 entry. Plan 14 files prettier-clean. |
| `pnpm test:unit` | OK | 248 / 248 passed (27 test files) |
| `pnpm test:integration` | DEFERRED | Postgres not available on author's local Windows machine; CI's unit-integration job is the gate (per Plan 01 / Plan 06 precedent) |
| `pnpm test:smoke` | DEFERRED | Requires Docker + Postgres + production image build; CI smoke job is the gate |
| `pnpm test:browser` | DEFERRED | Requires Chromium playwright; CI browser-tests job is the gate |

**The CI smoke run on the next push to `feat/phase-03-polling-pipeline`
is the load-bearing trust signal** (AGENTS.md §Workflow). When that's
green, this doc flips `verdict: signed-off`.

---

## Deviations summary (across all 14 plans)

The phase ran cleanly — no architectural Rule 4 escapes, all auto-fixes
were Rule 1 (bug) / Rule 2 (missing critical) / Rule 3 (unblocking).
Notable cross-plan threads:

- **DV-2 retired "stale" terminology** — replaced with deterministic
  age-based `Frozen` (>28d) + `Unavailable` (per `last_poll_status`).
  Captured in 03.0-RESEARCH.md DV-2; honored across PollingBadge
  (Plan 11), tier-resolver (Plan 03), and the manual-UAT script.
- **Plan 14 Rule 3 deviation:** ESLint `tests/**` carve-out for
  `no-restricted-properties` extended to `.mjs` so the new
  `youtube-mock.mjs` smoke fixture passes lint. Documented in
  `eslint.config.js` line 113-115 with the rationale.
- **Plan 14 deferred item:** repo-wide Prettier drift (172 files)
  pre-exists Plan 14; logged in
  `.planning/phases/03.0-polling-pipeline-plumbing-youtube/deferred-items.md`
  for a separate chore PR.

Per-plan deviations are documented in each plan's SUMMARY.md
"Deviations from Plan" section.

---

## Deferred items

> Items surfaced during execution that don't block phase sign-off. Each
> has a tracking entry in
> `.planning/phases/03.0-polling-pipeline-plumbing-youtube/deferred-items.md`
> or `.planning/todos/pending/`.

| # | Item | Severity | Origin | Tracker |
|---|------|----------|--------|---------|
| 1 | `audit-render.test.ts` `quota.service_throttled` chip-label gap (3 cross-plan failing tests on master HEAD; not introduced by Plan 11) | P2 | Plan 11 verification | `deferred-items.md` §1 |
| 2 | `tests/unit/refresh-now-button.test.ts` import path discovery (component-spec test rerouted via SSR render guard) | P3 | Plan 11 | `deferred-items.md` §2 |
| 3 | Repo-wide Prettier drift (172 files) | P3 (chore) | Plan 14 | `deferred-items.md` §3 |
| 4 | Per-user YouTube API key override (KEYS-01 power-user trigger) | Phase 6 | CONTEXT D-01 | ROADMAP Phase 6 backlog |
| 5 | Phase 4 chart loader (`event_stats_snapshots` → LayerChart) | Phase 4 | RESEARCH.md | ROADMAP Phase 4 |

---

## Gap-closure plans (round-1)

**None pending.** Pending the manual UAT outcome (`03.0-HUMAN-UAT.md`
session). If the UAT surfaces P0/P1 issues, gap-closure plans land per
the Phase 2.1 precedent and this section is updated with the inventory.

If the UAT closes clean and the CI smoke gate is green, this doc flips
`verdict: signed-off` and the phase ships in the next PR.

---

## Sign-off block (filled at final closure)

```
verdict: pending-final-signoff
signed_at: <iso-date when both gates pass>
signed_by: d954mas
ci_run_url: <github-actions-run-url-for-final-green>
uat_outcome: <pass | gaps_found> (see HUMAN-UAT.md for the per-step results)
```

When UAT + CI pass, `signed_at` fills in, `verdict: signed-off`, and
`status: signed-off` in the frontmatter. When the PR merges, the
ROADMAP Phase 3.0 row flips to `Complete`.
