---
phase: 03.0-polling-pipeline-plumbing-youtube
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - messages/en.json
  - tests/unit/paraglide.test.ts
  - tests/unit/tier-resolver.test.ts
  - tests/unit/youtube-quota-tracker.test.ts
  - tests/unit/youtube-snapshot-writer.test.ts
  - tests/unit/youtube-channel-adapter.test.ts
  - tests/unit/admin-middleware.test.ts
  - tests/unit/quota-events-per-day-source-id.test.ts
  - tests/integration/scheduler-enqueue.test.ts
  - tests/integration/queue-topology.test.ts
  - tests/integration/youtube-quota-throttle.test.ts
  - tests/integration/refresh-now.test.ts
  - tests/integration/refresh-poll-cooldown.test.ts
  - tests/integration/account-purge.test.ts
  - tests/integration/admin-quota.test.ts
  - tests/integration/events-manual-paste-dedup.test.ts
  - tests/integration/auto-import-quota-bypass.test.ts
  - tests/integration/poll-worker-tx-boundary.test.ts
  - tests/integration/ingest-paste-then-poll.test.ts
  - tests/integration/youtube-snapshot-read.test.ts
  - tests/integration/anonymous-401.test.ts
  - tests/integration/tenant-scope.test.ts
autonomous: true
requirements: [POLL-01, POLL-02, POLL-03, POLL-04, POLL-05, POLL-06, INGEST-02, INGEST-03, INGEST-04, VIZ-01, PRIV-04, KEYS-01]

must_haves:
  truths:
    - "PROJECT.md Key Decisions table updated to split YouTube (service-level) from Reddit + Steam (per-user) — DV-1 codified"
    - "REQUIREMENTS.md notes KEYS-01 reframed (per-user override deferred Phase 6); POLL-01..05 reframed under Active/Cold/Frozen"
    - "messages/en.json carries ~14 new Paraglide keys (PollingBadge variants × 5, refresh-now × 5, /admin/quota × 16, account banner × 1, BackfillPicker × 11) per UI-SPEC verbatim"
    - "paraglide.test.ts EXPECTED_KEYS snapshot includes the new keys; deprecated polling_badge_phase3_placeholder removed"
    - "All Wave 0 placeholder test files exist with named-plan it.skip stubs (Phase 2.1 precedent); each it.skip names the future plan that will land it"
    - "tests/integration/anonymous-401.test.ts MUST_BE_PROTECTED allowlist extends with /api/events/:id/refresh-poll, /api/me/account/purge, /api/admin/quota (anchored as it.skip until those routes ship in Wave 2 plans)"
    - "tests/integration/tenant-scope.test.ts cross-tenant matrix extends with refresh-poll + admin probes (anchored as it.skip)"
  artifacts:
    - path: messages/en.json
      provides: "All Phase 3.0 Paraglide keys"
      contains: "polling_badge_frozen"
    - path: tests/unit/paraglide.test.ts
      provides: "Updated EXPECTED_KEYS snapshot"
      contains: "polling_badge_frozen"
    - path: tests/unit/tier-resolver.test.ts
      provides: "Boundary suite placeholder for Plan 03"
      contains: "tier-resolver"
    - path: tests/integration/refresh-now.test.ts
      provides: "Refresh-now endpoint placeholder for Plan 08"
      contains: "refresh-poll"
    - path: tests/integration/admin-quota.test.ts
      provides: "Admin quota endpoint placeholder for Plan 07"
      contains: "admin/quota"
  key_links:
    - from: messages/en.json
      to: tests/unit/paraglide.test.ts
      via: "EXPECTED_KEYS snapshot includes all keys in en.json (alphabetical)"
      pattern: "polling_badge_unavailable"
    - from: .planning/PROJECT.md
      to: .planning/REQUIREMENTS.md
      via: "DV-1 split per-platform — both docs updated in same plan"
      pattern: "service-level"
    - from: tests/integration/anonymous-401.test.ts
      to: src/lib/server/http/app.ts
      via: "MUST_BE_PROTECTED entries pre-declared as it.skip; activation in Wave 2 Plans 07+08"
      pattern: "/api/admin/quota"
---

<objective>
Wave 0 Task #2 (CONTEXT D-03 follow-through): land documentation updates + UI Paraglide keys + ALL placeholder test files in one atomic plan so subsequent Wave 1+ plans drop into existing files (Phase 2.1 named-plan it.skip precedent).

Three workstreams:
1. **Doc updates** — PROJECT.md "Key Decisions" table splits YouTube (service-level) from Reddit + Steam (per-user) per DV-1; REQUIREMENTS.md adds inline notes on KEYS-01 reframing + POLL-01..05 tier-model retirement.
2. **Paraglide keys** — copy the EXACT new strings from 03.0-UI-SPEC.md "Copywriting Contract" into messages/en.json (~14 keys spanning PollingBadge variants, refresh-now affordance, /admin/quota page, AccountDeletedBanner activation, BackfillPicker). Remove the now-orphan `polling_badge_phase3_placeholder` key. Update the `EXPECTED_KEYS` snapshot in tests/unit/paraglide.test.ts.
3. **Placeholder test files** — instantiate all 17 test files referenced by 03.0-VALIDATION.md "Wave 0 Requirements" section. Each file ships with named-plan `it.skip(...)` stubs that name the downstream plan responsible for activating each assertion. EXTEND existing tests (anonymous-401 + tenant-scope) with new MUST_BE_PROTECTED + cross-tenant probe entries (anchored as `it.skip` until routes ship in Wave 2).

Purpose: Wave 1+ services and Wave 2+ routes have a place to land assertions immediately; the Nyquist sampling cadence holds from Plan 03 onward.
Output: Updated docs, updated en.json + paraglide test, 17 placeholder test files (or extensions to existing files).
</objective>

<execution_context>
@C:/Users/ROG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ROG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.0-polling-pipeline-plumbing-youtube/03.0-CONTEXT.md
@.planning/phases/03.0-polling-pipeline-plumbing-youtube/03.0-RESEARCH.md
@.planning/phases/03.0-polling-pipeline-plumbing-youtube/03.0-UI-SPEC.md
@.planning/phases/03.0-polling-pipeline-plumbing-youtube/03.0-VALIDATION.md
@AGENTS.md

# Existing tests being extended
@tests/unit/paraglide.test.ts
@tests/integration/anonymous-401.test.ts
@tests/integration/tenant-scope.test.ts

# Existing message file
@messages/en.json

# Phase 2.1 it.skip placeholder reference
@.planning/phases/02.1-architecture-realignment/02.1-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PROJECT.md + REQUIREMENTS.md doc updates (DV-1 split per-platform)</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/PROJECT.md (locate "Key Decisions" table — single row about per-user API keys; locate "API quotas" Context bullet; locate "Architecture" `api_keys_*` clause)
    - .planning/REQUIREMENTS.md (locate KEYS-01 entry; locate POLL-01..05 entries)
    - .planning/phases/03.0-polling-pipeline-plumbing-youtube/03.0-CONTEXT.md (DV-1, DV-2, DV-3 — verbatim narrative for the doc edits)
  </read_first>
  <action>
    **PROJECT.md edits:**

    1. In the "Key Decisions" table, find the row that says "Per-user API keys (YouTube/Reddit/Steam) distributes quotas, bounds blast radius" (or similar single-row formulation). REPLACE that single row with three rows:
       | Decision | Why |
       |----------|-----|
       | YouTube — operator-owned service-level keys (`SERVICE_YOUTUBE_API_KEYS` env, round-robin across keys, shared `youtube_video_snapshots` cache by video_id) | Public-data semantics: same view count regardless of who polled. Per-user keys add Cloud Console UX friction for indie devs. Per-user override deferred to Phase 6 trigger (≥1 power user trips 95% of operator's quota). |
       | Reddit — per-user OAuth (BYO Reddit app credentials, 3-ciphertext-column row in `api_keys_reddit`) | Reddit ToS pins API access to a specific user identity (Pitfall 11 shadowban risk). Per-user keys are the only viable model. |
       | Steam — per-user per-publisher API keys (envelope-encrypted in `api_keys_steam`) | Steam Web API key is tied to a Steamworks publisher account; the user's own data lives behind their own key. |

    2. In the Context section, find the "API quotas — why per-user keys matter" bullet (or equivalent). REWRITE the bullet to be per-platform:
       - YouTube: 10,000 units/day per operator key; service-level model multiplies envelope by `SERVICE_YOUTUBE_API_KEYS.length` (typically 1–3 keys per Google ToS soft-limit). 80% / 95% throttle gates pause Cold + auto-import / Active polling respectively.
       - Reddit: 60 requests/minute per OAuth app; per-user keys distribute the rate-limit envelope per tenant.
       - Steam: 100,000 calls/day per Web API key; per-user keys distribute by tenant.

    3. In the Architecture section, find the `api_keys_*` clause. ADD a sentence: "Phase 3.0 ships only `api_keys_steam` (Phase 2). `api_keys_reddit` lands in Phase 3.1. NO `api_keys_youtube` table — YouTube uses operator-owned service-level keys (`SERVICE_YOUTUBE_API_KEYS` env, plaintext)."

    Add a footer line under Key Decisions: "*Updated 2026-05-05 per Phase 3.0 CONTEXT DV-1.*"

    **REQUIREMENTS.md edits:**

    1. Find the KEYS-01 line. Append an inline note: "*(reframed Phase 3.0: service-level operator keys via `SERVICE_YOUTUBE_API_KEYS` env; per-user override deferred to Phase 6 trigger ≥1 power user trips 95% of operator quota — see ROADMAP Phase 3.0 detail)*"

    2. Find the POLL-01 line ("hot/warm/cold tiers"). Append an inline note: "*(reframed Phase 3.0: 3-tier Active 0–24h / Cold 1–28d / Frozen >28d + per-event `last_poll_status` for transient errors; original Hot/Warm/Cold + Stale 4-tier model RETIRED per CONTEXT DV-2)*"

    3. Find POLL-03 ("per-user 429 surfacing"). Append: "*(reframed Phase 3.0 for YouTube: service-level throttle visible only via `/admin/quota`; per-user 429 surfacing remains scope for Reddit (Phase 3.1) + Steam (Phase 3.2). DV-3.)*"

    4. Find POLL-05 ("Hot — checked Xm ago / Warm — every 6h / Cold — daily / Stale"). REPLACE the badge copy phrase with: "Hot · checked Xh ago / Cold · yesterday / Frozen · refresh to update / Unavailable · last seen Xd ago" *(reframed Phase 3.0 per CONTEXT D-NEW PollingBadge copy)*.

    5. Update the Traceability table KEYS-01 row from `Phase 3` to `Phase 3.0 (reframed; per-user override deferred Phase 6)`.

    Add a footer near the top: "**2026-05-05 — Phase 3.0 reframing.** KEYS-01, POLL-01, POLL-03, POLL-05 reframed under the service-level YouTube + Active/Cold/Frozen tier model. See `.planning/phases/03.0-polling-pipeline-plumbing-youtube/03.0-CONTEXT.md` decisions D-05, D-06, D-13, DV-1, DV-2, DV-3."
  </action>
  <verify>
    <automated>node -e "const a = require('fs').readFileSync('.planning/PROJECT.md','utf8'); const b = require('fs').readFileSync('.planning/REQUIREMENTS.md','utf8'); if (!a.includes('SERVICE_YOUTUBE_API_KEYS')) throw new Error('PROJECT.md missing SERVICE_YOUTUBE_API_KEYS'); if (!a.includes('per-user OAuth')) throw new Error('PROJECT.md missing Reddit per-user clause'); if (!b.includes('Phase 3.0 reframing')) throw new Error('REQUIREMENTS.md missing reframing footer'); if (!b.includes('Active 0–24h')) throw new Error('REQUIREMENTS.md missing tier reframe'); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - grep "SERVICE_YOUTUBE_API_KEYS" .planning/PROJECT.md succeeds
    - grep "per-user OAuth" .planning/PROJECT.md succeeds (Reddit row)
    - grep "per-publisher" .planning/PROJECT.md succeeds (Steam row)
    - grep "Phase 3.0 reframing" .planning/REQUIREMENTS.md succeeds
    - grep "Active 0–24h" .planning/REQUIREMENTS.md succeeds (POLL-01 reframe)
    - grep "Frozen · refresh to update" .planning/REQUIREMENTS.md succeeds (POLL-05 reframe)
    - grep "Phase 3.0 (reframed" .planning/REQUIREMENTS.md succeeds (KEYS-01 traceability table)
  </acceptance_criteria>
  <done>PROJECT.md Key Decisions split per-platform; REQUIREMENTS.md inline reframing notes on POLL-01/03/05 + KEYS-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Paraglide keys land in messages/en.json + EXPECTED_KEYS snapshot updated</name>
  <files>messages/en.json, tests/unit/paraglide.test.ts</files>
  <read_first>
    - messages/en.json (existing key style — alphabetical or grouped; preserve project convention)
    - .planning/phases/03.0-polling-pipeline-plumbing-youtube/03.0-UI-SPEC.md "Copywriting Contract" section — copy the EXACT English strings verbatim
    - tests/unit/paraglide.test.ts (existing EXPECTED_KEYS array — sorted alphabetically; new entries must be added in their sorted positions)
  </read_first>
  <behavior>
    - Test 1: Object.keys(messages/en.json).sort() equals EXPECTED_KEYS.sort()
    - Test 2: messages/en.json contains the literal strings "Hot · checked", "Cold · yesterday", "Frozen · refresh to update", "Unavailable · last seen", "Throttled — operator quota"
    - Test 3: The deprecated key polling_badge_phase3_placeholder is REMOVED from messages/en.json AND from EXPECTED_KEYS
  </behavior>
  <action>
    **EXTEND `messages/en.json`** — ADD these EXACT keys (copy from UI-SPEC verbatim; Paraglide ICU-style placeholders use `{name}` syntax):

    ```json
    "polling_badge_hot": "Hot · checked {hoursAgo}h ago",
    "polling_badge_cold_yesterday": "Cold · yesterday",
    "polling_badge_cold_days_ago": "Cold · {daysAgo}d ago",
    "polling_badge_frozen": "Frozen · refresh to update",
    "polling_badge_unavailable": "Unavailable · last seen {daysAgo}d ago",
    "polling_badge_throttled": "Throttled — operator quota",
    "polling_refresh_now_aria": "Refresh now",
    "polling_refresh_now_cooldown_tooltip": "Wait {minutesLeft}m before refreshing again",
    "polling_refresh_now_pending": "Refreshing…",
    "polling_refresh_now_done": "Polled just now",
    "polling_refresh_now_error": "Refresh failed. Try again.",
    "admin_quota_page_title": "Operator quota — YouTube",
    "admin_quota_page_intro": "Today's YouTube Data API v3 spend across operator-owned keys, in Pacific Time. Resets at midnight Pacific.",
    "admin_quota_section_keys_title": "API keys — today's usage",
    "admin_quota_section_audit_title": "Service-level audit — last 50 entries",
    "admin_quota_table_col_key_id": "Key id",
    "admin_quota_table_col_units_used": "Units used",
    "admin_quota_table_col_pct_of_daily": "% of 10 000",
    "admin_quota_table_col_status": "Status",
    "admin_quota_status_ok": "OK",
    "admin_quota_status_80_throttle": "80% throttle — Cold paused",
    "admin_quota_status_95_throttle": "95% throttle — Active paused",
    "admin_quota_keys_empty_heading": "No usage today.",
    "admin_quota_keys_empty_body": "Either no polls have run today, or SERVICE_YOUTUBE_API_KEYS is empty in this environment. The first scheduled poll will populate this table.",
    "admin_quota_audit_empty_heading": "No service-level audit entries yet.",
    "admin_quota_audit_empty_body": "Throttle events, purge runs, and poll failures will appear here as they happen.",
    "admin_quota_load_failed": "Couldn't load quota data. Try refreshing the page.",
    "admin_layout_breadcrumb": "Operator-only data — visible because your email is in ADMIN_EMAIL_ALLOWLIST.",
    "admin_layout_breadcrumb_aria": "Admin mode",
    "account_deleted_banner_permanent_delete_failed": "Permanent delete failed. Try again or contact support.",
    "account_deleted_banner_permanent_delete_pending": "Deleting…",
    "backfill_picker_section_title": "Initial backfill",
    "backfill_picker_section_blurb": "Imports recent videos from this channel into your inbox in one go. After that, new videos arrive automatically every 6 hours.",
    "backfill_picker_preset_1d_label": "1 day",
    "backfill_picker_preset_7d_label": "7 days",
    "backfill_picker_preset_30d_label": "30 days",
    "backfill_picker_preset_90d_label": "90 days",
    "backfill_picker_preset_everything_label": "Everything",
    "backfill_picker_preset_cost_low": "≈ 1 quota unit",
    "backfill_picker_preset_cost_everything": "up to ≈ 20 quota units (one-time)",
    "backfill_picker_helper_1d": "Picks up only new content from this moment — past videos won't be imported.",
    "backfill_picker_helper_default": "30 days fits most active devlogs without spending much quota.",
    ```

    **REMOVE** the now-orphan key `polling_badge_phase3_placeholder` (the Phase 2.1 placeholder string "Phase 3 will start polling") if present. UI-SPEC explicitly retires it. KEEP `polling_badge_manual` ("Manual entry — no polling") — UI-SPEC retains it for the never-polled-yet edge case.

    **EXTEND `tests/unit/paraglide.test.ts`** EXPECTED_KEYS array — add all the new keys in sorted alphabetical position; remove `polling_badge_phase3_placeholder` from the array. The test must continue to assert `Object.keys(en).sort()` equals `EXPECTED_KEYS.sort()` exactly.
  </action>
  <verify>
    <automated>pnpm test:unit -- paraglide.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep `polling_badge_frozen` messages/en.json succeeds (key present)
    - grep `polling_badge_throttled` messages/en.json succeeds
    - grep `polling_badge_phase3_placeholder` messages/en.json FAILS (deprecated key removed)
    - grep `admin_quota_status_80_throttle` messages/en.json succeeds
    - grep `backfill_picker_helper_1d` messages/en.json succeeds
    - grep `account_deleted_banner_permanent_delete_failed` messages/en.json succeeds
    - grep `polling_refresh_now_aria` messages/en.json succeeds
    - grep `polling_badge_phase3_placeholder` tests/unit/paraglide.test.ts FAILS (removed from EXPECTED_KEYS)
    - grep `polling_badge_frozen` tests/unit/paraglide.test.ts succeeds (added to EXPECTED_KEYS)
    - `pnpm test:unit -- paraglide.test.ts` exits 0
  </acceptance_criteria>
  <done>~42 new Paraglide keys in en.json (UI-SPEC verbatim); deprecated phase3_placeholder removed; EXPECTED_KEYS snapshot keeps the test green.</done>
</task>

<task type="auto">
  <name>Task 3: Placeholder test files (named-plan it.skip per VALIDATION.md Wave 0 Requirements)</name>
  <files>tests/unit/tier-resolver.test.ts, tests/unit/youtube-quota-tracker.test.ts, tests/unit/youtube-snapshot-writer.test.ts, tests/unit/youtube-channel-adapter.test.ts, tests/unit/admin-middleware.test.ts, tests/unit/quota-events-per-day-source-id.test.ts, tests/integration/scheduler-enqueue.test.ts, tests/integration/queue-topology.test.ts, tests/integration/youtube-quota-throttle.test.ts, tests/integration/refresh-now.test.ts, tests/integration/refresh-poll-cooldown.test.ts, tests/integration/account-purge.test.ts, tests/integration/admin-quota.test.ts, tests/integration/events-manual-paste-dedup.test.ts, tests/integration/auto-import-quota-bypass.test.ts, tests/integration/poll-worker-tx-boundary.test.ts, tests/integration/ingest-paste-then-poll.test.ts, tests/integration/youtube-snapshot-read.test.ts, tests/integration/anonymous-401.test.ts, tests/integration/tenant-scope.test.ts</files>
  <read_first>
    - .planning/phases/03.0-polling-pipeline-plumbing-youtube/03.0-VALIDATION.md "Wave 0 Requirements" + "Per-Task Verification Map"
    - .planning/phases/02.1-architecture-realignment/02.1-02-PLAN.md (Phase 2.1 Wave 0 it.skip placeholder pattern — every it.skip names the future plan)
    - tests/integration/anonymous-401.test.ts (existing MUST_BE_PROTECTED allowlist + sweep loop — the new entries must be wired into the same array)
    - tests/integration/tenant-scope.test.ts (existing cross-tenant matrix — extend with new probes)
    - tests/unit/youtube-channel-adapter.test.ts (already exists from Phase 2.1 — the STUB throws-error tests; check if the file should be EXTENDED or REWRITTEN. Action: extend with new it.skip stubs that Plan 06 will activate.)
  </read_first>
  <action>
    Each new file follows this skeleton — replace `<plan-NN>` with the activating plan number listed below:

    ```typescript
    // tests/<unit|integration>/<name>.test.ts
    // Phase 3.0 Wave 0 placeholder — assertions activate in Plan 03.0-<NN>.
    import { describe, it } from 'vitest';

    describe('<feature>', () => {
      it.skip('<assertion 1> — activated in Plan 03.0-<NN>', () => {});
      it.skip('<assertion 2> — activated in Plan 03.0-<NN>', () => {});
    });
    ```

    Per-file activation map (each it.skip names the activating plan):

    | File | Activated By | it.skip stubs (one per behavior, named-plan annotation) |
    |------|--------------|---------------------------------------------------------|
    | tests/unit/tier-resolver.test.ts | Plan 03.0-03 | "boundary 23h59m → active", "boundary 24h → cold", "boundary 27d23h59m → cold", "boundary 28d → frozen", "lastPollStatus=not_found overrides → unavailable", "lastPollStatus=private overrides → unavailable", "lastPollStatus=auth_error overrides → unavailable" |
    | tests/unit/youtube-quota-tracker.test.ts | Plan 03.0-03 | "round-robin selects next key per call (modulo length)", "isThrottled returns 80% when any key crosses 8000", "isThrottled returns 95% when any key crosses 9500", "incrementUsage UPSERTs by (date_pacific, api_key_id)" |
    | tests/unit/youtube-snapshot-writer.test.ts | Plan 03.0-04 | "writeSnapshot inserts row with polled_at truncated to minute", "writeSnapshot ON CONFLICT DO NOTHING on retry within same minute", "writeSnapshot updates events.last_polled_at to now()", "writeSnapshot increments youtube_service_quota_usage in same tx" |
    | tests/unit/youtube-channel-adapter.test.ts | Plan 03.0-06 | EXTEND existing file — keep Phase 2.1 STUB throws-error test if any; add it.skip stubs: "pollContent issues playlistItems.list and returns RawEvent[]", "pollStats issues videos.list batched (50 ids) for 1 unit", "pollStats maps 403 quotaExceeded → status:rate_limited", "pollStats maps 403 forbidden → status:auth_error", "pollStats maps 404 → status:not_found", "pollStats sends quotaUser=hash(userId) param" |
    | tests/unit/admin-middleware.test.ts | Plan 03.0-07 | "empty ADMIN_EMAIL_ALLOWLIST → throws NotFoundError for any user", "user email NOT in allowlist → throws NotFoundError", "user email in allowlist (case-insensitive) → calls next()" |
    | tests/unit/quota-events-per-day-source-id.test.ts | Plan 03.0-04 | "currentCount('events_per_day') filters source_id IS NULL", "auto-import paths bypass withQuotaGuard entirely (no count consumed)" |
    | tests/integration/scheduler-enqueue.test.ts | Plan 03.0-09 | "scheduler tick at Active boundary enqueues to poll.active", "scheduler tick at Cold boundary enqueues to poll.cold", "scheduler skips Frozen events (no enqueue)", "scheduler skips Unavailable events (no enqueue)" |
    | tests/integration/queue-topology.test.ts | Plan 03.0-09 | "boss has 6 queues declared (POLL_USER, POLL_COLD, INTERNAL_HEALTHCHECK, POLL_ACTIVE, PURGE_DAILY, YOUTUBE_QUOTA_RESET, YOUTUBE_CHANNEL_CONTEXT_BACKFILL)", "POLL_HOT and POLL_WARM rows are absent from pgboss.queue post-migrate" |
    | tests/integration/youtube-quota-throttle.test.ts | Plan 03.0-09 | "8000+ units in single key → scheduler pauses cold + auto-import only", "9500+ units in single key → scheduler pauses everything except poll.user", "first 80% threshold crossing emits quota.service_throttled audit row once per day", "second 80% crossing same day does NOT re-emit" |
    | tests/integration/refresh-now.test.ts | Plan 03.0-08 | "POST /api/events/:id/refresh-poll 200 enqueues to poll.user", "POST refresh-poll persists events.metadata.last_user_refresh_at", "second click within 5min → 429 with retry-after metadata", "cross-tenant event id → 404", "anonymous → 401" |
    | tests/integration/refresh-poll-cooldown.test.ts | Plan 03.0-04 | "service throws AppError 429 too_many_refreshes when within 5min window", "service.metadata payload includes minutesLeft", "5min after last refresh → service permits refresh again" |
    | tests/integration/account-purge.test.ts | Plan 03.0-08 | "DELETE /api/me/account/purge hard-deletes user/games/data_sources/events/api_keys_steam rows", "audit purge.completed row written under purged user_id with metadata.row_counts", "cross-tenant target → 404", "anonymous → 401" |
    | tests/integration/admin-quota.test.ts | Plan 03.0-07 | "GET /api/admin/quota with empty ADMIN_EMAIL_ALLOWLIST → 404", "GET /api/admin/quota with allowlisted user → 200 + today's quota rows + last 50 audit entries", "GET /api/admin/quota with non-allowlisted user → 404", "anonymous → 401" |
    | tests/integration/events-manual-paste-dedup.test.ts | Plan 03.0-04 | "second manual paste of same external_id → 422 duplicate_event", "soft-deleted dupe is INVISIBLE to the partial unique index (deleted_at filter)", "different user pasting same URL → succeeds (user-scoped uniqueness)" |
    | tests/integration/auto-import-quota-bypass.test.ts | Plan 03.0-04 | "currentCount('events_per_day') excludes events with source_id IS NOT NULL", "auto-import worker creating event when user is at 500/day cap → succeeds", "manual paste at 500/day cap → 429 quota_exceeded" |
    | tests/integration/poll-worker-tx-boundary.test.ts | Plan 03.0-09 | "snapshot write tx duration < 50ms even with 5s mock fetch latency", "events row not held in tx during fetch (concurrent SELECT during HTTP call succeeds immediately)" |
    | tests/integration/ingest-paste-then-poll.test.ts | Plan 03.0-09 | "paste YouTube URL → event created with source_id=NULL → next scheduler tick enqueues poll.active → mock YouTube returns ok → snapshot row exists" |
    | tests/integration/youtube-snapshot-read.test.ts | Plan 03.0-09 | "Phase 4 chart-loader contract — SELECT snapshots WHERE video_id=e.external_id JOIN events WHERE e.user_id=$1 returns only that user's video snapshots" |

    **EXTEND `tests/integration/anonymous-401.test.ts`** — add three new entries to MUST_BE_PROTECTED allowlist:
    ```typescript
    // Phase 3.0 Plan 02 — pre-declared; routes activate in Wave 2 Plans 07 + 08.
    '/api/events/:id/refresh-poll',
    '/api/me/account/purge',
    '/api/admin/quota',
    ```
    Also add three new it.skip blocks:
    - `it.skip('POST /api/events/:id/refresh-poll without session → 401 — activated in Plan 03.0-08', () => {});`
    - `it.skip('DELETE /api/me/account/purge without session → 401 — activated in Plan 03.0-08', () => {});`
    - `it.skip('GET /api/admin/quota without session → 401 (NOT 404 — auth gate fires before allowlist gate) — activated in Plan 03.0-07', () => {});`

    **EXTEND `tests/integration/tenant-scope.test.ts`** — add it.skip blocks for new cross-tenant probes:
    - `it.skip('cross-tenant POST /api/events/:id/refresh-poll → 404 — activated in Plan 03.0-08', () => {});`
    - `it.skip('cross-tenant DELETE /api/me/account/purge — N/A (account-self only) — activated in Plan 03.0-08', () => {});`
    - `it.skip('GET /api/admin/quota for non-allowlisted user → 404 (matches anonymous; allowlist is the gate) — activated in Plan 03.0-07', () => {});`
  </action>
  <verify>
    <automated>pnpm test 2>&1 | grep -E "tier-resolver|refresh-now|admin-quota|account-purge|youtube-snapshot-writer" | head -20 && pnpm test:unit && pnpm test:integration -- --reporter=verbose 2>&1 | grep -c "skipped"</automated>
  </verify>
  <acceptance_criteria>
    - File tests/unit/tier-resolver.test.ts exists with at least 7 it.skip entries
    - File tests/unit/youtube-quota-tracker.test.ts exists
    - File tests/unit/youtube-snapshot-writer.test.ts exists
    - File tests/unit/admin-middleware.test.ts exists
    - File tests/unit/quota-events-per-day-source-id.test.ts exists
    - File tests/integration/scheduler-enqueue.test.ts exists
    - File tests/integration/queue-topology.test.ts exists
    - File tests/integration/youtube-quota-throttle.test.ts exists
    - File tests/integration/refresh-now.test.ts exists
    - File tests/integration/refresh-poll-cooldown.test.ts exists
    - File tests/integration/account-purge.test.ts exists
    - File tests/integration/admin-quota.test.ts exists
    - File tests/integration/events-manual-paste-dedup.test.ts exists
    - File tests/integration/auto-import-quota-bypass.test.ts exists
    - File tests/integration/poll-worker-tx-boundary.test.ts exists
    - File tests/integration/ingest-paste-then-poll.test.ts exists
    - File tests/integration/youtube-snapshot-read.test.ts exists
    - grep "MUST_BE_PROTECTED" tests/integration/anonymous-401.test.ts succeeds AND grep "/api/admin/quota" tests/integration/anonymous-401.test.ts succeeds
    - grep "Plan 03.0-08" tests/integration/tenant-scope.test.ts succeeds (refresh-poll cross-tenant probe annotated)
    - grep -r "it.skip.*Plan 03.0-" tests/ | wc -l shows ≥40 named-plan stubs across files
    - `pnpm test` exits 0 (skipped tests do not count as failures)
  </acceptance_criteria>
  <done>17 placeholder test files instantiated with named-plan it.skip stubs; anonymous-401 + tenant-scope sweeps extended with new route entries; Wave 1+ plans drop into existing files.</done>
</task>

</tasks>

<verification>
- pnpm test (skipped tests show as skipped, no failures)
- pnpm lint (no new violations from added test files or extended docs)
- Doc updates apply: PROJECT.md splits per-platform, REQUIREMENTS.md reframing footer present
- en.json carries 42 new keys; deprecated polling_badge_phase3_placeholder removed
- 17 new test files exist + 2 existing tests extended (anonymous-401 + tenant-scope)
</verification>

<success_criteria>
1. PROJECT.md "Key Decisions" table reflects DV-1 per-platform split (3 rows: YouTube/Reddit/Steam)
2. REQUIREMENTS.md inline notes annotate POLL-01/03/05 + KEYS-01 reframing per CONTEXT
3. messages/en.json contains all UI-SPEC Paraglide keys (polling_badge_*, polling_refresh_now_*, admin_quota_*, admin_layout_*, account_deleted_banner_permanent_delete_*, backfill_picker_*)
4. tests/unit/paraglide.test.ts EXPECTED_KEYS snapshot includes new keys, removes polling_badge_phase3_placeholder
5. 17 placeholder test files exist; each it.skip names the activating Wave 1/2/3 plan
6. anonymous-401.test.ts MUST_BE_PROTECTED allowlist extended with 3 new routes
7. tenant-scope.test.ts cross-tenant matrix extended with 3 new probes
8. pnpm test stays green (skipped == OK)
</success_criteria>

<output>
After completion, create `.planning/phases/03.0-polling-pipeline-plumbing-youtube/03.0-02-SUMMARY.md` summarizing: doc edits made (line counts), Paraglide keys added/removed, placeholder test files instantiated (17 total), Wave 1+ test files now wired for activation. Note any deviation from this plan with reasoning.
</output>
