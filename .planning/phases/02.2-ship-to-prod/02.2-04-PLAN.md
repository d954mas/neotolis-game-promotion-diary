---
phase: 02.2-ship-to-prod
plan: 04
type: execute
wave: 2
depends_on: ["02.2-03"]
files_modified:
  - src/routes/settings/account/+page.svelte
  - src/routes/settings/account/+page.server.ts
  - src/routes/login/+page.svelte
  - src/routes/(app)/+layout.svelte
  - src/lib/components/AccountDeletedBanner.svelte
  - src/lib/components/ConfirmDialog.svelte
  - messages/en.json
  - tests/unit/paraglide.test.ts
  - tests/browser/account-flows.test.ts
autonomous: true
requirements: [D-12, D-13, D-15, D-16, D-S3]

must_haves:
  truths:
    - "/settings/account page shows Export and Delete buttons"
    - "Account-delete confirm dialog requires user to type literal 'DELETE' before button activates (Phase 2.1 ConfirmDialog 'Type DELETE' variant)"
    - "When user.deletedAt IS NOT NULL on session reconnect, an AccountDeletedBanner appears with restore + permanent-delete actions and a 'N days left' countdown"
    - "/login renders the early-access disclaimer copy from Paraglide messages"
    - "Auth-gated route group emits <meta name=\"robots\" content=\"noindex,nofollow\"> in the rendered HTML"
    - "Public pages (/login, /privacy in Plan 02.2-05, /terms in 02.2-05, /about in 02.2-05) do NOT emit the noindex meta"
  artifacts:
    - path: "src/routes/settings/account/+page.svelte"
      provides: "Settings/Account page with Export and Delete buttons + restore-banner"
      min_lines: 80
    - path: "src/lib/components/AccountDeletedBanner.svelte"
      provides: "Banner shown when deleted_at IS NOT NULL — Restore / Permanently Delete Now actions"
      min_lines: 30
    - path: "messages/en.json"
      provides: "~25 new Paraglide keys for /settings/account, /login disclaimer, account banner, ConfirmDialog Type DELETE variant"
      contains: "settings_account_"
  key_links:
    - from: "src/routes/settings/account/+page.svelte Export button"
      to: "GET /api/me/export endpoint (Plan 02.2-03)"
      via: "fetch('/api/me/export', {method:'GET'}) + a[download] anchor"
      pattern: "/api/me/export"
    - from: "src/routes/settings/account/+page.svelte Delete button"
      to: "DELETE /api/me/account endpoint (Plan 02.2-03)"
      via: "ConfirmDialog Type-DELETE variant + fetch('/api/me/account', {method:'DELETE'})"
      pattern: "/api/me/account"
    - from: "src/lib/components/AccountDeletedBanner.svelte Restore button"
      to: "POST /api/me/account/restore endpoint (Plan 02.2-03)"
      via: "fetch('/api/me/account/restore', {method:'POST'})"
      pattern: "/api/me/account/restore"
    - from: "src/routes/(app)/+layout.svelte"
      to: "noindex meta tag rendered in HTML head for auth-gated routes"
      via: "svelte:head + <meta name='robots' content='noindex,nofollow'>"
      pattern: "noindex,nofollow"
---

<objective>
Settings/Account page (Export + Delete UI), AccountDeletedBanner with 60-day grace, /login early-access disclaimer, and auth-gated `noindex,nofollow` meta. Reuses Phase 2.1 ConfirmDialog "Type DELETE" pattern (D-S3) and the GDPR endpoints from Plan 02.2-03.

Purpose: The user-facing surface for the application code in Plans 02.2-02 + 02.2-03. Without this UI, the buttons exist only as `curl` commands. D-12 + D-13 are also small standalone copy tasks that batch cleanly with the settings UI because they share the same Paraglide message set.

Output: One new SvelteKit route folder (`/settings/account`) + 1 new component (`AccountDeletedBanner`) + ConfirmDialog Type-DELETE variant extension + /login disclaimer + auth-gated layout noindex meta + ~25 Paraglide message additions + browser test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02.2-ship-to-prod/02.2-CONTEXT.md
@.planning/phases/02.2-ship-to-prod/02.2-RESEARCH.md
@.planning/phases/02.2-ship-to-prod/02.2-03-SUMMARY.md
@src/lib/components/ConfirmDialog.svelte
@src/lib/components/RecoveryDialog.svelte
@src/routes/settings/+page.svelte
@src/routes/settings/+page.server.ts
@src/routes/login/+page.svelte
@src/routes/(app)/+layout.svelte
@src/routes/+layout.svelte
@messages/en.json
@tests/unit/paraglide.test.ts

<interfaces>
<!-- Patterns the executor MUST follow. -->

From `src/lib/components/ConfirmDialog.svelte` (Phase 2.1 component — D-S3 says reuse for Type-DELETE pattern):
- The ConfirmDialog already exists. Inspect its props to determine if a "Type DELETE" variant exists. If not, ADD a `requireText?: string` prop that, when set, renders a text input the user must fill with the literal string before the confirm button activates. Default null preserves existing behavior. Phase 2.1 RecoveryDialog.svelte has a similar pattern — match its idiom.

From `src/routes/(app)/+layout.svelte` (or whatever the auth-gated layout is named — verify via `ls src/routes/(*)/+layout.svelte` and the +layout.server.ts patterns):
- Auth-gated routes (everything except /login, /privacy, /terms, /about) live in a route group whose +layout.svelte is the right place for the noindex meta tag. Verify the current layout structure before placing the meta.

From `messages/en.json` (Phase 02.1 has ~156 keys per STATE; this plan adds ~25):
- Each new key follows the existing convention: `<page>_<concern>_<text>`. Examples: `settings_account_title`, `settings_account_export_button`, `settings_account_delete_button`, `settings_account_grace_explainer`, `account_deleted_banner_title`, `account_deleted_banner_days_left`, `account_deleted_banner_restore_button`, `account_deleted_banner_permanent_delete_button`, `confirm_dialog_type_delete_label`, `confirm_dialog_type_delete_placeholder`, `login_early_access_disclaimer`.

From `tests/unit/paraglide.test.ts` (existing EXPECTED_KEYS keyset snapshot — append the ~25 new keys, alphabetically sorted, matching existing snapshot style).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Paraglide messages + ConfirmDialog Type-DELETE variant</name>
  <read_first>
    - messages/en.json (current key list — verify existing key naming convention)
    - src/lib/components/ConfirmDialog.svelte (current props + render shape — needed before extending)
    - src/lib/components/RecoveryDialog.svelte (Phase 02.1 reference; modal cross-app pattern)
    - tests/unit/paraglide.test.ts (EXPECTED_KEYS list — alphabetical insertion needed)
  </read_first>
  <files>messages/en.json, tests/unit/paraglide.test.ts, src/lib/components/ConfirmDialog.svelte</files>
  <action>
**A. Add ~25 new keys to `messages/en.json`** (preserve existing alphabetical sort if used; otherwise append after existing keys; use the keys verbatim — the executor must match these strings exactly so later code references resolve):

```json
"account_deleted_banner_days_left": "{days} days left to restore",
"account_deleted_banner_permanent_delete_button": "Permanently delete now",
"account_deleted_banner_permanent_delete_confirm": "This is irreversible. Type DELETE to confirm.",
"account_deleted_banner_restore_button": "Restore my account",
"account_deleted_banner_title": "Account scheduled for deletion",
"confirm_dialog_type_delete_label": "Type DELETE to confirm",
"confirm_dialog_type_delete_placeholder": "DELETE",
"login_early_access_disclaimer": "Early access. Auto-import from registered sources arrives in a future release; manual paste works now.",
"settings_account_delete_button": "Delete my account",
"settings_account_delete_confirm_body": "This will soft-delete your account. You can restore within {days} days. After that, your data is permanently purged.",
"settings_account_delete_confirm_title": "Delete account?",
"settings_account_delete_failed": "Delete failed. Please try again.",
"settings_account_export_button": "Export my data",
"settings_account_export_failed": "Export failed. Please try again.",
"settings_account_export_in_progress": "Preparing your export…",
"settings_account_grace_explainer": "Deleting your account starts a {days}-day grace period. During that window you can restore everything from this page.",
"settings_account_link": "Account",
"settings_account_section_danger_title": "Danger zone",
"settings_account_section_export_blurb": "Download every record we have for your account as a single JSON file (Article 20 — Data Portability).",
"settings_account_section_export_title": "Export your data",
"settings_account_title": "Account"
```

If the current `messages/en.json` is alphabetically sorted (verify by reading), insert each key in its alphabetical position. If not sorted, append at the end of the JSON object (preserving valid JSON).

**B. Update `tests/unit/paraglide.test.ts` EXPECTED_KEYS list:**

The keyset snapshot test asserts `Object.keys(messages).sort()` matches a hardcoded EXPECTED_KEYS array. ADD the 21 new keys (sorted alphabetically into the existing list). This test is the locale-add invariant guard (Phase 1 D-41 / Phase 02.1 Plan 02.1-03 STATE) — every new key MUST appear here or the test fails.

**C. Extend `src/lib/components/ConfirmDialog.svelte` to support "Type DELETE" pattern:**

ADD a new optional prop `requireText?: string`. When set:
- Render a `<input type="text" placeholder={m.confirm_dialog_type_delete_placeholder()} bind:value={typedText}>` below the dialog body, labelled with `{m.confirm_dialog_type_delete_label()}`.
- The confirm button is disabled when `requireText !== null && typedText !== requireText`.

When `requireText` is null/undefined, the dialog behaves exactly as before (no behavior change for existing call sites). Verify by running `pnpm test:browser` — existing ConfirmDialog tests must still pass.

Match the existing ConfirmDialog Svelte 5 pattern (props via `$props()`, state via `$state(...)`, etc.) — do NOT introduce Svelte 4 idioms.

**D. Run tests:** `pnpm test:unit -- paraglide.test.ts && pnpm test:browser` — both green.
  </action>
  <verify>
    <automated>pnpm test:unit -- paraglide.test.ts && pnpm typecheck && pnpm lint -- src/lib/components/ConfirmDialog.svelte messages/en.json</automated>
  </verify>
  <acceptance_criteria>
    - `messages/en.json` contains all 21 new key strings (verify: `grep -c "settings_account_\|account_deleted_banner_\|confirm_dialog_type_delete_\|login_early_access_disclaimer" messages/en.json` returns 21).
    - `messages/en.json` is valid JSON (verify: `node -e "JSON.parse(require('fs').readFileSync('messages/en.json'))"` exits 0).
    - `tests/unit/paraglide.test.ts` EXPECTED_KEYS contains all 21 new keys.
    - `pnpm test:unit -- paraglide.test.ts` exits 0 (keyset snapshot matches).
    - `src/lib/components/ConfirmDialog.svelte` exports a `requireText?: string` prop (verify: grep for `requireText` in the file).
    - `src/lib/components/ConfirmDialog.svelte` renders an `<input>` element when `requireText` is set (grep for `<input` and conditional render around it).
    - `pnpm typecheck` exits 0.
    - Existing browser tests for ConfirmDialog still pass: `pnpm test:browser` exits 0 with no regressions.
  </acceptance_criteria>
  <done>
21 Paraglide keys added; EXPECTED_KEYS snapshot extended; ConfirmDialog has requireText prop with input-gated confirm button; existing tests still pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: /settings/account page + AccountDeletedBanner + /login disclaimer + auth-gated noindex meta + browser test</name>
  <read_first>
    - src/routes/settings/+page.svelte (existing /settings page — note structure; /settings/account is a sibling sub-route, NOT a nested replacement)
    - src/routes/settings/+page.server.ts (server-load pattern for retentionDays)
    - src/routes/login/+page.svelte (existing login page — disclaimer goes here)
    - src/routes/(app)/+layout.svelte (or whatever auth-gated route group exists — check directory naming conventions; if no `(app)` group, the auth-gated layout is `src/routes/+layout.svelte` with auth check, but auth-public pages also use it; in that case create the noindex via a per-page check)
    - src/routes/+layout.svelte (root layout; svelte:head usage pattern)
    - src/lib/components/RecoveryDialog.svelte (modal pattern reference)
    - src/routes/+layout.server.ts (current shared-load pattern; if `data.user.deletedAt` is exposed there, the AccountDeletedBanner conditional in layout can use it; otherwise extend the load function)
    - tests/browser/*.test.ts (existing browser test file patterns — clone for the new account-flows test)
  </read_first>
  <files>src/routes/settings/account/+page.svelte, src/routes/settings/account/+page.server.ts, src/routes/login/+page.svelte, src/routes/(app)/+layout.svelte, src/lib/components/AccountDeletedBanner.svelte, src/routes/+layout.server.ts, tests/browser/account-flows.test.ts</files>
  <action>
**A. Create `src/routes/settings/account/+page.server.ts`:**

```typescript
import type { PageServerLoad } from "./$types";
import { env } from "$lib/server/config/env";

export const load: PageServerLoad = async ({ parent }) => {
  const parentData = await parent();
  return {
    ...parentData,
    retentionDays: env.RETENTION_DAYS,
  };
};
```

The `parent()` call inherits the `user` and `supportEmail` already loaded by the parent +layout.server.ts (the same way Phase 2.1 routes do — verify the existing pattern in `src/routes/settings/+page.server.ts`). If retentionDays is already exposed via the root layout (per Plan 02-10 STATE: "RETENTION_DAYS routed via +layout.server.ts"), this load can be even simpler — just return parentData.

**B. Create `src/routes/settings/account/+page.svelte`** (~120 lines):

Structure:
- Page heading via `m.settings_account_title()`
- Export section: heading via `m.settings_account_section_export_title()`, blurb via `m.settings_account_section_export_blurb()`, button "Export my data" → calls `fetch('/api/me/export', {method:'GET'})` and triggers download. Use a hidden `<a download={filename} href={blobUrl}>` anchor pattern (standard SvelteKit file-download idiom).
- Danger zone section: heading via `m.settings_account_section_danger_title()`, explainer via `m.settings_account_grace_explainer({days: data.retentionDays})`, "Delete my account" button.
- Delete button → opens ConfirmDialog with `requireText="DELETE"`, body text `m.settings_account_delete_confirm_body({days: data.retentionDays})`, confirm action calls `fetch('/api/me/account', {method:'DELETE'})` then `goto('/login')` after success.
- Failure path: surface `m.settings_account_export_failed()` / `m.settings_account_delete_failed()` via existing InlineError component.

Use existing components: `PageHeader.svelte`, `EmptyState.svelte` not relevant here, `InlineError.svelte`, `ConfirmDialog.svelte` (with new requireText prop).

**C. Create `src/lib/components/AccountDeletedBanner.svelte`** (~50 lines):

Structure:
- Renders a banner at the top of the layout when `data.user.deletedAt` is non-null.
- Computes `daysLeft = max(0, retentionDays - daysSince(deletedAt))`.
- Title via `m.account_deleted_banner_title()`.
- Days-left line via `m.account_deleted_banner_days_left({days: daysLeft})`.
- Two buttons:
  - "Restore my account" → `fetch('/api/me/account/restore', {method:'POST'})` then `invalidateAll()` to clear the banner.
  - "Permanently delete now" → opens ConfirmDialog with `requireText="DELETE"` and confirm body `m.account_deleted_banner_permanent_delete_confirm()`. Confirm calls — wait, Phase 02.2 does NOT ship a "permanent delete now" endpoint (the purge worker is Phase 3). The button must surface a friendly explainer "This will be available after the 60-day grace period" OR be hidden in 2.2. RECOMMENDED: hide the button in 2.2 (only render Restore + a passive "Or wait {days} days for permanent deletion" text). Document this in SUMMARY as a Phase 3 PUTOFF.

**D. Update `src/routes/+layout.server.ts` (or appropriate parent load):**

Ensure `data.user` includes `deletedAt`. If `getMe` (services/me.ts) returns the user via `toUserDto`, verify toUserDto exposes `deletedAt` (it should, since the column was added in Plan 02.2-01 — but the DTO may need extension). If toUserDto strips deletedAt (current behavior likely yes), ADD `deletedAt: row.deletedAt?.toISOString() ?? null` to the projection.

**W2 — Acceptance criteria for the toUserDto extension** (this DTO is consumed by Plan 02.2-03 export-strip tests; extending it is a contract-extending change, so guard against regression):

1. After extending `toUserDto` in `src/lib/server/dto.ts`, ADD a brief inline comment immediately above the new line: `// Phase 02.2: deletedAt exposed for /settings/account banner; export envelope test in tests/unit/dto.test.ts asserts ciphertext-only fields are stripped.`
2. Run `pnpm test:integration -- account.test.ts && pnpm test:unit -- dto.test.ts` — both MUST be green. The existing Plan 02.2-03 export-strip test continues to pass (deletedAt is a timestamp, not a ciphertext column, so it stays in the export envelope) AND the new banner conditional in the layout reads `data.user.deletedAt` correctly.
3. The new `deletedAt` field in `toUserDto` is intentional surface area: it appears in the export JSON envelope AND in the runtime layout-load object. Document this in the SUMMARY.

**E. Mount `AccountDeletedBanner` in the auth-gated layout:**

In `src/routes/(app)/+layout.svelte` (or whatever the auth-gated layout is), import the banner and render it conditionally:

```svelte
<script lang="ts">
  import AccountDeletedBanner from '$lib/components/AccountDeletedBanner.svelte';
  let { data, children } = $props();
</script>

<svelte:head>
  <meta name="robots" content="noindex,nofollow" />
</svelte:head>

{#if data.user?.deletedAt}
  <AccountDeletedBanner deletedAt={data.user.deletedAt} retentionDays={data.retentionDays} />
{/if}

{@render children()}
```

**F. Add /login early-access disclaimer:**

In `src/routes/login/+page.svelte`, ADD a copy block rendering `m.login_early_access_disclaimer()`. Place it BELOW the existing "Continue with Google" button or ABOVE in a non-intrusive style — match existing visual hierarchy.

**G. Confirm public pages (login, /privacy, /terms, /about) do NOT inherit the noindex:**

The noindex meta lives in the auth-gated `(app)` layout. `/login` is OUTSIDE that group. `/privacy`, `/terms`, `/about` (Plan 02.2-05) will likewise live outside. Verify by inspecting the route directory structure — these public pages should NOT be inside `(app)/`. If they are, restructure or use a per-page `<svelte:head>` override.

**H. Create browser test `tests/browser/account-flows.test.ts`:**

3 live tests (matching Plan 02.2-VALIDATION.md "browser" rows):

1. **/login renders the early-access disclaimer copy** — visit /login; assert the page text contains the literal string from `m.login_early_access_disclaimer()` ("Early access. Auto-import from registered sources arrives in a future release; manual paste works now.").

2. **Auth-gated route group emits noindex meta; public pages do NOT inherit it** — sign in via mock OAuth; visit `/feed`; assert `<meta name="robots" content="noindex,nofollow">` is present in the page source. Then visit `/login` (signed out or before sign-in); assert the noindex meta is NOT present. EXTEND the SAME test (do not create a new file): also visit `/privacy`, `/terms`, `/about` (anonymous; these public routes ship in Plan 02.2-05 and are mounted by the time browser tests run in CI) and assert NONE of them contain `<meta name="robots" content="noindex,nofollow">` in their HTML. This protects the D-13 invariant: only auth-gated routes get the noindex meta; public pages stay indexable.

3. **Account-delete confirm dialog requires "Type DELETE"** — sign in; visit `/settings/account`; click "Delete my account"; the ConfirmDialog opens; assert the confirm button is DISABLED. Type "DELETE" into the text input; assert the confirm button becomes ENABLED.

Use the existing browser test setup (the `pnpm test:browser` runner; reference any prior browser test for setup pattern).

**I. Run all tests:**

`pnpm test:unit && pnpm test:integration && pnpm test:browser` — green.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint && pnpm test:browser -- account-flows.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/routes/settings/account/+page.svelte` exists (>= 80 lines including imports + body + style).
    - File `src/routes/settings/account/+page.server.ts` exists and exports a `load` function.
    - The page renders elements containing `m.settings_account_export_button()` and `m.settings_account_delete_button()` calls (grep for these literals in the .svelte file).
    - The Delete button click path calls `ConfirmDialog` with `requireText="DELETE"` (grep for `requireText="DELETE"` or `requireText={"DELETE"}`).
    - File `src/lib/components/AccountDeletedBanner.svelte` exists (>= 30 lines).
    - The banner accepts props `deletedAt` and `retentionDays`.
    - The banner calls `fetch('/api/me/account/restore', {method:'POST'})` (grep for this literal).
    - `src/routes/(app)/+layout.svelte` (or the auth-gated layout) contains `<meta name="robots" content="noindex,nofollow"` literal.
    - `src/routes/login/+page.svelte` contains a call to `m.login_early_access_disclaimer()` (grep).
    - `tests/browser/account-flows.test.ts` exists with 3 live `it(` blocks (no it.skip).
    - `pnpm test:browser -- account-flows.test.ts` reports 3 tests passed.
    - `pnpm typecheck` exits 0.
    - `pnpm lint` exits 0 (no `process.env` reads outside env.ts).
    - Verify no public page inherits noindex: `/login`, `/privacy` (Plan 02.2-05), `/terms`, `/about` should NOT be inside `(app)/` route group.
  </acceptance_criteria>
  <done>
/settings/account page renders Export + Delete; AccountDeletedBanner appears when deletedAt set; /login has disclaimer; auth-gated layout has noindex meta; public pages do not; ConfirmDialog Type-DELETE works; 3 browser tests live and passing.
  </done>
</task>

</tasks>

<deferred>
**QUOTA-01 / QUOTA-02 dashboard UI deferred to Phase 6.**

This plan ships the `/settings/account` page with Export + Delete actions and the auth-gated `AccountDeletedBanner`, but does NOT add a quota-usage indicator (e.g. "You have used 12 / 50 games"). Per CONTEXT.md "Deferred Ideas" and RESEARCH.md, the **Quota dashboard UI** belongs to Phase 6.

What 02.2 ships instead: Plan 02.2-02 wires the 429 surface (`{error:"quota_exceeded", metadata:{kind, limit, current}}`) at the service + HTTP boundary, and the `quota.limit_hit` audit event makes hitting a limit observable in the audit log. That is the minimum credible compliance surface for open signup (D-08); a usage-progress UI is operator-comfort polish that Phase 6 will deliver.

No task in this plan adds a quota indicator. If the executor finds themselves adding one, STOP and confirm with the user — it is out of scope for 02.2.
</deferred>

<verification>
**Plan-level verification:**

1. `pnpm typecheck && pnpm lint` — green
2. `pnpm test:unit && pnpm test:integration && pnpm test:browser` — green
3. Smoke test still green (D-31 selfhost.yml unchanged)
4. `git status` — only declared files touched; `docker-compose.selfhost.yml` UNCHANGED

**Manual UAT (Russian, in Plan 02.2-08):**
- Step 5 (Экспорт): нажать "Export my data" в `/settings/account`, ожидать `diary-export-YYYY-MM-DD.json` в загрузках, открыть и проверить ключи.
- Step 6 (Удаление): нажать "Delete my account", ввести "DELETE" в подтверждение, подтвердить. Ожидать redirect на `/login`. Зайти заново через Google. Ожидать banner "Account scheduled for deletion in N days." Нажать Restore. Ожидать возврат всех данных.
</verification>

<success_criteria>
- `/settings/account` page renders Export + Delete UI with Phase 2.1 ConfirmDialog Type-DELETE pattern.
- AccountDeletedBanner appears at top of auth-gated layout when `user.deletedAt` is non-null with Restore action.
- /login renders early-access disclaimer copy from Paraglide.
- Auth-gated layout emits `<meta name="robots" content="noindex,nofollow">`.
- Public pages (/login + Plan 02.2-05's /privacy, /terms, /about) do NOT emit noindex.
- 21 Paraglide keys added; EXPECTED_KEYS snapshot updated.
- 3 browser tests live and passing.
- "Permanent delete now" button is hidden in 2.2 with PUTOFF comment for Phase 3 purge worker.
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-ship-to-prod/02.2-04-SUMMARY.md` documenting:
- Files added (page route, banner component, browser test)
- Paraglide keys added (count + naming convention)
- Auth-gated layout structural decision (`(app)` group vs per-page override)
- Phase 3 PUTOFF: "Permanent delete now" button (purge endpoint not in 2.2)
- ConfirmDialog `requireText` prop addition (backward compatible)
- Note: toUserDto extended to include `deletedAt` for layout banner conditional (verify Plan 02.2-03's export envelope test still passes — deletedAt timestamp is NOT a ciphertext column, so it's fine to expose)
</output>
