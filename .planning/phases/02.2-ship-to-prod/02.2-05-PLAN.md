---
phase: 02.2-ship-to-prod
plan: 05
type: execute
wave: 1
depends_on: ["02.2-01"]
files_modified:
  - src/routes/privacy/+page.svelte
  - src/routes/privacy/+page.server.ts
  - src/routes/terms/+page.svelte
  - src/routes/terms/+page.server.ts
  - src/routes/about/+page.svelte
  - src/routes/about/+page.server.ts
  - messages/en.json
  - tests/unit/paraglide.test.ts
  - tests/integration/public-pages.test.ts
autonomous: true
requirements: [D-09, D-10, D-14, D-S4, AGENTS-INV-7]

must_haves:
  truths:
    - "GET /privacy returns 200 and renders the GDPR Article 17 magic phrase + SUPPORT_EMAIL from server-side load"
    - "GET /terms returns 200 and renders the 'early access' magic phrase + SUPPORT_EMAIL"
    - "GET /about returns 200 and renders the GitHub repo link + SUPPORT_EMAIL"
    - "Anonymous users can access all 3 public pages without authentication (200, not 401)"
    - "Privacy Policy accurately describes the encryption story: 'OAuth tokens AND per-user API keys are envelope-encrypted at rest' (verified accurate per RESEARCH §Open Questions resolution)"
    - "No literal `neotolis.games@gmail.com` string appears anywhere in src/ except the env.ts schema default placeholder (which is empty) — every reference goes through `data.supportEmail` from server load"
  artifacts:
    - path: "src/routes/privacy/+page.svelte"
      provides: "Privacy Policy rendering ~150 lines of legal copy from Paraglide messages with SUPPORT_EMAIL substitution"
      min_lines: 50
    - path: "src/routes/terms/+page.svelte"
      provides: "Terms of Service rendering ~100 lines of legal copy from Paraglide messages with SUPPORT_EMAIL substitution"
      min_lines: 40
    - path: "src/routes/about/+page.svelte"
      provides: "About page (~30 lines) — project description + GitHub link + SUPPORT_EMAIL footer"
      min_lines: 20
    - path: "messages/en.json"
      provides: "~40 new Paraglide keys for /privacy /terms /about copy blocks"
      contains: "privacy_"
  key_links:
    - from: "src/routes/privacy/+page.svelte"
      to: "data.supportEmail (loaded server-side, not hardcoded)"
      via: "{data.supportEmail} interpolation in Svelte template"
      pattern: "data\\.supportEmail"
    - from: "src/routes/privacy/+page.server.ts"
      to: "env.SUPPORT_EMAIL (single zod schema reader)"
      via: "import { env } from '$lib/server/config/env'"
      pattern: "env\\.SUPPORT_EMAIL"
    - from: "src/routes/about/+page.svelte"
      to: "https://github.com/d954mas/neotolis-diary (canonical repo link)"
      via: "<a href='https://github.com/d954mas/neotolis-diary'>"
      pattern: "github\\.com/d954mas/neotolis-diary"
---

<objective>
Three public SvelteKit routes — `/privacy`, `/terms`, `/about` — rendering Paraglide-keyed copy with `SUPPORT_EMAIL` and `RETENTION_DAYS` injected from server-side load (not hardcoded). Flips Plan 02.2-01's 8 placeholder integration tests in `public-pages.test.ts` to live.

Purpose: Legal compliance surface for open signup (D-08). Privacy/ToS exist so the operator (author) can submit Google OAuth verification (D-20), so users have GDPR Article 15/16/17/20 rights documented, and so the SUPPORT_EMAIL is visible. /about is OAuth-verification "App home page" + project credibility. All three pages are auto-indexable (per D-13 — no noindex on public pages), inherit no auth-gated layout.

Output: 3 route folders (each with +page.svelte + +page.server.ts) + ~40 Paraglide messages + 8 live integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02.2-ship-to-prod/02.2-CONTEXT.md
@.planning/phases/02.2-ship-to-prod/02.2-RESEARCH.md
@.planning/phases/02.2-ship-to-prod/02.2-01-SUMMARY.md
@src/routes/+layout.svelte
@src/routes/+layout.server.ts
@src/routes/login/+page.svelte
@src/routes/login/+page.server.ts
@src/lib/server/config/env.ts
@messages/en.json
@tests/unit/paraglide.test.ts
@tests/integration/public-pages.test.ts

<interfaces>
<!-- Patterns the executor MUST follow. -->

From RESEARCH §6 "Privacy / ToS template source" — the Privacy Policy structure (~150 lines) covers 14 sections (1. Who we are; 2. What data we collect; ... 14. Contact). The ToS structure (~100 lines) covers 11 sections (1. Service description; ... 11. Contact). The /about page (~30 lines) is project description + GitHub link + footer. Section text comes from Paraglide messages — verbatim copy locked in RESEARCH §6.

From `src/routes/login/+page.server.ts` (existing public-route load pattern):
- `+page.server.ts` exports a `load` function returning `{supportEmail: env.SUPPORT_EMAIL}`. The page consumes via `let { data } = $props()`.

From RESEARCH §"Open Question 1 RESOLVED" — the Privacy Policy CAN say:
"All OAuth tokens (Google access + refresh tokens) AND per-user API keys (Steam, future YouTube/Reddit) are envelope-encrypted at rest with a per-row DEK wrapped by a KEK loaded from the environment. Plaintext is never written to disk and never returned via the API."

This is accurate because `src/lib/server/auth-adapter.ts` (`encryptedDrizzleAdapter`) was already wired in Phase 1 (verified during context-gathering). DO NOT add an encryption task; just write the truthful copy.

From AGENTS.md §7 "No public dashboards":
- `/privacy`, `/terms`, `/about` are public PAGES (not data dashboards). Static content + SUPPORT_EMAIL/RETENTION_DAYS injection. No user data exposed. Compliant.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Paraglide messages (~40 keys for /privacy /terms /about copy)</name>
  <read_first>
    - messages/en.json (existing key list — note alphabetical sort if present)
    - .planning/phases/02.2-ship-to-prod/02.2-RESEARCH.md §6 "Privacy / ToS template source" (verbatim section list — derive Paraglide keys from sections 1-14 for Privacy + 1-11 for ToS)
    - tests/unit/paraglide.test.ts (EXPECTED_KEYS — append the new keys)
  </read_first>
  <files>messages/en.json, tests/unit/paraglide.test.ts</files>
  <action>
ADD ~40 new keys to `messages/en.json`. Each key follows `<page>_<section>_<part>` naming. The values below are the locked copy text (English; Russian/other locales will be content-only additions later per Phase 1 D-17/D-18). The executor MUST type these verbatim — they are the legal-compliance surface and CI grep tests assert specific magic phrases.

Privacy keys (one per section heading + body block; ~22 keys):

```json
"privacy_title": "Privacy Policy",
"privacy_last_updated": "Last updated: {date}",
"privacy_section_who_we_are_title": "Who we are",
"privacy_section_who_we_are_body": "Neotolis Game Promotion Diary is operated by an indie game developer. The service is open-source and MIT-licensed; the source code lives at https://github.com/d954mas/neotolis-diary. For all data-protection questions, contact {supportEmail}.",
"privacy_section_data_collected_title": "What data we collect",
"privacy_section_data_collected_body": "Identity: your Google email address and your unique Google account identifier. Usage: every API request's IP address and user-agent are recorded in the per-user audit log. User content: game cards, registered data sources, events (videos, posts, conferences), notes, tags — exactly the content you enter. OAuth tokens (Google access + refresh tokens) AND per-user API keys are envelope-encrypted at rest with a per-row DEK wrapped by a KEK loaded from the environment.",
"privacy_section_lawful_basis_title": "Lawful basis (GDPR Art. 6)",
"privacy_section_lawful_basis_body": "We process your data to perform the service contract you initiated by signing in (Article 6(1)(b)). We do not use your data for marketing, analytics, advertising, or training of AI models.",
"privacy_section_processors_title": "Who we share with",
"privacy_section_processors_body": "Cloudflare Inc. — CDN, TLS termination, DDoS protection, edge caching. Google LLC — OAuth identity provider. Cloudflare R2 — encrypted off-site backups (server-side encryption + bucket-lock retention).",
"privacy_section_storage_title": "Where data is stored",
"privacy_section_storage_body": "Primary database lives on a VPS in the EU. Backups are stored in Cloudflare R2 with server-side encryption.",
"privacy_section_retention_title": "Retention",
"privacy_section_retention_body": "Active accounts: stored for as long as the account is open. Soft-deleted accounts: kept for {retentionDays} days, then purged. Backups: rotating 30-day window via R2 bucket-lock retention. Worst-case latency to physical disappearance: ~{worstCaseDays} days ({retentionDays} active + 30 backup overlap).",
"privacy_section_rights_title": "Your rights (GDPR Articles 15 to 22)",
"privacy_section_rights_body": "Article 15 — Right of Access: download every record we have about you via Settings → Account → Export my data. Article 16 — Right to Rectification: edit anything in-app, or email {supportEmail}. Article 17 — Right to Erasure: delete your account from Settings → Account → Delete my account. A {retentionDays}-day grace period lets you restore. Article 20 — Right to Data Portability: same export endpoint; the JSON shape is documented in our public install runbook. Article 21 — Right to Object: email {supportEmail}. Article 22 — No automated decision-making: we do not run AI on your data.",
"privacy_section_cookies_title": "Cookies",
"privacy_section_cookies_body": "We set one session cookie (Better Auth) for authentication. We do not use analytics cookies, tracking pixels, or third-party advertising.",
"privacy_section_children_title": "Children",
"privacy_section_children_body": "The service is intended for adults (18+). We do not knowingly collect data from children. If we learn we have inadvertently received children's data, we will delete it on notification to {supportEmail}.",
"privacy_section_security_title": "Security measures",
"privacy_section_security_body": "Envelope encryption (AES-256-GCM) for at-rest secrets, TLS 1.3 with HSTS in transit, per-tenant audit log, INSERT-only audit trail, source code public on GitHub.",
"privacy_section_changes_title": "Updates to this policy",
"privacy_section_changes_body": "Last updated date appears at the top of this page. Material changes are notified via in-app banner.",
"privacy_section_complaints_title": "Complaints",
"privacy_section_complaints_body": "You can complain to your national Data Protection Authority. The European Data Protection Board lists national authorities at https://edpb.europa.eu/about-edpb/about-edpb/members_en.",
"privacy_section_contact_title": "Contact",
"privacy_section_contact_body": "For privacy questions, exercise of rights, or any data-protection concern: {supportEmail}",
```

ToS keys (~12 keys):

```json
"terms_title": "Terms of Service",
"terms_last_updated": "Last updated: {date}",
"terms_section_service_description_title": "Service description",
"terms_section_service_description_body": "Neotolis Game Promotion Diary is a self-tracking diary for indie game developers. You log promotion activity (videos, posts, conferences, press) across platforms and the service tracks engagement metrics over time.",
"terms_section_account_title": "Account creation",
"terms_section_account_body": "Sign-in is via Google OAuth. One developer account per Google account.",
"terms_section_acceptable_use_title": "Acceptable use",
"terms_section_acceptable_use_body": "No spam, no impersonation, no abuse of registered data sources, no automated scraping at quota-busting rates, no reverse-engineering of internal APIs.",
"terms_section_limits_title": "Per-user limits",
"terms_section_limits_body": "Default limits: {gamesLimit} games, {sourcesLimit} data sources, {eventsLimit} events per rolling 24-hour window. Hitting a limit is not a service failure — contact {supportEmail} if your usage genuinely exceeds these.",
"terms_section_early_access_title": "Early access disclaimer",
"terms_section_early_access_body": "The service is in early access. Data preservation is best-effort. Major schema changes may force a re-import. The service may be paused with 14 days' notice.",
"terms_section_open_source_title": "Open source",
"terms_section_open_source_body": "The code is MIT-licensed at https://github.com/d954mas/neotolis-diary. The hosted SaaS instance is offered as-is.",
"terms_section_warranty_title": "No warranty",
"terms_section_warranty_body": "The service is provided AS IS, without warranty of any kind. The operator is not liable for indirect, special, or consequential damages.",
"terms_section_termination_title": "Termination",
"terms_section_termination_body": "You may delete your account from Settings → Account at any time. The operator may terminate accounts violating the Acceptable Use clause with 7 days' notice (except in cases of clear ongoing abuse, in which case termination is immediate).",
"terms_section_changes_title": "Changes",
"terms_section_changes_body": "Last updated date appears at the top. Material changes are notified via in-app banner.",
"terms_section_contact_title": "Contact",
"terms_section_contact_body": "For terms questions: {supportEmail}",
```

About keys (~6 keys):

```json
"about_title": "About",
"about_intro_body": "Neotolis Game Promotion Diary is a free, MIT-licensed self-tracking diary for indie game developers. Replace messy Google Sheets with a structured, secure, query-friendly diary.",
"about_repo_link_label": "GitHub repository",
"about_canonical_instance_label": "Canonical SaaS instance",
"about_links_privacy": "Privacy Policy",
"about_links_terms": "Terms of Service",
"about_footer_contact": "Contact: {supportEmail}",
```

Add all keys to `messages/en.json` and update `tests/unit/paraglide.test.ts` EXPECTED_KEYS — `pnpm test:unit -- paraglide.test.ts` must pass with the keyset snapshot extended. Verify JSON validity: `node -e "JSON.parse(require('fs').readFileSync('messages/en.json'))"` exits 0.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('messages/en.json'))" && pnpm test:unit -- paraglide.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `messages/en.json` is valid JSON.
    - `messages/en.json` contains all `privacy_*` keys (verify count: `grep -c '"privacy_' messages/en.json` returns 22 or more).
    - `messages/en.json` contains all `terms_*` keys (count >= 12).
    - `messages/en.json` contains all `about_*` keys (count >= 6).
    - `messages/en.json` contains the literal substring "Right to Erasure" (in privacy_section_rights_body) — this is the magic phrase Plan 02.2-01 placeholder asserts on.
    - `messages/en.json` contains the literal substring "early access" (case-insensitive) in terms_section_early_access_body.
    - `tests/unit/paraglide.test.ts` EXPECTED_KEYS list contains the new ~40 keys.
    - `pnpm test:unit -- paraglide.test.ts` exits 0 with the extended snapshot passing.
  </acceptance_criteria>
  <done>
~40 Paraglide keys added; EXPECTED_KEYS snapshot extended; messages/en.json valid; magic phrases present.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 3 SvelteKit routes (/privacy, /terms, /about) + flip 8 placeholder integration tests live</name>
  <read_first>
    - src/routes/login/+page.server.ts (existing public-route load pattern)
    - src/routes/login/+page.svelte (existing public-route shape — match for /about which is similarly thin)
    - src/lib/server/config/env.ts (env.SUPPORT_EMAIL + env.RETENTION_DAYS + env.LIMIT_GAMES_PER_USER + env.LIMIT_SOURCES_PER_USER + env.LIMIT_EVENTS_PER_DAY all exposed)
    - src/routes/+layout.server.ts (root layout load — verify what's in `parentData` for child loaders)
    - tests/integration/public-pages.test.ts (placeholder file from 02.2-01 — flip 8 it.skip to live)
  </read_first>
  <files>src/routes/privacy/+page.svelte, src/routes/privacy/+page.server.ts, src/routes/terms/+page.svelte, src/routes/terms/+page.server.ts, src/routes/about/+page.svelte, src/routes/about/+page.server.ts, tests/integration/public-pages.test.ts</files>
  <action>
**A. Create `src/routes/privacy/+page.server.ts`:**

```typescript
import type { PageServerLoad } from "./$types";
import { env } from "$lib/server/config/env";

export const load: PageServerLoad = async () => {
  return {
    supportEmail: env.SUPPORT_EMAIL,
    retentionDays: env.RETENTION_DAYS,
    worstCaseDays: env.RETENTION_DAYS + 30,  // 60 active + 30 backup
    lastUpdated: "2026-05-01",  // hardcoded ISO date; bump on material changes
  };
};
```

**B. Create `src/routes/privacy/+page.svelte`** (~150 lines):

Renders the 14 sections via Paraglide messages. Each section: `<h2>{m.privacy_section_X_title()}</h2><p>{m.privacy_section_X_body({supportEmail: data.supportEmail, retentionDays: data.retentionDays, worstCaseDays: data.worstCaseDays})}</p>`. Use `<svelte:head><title>{m.privacy_title()}</title></svelte:head>`. Order matches RESEARCH §6 Privacy structure (1. Who we are → 14. Contact). Include the last_updated line near the top.

Do NOT inherit auth-gated layout (Plan 02.2-04 noindex meta is in `(app)/+layout.svelte`; this route lives at `/privacy/+page.svelte`, OUTSIDE `(app)`). Verify by checking that the page renders WITHOUT the noindex meta.

**C. Create `src/routes/terms/+page.server.ts`:**

```typescript
import type { PageServerLoad } from "./$types";
import { env } from "$lib/server/config/env";

export const load: PageServerLoad = async () => {
  return {
    supportEmail: env.SUPPORT_EMAIL,
    gamesLimit: env.LIMIT_GAMES_PER_USER,
    sourcesLimit: env.LIMIT_SOURCES_PER_USER,
    eventsLimit: env.LIMIT_EVENTS_PER_DAY,
    lastUpdated: "2026-05-01",
  };
};
```

**D. Create `src/routes/terms/+page.svelte`** (~100 lines):

Renders the 11 ToS sections via Paraglide. Pass `gamesLimit`, `sourcesLimit`, `eventsLimit`, `supportEmail` into messages that need them.

**E. Create `src/routes/about/+page.server.ts`:**

```typescript
import type { PageServerLoad } from "./$types";
import { env } from "$lib/server/config/env";

export const load: PageServerLoad = async () => {
  return {
    supportEmail: env.SUPPORT_EMAIL,
    domain: env.DOMAIN,
  };
};
```

**F. Create `src/routes/about/+page.svelte`** (~30 lines):

```svelte
<script lang="ts">
  import * as m from '$lib/paraglide/messages';
  let { data } = $props();
</script>

<svelte:head><title>{m.about_title()}</title></svelte:head>

<main>
  <h1>{m.about_title()}</h1>
  <p>{m.about_intro_body()}</p>
  <ul>
    <li><a href="https://github.com/d954mas/neotolis-diary">{m.about_repo_link_label()}</a></li>
    {#if data.domain}
      <li><a href="https://{data.domain}">{m.about_canonical_instance_label()}</a></li>
    {/if}
    <li><a href="/privacy">{m.about_links_privacy()}</a></li>
    <li><a href="/terms">{m.about_links_terms()}</a></li>
  </ul>
  <footer>
    {m.about_footer_contact({supportEmail: data.supportEmail || '(not configured)'})}
  </footer>
</main>
```

**G. Flip 8 placeholder tests in `tests/integration/public-pages.test.ts` to live:**

Each test uses `app.request("/privacy", ...)` (or /terms, /about). The 8 tests:

1. `GET /privacy returns 200 and contains "Right to Erasure" magic phrase` — body text contains the literal "Right to Erasure" (from privacy_section_rights_body via Paraglide render).
2. `GET /privacy renders SUPPORT_EMAIL value` — set test env `SUPPORT_EMAIL=test@example.com`; body contains "test@example.com".
3. `GET /privacy renders RETENTION_DAYS value` — body contains "60" or whatever env.RETENTION_DAYS is at test time.
4. `GET /terms returns 200 and contains "early access"` — body contains the literal (case-insensitive grep).
5. `GET /terms renders SUPPORT_EMAIL value` — same as 2.
6. `GET /about returns 200 and contains GitHub repo link` — body contains "github.com/d954mas/neotolis-diary".
7. `GET /about renders SUPPORT_EMAIL value` — same as 2.
8. `Anonymous user can access /privacy /terms /about without auth (200 not 401)` — make ALL 3 requests as unauthenticated; assert each returns 200.

The integration test already has request-helper infrastructure (Phase 02.1 patterns). For tests asserting on rendered HTML, use `await response.text()` and substring check. For SUPPORT_EMAIL injection in tests, set process.env.SUPPORT_EMAIL BEFORE app.boot — match how other env-dependent integration tests handle it (Phase 02-07 STATE: "unit tests seed env via process.env.X ??= before await import(...)").

**H. Run tests:** `pnpm test:integration -- public-pages.test.ts` — 8 passed, 0 skipped.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint && pnpm test:integration -- public-pages.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/routes/privacy/+page.svelte` exists.
    - File `src/routes/privacy/+page.server.ts` exists and reads `env.SUPPORT_EMAIL` (grep).
    - File `src/routes/terms/+page.svelte` exists.
    - File `src/routes/terms/+page.server.ts` exists and reads `env.SUPPORT_EMAIL` (grep).
    - File `src/routes/about/+page.svelte` exists and contains the literal `https://github.com/d954mas/neotolis-diary` (grep).
    - File `src/routes/about/+page.server.ts` exists.
    - All 3 +page.server.ts files import `env` from `$lib/server/config/env` (grep) and read `env.SUPPORT_EMAIL` (grep).
    - NO file in `src/routes/{privacy,terms,about}/` contains the literal `process.env` (grep returns 0).
    - NO file in `src/routes/{privacy,terms,about}/` contains the hardcoded literal `neotolis.games@gmail.com` (grep returns 0 — substitution must go through `data.supportEmail`).
    - `tests/integration/public-pages.test.ts` contains 0 `it.skip(` blocks.
    - `tests/integration/public-pages.test.ts` contains 8 LIVE `it(` blocks.
    - `pnpm test:integration -- public-pages.test.ts` reports 8 passed, 0 failed.
    - `pnpm typecheck && pnpm lint` exit 0.
    - The 3 routes are NOT inside `src/routes/(app)/` (route group) — verify by directory check.
  </acceptance_criteria>
  <done>
3 public route folders exist; each renders Paraglide-keyed copy with SUPPORT_EMAIL injected from server load (NO hardcoded literal); 8 integration tests live and passing; routes are public (not behind auth); no SaaS-leak grep tripwire (D-30) fires.
  </done>
</task>

</tasks>

<verification>
**Plan-level verification:**

1. `pnpm typecheck && pnpm lint` — green
2. `pnpm test:unit && pnpm test:integration` — green
3. SaaS-leak grep dry-run: `grep -rn 'neotolis\.games@gmail\.com' src/ --include='*.svelte' --include='*.ts'` returns 0 hits (Plan 02.2-07 will codify this in CI).
4. `git status` — only declared files touched.

**Manual UAT (Russian, in Plan 02.2-08):** Step 4 (Privacy/Terms): открыть `/privacy`, `/terms`, `/about`; проверить, что contact email = SUPPORT_EMAIL из .env.
</verification>

<success_criteria>
- 3 public routes ship with +page.svelte + +page.server.ts.
- ~40 Paraglide keys added; EXPECTED_KEYS extended.
- SUPPORT_EMAIL substituted dynamically (no hardcoded literal).
- Privacy Policy accurately describes encryption story (OAuth tokens + per-user API keys envelope-encrypted at rest).
- 8 placeholder integration tests flipped to live; all PASSING.
- Routes are public (anonymous → 200, not 401).
- Routes are NOT behind auth-gated `(app)` layout (no noindex meta inherited).
- No SaaS-leak grep regression.
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-ship-to-prod/02.2-05-SUMMARY.md` documenting:
- 3 routes created
- ~40 Paraglide keys added (count by category: privacy/terms/about)
- The locked legal copy decisions (encryption story per Open Question 1 RESOLVED; retention model per D-15)
- 8 placeholder tests flipped live
- SaaS-leak grep dry-run result (0 hits)
- Note about /about's `${DOMAIN}` substitution (only renders the canonical-instance link if DOMAIN is configured — empty in test/CI environments)
</output>
