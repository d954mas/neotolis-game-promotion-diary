---
phase: 02.2-ship-to-prod
plan: 07
type: execute
wave: 1
depends_on: ["02.2-01"]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [D-21, D-24, D-30, D-31, D-S1]

must_haves:
  truths:
    - "A new docker-build-publish job runs after lint-typecheck + unit-integration + smoke + browser-tests succeed on master push"
    - "The job pushes images to ghcr.io/${owner}/neotolis-diary tagged with git-sha + master + latest"
    - "The smoke job's SaaS-leak grep step is extended to fail on hardcoded `neotolis.games@gmail.com` outside privacy/terms/about route folders + env.ts"
    - "The smoke job's SaaS-leak grep step is extended to fail on `APP_MODE === 'saas'` literal anywhere in src/"
    - "docker-compose.selfhost.yml continues to be the smoke gate's compose file (D-31 invariant — no change)"
    - "No existing CI assertion is weakened (every prior grep / test / job condition stays as-is)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Extended CI YAML with new docker-build-publish job + extended SaaS-leak grep"
      contains: "docker-build-publish"
  key_links:
    - from: ".github/workflows/ci.yml docker-build-publish job"
      to: "ghcr.io/${owner}/neotolis-diary"
      via: "docker/build-push-action@v7 with cache-from/cache-to gha"
      pattern: "ghcr\\.io"
    - from: ".github/workflows/ci.yml smoke job SaaS-leak step"
      to: "fails on neotolis.games@gmail.com outside templated routes (D-30)"
      via: "grep -rn"
      pattern: "neotolis\\\\.games@gmail\\\\.com"
---

<objective>
Add the GHCR build-publish CI job and extend the SaaS-leak grep step. After Plan 02.2-07 lands and merges to master, every push to master that passes all gates triggers an image build + push to `ghcr.io/d954mas/neotolis-diary:<sha>` + `:master` + `:latest`. The author can then run `pnpm deploy` (Plan 02.2-06) to roll the new image onto the VPS.

Purpose: The first half of D-21 (build pipeline). The second half — flipping GHCR image visibility from private to public — is a one-time UI step in `docs/deploy/install.md` (Plan 02.2-08), per RESEARCH §"Common Pitfalls Pitfall 1". CI changes are minimal because the existing smoke gate already exercises the production image via `tests/smoke/self-host.sh`; no need to duplicate.

Output: Single CI YAML edit — one new job + one grep step extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02.2-ship-to-prod/02.2-CONTEXT.md
@.planning/phases/02.2-ship-to-prod/02.2-RESEARCH.md
@.github/workflows/ci.yml
@Dockerfile

<interfaces>
<!-- Locked YAML from RESEARCH §"Code Examples Example 1". -->

```yaml
docker-build-publish:
  if: github.event_name == 'push' && github.ref == 'refs/heads/master'
  needs: [lint-typecheck, unit-integration, smoke, browser-tests]
  runs-on: ubuntu-latest
  permissions:
    contents: read
    packages: write
  steps:
    - uses: actions/checkout@v4
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v4
    - name: Login to GHCR
      uses: docker/login-action@v4
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    - name: Extract metadata for tags + labels
      id: meta
      uses: docker/metadata-action@v6
      with:
        images: ghcr.io/${{ github.repository_owner }}/neotolis-diary
        tags: |
          type=sha,format=long
          type=raw,value=latest,enable={{is_default_branch}}
          type=raw,value=master,enable={{is_default_branch}}
    - name: Build and push
      uses: docker/build-push-action@v7
      with:
        context: .
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max
```

SaaS-leak grep extension (RESEARCH "CI extensions"):
```yaml
- name: SaaS-leak grep — neotolis.games@gmail.com
  run: |
    if grep -rn 'neotolis\.games@gmail\.com' src/ --include='*.ts' --include='*.svelte' \
       | grep -v '^src/routes/privacy/\|^src/routes/terms/\|^src/routes/about/\|^src/lib/server/config/env\.ts'; then
      echo "ERROR: hardcoded support email found outside templated routes (D-30)"
      exit 1
    fi
- name: SaaS-leak grep — APP_MODE === 'saas'
  run: |
    if grep -rn "APP_MODE === 'saas'" src/ --include='*.ts'; then
      echo "ERROR: APP_MODE === 'saas' branching found in src/ (D-30, self-host parity)"
      exit 1
    fi
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add docker-build-publish job + extend SaaS-leak grep</name>
  <read_first>
    - .github/workflows/ci.yml (current 4 jobs: lint-typecheck, unit-integration, smoke, browser-tests; locate the existing SaaS-leak grep step in the smoke job — confirmed at line 222-242 per RESEARCH "Sources" section)
    - Dockerfile (verify the GHCR labels added in Plan 02.2-06 exist; the metadata-action will read them automatically via OCI labels)
    - .planning/phases/02.2-ship-to-prod/02.2-RESEARCH.md "Code Examples Example 1" (verbatim YAML)
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
**A. Append the new `docker-build-publish` job at the end of `.github/workflows/ci.yml`:**

Below the existing `browser-tests` job (or wherever the last job ends), ADD the verbatim YAML from RESEARCH §"Code Examples Example 1" (cited in <interfaces> above). Indentation MUST match existing job indentation (typically 2 spaces under the `jobs:` key).

Key invariants:
- `if: github.event_name == 'push' && github.ref == 'refs/heads/master'` — runs ONLY on master pushes (NOT PRs; PRs build but don't push).
- `needs: [lint-typecheck, unit-integration, smoke, browser-tests]` — fan-in: all 4 prior gates must succeed.
- `permissions: { contents: read, packages: write }` — required for GHCR push from `GITHUB_TOKEN`.
- `tags:` block produces 3 tags per push: git-sha (long form), `master`, `latest`.
- `cache-from: type=gha` + `cache-to: type=gha,mode=max` — speeds up subsequent builds via Actions cache.

If the existing CI uses different action versions in other jobs (e.g. `actions/checkout@v3`), do NOT downgrade — keep the new job at v4 of checkout for parity with `docker/login-action@v4`. Existing other-jobs versions stay untouched.

**B. Extend the smoke job's SaaS-leak grep step:**

Find the existing SaaS-leak grep step in the smoke job (per RESEARCH §"Sources": "ci.yml — three jobs; SaaS-leak grep step at line 222-242"). The existing step likely greps for `analytics.neotolis` / `admin@neotolis` / `CF-Connecting-IP outside trusted-proxy module` per Phase 1 STATE. ADD 2 new sub-steps OR 2 new grep lines within the existing run block:

Option 1 — extend the existing run block:
```yaml
- name: SaaS-leak grep
  run: |
    # ... existing grep lines ...
    
    # Phase 02.2 D-30 extensions:
    if grep -rn 'neotolis\.games@gmail\.com' src/ --include='*.ts' --include='*.svelte' \
       | grep -v '^src/routes/privacy/\|^src/routes/terms/\|^src/routes/about/\|^src/lib/server/config/env\.ts'; then
      echo "ERROR: hardcoded support email found outside templated routes (D-30)"
      exit 1
    fi
    
    if grep -rn "APP_MODE === 'saas'" src/ --include='*.ts'; then
      echo "ERROR: APP_MODE === 'saas' branching found in src/ (D-30, self-host parity)"
      exit 1
    fi
```

Option 2 — add as 2 separate steps after the existing one. Either works. Match the existing style.

**Caveat:** the grep `-v` exclusion uses `^src/routes/privacy/` etc. as the line-prefix patterns (because `grep -rn` outputs `<path>:<line>:<text>`). Verify the exclusion works by running locally:

```bash
grep -rn 'neotolis\.games@gmail\.com' src/ --include='*.ts' --include='*.svelte'
```

After Plan 02.2-05 lands, this should return ONLY references in `src/routes/privacy/`, `src/routes/terms/`, `src/routes/about/`, plus the empty default in `src/lib/server/config/env.ts` (literal in the comment "self-host operators MUST configure"). After Plan 02.2-08 lands, also `docs/deploy/install.md` may have references but the grep is `src/`-scoped so docs/ are not scanned. The grep should pass.

**Important:** Plan 02.2-05's privacy/terms/about pages do NOT actually CONTAIN the literal `neotolis.games@gmail.com` — they substitute via `data.supportEmail`. So in test/CI environments where SUPPORT_EMAIL is empty or set to a CI-friendly value (`test@example.com`), the literal won't appear in src/. The `-v` exclusion is defensive: even if a future PR adds the literal in those routes, it's whitelisted; otherwise CI fails.

**C. Verify the workflow file is valid YAML:**

```bash
node -e "require('js-yaml').load(require('fs').readFileSync('.github/workflows/ci.yml','utf-8'))"
```

Should not throw.

**D. Locally simulate the SaaS-leak grep** (sanity check before commit):

```bash
grep -rn 'neotolis\.games@gmail\.com' src/ --include='*.ts' --include='*.svelte' || echo "(no hits — clean)"
grep -rn "APP_MODE === 'saas'" src/ --include='*.ts' || echo "(no hits — clean)"
```

Both must return "(no hits — clean)". If anything fires, that's a bug in another plan's output that Plan 02.2-07 caught — fix the offending plan.

**E. CI dry-run note:** The new `docker-build-publish` job's actual execution can only be verified by merging to master and watching the GitHub Actions run. Plan 02.2-08's runbook documents the post-merge verification step (`gh run list --workflow ci.yml --branch master`).
  </action>
  <verify>
    <automated>node -e "require('js-yaml').load(require('fs').readFileSync('.github/workflows/ci.yml','utf-8'))" && (! grep -rn 'neotolis\.games@gmail\.com' src/ --include='*.ts' --include='*.svelte' | grep -v '^src/routes/privacy/\|^src/routes/terms/\|^src/routes/about/\|^src/lib/server/config/env\.ts') && (! grep -rn "APP_MODE === 'saas'" src/ --include='*.ts')</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` is valid YAML.
    - File contains a job named `docker-build-publish` (grep for `^  docker-build-publish:` returns 1 match).
    - The job has `if: github.event_name == 'push' && github.ref == 'refs/heads/master'` (or equivalent semantically — accepts variations like `${{ ... }}` syntax).
    - The job's `needs:` array includes all 4 prior jobs: lint-typecheck, unit-integration, smoke, browser-tests.
    - The job declares `permissions: { contents: read, packages: write }`.
    - The job uses `docker/login-action@v4`, `docker/setup-buildx-action@v4`, `docker/metadata-action@v6`, `docker/build-push-action@v7` (verify each grep).
    - The metadata-action `tags:` block contains `type=sha,format=long`, `type=raw,value=latest,enable={{is_default_branch}}`, `type=raw,value=master,enable={{is_default_branch}}`.
    - The build-push-action has `cache-from: type=gha` and `cache-to: type=gha,mode=max`.
    - The smoke job (or an extended grep step) contains the literal `neotolis\.games@gmail\.com` (the grep pattern, escaped — verify by reading the YAML's run block).
    - The smoke job (or extended grep step) contains the literal `APP_MODE === 'saas'`.
    - Locally executing the SaaS-leak grep returns 0 hits (no offending strings in src/).
    - Existing 4 jobs are NOT modified except for the smoke job's grep step extension (their `runs-on:`, `steps:`, etc. unchanged).
  </acceptance_criteria>
  <done>
docker-build-publish job appended; SaaS-leak grep extended; YAML valid; no existing assertion weakened; local grep dry-runs return clean.
  </done>
</task>

</tasks>

<verification>
**Plan-level verification:**

1. `node -e "require('js-yaml').load(...)"` — YAML valid.
2. Local SaaS-leak grep dry-run — clean.
3. `git diff master -- docker-compose.selfhost.yml` — empty (D-31).
4. `git status` — only `.github/workflows/ci.yml` touched.
5. The actual CI run after merge — verified post-merge in Plan 02.2-08 runbook step.

**Manual UAT (Russian, in Plan 02.2-08):** Step at the end of runbook §4 first-deploy: `gh run list --workflow ci.yml --branch master --limit 1` should show the docker-build-publish job green; `docker pull ghcr.io/d954mas/neotolis-diary:latest` should succeed (after the manual UI step to flip image visibility to public — also documented in install.md per Pitfall 1).
</verification>

<success_criteria>
- New `docker-build-publish` job in `.github/workflows/ci.yml` per RESEARCH "Code Examples Example 1" verbatim shape.
- SaaS-leak grep step extended with 2 new patterns: `neotolis.games@gmail.com` (with templated-routes whitelist) + `APP_MODE === 'saas'`.
- Existing CI assertions UNCHANGED.
- Local grep dry-run returns 0 hits (no SaaS leaks in src/).
- `docker-compose.selfhost.yml` UNCHANGED (D-31).
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-ship-to-prod/02.2-07-SUMMARY.md` documenting:
- New job inserted at line N of ci.yml
- SaaS-leak grep extension lines added
- Action version pins (`@v4` / `@v6` / `@v7`)
- Note: actual GHCR push verification is post-merge (in install.md UAT)
- Note: image visibility flip (private→public) is a one-time UI step in install.md (Plan 02.2-08), not in CI
- Local grep dry-run results (must be 0 hits)
</output>
